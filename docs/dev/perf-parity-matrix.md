# Performance-technique parity matrix

The living answer to "did this optimization reach every engine and mode it should have?" — created 2026-07-04 after the operator hit repeated cases of an optimization landing in one engine or mode but not its siblings (sync vs migrate, reader vs backup). Ground-truthed at `12feb24a` (v0.99.175); every cell cites code or names the absence. Updated 2026-07-04 by three chunks (perf-parity-checker verified faithful at `20668ed7`): replay-path hardening (gaps #2/#4 closed + the chain-tail identity-sequence correctness fix), migrate quick wins (gap #6 closed; new rows 15 shared-snapshot + 16 analyze-after), and the gap-#3 cold-start defaults chunk (binlog leg resolved; VStream leg reclassified deliberate). Updated again 2026-07-04 by the ADR-0149 chunk: gap #1 (backup within-table read chunking) closed for the PG lazy-snapshot cell, residual engine cells reclassified deliberate. Updated again 2026-07-04 by the ADR-0150 chunk: row 3's MySQL INSERT cells moved from the fixed 500-row cap to byte-targeted ~1 MiB statements (10k-row safety ceiling + 60k placeholder bound), matching the CDC applier's ADR-0139 statement size. Updated 2026-07-08 by the audit-N-15b chunk: new row 17 (target reparent/storage-grow ride-out) declares the ADR-0108/0110/0114 coverage per mode — the MySQL overlapped index builds were the one cell without the ride-out and now carry it on both paths; row 5 + the deliberate-absences entry corrected for the v0.99.200 serial overlap (the old "PS declines overlap" wording lagged the code). Updated 2026-07-08 by the audit-N-15a chunk (ADR-0153): new row 18 (MySQL statement protocol) — the PlanetScale/Vitess flavors default to client-side interpolation (1 RTT/statement; −33% bulk copy, −26% CDC burst drain at ~100 ms RTT on real PS), vanilla stays binary (deliberate), explicit DSN wins. Updated 2026-07-09 by the VStream-COPY FLOAT display-rounding fix: not a new technique but a correctness re-read with a throughput COST — the VStream **CS** (sync cold-start) and **BK** (backup) cells now carry ONE extra source read pass over each repairable single-precision FLOAT table (the `(col*1E0)` exact re-read that repairs the rowstreamer's 6-sig-digit rounding), bounded to the PK + FLOAT columns and cursor-paginated (O(1) mem) on CS / a per-table PK→float map on BK CAPPED at `--float-reread-max-rows` (default 2M, serial sweep → one table at a time; over-cap falls back loudly to rounded/refuse — never unbounded, ADR-0071); `--no-float-exact-reread` removes the pass (rounded, consistent). Documented in throughput-tuning; row 18's VStream FLOAT note updated below. Updated 2026-07-08 by the audit V-1 + P′-1 chunk: new row 19 (batched catalog existence probes) — the MySQL schema phases' three N+1 detect/verify sites (VerifyIndexes, per-table index detect-then-skip, per-FK probe) collapse to one chunked `IN (…)` read apiece; and row 6's CS cell — the VStream snapshot drain (the engines' last unbuffered `ir.Row` channel) now carries the house 64 buffer + 16-row batched pops (was one row per mutex acquisition + broadcast), its former deliberate-absence entry retired by the audit. Updated 2026-07-10 by the audit PERF-P1 + ARCH-F3 chunk: the VStream-COPY FLOAT-repair WRITE side (the sync cold-start **CS** cell's post-COPY PK-keyed UPDATE) moved from one `UPDATE … SET floats WHERE pk` per row (O(rows) round-trips — hours-to-days on a WAN PlanetScale target) to a batched UPDATE-against-a-VALUES/UNION join, 500 rows/statement (O(rows/batch) round-trips, ~500× fewer statements) for BOTH mysql and postgres targets; the repaired values + the set of rows updated are byte-identical (same `prepareApplierValue` shaping — Postgres casts each first-row VALUES column to its target storage type so the coercion matches the per-row assignment). The two engine writers' shared skeleton is hoisted to `internal/engines/internal/floatrepair` (ARCH-F3). Reaches the sync-cold-start FLOAT-repair write cell for BOTH engines; the BK (backup) FLOAT-patch path is in-memory and unaffected. Row 18's VStream CS note updated below. Updated 2026-07-15 by the audit MED-A1 chunk: new row 20 (ADR-0148 deploy-request index-build fallback) — previously invisible to the matrix and threaded on the migrate path ONLY; now armed/threaded on `restore` and the `sync start` cold-start too, with the unarmed siblings (fleet `sync run` specs, `sync from-backup` broker reset-leg) filed as gap #12 rather than implied. Same update: new rows 21–24 declare the postures of the modes/engines that had been invisible to the matrix (export-as-parquet, backfill, expand-contract, flat-file/mydumper sources), and the stale 2026-07-05-refactor citations are fixed (`pipeline/chunk.go` → `migcore/chunk.go`; `redact.go:141` → `:59`). Updated 2026-07-17 by the gap-#13 assess-first chunk: row 1's flat-file cell and row 24 moved from UNVERIFIED to ✅ VERIFIED — the ADR-0076 cross-table pool is source-agnostic and reaches flat-file/mydumper sources (dedicated per-table `OpenRowReader`), pinned by `TestMydumperMigrate_CrossTablePool_PG` (mydumper→PG at `--table-parallelism 4`, byte-identical to the serial leg); gap #13 CLOSED; flat-file WITHIN-table parallelism added to the deliberate-absences list (no PK addressing / sequential single-file stream — a new file-partition work-item surface, not a clean chunk). Updated 2026-07-17 by the audit E1/E2 chunk (MariaDB flavor coverage): the `mariadb` flavor (roadmap item 73, ADR-0168..0171) is a mysql-engine flavor with `usesVStream()=false`, so it INHERITS every mysql-family technique on the SAME code paths as vanilla — cross-table native-binlog concurrent cold-start K=auto min(4, tables) FTWRL N-snapshot (row 1 CS), the shared row writers/appliers (row 3), coalesced ~1 MiB CDC apply (rows 3/10), and the DEFAULT binary statement protocol (row 18 — the `{vanilla, MariaDB}` class, NOT the interpolated PS/Vitess default). Its only distinct surface is the CDC POSITION shape (domain GTIDs, ADR-0170) and the native uuid/inet binlog VALUE decode (ADR-0171), neither a throughput technique — verified NO MariaDB-only asymmetry, so the gap was matrix completeness, not a silent code gap; rows 1-CS and 18 now name MariaDB. Updated 2026-07-18 by the audit Batch A F-D5 chunk (row-level `--where` filtering, ADR-0173/0174): new row 25 declares the filtered-sync source-side stream-reduction technique — migrate + cold-start snapshot push the predicate into the source read on ALL engines, and VStream cold-start ADDITIONALLY gets a server-side `Filter.Rules` WHERE on the COPY (ADR-0174 Piece 2), but binlog/PG steady-state CDC is client-side-only (deliberate — no server CDC filter exists) and filtered VStream WARM-RESUME streams the whole keyspace unfiltered (new gap #14 / F-P1, Batch B closes it). The engine-asymmetric Piece-2 reduction was previously unrecorded. Updated 2026-07-23 by the audit PERF-1 chunk: new row 26 (target vacuum-health advisories, roadmap 2026-07-22) — not a throughput technique but tracked here for the same reason as row 20 (it landed mode-asymmetric): the dead-tuple/XID-age alerter rides the streamer's telemetry sidecars, so sync cold-copy + CDC apply are covered while one-shot `migrate` — the named heaviest-write window — has NO hook; that migrate cell is now an EXPLICIT filed gap (roadmap "Target-side autovacuum" entry), not an implied one, and the MySQL-target sibling stays a cited deferral (`ir/target_vacuum_health.go`). Updated 2026-08-05 by a `v0.112.1..HEAD` re-derivation — the maintenance protocol's rule (1) was NOT met by items 114, 130, 131, 136 or 138, so this pass is the arrears rather than a routine refresh, and the shape of the arrears is worth naming: five chunks in one delta each changed a throughput or memory technique and none touched this file. Landed here: **two cells corrected** — row 17's MySQL `LOAD DATA` cell said "no retry — item 114" and item 114 has SHIPPED (every segment now rides `flushWithReparentRetry`, `load_data_writer.go:201`), and row 4 / gap 18 framed PG `ImportRawCopy` as the unfiled twin of `LOAD DATA` when it is now the ONLY unretryable one-shot write lane in the tree, so gap 18 is RE-PRICED rather than reworded. **Six techniques given their first cell** — the copy gate's evidence-gated additive increase + BASE/CHUNK token split + `max(aimdTarget, baseHeld+1)` floor (item 136, row 11), the grow gate's per-WINDOW escalation + run-level quiesce ceiling (item 138, row 17), `LOAD DATA` segmentation and its `--max-buffer-bytes` coupling (item 114, new row 27), lane `RouteScope` (item 131, new row 28), bounded smart-compaction output (item 130, new row 29 — `backup compact` was invisible to this matrix as a MODE, exactly what rows 21–24 exist to prevent), and the change-lane chunk byte ceilings (new row 30, which is also why gap 16 is re-scoped: it was closed for the DATA lane only). New row 31 declares `backup verify`'s posture, including the read-amplifying `--depth read` (item 129) that is FULLY SERIAL where backup and restore both pool. Gap 15 re-scoped (a second, likelier lane cliff), row 12's CDC cell corrected for `CheckpointOnlyAtTxBoundary` now being true on the PG applier too, row 17 given the new keyless-table replay carve-out, and ~20 drifted `file:line` citations re-derived (`restore.go:543`→`:610`, `migrate_phases.go:595`→`:617`, `pg/row_writer.go:867`→`:873` / `:897,901`→`:903,907` / `:874`→`:880`, `migrate_table_pool.go:118`→`:90` and `:225`→`:233`, `backup_table_pool.go:161,437`→`:162,448`, `restore_table_pool.go:115`→`:116`, `streamer_coldstart_parallel.go:180`→`:194`, `migrate_parallel.go:1380`→`:1385` and `:756`→`:775,798`, `backup_parallel.go:547`→`:560`, `cdc_vstream_snapshot.go:198`→`:199`, `load_data_writer.go:41`→`:96` and `:131,175` retired outright, `row_writer_batch.go:156`→`:166`, `connection_budget.go:283`→`:311` and `:317`→`:342-347`, `streamer_apply_concurrency.go:101-114`→`:116-128` with its `:43-48,91-94` stale-comment flag retired because those comments were FIXED on 2026-08-04).

**Maintenance protocol.** This matrix is hand-maintained and therefore rots — the same failure mode as the CI shard list before `check-shard-coverage.sh`. Two defenses: (1) any chunk that adds or extends a performance technique MUST update its row in the same PR (the CLAUDE.md working agreement), and (2) the `perf-parity-checker` agent re-derives the matrix from code on demand and reports drift — run it after perf chunks land and before releases. A cell that is deliberately absent must say so and cite the rationale (ADR/bug/comment); an uncited ABSENT is presumed accidental and belongs on the gap list below.

Modes: **MIG** migrate bulk-copy · **CS** sync cold-start (per source flavor) · **BK** backup write · **RST** restore (full) · **CR** chain-restore incremental replay · **BRK** broker/from-backup replay · **CDC** steady-state apply.

Some shipped commands are not one of those seven — `backup export-as-parquet`, `backfill`, `expand-contract`, `backup compact`, `backup verify`. They still get a row (21–24, 29, 31), filed under the closest column with the rest `n/a`, because a command with no row is a command whose posture nobody is checking; that is what rows 21–24 were added to prevent and why `backup compact` and `backup verify` joined them on 2026-08-05.

| # Technique | MIG | CS | BK | RST | CR | BRK | CDC |
|---|---|---|---|---|---|---|---|
| 1. Cross-table parallelism | ✅ all live server engines, `--table-parallelism` 0→4 (`migcore/chunk.go:96`, pool `migrate_table_pool.go:90` `runBulkCopyTablePool`, ADR-0076); flat-file/mydumper sources ✅ VERIFIED against the pool (source-agnostic per-table `OpenRowReader`; `TestMydumperMigrate_CrossTablePool_PG`, gap #13 CLOSED 2026-07-17) — see row 24 | SPLIT: PG-slot ✅ full pool (`streamer_coldstart_parallel.go:194` `runColdStartParallel`, gated on `SnapshotImporterOpener`; it re-enters `runBulkCopyPhases` — `migrate.go:1537` — so every gate that file constructs, incl. the ADR-0123 copy gate at `:1599`, reaches this path too); MySQL-binlog (vanilla + **MariaDB** — both `usesVStream()=false`; MariaDB cold-starts from `@@gtid_binlog_pos`, domain-GTID CDC ADR-0170) ✅ default auto min(4, tables) FTWRL N-snapshot (`defaultNativeCopyTableParallelism`, `mysql/cdc_snapshot_concurrent.go` — gap-3 flip; `=1` is the serial opt-out; FTWRL-denied → loud serial fallback); VStream DELIBERATE 1 (see deliberate-absences list); trigger flavors ABSENT (single-conn snapshot, INFO names it — gap-3 survey; parallelizing is a separate L item) | ✅ 0→4, snapshot-capability-gated else loud serial (pool `backup_table_pool.go:162`, gate + INFO `resolveBackupTableParallelism` `:448`, the loud-serial INFO `:458`; ADR-0084/0088) | ✅ engine-generic (`restore_table_pool.go:116`) | full-leg via Restore; replay single-stream by nature | reset-leg via ChainRestore; knobs NOT plumbed (auto only) | n/a |
| 2. Within-table chunking | ✅ int-PK divide + sampled keyset for string/uuid/composite, threshold 80k, ≤64 chunks, work-stealing (`migcore/chunk.go` — relocated from `pipeline/chunk.go` in the 2026-07-05 refactor; ADR-0019/0096/0123); flat-file/mydumper sources DELIBERATE absent (no PK addressing in file chunks — row 24) | PG-slot ✅ (but import readers DELIBERATELY decline the exact-count never-ANALYZEd estimate fallback — see deliberate-absences list); MySQL-binlog concurrent path ✅; VStream ABSENT read-side (vtgate-internal); trigger flavors ABSENT; D1 deferred (`sqlite/d1_rows.go:54`) | ✅ PG lazy-snapshot: same strategies + shared `clampParallelChunkCount`, range readers minted off the exported snapshot, one table×within budget gate w/ tail stealing, `--bulk-parallelism` 0→auto (`backup_parallel.go`, ADR-0149 — gap #1 closed for this cell); MySQL-eager / non-snapshot fallback / SQLite / VStream DELIBERATE single-stream (see deliberate-absences list) | ✅ write-side chunk fan-out (ADR-0112) | ABSENT (sequential stream) | ABSENT | n/a |
| 3. Bulk write mechanism | PG binary COPY + grow-gate + upsert fallback (dispatcher `pg/row_writer.go:477`; `writeViaCopy:533`, `writeViaCopyChunked:579`, `writeViaBatch:873`, `writeViaBatchIdempotent` `row_writer_batch.go:90` — the old `:447` citation now lands in `CompositePKLeadsWith`, unrelated) + **bind-parameter clamp** (`maxBindParamsPerStmt = 65535`, `clampRowsToBindLimit`, `pg/row_writer.go:41,48`, applied `:880` + `row_writer_batch.go:96`, audit Q3 `7a8f638f`: at the 500-row default a 131-column table binds 65,500 and 132 binds 66,000 → statement rejected outright). Shared engine-neutral fourth clamp: `internal/engines/internal/floatrepair/floatrepair.go:170` `maxBindParamsPerBatch = 60000`; MySQL LOAD DATA (`row_writer.go:475` dispatch → `load_data_writer.go:96` `writeLoadData`; **SEGMENTED since item 114 — see row 27**) on the flavors that declare `BulkLoadLoadDataInfile`, which is vanilla + MariaDB ONLY (`mysql/flavor.go:125,262`) — else byte-targeted ~1 MiB INSERT, which is the PlanetScale path and, because `flavorFor` aliases Vitess onto PlanetScale (`flavor.go:100-102`), the Vitess one too (`flavor.go:170` `BulkLoadBatchedInsert`; `defaultStatementByteTarget` 1 MiB + 10k-row ceiling + 60k placeholder bound, `mysql/row_writer_bytebatch.go`, ADR-0150 — was a fixed 500-row cap; plain + idempotent + both fan-outs; once-per-writer PS tier-CPU hint); SQLite multi-row under 900-param cap; D1-as-target ABSENT (stage-local instead) | same writers; VStream forced idempotent (Bug 125) shares the ADR-0150 byte target | JSONL chunks + fast JSON codec (`backup_chunk_fast.go`) | same WriteRows (ADR-0150 reaches MySQL-target restore) | ChangeApplier | ChangeApplier | PG pipelined / MySQL coalesced at the same ~1 MiB (ADR-0139/0140) |
| 4. Raw same-engine byte-pipe (ADR-0078) | ✅ PG→PG only, 6-gate; MySQL ABSENT. **Resilience — and this cell's framing CHANGED on 2026-08-05:** `ImportRawCopy` Awaits (`pg/raw_copy.go:174`) + Trips (`:217`) the grow gate as of v0.110.1 but CANNOT retry — one-shot `io.Reader`, nothing buffered to re-drive. Until item 114 that was a property SHARED with MySQL `LOAD DATA`, and the twin framing is what kept this filed nowhere. Item 114 has now shipped (`load_data_writer.go:201` drives every segment through `flushWithReparentRetry`), so **PG raw-COPY is the ONLY unretryable one-shot write lane left in the tree** — the analogy that made it look like a peer is gone, and gap 18 is re-priced accordingly rather than merely reworded. NB the in-code comment at `raw_copy.go:212-213` still says "Same property as MySQL's LOAD DATA path"; that sentence is now FALSE and is the kind of true-when-written invariant CLAUDE.md's 2026-07-28 rule is about. Scope the residual precisely: the migrate WHOLE-TABLE raw leg *is* covered by the outer `copyTableWithSourceReadRetry` truncate-restart (`migrate_bulk.go:172`, error preserves the retriable wrap via `%w`); genuinely unprotected are the CHUNKED leg (`copyChunkRaw`, `migrate_parallel.go:1385`, returns bare — a raw chunk records no cursor until terminal, so the chunk replays from zero) and sync cold-start (`streamer_coldstart_parallel.go:325` sets `rc := resumeContext{}`, no breadcrumb at all). NB `raw_copy.go:219-231` tells the operator to "re-run the copy for this table" on a leg the wrapper may already be retrying | PG-slot fast path inherits; others ABSENT | ABSENT (different product) | ABSENT | ABSENT | ABSENT | n/a |
| 5. Deferred indexes/constraints | ✅ default; PG overlap ADR-0077; MySQL combined-ALTER ADR-0080; PS/Vitess serial build-as-copied overlap since v0.99.200 (concurrent ALTERs declined, deliberate); `--upfront-indexes` both engines; PS walled-build fallback armed here (row 20) | ✅ deferred; `--upfront-indexes` wired to sync cold-start on ALL THREE paths as of v0.107.0 (serial `streamer_coldstart.go`, parallel `streamer_coldstart_parallel.go`, multidb `streamer_multidb.go`) — the prior "deliberately not wired" note was wrong: item 111 phase 2 wired the serial caller only, and the parallel path (the DEFAULT for a PG source with a shareable snapshot) silently passed a hardcoded false; PS walled-build fallback armed since MED-A1 (row 20) | n/a | segment-0 carries indexes; PS walled-build fallback armed since MED-A1 (row 20) | same | same | index DDL not forwarded (item 24, demand-gated) |
| 6. Buffered pipeline (64) | ✅ every hop incl. redact tee (`redact.go:59`) + shard-stamp tee (`shard_value_wrap.go:68`) — gap 6 closed | readers all 64; VStream drain ✅ 64 + 16-row batched pops (`cdc_vstream_snapshot.go` ReadRows/`snapshotDrainBatch`, audit P′-1 closed 2026-07-08 — the byte-cap rowBuffer still owns backpressure; was unbuffered + one row per lock/broadcast) | inherited | ✅ 64 (`restoreChunkGroup` rowCh; gap #2 closed 2026-07) | ✅ 64 (`ChainRestore.applyIncremental` changesCh; gap #2 closed 2026-07) | ✅ 64 (`SyncFromBackup.applyIncremental` changesCh; gap #2 closed 2026-07) | laneapply queues |
| 7. Session/conn tuning | PG index-build mem+workers (`--index-build-mem`); MySQL `foreign_key_checks=0` DSN-wide; `unique_checks=0`/`sql_log_bin=0` never evaluated — GAP #11 | same | olap on full-scan reads | no bulk-load GUCs on restore | — | — | PG apply pins `synchronous_commit=on` (F7 durability) + `session_replication_role=replica` when superuser |
| 8. Read batching / olap | keyset batch 5000 default; olap on VStream-flavor full scans only (Bug 132 keeps it off chunked reads — deliberate) | flavor-specific (VStream 50k/10s/64MiB — that is the `maxBufferBytes` rowBuffer cap, NOT to be conflated with the gRPC message ceiling below; D1 1000 + one-page prefetch, ADR-0151 — serves MIG's D1 source path too). **VStream gRPC receive ceiling (v0.109.1, `8c013dd2`):** `defaultVStreamMaxRecvBytes = 128<<20` (`mysql/cdc_vstream.go:2334`) applied via `grpc.MaxCallRecvMsgSize` in `vstreamDialOptions` (`:729`), DSN override `vstream_max_recv_bytes`, loud on malformed — a hard FEASIBILITY bound (a single wide row cannot be split across messages), not a tuning knob; the size-specific gRPC phrasing is also reclassified TERMINAL ahead of `vitessRetriableSubstrings` (`mysql/reader_errors.go`). **Sibling sweep PASSES, recorded rather than implied:** both hand-built clients — `cdc_vstream.go:399` (CDC) and `cdc_vstream_snapshot.go:199` (snapshot COPY, which `backup_snapshot_vstream.go:123` also rides) — share `vstreamDialOptions`, and those are the only two `grpc.NewClient` sites in the tree, so the ceiling reaches CS + BK + CDC uniformly | ✅ olap applies; 100k chunks | stored 100k | batch 100 | same + 30s poll | AIMD-sized |
| 9. Apply lanes (SetApplyConcurrency) | n/a | n/a | n/a | via broker plumb | ✅ 0→4 | ✅ | ✅ streamer; laneapply core |
| 10. Pipelined apply (ADR-0092/0138) | n/a | n/a | n/a | inherits | PG-target only | PG-target only | PG-target only; MySQL substitutes coalescing (ADR-0139/0140, deliberate); sqlite/D1 no ChangeApplier (deliberate) |
| 11. Connection budget/clamp | ✅ prober + product ceiling + **full AIMD, not just the shrink** (ADR-0116/0123, `migcore/copy_parallelism_gate.go`). Three parts, all of which were uncelled until 2026-08-05 (item 136 / Bug 228). (a) **Evidence-gated ADDITIVE INCREASE** — `NoteOpenSucceeded` (`:244-268`) gives back ONE unit of a shrunk cap, and only on an open the target actually seated, called at `migrate_parallel.go:747` right after `openChunkConnWithRetry` returns clean. The gating is the point: a still-saturated target's opens are still failing 53300, so it grows the cap zero times while any concurrent 53300 halves it — multiplicative decrease dominates by construction. Without it a transient that CLEARED left the copy pinned at parallelism 1 for the rest of a multi-hour table, because nothing else in the gate could raise the cap. (b) **BASE vs CHUNK token classes** — `AcquireBase`/`ReleaseBase` (`:189,197`) declare the long-lived token a table-pool worker holds for a WHOLE table while blocked in `errgroup.Wait` on chunk workers that need tokens of their own; `Acquire`/`Release` (`:180,194`) are the short-lived chunk kind. (c) The **floor** `effective = max(aimdTarget, baseHeld+1)` (`recomputeEffectiveLocked`, `:286-292`), deliberately NOT clamped to the ceiling — the reachable case is a `--resume` whose recorded chunk plan re-engages chunking at `withinParallelism 1`, where clamping would reproduce Bug 228's deadlock with no 53300 involved. **Sibling sweep PASSES, recorded rather than implied** — one gate is constructed per run at `migrate.go:1599-1605` inside `runBulkCopyPhases`, whose two callers are migrate (`migrate.go:904`) and the sync cold-start fast path (`streamer_coldstart_parallel.go:327`), so both get (a)+(b)+(c); backup builds its own at `backup_parallel.go:170` and takes (b)+(c) (`backup_table_pool.go:218`) but NOT (a) — a **cited deliberate absence**, and the citation still holds (`backup_table_pool.go:210-217`: nothing on the backup read side calls `ShrinkAndBackoff`, so the cap never shrinks and an additive increase would be a no-op; the base-class declaration is made anyway, "the class, not the reachable instance"). The serial cold-start is the other cited absence and it also holds — `coldStartRunCopy` (`streamer_coldstart.go:960`) uses `bulkCopyOpts`, never `runBulkCopyPhases`, so no gate is constructed and there is no parallelism to govern (`migrate_table_pool.go:132-134` names it). `ShrinkAndBackoff` has exactly ONE caller in the tree (`migrate_parallel.go:736`), so the whole shrink half is migrate/cold-start-only by construction. The retry BUDGET is per-chunk rather than run-wide (item 126, `:116-143`) — before that, sixteen workers meeting a slot shortage at nearly the same moment burned the whole `MaxRetries` between them and the seventh aborted the run while workers 1–6 were still in their FIRST backoff; the DECREASE stays run-wide deliberately. The budget key was the chunk index ALONE — not unique across tables under the run-wide gate, so table A's chunk 3 shared a budget with table B's, bounded and LOUD (an early abort, never a stall). **FIXED 2026-08-05 (item 137):** the key is `copyChunkKey{table, chunk}`, `ShrinkAndBackoff` takes the table, and the uniqueness argument is pinned by `TestShrinkAndBackoff_BudgetIsPerTableChunkNotChunkIndex` rather than asserted in a comment | budget −1 CDC conn (PG fast path); native-MySQL W(readers, now default 4)×D stays OUTSIDE the preflight — operator contract INFO, ADR-0101 §5 (folding W in is the deferred follow-on; the old parenthetical "no MySQL prober exists" was stale — see the CDC cell) | reader budget −1; no live headroom clamp — GAP #8 | ✅ + live CPU/mem headroom clamp (ADR-0115, `restore_table_pool.go:303`). No `CopyParallelismGate` at all on this path — DECLARED, not a filed gap: restore's writer-side pool has no 53300 classifier feeding a shrink, and the headroom clamp is the live-pressure response it does have | via broker | auto only | ✅ **all five engines probe.** `_ ir.TargetConnectionBudgetProber = Engine{}` is compile-asserted on MySQL (`mysql/capabilities_assert.go:48`, impl `connection_budget.go:311`, ceiling folded `:342-347`) as well as PG, and all four MySQL flavors register `Engine{}` values, so `--max-target-connections` is NOT inert and lanes are `min(4, EffectiveParallelism)` on both — same rule, not fixed-4. **GAP #10 RETIRED (was stale at filing: the prober landed `9c042c10`, v0.100.0, ten days before this matrix was ground-truthed).** The real unwatched behaviour the old cell concealed: a probe that fails/refuses **degrades CDC apply to SERIAL (1 lane)** (`streamer_apply_concurrency.go:116-128`) — a throughput cliff, deliberately not a refusal so a warm resume into a tight target still runs. The stale "today: MySQL / PlanetScale-MySQL" comments this cell used to flag are FIXED (2026-08-04): both sites now say the `!ok` branch is DEAD for every shipped engine and name no engines (`streamer_apply_concurrency.go:48-53,104-107`) — `capabilities_assert.go:48` + `connection_budget.go:311` (prober) and `:342-347` (ceiling fold) are the compile-asserted ground truth. **Second cliff, added 2026-08-05:** a failed non-PK-unique-index probe (item 131) fails CLOSED and drops that TABLE to one lane — see row 28 and the re-scoped gap 15 |
| 12. Checkpoint amplification | ✅ O(1) per-table rows (migratestate, ADR-0082) | VStream durable watermark; native-MySQL in-memory only (cross-process resume DEFERRED — GAP #5) | ✅ O(1) on append stores; O(tables) on object stores (named wart) | chunk-granular | frontier | per chunk | laneapply frontier 2000-cadence. **The SERIAL batch loop got FEWER checkpoint writes on 2026-08-05 (item 132):** `CheckpointOnlyAtTxBoundary` is now `true` on the Postgres applier (`pg/change_applier_batch.go:160`) as it already was on MySQL (`mysql/change_applier_batch.go:136`), so a flush landing mid-source-transaction commits its DATA and withholds the position (`appliershared/batch_loop.go:766`, `commitBatch`). It is a CORRECTNESS fix, not an optimization — the flag reads like a target knob and encodes a SOURCE property ("is a position stamped mid-source-transaction a legal restart point?"), and for a MySQL binlog source it is false in both position modes. Tracked here for its throughput/retention side effects, in both directions: fewer position writes per batch on the serial path, but the `AfterCommit` slot-ack is withheld with the position (`batch_loop.go:795-798`), so on a **PG source** — which does emit `TxBegin`/`TxCommit` (`pg/cdc_reader.go:1164,1227`) — `confirmed_flush_lsn` now advances only at transaction boundaries and a long source transaction holds more WAL on the source for its duration. Bounded to the span of ONE source transaction, and inert for a marker-less source (VStream, every trigger-CDC engine) via the `inSourceTx` guard |
| 13. Compression | n/a | n/a | zstd SpeedDefault, level not tunable — GAP #9; 32% of backup CPU (roadmap frontier) | decode | same | same | n/a |
| 14. Other | fast-loader ADR-0043; chunk-open retry ADR-0146; OLAP verify count ADR-0147 | write fan-out ADR-0097: MySQL-target only, PG-target serial — GAP #7 | resume re-streams partial tables (Bug 135, deliberate) | chunk fetch retry ADR-0117 | ✅ one-chunk read-ahead (`streamIncrementalChanges` fetcher; gap #4 closed 2026-07, ADR-0117 per-chunk retry preserved); chain-tail identity-sequence re-sync (correctness fix 2026-07, `syncIdentitySequencesAtTail` — the 23505-at-tail bug) | chain cache 2 GETs/idle; inherits both 2026-07 replay fixes via its ChainRestore reset-leg + its own buffered changesCh | `--apply-delay` backpressure by design |
| 15. Shared exported snapshot (reader consistency) | ✅ PG-source: plain-SQL `SnapshotExporter` + importer-pinned pool/chunk readers, released at copy-phase end, WARN-fallback to per-conn views (`migrate_snapshot.go`, `postgres/snapshot_exporter.go`); MySQL/SQLite deliberate ABSENT (no shareable snapshot — per-session/FTWRL story, `ir/interfaces.go` SnapshotImporter note) | PG-slot ✅ (ADR-0079 EXPORT_SNAPSHOT); MySQL/VStream deliberate ABSENT (same rationale) | ✅ slot-anchored (`postgres/backup_snapshot.go`, ADR-0084) | n/a (reads immutable chunks) | n/a | n/a | n/a |
| 16. Post-load ANALYZE (`--analyze-after`) | ✅ opt-in, all 3 target engines via `ir.TableAnalyzer` (advisory: per-table WARN, never fails; `pipeline/analyze_after.go`) | ✅ opt-in as of v0.107.0, all three sync cold-start paths (the prior "deliberate ABSENT — statistics churn under CDC apply" rationale did not survive review: the serial cold-start had honoured the flag since item 111, so the parallel path's hardcoded false was an oversight, not a decision) | n/a | deliberate ABSENT — restore is often a rehearsal target; demand-gated (same S-size lever if asked) | same | same | n/a |
| 17. Target reparent/storage-grow ride-out (ADR-0108/0110/0114) | ✅ rows — **NINE write cores, not two** (a derived roster now proves it: `grow_gate_retry_posture_test.go` in both engine packages classifies every gate-touching core as retrying-or-not, fail-by-default). FIVE retry, FOUR do not (PG `writeViaBatch`, PG `ImportRawCopy`, and both float-repair `ExecBatch` cores) — so the run-level quiesce ceiling is a probabilistic SHIELD for those four, never a floor, and a non-retrying core that meets a transient first dies whatever the gate does. Loud in every case, never a silent under-copy. PG copy-chunk retry (`pg/row_writer_grow_gate.go`) + MySQL flush retry + grow-gate (`mysql/row_writer_reparent_retry.go`), plus six added across `8a18887c` (v0.109.1) and `c0312c34` (v0.110.1): PG `writeViaBatch` (`row_writer.go:903,907`, Await+Trip, **no replay** — a plain-INSERT ack is ambiguous), PG `writeViaBatchIdempotent` (`row_writer_batch.go:166`, full `copyChunkWithRetry` — the core itself at `row_writer_grow_gate.go:203` — an upsert converges), PG `ImportRawCopy` (`raw_copy.go:174,217`, **no retry**, one-shot reader — now the ONLY one, see row 4), PG float-repair (`row_writer_float_repair.go:84,101`), MySQL `writeLoadData` (**RETRIES as of item 114, 2026-08-05** — the old "no retry" note is dead: `flushLoadDataSegment` (`load_data_writer.go:201-248`) drives EVERY bounded segment through the same `flushWithReparentRetry` (`row_writer_reparent_retry.go:160`) the two batched cores use, so the Await (`:178`), the Trip (`:208`), the transient classification, the fresh-connection re-acquire, the wall-clock bound and the keyless carve-out all come from the shared helper instead of being absent. What makes the replay exactly-once rather than at-least-once is a real-MySQL fact, not an assumption: LOAD DATA **LOCAL** INFILE downgrades a duplicate-key error to a warning and skips the row, so a byte-identical replay converges on a keyed table — ground-truthed by `TestLoadDataLocal_DuplicateKeyIsAWarningAndSkipsTheRow`, and the cost is a replay-aware warning probe that must not swallow a real coercion warning), MySQL float-repair (`row_writer_float_repair.go:67,92`). **Keyless-table replay carve-out, new 2026-08-05 (`b747c600`, audit B-9), on BOTH engines:** a re-send is safe on a keyed table because the two outcomes are distinguishable after the fact (rolled back ⇒ clean apply; committed-but-unacked ⇒ 1062/23505 the writer tolerates as proof the rows landed) and on a KEYLESS table they are not — both look like a clean apply and one has silently doubled the batch. So both retry cores now REFUSE before replaying rather than risk the double: MySQL `row_writer_reparent_retry.go:225-227` → `errKeylessAmbiguousReplay` (`:352`), PG `row_writer_grow_gate.go:246-248` → its twin (`:315`). The Trip still fires first in both — the transient was real, so the sibling lanes should still quiesce — which means the carve-out costs a keyless table its ride-out, not the run its coordination. Two of those (`ImportRawCopy`, float-repair) sit on the sync cold-start VStream→PG path, the exact auto-grow shape the gate exists for. **Gate:** `postgres/grow_gate_roster_test.go` + `mysql/grow_gate_roster_test.go` DISCOVER lanes by signature (`<-chan ir.Row` / `[]ir.Row` / `io.Reader`) with an anti-vacuity floor of 5 — the predecessor `TestGrowGate_ReachesEveryWriteCore` built a bare `&RowWriter{}` and reached NO core (mutation-proved: all three wirings reverted, both suites green). **PROACTIVE vs signal-only split:** sync cold-start ×3 get `storageRecoveredProbe` + `startStorageHeadroomWatch`; CDC apply gets the watch; **migrate and restore get neither** (`NewGrowGate(ctx, nil)`). Migrate's is deliberate and its citation HOLDS (`migrate_phases.go:617` — `Migrator` carries no `TargetTelemetry`). **Restore's does not:** `restore.go:610` justifies it as "matching the migrate cold-copy", but `Restore` HAS the seam (`restore.go:155`) and already samples it per tick for the parallelism clamp (`restore_table_pool.go:307`) — the analogy's premise is false for the analogue, so a restore into an auto-grow target must have a lane FAIL before anyone backs off and cannot reopen early on recovery. Reclassified ACCIDENTAL → gap list; **ESCALATION MODEL, item 138 (2026-08-05) — previously uncelled, and it changed how much of a run the gate may stop.** The ADR-0110 ladder advanced once per *Trip*. It is meant to express TEMPORAL persistence ("still bad after we waited"), but every lane parks in `Await` while the gate is closed, so the only trips that can arrive during a window are from lanes that were mid-flush when it closed — all reporting the SAME target event. The ladder was being driven by FAN-OUT WIDTH: at the default W × D (16 lanes on sync cold-start, up to 32 on migrate) one connection drop produced 13–16 trips within ~4 ms and burned the ladder to its 30 s cap in those 4 ms. Ground truth, from a field log and reproduced deterministically: across 246 close/reopen pairs the hold matched `backoff(tripCount)` to the millisecond. The fix splits three bounds that answer three different questions (`migcore/grow_gate.go:139-157`): `cycle` — the EPISODE ladder, now advancing exactly ONE rung per *window* and resetting after `GrowGateEpisodeIdle` (60 s, `:105`) of healthy open time; `maxHold` — the per-WINDOW ceiling only (20 min, `:98`, and its doc now says so in those words at `:94-97`); and the NEW **run-level quiesce ceiling** — over any trailing `GrowGateQuiesceWindow` (5 min, `:125`) the gate may hold the copy closed for at most `GrowGateMaxQuiesceShare` (0.5, `:126`) of it, past which it declines to close at all and the per-lane retry budgets carry the run alone (`quiesceAllowanceLocked`, `:284-306`). That third bound is the one that could see the field shape: 246 windows, none remotely near the 20-minute max-hold, totalling 73 minutes of quiesce in a 91-minute run — a bound scoped to the window is structurally blind to it. The safety argument for declining to close cites an environmental fact and that fact is CHECKED, not asserted (the premise-naming step): ADR-0110's cost case assumes ~W × D lanes "hammer" a struggling target, and the same field log shows 1061 of 2195 lane retries already at their own 30 s cap — so a gate that declines hands the target back to lanes attempting roughly once per 30 s each. `TestGrowGate_DecliningToCloseLeavesTheLaneBackoffIntact` is what binds the argument to the code. **Reach:** the model lives in the shared `migcore` primitive, so every mode that constructs a gate gets it identically — sync cold-start ×3 (`streamer_coldstart.go:1010`, `streamer_coldstart_parallel.go:299`, `streamer_multidb.go:712`), migrate (`migrate_phases.go:617`) and restore (`restore.go:610`); the proactive-vs-signal-only split above is unchanged by it. Post-copy DDL phases: pipeline retry gated on both engines' `IsTransientError` (`migcore/ddl_phase_retry.go`); overlapped index builds: PG (item 42, `pg/schema_writer_index_overlap.go:229`) + MySQL BOTH overlap paths — vanilla workers and the VStream serial branch (`mysql/schema_writer_index_reparent_retry.go`, audit N-15b closed 2026-07-08; was the one overlap cell without the ride-out) | inherits — the fast-parallel sync cold-start funnels through the same row writers + `BuildTableIndexesFromChannel` | n/a (writes land on the blob store, not a reparenting target) | inherits the row writers' retry (row 3: same WriteRows) | apply retry ADR-0038 | same | apply retry ADR-0038 (same classifier, no second policy) |
| 18. MySQL statement protocol (ADR-0153, audit N-15a) | ✅ MySQL-target: PS/Vitess flavors default `interpolateParams=true` at the `parseDSNForFlavor` choke point — 1 COM_QUERY RTT/statement vs the hidden per-statement COM_STMT_PREPARE (−33% bulk copy at ~100 ms RTT, real-PS bench 2026-07-08); vanilla + **MariaDB** DELIBERATE binary protocol (the `{vanilla, MariaDB}` class — both `!usesVStream()` at the `connect.go` gate, so neither flips `interpolateParams`; LAN/self-hosted RTT, conservatism free, manual DSN opt-in documented in throughput-tuning); explicit DSN wins; unsafe-collation AND unsafe-charset DSNs step aside with WARN (explicit unsafe combos refused loudly — sluice polices the charset= hole the driver misses) | ✅ WRITE side inherits — every VStream-flavor cold copy funnels through the same row writers/appliers; source ROW-DATA READ sessions (row reader + snapshot readers) DELIBERATELY stay binary — the read-fidelity exemption, retained as defense in depth (MySQL FLOAT→text does not round-trip float32 — the underlying full-scan display-rounding itself is FIXED in this branch via the (col * 1E0) DOUBLE promotion in the shared read projection, `selectColumnExpr`; pinned by `TestRowReader_FloatFullScan_ExactRoundTrip` + `TestInterpolationFlavorDefault_RowReaderKeepsBinaryProtocol`; G4 note RESOLVED by vttestserver ground truth 2026-07-09 (`TestVStream_FloatCarrierParity`): the VStream CDC leg is float32-EXACT — vttablet re-encodes the binlog float32 bits as shortest-round-trip text, so only the float64 carrier's low bits differ from the reader's float64(float32(x)), observable solely by a double-width target [the informational, confirmed + pinned]; the VStream COPY leg however carries mysqld's bare-column 6-sig-digit display-rounded text — REAL float32-level loss, now FIXED 2026-07-09 via a post-COPY exact SQL re-read [`(col*1E0)`], EXACT-by-default on both the sync cold-start [re-read + BATCHED PK UPDATE against a VALUES/UNION join, 500 rows/statement = O(rows/batch) round-trips, byte-identical values — audit PERF-P1/ARCH-F3 2026-07-10; CDC-heals] and backup [snapshot-reader FLOAT-patch] paths, with `--no-float-exact-reread`/`--strict-float` escape hatches; this adds the extra source read pass the intro notes to the VStream CS + BK cells) | n/a — backup writes land on the blob store, and its source row reads carry the same deliberate read-session exemption | ✅ MySQL-target restore: same WriteRows/cfg | inherits via RST | inherits via its reset-leg | ✅ serial pool + ADR-0104 lane pool (pipelineCfg inheritance pinned: `TestInterpolationFlavorDefault_ApplierLanePoolCfg`); −26% burst drain, −105 ms/flush p95 ≈ 1 RTT; wire-pinned Com_stmt_prepare = 0 (`TestInterpolationFlavorDefault_ProtocolPin_EndToEnd`) |
| 19. Batched catalog existence probes (audit V-1) | ✅ MySQL-target: all three formerly-N+1 sites — the SLUICE-E-INDEX-MISSING verify, the per-table index detect-then-skip, and the per-FK constraint probe — read the catalog in one chunked `table_name IN (…) AND name IN (…)` query per [catalogProbeChunk=400] objects (`mysql/schema_writer.go probeCatalogPairs`; a 200-index PS schema verifies in 1 metadata query, not 200 serial vtgate RTTs; ci-fold set compare preserves the old information_schema ci `=` semantics); PG-target: the index BUILD needs no probe (`CREATE INDEX IF NOT EXISTS`, deliberate) — but that same silent no-op is why PG gained the `SLUICE-E-INDEX-MISSING` verify in this release, and it reads existence + `indisunique` for the whole expected set in ONE batched `pg_index` query (`postgres/index_verify.go probePGIndexes`, `relname = ANY($2)` — no chunking needed, the array rides one parameter); the per-FK probe remains N+1 — LOW residual (managed-PG/LAN RTT, not the vtgate target the audit priced) | ✅ inherits — sync cold-start funnels through the same SchemaWriter phases (`BuildTableIndexesFromChannel`/`VerifyIndexes`/`CreateConstraints`) | n/a (no target schema apply) | ✅ inherits (restore applies schema via the same writer) | inherits via RST | inherits via its reset-leg | DELIBERATE single probe — CDC shape deltas arrive one object at a time (`indexExists` note) |
| 20. PS deploy-request index-build fallback (ADR-0148; not a throughput technique — an UNBLOCKING recovery for the walled deferred build, tracked here because it landed migrate-only, the audit-MED-A1 instance of this matrix's failure mode) | ✅ armed via `migrate --planetscale-org` + service token (`cli.go`, `migrate_index_fallback.go`); threaded `migrate.go` → `migcore.ApplyIndexBuildFallback` | ✅ armed on `sync start` since MED-A1 (same flag set; org shared with telemetry — `telemetryParamsSharedOrg`); threaded on BOTH cold-start branches (`streamer_coldstart.go`, `streamer_multidb.go`); fleet `sync run` specs ARMED since gap #12 close (2026-07-17): per-sync `planetscale-*` YAML keys (service token env-first) → `SyncSpec.resolveIndexFallback` → `buildStreamerFromSpec` sets `Streamer.IndexBuildFallback`; shared-org reconciled via `telemetryParamsSharedOrg` in `buildSupervisedFleet` + `SyncSpec.isFallbackOnlyArming` at config-load | n/a (no target schema apply) | ✅ armed on `restore` since MED-A1; threaded `backup/restore.go` before Phase-4 CreateIndexes; chain dispatch carries it to the segment-0 full | via RST (segment-0) | ✅ broker ARMED since gap #12 close (2026-07-17): `SyncFromBackupCmd` `--planetscale-*` flags (org explicit-only, no telemetry surface) → `SyncFromBackup.IndexBuildFallback` → `newColdStartChainRestore` carries it to the `--reset-target-data` segment-0 full | n/a (index DDL not forwarded, row 5) |
| 21. Export-as-parquet (`backup export-as-parquet`, ADR-0164) | n/a | n/a | DELIBERATE serial per-table + per-chunk decode→encode, fixed zstd: one Parquet file per table is the warehouse-glob consumption shape (ADR-0164 §alternatives — per-chunk files rejected); compression fixed at zstd ("fewer surfaces", the chunk-side benchmark ratified it); row groups align to source chunks for the verify cross-reference, with the 128 MiB byte-roll splitting an oversized chunk's group to bound writer memory (ADR-0164 amendment, audit MED-P3). None of the backup family's table/chunk parallelism reaches this exporter — DECLARED absence (was undeclared, audit 2026-07-15); parallelize on evidence. Scratch staging honours `--stage-dir` | n/a | n/a | n/a | n/a |
| 22. Backfill (`sluice backfill`, ADR-0159 — same-DB expand-contract UPDATEs; closest to MIG so declared here) | DELIBERATE serial: one keyset-chunked bounded UPDATE at a time (ADR-0159 §"Sequential keyset walk" — the chunk size is intentionally the write-lock budget; §alternatives rejects parallel chunk workers for Phase 1). mysql all-flavors + postgres | n/a | n/a | n/a | n/a | n/a | n/a |
| 23. Expand-contract (`sluice expand-contract` / `sluice deploy-ddl`, ADR-0162/0165) | Control-plane orchestration only — schema ships via PS deploy requests (VReplication, PlanetScale-internal pacing) and data movement delegates to `sluice backfill` (row 22). No sluice-side parallelism surface to declare; DELIBERATE | n/a | n/a | n/a | n/a | n/a | n/a |
| 25. Filtered-sync source-side stream reduction (`--where`, ADR-0173/0174 — a transfer-reducing technique, tracked here because Piece 2 lands on ONE flavor/mode only) | ✅ ADR-0173 Phase 1 pushes the predicate into the source `SELECT … WHERE` (server-evaluated, index-aware; raw-copy fast path disabled per filtered table) for all live server engines (`migcore/row_filter.go` ApplyRowFilters) | SPLIT: the cold-start SNAPSHOT reuses the same source-read push-down for all engines; VStream ADDITIONALLY pushes a server-side `Filter.Rules` WHERE into the VStream COPY so only in-scope rows transfer (ADR-0174 Piece 2, `cdc_vstream_snapshot.go` `vstreamCopyFilterRules`), on BOTH cold-start AND warm-resume (Batch B v0.99.281 — gap #14 CLOSED). PAD-SPACE-collation columns stream UNFILTERED server-side + client-filtered on VStream (server filter is NO-PAD; v0.99.282 A0 + #66 fallback — more wire traffic for that table, correctness preserved) | n/a | n/a | n/a | n/a | binlog steady-state has NO source-side stream filter — DELIBERATE for native MySQL (no server-side CDC filter exists; predicate evaluated CLIENT-SIDE per change, ADR-0173 Phase 2 `where_cdc_filter.go`). **PG 15+ steady-state IS server-side-filtered since the ADR-0176 push-down** (per-table publication row filter on the stream's own publication, `where_pushdown_pg.go` envelope-gated; outside-envelope predicates and PG<15 fall back to unfiltered-server + client-filter; client eval stays on as the belt). VStream cold-start + warm-resume server-side-filtered (gap #14 CLOSED v0.99.281), except PAD-SPACE-collation columns (v0.99.282 A0 + #66 fallback). **EXPLICIT GAP (filed, audit 2026-07-23 DOC-2): the trigger-CDC flavors (pgtrigger / sqlite-trigger / d1-trigger) have no analogous change-log-side reduction — the poll transfers all rows and filters client-side; demand-gated (a `WHERE` on the change-log poll query is the natural shape if a filtered trigger-CDC workload appears).** |
| 26. Target vacuum-health advisories (`--notify-dead-tuple-ratio` / `--notify-xid-age`, roadmap 2026-07-22 — ADVISORY, not a throughput technique; tracked here because it landed mode-asymmetric, the row-20 precedent) | **ABSENT — EXPLICIT GAP** (audit 2026-07-23 PERF-1, filed in the roadmap entry, demand-gated): one-shot migrate is the docs' own named heaviest-write window but the `Migrator` has no notifier-sidecar seam; wiring it = port the sink/threshold flags + a run-scoped sidecar | ✅ covers cold-copy (started from `startTelemetrySidecars`, before the copy phase) | n/a (writes land on the blob store) | ABSENT — same missing seam class as MIG; un-filed (restore is often a rehearsal target; fold into the migrate wiring if demanded) | same | same | ✅ `startVacuumHealthNotifier` ticks for the whole apply phase (`vacuum_health_notify.go`; PG targets only — the MySQL-target sibling is a cited deferral in `ir/target_vacuum_health.go`) |
| 24. Flat-file sources (mydumper ADR-0161; csv/tsv/ndjson ADR-0163 — MIG sources only, no CDC/writer surfaces) | mydumper: single-reader whole-table copy, DELIBERATE — file chunks have no PK addressing, so the reader implements none of the batched/counter/bounds surfaces (ADR-0161 §row-reader) and rows 1–2 never engage; csv/tsv/ndjson: stage-into-SQLite then the sqlite staged readers (sqlite posture applies; D1/sqlite within-table chunking deferred, `d1_rows.go:54`) — a single csv/tsv/ndjson file is one table + a sequential byte stream (RFC-4180 quoted newlines forbid cheap row-boundary byte-splitting; the read-back is a fast local SQLite scan), so single-reader is an INHERENT deliberate absence there. Cross-table pool behaviour against these sources ✅ VERIFIED source-agnostic (`TestMydumperMigrate_CrossTablePool_PG`; gap #13 CLOSED 2026-07-17) — the mydumper multi-table dump copies through the ADR-0076 pool at `--table-parallelism N`; within-table stays DELIBERATE absent (no PK addressing). Staging: stage-ONCE per source (schema+row readers share one staged copy, audit MED-P2) + `--stage-dir` relocation | n/a | n/a | n/a | n/a | n/a | n/a |
| 27. `LOAD DATA` segmentation + its `--max-buffer-bytes` coupling (item 114, 2026-08-05) | ✅ MySQL-target, and **only where the flavor declares `BulkLoadLoadDataInfile` — vanilla + MariaDB** (`mysql/flavor.go:125,262`). PlanetScale declares `BulkLoadBatchedInsert` (`:170`) and Vitess aliases onto PlanetScale (`flavor.go:100-102`), so neither is reached and neither is a gap — a different bulk mechanism, row 3. The table now loads as a SEQUENCE of bounded LOAD DATA statements (`defaultLoadDataSegmentBytes = 16 MiB`, `load_data_writer.go:861`) instead of one statement per table; the resilience half is row 17. Segmentation is UNCONDITIONAL here — one code path, deliberately unlike the PG COPY twin which gates its chunked form on `growGate != nil` — so a future caller that forgets to wire the gate cannot silently inherit the un-resumable form, and there is only one encoder to keep byte-identical. **The operator-visible part is the COUPLING, and it is the reason this row exists:** `loadDataSegmentTarget` (`:254-259`) lowers the segment budget to `--max-buffer-bytes` whenever that is smaller, so **lowering `--max-buffer-bytes` for MEMORY reasons silently buys a bulk-copy slowdown**. Priced linearly in segment count, because an extra segment costs exactly one extra target COMMIT and nothing else: over a 7.2 MiB corpus, 8 MiB budget 0.99×, 4 MiB 1.09×, 2 MiB 1.19×, 1 MiB 1.46×, 256 KiB 2.59×. **That measurement is REPRODUCIBLE IN-REPO** — `TestLoadDataSegments_ThroughputCost` (`row_writer_loaddata_resume_integration_test.go:565`, `integration` tag, real MySQL 8.0) — and it pins the MODEL rather than a wall-clock ratio, because an absolute A-then-B comparison reported a fictitious 2.2× regression that was pure InnoDB buffer-pool ordering. At the shipped 16 MiB the copy is within noise of the pre-item-114 single-statement form, and still commits ~16× less often than the batched-INSERT core this writer falls back to. Memory is the genuinely new cost and the binding constraint: one segment's bytes are held per CONCURRENT WRITER (`--table-parallelism × --bulk-parallelism`, auto 4 × min(8,NumCPU)) where the streaming pre-item-114 form held 64 KiB | ✅ inherits — the sync cold-start funnels through the same `RowWriter` | n/a (backup writes land on the blob store) | ✅ MySQL-target restore: same `WriteRows` | inherits via RST | inherits via its reset-leg | n/a — the CDC applier uses the batched/multi-row cores, never LOAD DATA |
| 28. Lane `RouteScope` (item 131, 2026-08-05 — a DEFAULT-ON change to concurrent-apply throughput, in both directions) | n/a | n/a | n/a | via broker plumb (row 9) | ✅ inherits — the lane pool is the engines' `ChangeApplier`, one implementation per engine | ✅ same | ✅ `laneapply.Router.LaneForRoute` (`internal/laneapply/router.go:96`) is now the SINGLE place a route becomes a lane, and what the hash covers is the route's `Scope`. `RouteScopeKey` (hash = table + PK, distinct rows spread across lanes) is taken ONLY for a table an engine has PROVEN carries no non-PK uniqueness constraint; everything else — including a probe that FAILED — takes the zero-value `RouteScopeTable` and every change on that table lands on one in-order lane. Fail-closed by zero value is deliberate (`router.go:41-48`): a fail-OPEN default would silently re-arm the loss item 131 fixed, and that loss is invisible because both lanes report success. Implementors: MySQL `RouteForChange` + `tableHasNonPKUniqueIndex` (`change_applier_concurrent.go:207,249`, `information_schema` probe, covers vanilla/PlanetScale/Vitess — one applier, three registrations), PG (`change_applier_concurrent.go:245,314`, `pg_index` `indisunique AND NOT indisprimary` OR `pg_constraint contype='x'`), pgtrigger by inheritance; sqlite/D1/trigger/flat-file engines EXEMPT (`OpenChangeApplier` returns `ErrNotImplemented` — no apply path to route). Probe failure is cached-never, WARNed once per table (`mysql:132-135`, `pg:148-151`) so the cliff is visible — see gap 15. **THROUGHPUT DIRECTION, and this is deliberately NOT presented as measured-in-repo:** the roadmap/ADR-0105 §Cost record ~2.6× slower on a single unique-carrying table (8k inserts, W=4) and ~6× FASTER at 4 tables × 500 rows — the reversal being whole-table routing improving ADR-0140 coalescing locality — with 8 and 32 tables within ±10%. **There is NO in-tree benchmark for any of those numbers.** `internal/laneapply` and both engines' item-131 tests are correctness gates (`TestLaneRouteRoster_SecondaryUniqueTablesAreSingleLane`, `TestLaneForRoute_ScopeDecidesTheHash`), and the one throughput test in the neighbourhood (`pg/change_applier_throughput_integration_test.go`) measures ADR-0092 pipelining, not scope. So the honest cell is the DIRECTION — slower on a hot unique-carrying single table, faster on a many-table schema, table-count-dependent — and the figures are ADR prose that a reader cannot reproduce from this repo |
| 29. Bounded smart-compaction output (item 130, 2026-08-05) — and `backup compact` declared as a MODE | n/a | n/a | ✅ (the closest column; `backup compact` is not one of the seven modes above, and that invisibility is what rows 21–24 exist to prevent). `applySmartCompactionToIncremental` (`chain_compact_smart.go:1499`) is the ONLY path that re-encodes change-chunk bytes; the naive path moves chunks verbatim via `copyFile` (`chain_compact.go:997,1036`) and `backup prune` rewrites no bytes, so neither accumulates an emitted stream — exempt for that reason, not for lack of looking. Item 127 capped what the compactor RETAINS (`defaultMaxRetainedBytes = 32 MiB` of ESTIMATED WIRE bytes — explicitly NOT a heap budget; live heap for the same events is a multiple, `chain_compact_smart.go:563-591`). Item 130 capped what it EMITS: output distributes across the incremental's existing chunk slots under a `DefaultBackupChunkBytes` budget (the budget parameter at `:1526`, its default at `:1538-1540`; the streaming sink that spends it at `:1722` `chunkStreamSink`) instead of packing slot 0, so the rewritten chunk is no longer one unbounded blob however many chunks the incremental had. The read/write collision is resolved with a WRITE-BEHIND CURSOR — output slot *i* is written only once input chunk *i* has been fully read and closed — which is what makes the in-place rewrite safe and is itself gated (`TestSmartCompactOutput_NeverWritesAChunkItHasNotRead`, backed by a store that refuses a Put to a path with an open read handle). **The bound is stated honestly rather than rounded to the budget:** a full slot cannot flush early, so the peak is `max(budget, largest input chunk)` (`:1668-1700`), and the degraded case is TODAY'S NORMAL case — the tempting "recent binaries cap a change chunk at 64 MiB so I ≤ B" was ground-truthed FALSE twice (the ceiling was unreleased, and it had reached one of the two writers), which is row 30. Measured ceiling 65,890 B against 1,378,986 B unbounded on the same corpus (20.9×), plaintext AND encrypted, on a real accounting hook rather than `runtime.MemStats` (`TestSmartCompactOutput_PeakChunkBufferStaysUnderTheBudget`). **Compaction is FULLY SERIAL** — no errgroup, no worker pool, in either the smart or the naive path — declared in the same shape row 21 declares the parquet exporter's, so the absence is on the record rather than implied; parallelize on evidence | n/a | n/a | n/a | n/a |
| 30. Change-chunk byte ceiling (audit 2026-08-05 C-3 + its sibling fix) | n/a | n/a | ✅ **BOTH change-chunk writers now roll on bytes as well as event count, against the same `DefaultBackupChunkBytes = 64 MiB`** (`backup/backup.go:118`) the data lane uses: the incremental capture loop (`internal/pipeline/incremental.go:1284-1285`) and the continuous `backup stream run` rollover (`internal/pipeline/stream.go:1900-1901`). The second is the sibling the C-3 fix missed — its own commit enumerated "the change lane" as covered and covered ONE of that lane's two writers, which is the exact shape the sibling-sweep step exists for, and the missed one writes MOST change chunks in practice. `RolloverMaxBytes` is NOT this ceiling: it bounds a whole rollover, counts COMPRESSED bytes, and only closes on a transaction boundary. The ceiling counts UNCOMPRESSED bytes for the same reason the data lane does — chunk boundaries feed the content-addressed same-path upload skip, so where a chunk ends must not depend on how well it compressed. This is why gap 16 is re-scoped below: it was closed for the DATA lane only | n/a | n/a | n/a | n/a |
| 31. `backup verify` (incl. `--depth read`, item 129, 2026-08-05) | n/a | n/a | ✅ (closest column; the command is not one of the seven modes). **It earns a row** — the alternative was leaving a shipped command whose new depth is read-amplifying unmentioned, which is the row-21 lesson. `--depth read` streams every chunk through its own real `blobcodec.ChunkReader` / `ChangeChunkReader` with the exact (CEK, AAD, codec) triple `restore` resolves, and discards the rows: **one full read + decrypt + decompress + parse per chunk** (`verify_read_depth.go`, cost stated at `:83-88`). The zero value is `VerifyDepthHash` and that is load-bearing — the CLI sets the field and every other construction gets the Go zero value, so a depth whose zero value meant "read" would make every non-CLI verify pay a full read (the v0.99.51 trap, inverted). **The verify scan is FULLY SERIAL** — `verifyBackupScan` (`backup/restore.go:1742`) is a plain `for _, rec := range records` with no errgroup anywhere in the file's verify path — while backup (row 1, `--table-parallelism` 0→4 × `--bulk-parallelism`) and restore (row 1 + the ADR-0115 headroom clamp) both pool. DECLARED absence rather than a filed gap, and the reason is the same one that makes the depth non-default: hash-depth is what a cron probe runs against object storage, `--depth read` is what you run before you rely on the backup, and neither shape has produced evidence that the wall-clock matters yet. If a large-chain `--depth read` becomes routine, this is the cell to revisit — the work is embarrassingly parallel per chunk. Worth pairing with the 2026-08-01 rule: this depth exists BECAUSE the hash depth's evidence was the same artifact it was checking, and the independent evidence (does the chunk's own reader read it?) was cheaply available and unused — Bug 226 is what that cost | n/a | n/a | n/a | n/a |

## Ranked gap list (accidental asymmetries; see docs/research/perf-gap-analysis-2026-07.md for full detail)

1. ~~**Backup within-table read parallelism ABSENT** (`backup.go:1088`) — one huge table backs up single-stream while migrate chunks it and restore fans the writes.~~ **CLOSED 2026-07 for the PG lazy-snapshot cell (ADR-0149):** eligible tables split into PK ranges read off the SAME exported snapshot via the pool's importer factory, budget-gated at `--table-parallelism × --bulk-parallelism` with tail work-stealing; incl. the 59c55e27 estimator fix on the backup snapshot primary + importer-minted readers (`ir.ExactCountEstimateOptIn`). Residual engine cells (MySQL-eager, non-snapshot fallback, SQLite, VStream) are cited deliberate absences, not accidental gaps — see the ADR's coverage declaration.
2. ~~**Restore/chain-restore/broker channels unbuffered** vs migrate's 64 everywhere.~~ **CLOSED 2026-07 (replay-path hardening chunk):** all three hops now carry the shared `rowChanBuffer` (64) — `restoreChunkGroup`'s rowCh plus both `applyIncremental` changesCh hops.
3. **Sync cold-start parallelism defaults PG-only** — ~~MySQL-binlog defaults serial where migrate defaults 4~~ **RESOLVED (binlog leg) by the gap-3 defaults chunk**: `defaultNativeCopyTableParallelism` = auto min(4, tables) (consistency-identical FTWRL single-cut, no cross-process resume to destabilize; serial opt-out `=1`, FTWRL-denied → loud serial fallback). The VStream leg was surveyed and **reclassified DELIBERATE** (see the deliberate-absences list: un-persisted K would silently re-partition a durable-watermark resume across the upgrade boundary — ADR-0099 §5 silent-loss class); a sequential multi-table VStream cold-copy now INFO-names the knob. Residual: trigger flavors have no parallel path (single-conn snapshot consistency; INFO added) — L effort, separate item.
4. ~~**No chunk prefetch in chain-replay** — fetch N+1 never overlaps apply N.~~ **CLOSED 2026-07 (replay-path hardening chunk):** `streamIncrementalChanges` now runs a single-chunk read-ahead fetcher (fetch+SHA-verify N+1 while N applies; unbuffered handoff = exactly one ahead, never an N-deep pipeline; ADR-0117 per-chunk fetch retry unchanged). Deliberately NOT extended to the broker's `streamIncrementalWithPosition` copy in the same pass — its dominant cadence is the 30s poll tick on small tails; revisit on evidence.
5. **Native-MySQL snapshot cross-process resume deferred** (in-memory cursors only). MEDIUM, M.
6. ~~**Redact/shard-stamp tees unbuffered** on the migrate hot path when engaged.~~ **CLOSED** — both tees carry the standard `rowChanBuffer` (`redact.go:59`, `shard_value_wrap.go:68`; pinned by `TestRedactRows_BufferedRelay` / `TestShardStampRows_BufferedRelay`).
7. **ADR-0097 write fan-out PG-target ABSENT** (MySQL-target only). MEDIUM, M.
8. **Backup lacks restore's live headroom clamp** (ADR-0115 asymmetry). LOW-MED, S–M.
9. **zstd level fixed** (32% of backup CPU). MEDIUM at scale, S knob / M parallel.
10. ~~**MySQL apply lanes fixed-4, budget flag inert** vs PG slot-probe.~~ **RETIRED 2026-08-04 — this gap never existed as filed.** MySQL implements `TargetConnectionBudgetProber` (compile-asserted, `mysql/capabilities_assert.go:48`), landed `9c042c10` in v0.100.0 — *ten days before this matrix was ground-truthed*, so the entry was stale at filing. Both halves were wrong: lanes are `min(4, EffectiveParallelism)` on MySQL exactly as on PG, and `--max-target-connections` is folded into the probe (`connection_budget.go:317`). Derived by asserting over the live engine registry rather than reading the in-code comment — which is the actual root cause: `streamer_apply_concurrency.go:43-48,91-94` still describe the `!ok` branch as "today: MySQL / PlanetScale-MySQL", and that branch is DEAD for every shipped engine. **Replaced by gap 15** (the behaviour the false gap concealed).
15. **A failed probe silently drops concurrent CDC apply to one lane — and there are now TWO of them, on different scopes.** RE-SCOPED 2026-08-05: as filed this described one cliff, and item 131 added a second that is likelier to fire. (a) **Connection-budget probe, RUN-scoped** (`streamer_apply_concurrency.go:116-128`): a probe that fails or refuses degrades the whole stream's apply to SERIAL (1 lane). Deliberately not a refusal — the cold-start preflight owns the loud one, and a warm resume into a tight target should still run — but it is a 4×-to-1× cliff reported only at DEBUG. (b) **Non-PK-unique-index probe, TABLE-scoped and fail-closed** (`mysql/change_applier_concurrent.go:249`, `postgres/change_applier_concurrent.go:314`): a catalog probe that errors returns "has a unique index", so that table takes `RouteScopeTable` and every change on it lands on ONE in-order lane. Fail-closed is CORRECT and must not change — returning false would silently re-arm item 131's row loss, which is invisible because every lane reports success — and unlike (a) it already WARNs once per table (`mysql:132-135`, `pg:148-151`) and is not cached, so a later probe corrects it. But (b) fires per-table on an ordinary transient rather than needing a constrained target, it fires on the DEFAULT path (concurrent apply is default-on at W=4), and its cost is table-shape-dependent in BOTH directions (row 28) rather than uniformly 4×→1×. Neither cliff has a metric. LOW-MED, S (raise (a) to INFO; give both a counter so "why did apply slow down" is answerable from telemetry rather than a DEBUG log).
16. ~~**The backup chunk writer has NO byte ceiling**~~ **CLOSED 2026-08-04 (item 116 P3) for the DATA lane — and the "no uncapped buffering path remains" claim was WRONG when written, closed for the CHANGE lane on 2026-08-05 (re-scoped here rather than left standing).** The sentence below said "the backup WRITE side is no longer the one buffering path without a byte cap"; the backup write side has THREE chunk writers, and P3 reached one. Audit C-3 added the ceiling to `internal/pipeline/incremental.go:1284-1285`, and its own commit then enumerated "the change lane" as covered while `internal/pipeline/stream.go`'s `changeChunkBuffer.processChange` — the continuous `backup stream run` rollover, which writes MOST change chunks — still rolled on event count alone until `100c5a9b` added `stream.go:1900-1901`. Two successive fixes each claimed a lane and covered a writer. See row 30; the closure below is the data lane's. Original P3 closure: `DefaultBackupChunkBytes = 64 << 20` now rolls the chunk beside the row cap (`backup_parallel.go:560`), matching `pgCopyChunkBytes`. The ceiling counts UNCOMPRESSED bytes deliberately: chunk boundaries feed the content-addressed same-path upload skip, so where a chunk ends must not depend on how well it compressed (pinned by `TestChunkByteCeiling_BoundariesAreCodecIndependent` + `_RerunSkipsReuploadingIdenticalChunks`). Resume is unaffected — it works at TABLE granularity (Bug 135), so nothing keys on a boundary. Original finding: — the chunk streamer rolled on `RowCount() >= chunkRows` and nothing else, while the chunk accumulates in memory. It is the ONLY buffering path in the tree without a byte cap beside its row cap (cf. `ir.MaxBufferBytesSetter` on both row writers, `ByteCap` in `appliershared/batch_loop.go:169`, `pgCopyChunkBytes`, and sluice's own downstream parquet exporter at `export_parquet.go:85` `128<<20` — row 21 records THAT byte-roll as a positive while the writer feeding it has none). Measured in item 116 P3 (committed floor, now `TestChunkRoll_MeasuresRowWidth`): at 500 rows, widening 64 B → 64 KiB grows the buffered chunk 50 KB → 32.8 MB (644×); extrapolated to the shipped `DefaultBackupChunkRows = 100_000`, ~6.1 GiB for one chunk. Exactly the v0.109.1 field shape (wide `mediumtext`/`json`) — the VStream READ side got its 128 MiB ceiling there; the backup WRITE side buffering the same rows did not. MEDIUM, S.
17. **Cloud-store manifest checkpoint amplification** — promoted out of "deliberate absence (named wart)", because the measurement does not match the framing. `manifestCommitter.appendChunk` full-rewrites unless the store is an `irbackup.Appender`, and `blobcodec.LocalStore` is its ONLY implementor, so S3/GCS/Azure all take the slow path: 50 tables × 8 chunks = 451 Puts / 50.4 MB legacy vs 1 Put / 74 KB sidecar, scaling with chunk count AND table count (4× tables → 15.7× bytes; item 116 P4). "Wart" reads as a rounding error; this is the checkpoint cost every cloud backup pays. Note the gate is narrower than its name: `TestManifestCommitter_SidecarCheckpointCost_10kTables` pins the FAST path only. MEDIUM, M.
18. **PG raw-COPY has no resume point and is filed nowhere — RE-PRICED 2026-08-05, because the comparison the original price rested on no longer exists.** As filed this was "the unfiled twin of MySQL `LOAD DATA`, which is roadmap item 114", i.e. one of two peers where one happened to have a ticket. Item 114 has SHIPPED (`a3f15405`): `LOAD DATA` is segmented and every segment rides `flushWithReparentRetry`. `raw_copy.go` did not change in that delta and its retry story is unchanged, so **PG raw-COPY is now the ONLY write lane in the tree that cannot ride a transient at all** — an outlier rather than half of a pair, and the in-code comment at `raw_copy.go:212-213` still asserting the shared property is now false. That is a straight upgrade in priority, not a rewording: the lane is the DEFAULT for a same-engine no-transform PG→PG copy, its unprotected legs are the chunked one (`migrate_parallel.go:1385`, replays a chunk from zero because a raw chunk records no cursor until terminal) and sync cold-start (`streamer_coldstart_parallel.go:325`, no breadcrumb at all), and item 114's own fix shape is the obvious template — segment the pipe and hold one segment's bytes so there is something to re-drive. Note the two are NOT identical: `LOAD DATA LOCAL`'s duplicate-key-becomes-a-warning behaviour is what makes its replay convergent, and PG COPY has no such property, so a raw replay needs either a segment-boundary cursor or the same keyless-style refusal (row 17). MEDIUM (was LOW-MED), M.
19. **Restore's grow gate is signal-only on a false premise** — `restore.go:543` cites "matching the migrate cold-copy", but migrate is signal-only because it *cannot* be proactive (no telemetry seam) while `Restore` holds one (`restore.go:155`) and already samples it per tick (`restore_table_pool.go:307`). See row 17. MEDIUM, S.
11. **MySQL bulk-session tuning never evaluated** (`unique_checks=0`, `sql_log_bin=0`). Unknown gain (ADR-0042 found InnoDB floor), S to trial.
12. **ADR-0148 index-build fallback unarmed on the fleet `sync run` specs and the `sync from-backup` broker reset-leg** (audit MED-A1 residue). ✅ CLOSED 2026-07-17. Fleet `sync run` now carries the per-sync `planetscale-*` YAML keys (service token env-first; the shared `planetscale-org` reconciled with the ADR-0126 telemetry opt-in — a fallback-only arming turns telemetry off with a WARN, never trips the all-or-nothing refusal); `SyncSpec.resolveIndexFallback` → `buildStreamerFromSpec` populates `Streamer.IndexBuildFallback`. The `sync from-backup` broker carries restore's `--planetscale-*` flag set (org explicit-only, no telemetry surface) → `SyncFromBackup.IndexBuildFallback` → `newColdStartChainRestore` threads it onto the `--reset-target-data` segment-0 full. No unarmed sibling remains for row 20.
13. ~~**Flat-file/mydumper sources never verified against the migrate cross-table pool** (row 24) — row 1's "all engines" predates them; their within-table absence is deliberate (no PK addressing) but the cross-table behaviour is unpinned.~~ **CLOSED 2026-07-17 (assess-first chunk):** the cross-table pool is SOURCE-AGNOSTIC — it opens a dedicated source reader per concurrent table via the engine's `OpenRowReader` (`migrate_table_pool.go:233` `openTablePair` → `openChunkReader`/`openOneChunkConn`, `migrate_parallel.go:775,798`, → `deps.source.OpenRowReader`), so a multi-table mydumper dump copies through the same bounded pool the DB engines use, no flat-file gating. Pinned by `TestMydumperMigrate_CrossTablePool_PG` (mydumper→PG, a many-table/many-chunk-file synthetic dump migrated at `--table-parallelism 4` — resolved `table_parallelism=4` confirmed — then at 1, byte-identical rows both legs: parallelism changes throughput, never the data). The pool bound itself stays source-agnostically unit-pinned (`TestRunBulkCopyTablePool_PeakBoundedByParallelism`). WITHIN-table parallelism remains a deliberate absence for these sources (dump chunk files carry no PK addressing; the pipeline-integrated multi-writer form would need a new file-partition work-item surface + per-partition resume — a new orchestrator surface, not a clean chunk) — see the deliberate-absences list and row 24.
14. **Filtered VStream warm-resume** (audit 2026-07-18 F-P1; row 25) — **CLOSED by Batch B (v0.99.281, `6e1c4473`).** ADR-0174 Piece 2's server-side `Filter.Rules` WHERE push-down originally landed on the VStream COLD-START copy ONLY, so a warm resume / crash-restart re-opened the `vstreamCDCReader` WITHOUT the row-filter rules and pulled 100% of the keyspace change traffic, discarding ~99% client-side in `route()`. Batch B threads the `--where` rules into the warm-resume VStream open (`vstreamCDCTailFilterRules` + `SetServerSideRowFilters`, `streamer_warm_resume.go`), so a resumed filtered stream is server-side-filtered too. Correctness was preserved throughout (the client-side eval is faithful); the fix removes the steady-state restart cost. **Caveat (v0.99.282 A0 → #66 client-side fallback):** the VStream server-side filter is NO-PAD, so a `--where` on a PAD-SPACE-collation string column can't be reduced faithfully at the source. sluice streams those tables UNFILTERED server-side and filters them client-side with the PAD-faithful comparator — correctness is preserved on both legs; the perf trade is more wire traffic for that one table (it isn't source-reduced). See row 25.

## Deliberate absences (cited — do not "fix" without revisiting the rationale)

VStream cold-copy default K=1 (gap-3 survey: K is NOT persisted in the position token, so a default flip silently re-derives a DIFFERENT table→stream partition for any durable-watermark job resumed across the upgrade boundary — missed/double-copied in-progress table, the ADR-0099 §5 silent-loss class; plus the set-min-stitch divergence-refusal class and the `--max-buffer-bytes`/K per-stream split a surprise K>1 would spring on every cold start. The sequential path INFO-names `--vstream-copy-table-parallelism`) · trigger-flavor serial cold-start (single-conn snapshot/anchor consistency argument; INFO added in pgtrigger + sqlite-trigger/D1 openers; parallelizing is the gap-3 residual L item) · olap off chunked reads (Bug 132) · PS/Vitess CONCURRENT index-overlap decline (ADR-0080; the serial build-as-copied overlap ships since v0.99.200 — only concurrent ALTERs against vtgate stay declined) · MySQL no pgx.Batch pipelining (coalescing ADR-0139/0140 substitutes) · backup partial-table resume-from-scratch (Bug 135) · sync cold-start import readers decline the exact-count never-ANALYZEd estimate fallback (`ir.ExactCountEstimateOptIn` never applied in `postgres/snapshot_importer.go` minting — the ADR-0079 v1.1 cost decision, reaffirmed ADR-0149 §6 and pinned by `TestExportSnapshot_EstimateRowCount_NeverAnalyzed`; consequence: a fresh never-ANALYZEd PG source cold-starts single-stream where migrate/backup chunk) · backup within-table chunking limited to the LAZY-importer snapshot path (ADR-0149: MySQL-eager FTWRL readers are a fixed supply with no importer to mint range readers — the idle-reader-stealing alternative is a rejected-for-now in the ADR; the non-snapshot fallback has no consistent multi-reader view; SQLite has no importer surface; PlanetScale/Vitess backup rides VStream-COPY where read-side chunking is vtgate-internal — one INFO names the engaged/not-engaged mode per run) · ~~object-store O(tables) manifest rewrites (named wart)~~ **PROMOTED to ranked gap 17 (2026-08-04)** — measured at 451 Puts / 50.4 MB vs 1 Put / 74 KB, which "wart" does not describe · sqlite/D1 not apply targets · D1 within-table chunking deferred (`d1_rows.go:54`) · flat-file WITHIN-table parallelism (mydumper ADR-0161 / csv-tsv-ndjson ADR-0163): the mydumper reader implements NONE of the batched/bounds surfaces (`mydumper/capabilities_assert.go` — dump chunk files carry no PK addressing), and a single csv/tsv/ndjson file is a sequential byte stream (RFC-4180 quoted newlines forbid byte-splitting; staged read-back is a local SQLite scan), so the DB engines' PK-range within-table chunker never engages; the pipeline-integrated multi-writer form would need a NEW file-partition work-item surface + per-partition resume, beyond a clean chunk — the CROSS-table pool (row 1, gap #13 CLOSED 2026-07-17) already delivers the multi-file parallelism win for the common multi-table dump · raw-copy PG→PG-only gates (ADR-0078 "widen on evidence") · **copy-gate ADDITIVE INCREASE absent on backup's within-table chunk acquires** (`backup_parallel.go:340,343` takes/returns a token but never calls `NoteOpenSucceeded`) — cited at `backup_table_pool.go:212-217`: nothing on the backup READ side calls `ShrinkAndBackoff`, so the cap never shrinks and an increase would be a no-op; the BASE-token class is declared there anyway, deliberately, as the class rather than the reachable instance · **no copy gate at all on the serial sync cold-start** (`coldStartRunCopy`, `streamer_coldstart.go:960`, goes through `bulkCopyOpts` and never `runBulkCopyPhases`) — there is no parallelism for a gate to govern; named at `migrate_table_pool.go:132-134` · **no `CopyParallelismGate` on restore** — the writer pool has no 53300 classifier to shrink on, and ADR-0115's live headroom clamp is the pressure response it does have (row 11 RST) · **smart/naive chain compaction and `backup verify` are fully serial** (rows 29 and 31) — neither has a worker pool of any kind while backup and restore both do; declared so the absence is a decision on the record, and both say "parallelize on evidence" · **`LOAD DATA` segmentation reaches vanilla + MariaDB only** — PlanetScale declares `BulkLoadBatchedInsert` and Vitess aliases onto it (`mysql/flavor.go:100-102,170`), so those flavors take a different bulk mechanism entirely rather than an unsegmented LOAD DATA (row 27; a capability difference, not a gap).
