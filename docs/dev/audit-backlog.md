# Audit backlog — the findings that are NOT roadmap items

Findings from the periodic blind audits that are real, confirmed, and **too small or too cross-cutting to earn their own roadmap item** — but not so small that losing them is free.

## Why this file exists

The audit reports live in `workspace/`, which is **gitignored** (`.gitignore:99`). Every audit since 2026-08-01 has recorded its MEDIUMs, LOWs and gate proposals there and nowhere else, so the entire backlog existed on exactly one machine with no copy. The 2026-08-05 audit flagged this as a finding in its own right — and it had already caused two concrete errors:

- the "nine of ten §4 gate proposals are open" figure was repeated as fact in the roadmap **and** in project memory for four days; the true count, re-derived from the code, is **6 built / 2 partial / 2 open**;
- the 08-04 MEDIUMs were attributed to the 08-01 audit in the roadmap paragraph that was supposed to be tracking them.

Both are the doc-lags-code shape the working agreements name. A note *about* backlog durability rots exactly like anything else does, so the durable copy is here, in git.

**Scope rule:** anything with a confirmed silent-loss consequence becomes a numbered roadmap item, not a line in this file. This is for the tier below that.

**Staleness caveat (2026-08-18 triage).** A ground-truth pass over the un-struck entries found the "open" section is itself doc-lags-code: EVERY high-value candidate filed before ~2026-08-13 that was checked had already been fixed in code and never struck here (B-2c, D-1/2/3, Bug 239, the Bug 244 restore sibling, C1-1's SQLite/D1 lane — all now struck above with their code proof). Reassuringly, that pass found **zero still-open silent-loss items**. But the lesson is the project's own rule turned on this file: **before executing any un-struck entry older than 2026-08-13, ground-truth it against the code — the backlog text is not reliable for pre-08-13 entries.** The genuinely-open work concentrates in the freshest (2026-08-17 Tier-3) section plus the design-gated / needs-infra items.

## Region-move findings (2026-08-18, from a real PlanetScale us-east-1→us-east-2 move)

- **FK-GAP-1 — a safe-migrations-ON PlanetScale target cannot get sluice-created foreign keys today (loud, no silent loss).** Confirmed while building the v0.129.0 FK pre-flight. sluice's FK constraints phase is direct DDL: the validating `ADD FOREIGN KEY` and the item-109 metadata-only escape (`addForeignKeyMetadataOnly`, `SET foreign_key_checks=0` + direct `ALTER`) both go straight to the server. PlanetScale **safe-migrations** blocks direct DDL (errno 1105) — and `isConstraintBuildWalled` (`schema_writer_constraint_fk_wall.go`) explicitly EXCLUDES 1105 (a metadata-only re-issue cannot make a direct-DDL-blocked ALTER succeed), while there is **no deploy-request fallback for FK constraints** the way ADR-0148 provides for indexes (Vitess Online DDL refuses FK-participating tables without the experimental `--unsafe-allow-foreign-keys`). So on safe-migrations-ON, both paths hit 1105 at the constraints phase and `--resume` re-hits it deterministically. **Mitigated (not fixed) in v0.129.0** by the FK pre-flight, which WARNs before the copy (enabled+safe-migrations-ON) to use `--skip-foreign-keys` or disable safe migrations for the window. **Fix shape (feature):** an FK-constraint deploy-request path (Vitess Online DDL with `--unsafe-allow-foreign-keys`, gated + opt-in), OR keep the WARN + document the limitation. Not the failing user's issue (theirs was safe-migrations OFF), but a real gap for the common production-branch case.
- **INDEX-SPLIT-STALE-STATS-SIBLING — the 64 GiB FK-shortcut probe (`indexFallbackTableClearlyHuge`) shares the stale-target-stats weakness that ADR-0184's index-split fix just corrected.** It reads the freshly-copied TARGET's `DATA_LENGTH`, which is stale/near-zero on PlanetScale right after the copy (measured: 16 KB for a 36 MB table), so the FK metadata-only shortcut never engages there either. LOWER consequence than the index-split bug (a mis-route only costs one doomed direct FK-ADD attempt, which the wall-recovery then handles), so filed not fixed. Fix shape: give it the same source-size hint the index-split now uses (`ir.TableByteSizeEstimator`). Noted at the constant's definition.
- **TINYINT1-VSTREAM-OVERRIDE — make `--type-override <col>=smallint` actually preserve the integer on a PlanetScale/Vitess (VStream) source (enhancement, no silent loss — the refusal already prevents that).** The TINYINT(1)-out-of-range refusal (`SLUICE-E-VALUE-TINYINT1-RANGE`, shipped after the field report of 0..6 values collapsed to 1) halts on every read path, so no data is lost. But `--type-override` only re-types the bulk-copy / binlog paths (which decode off the IR schema); `decodeVStreamCell` keys the boolean decision off the replication wire's own `column_type` (`isMySQLBoolColumnType(field.GetColumnType())`), so an override there re-types the target column while the value is still collapsed to bool at read time. Today the only VStream remedy is changing the source column's type (the reported user did exactly this — `TINYINT(1)`→`SMALLINT`), which the refusal's hint leads with. **Fix shape:** thread the overridden IR column types (or a per-table "these columns are NOT boolean" set) into `decodeVStreamRow`/`decodeVStreamCell` so an int-override suppresses the wire-driven boolification and returns the raw integer. Note: this is the same threading pattern that carried the old `boolWarn` (removed when the refusal landed), so the plumbing precedent exists. Until then the VStream `--type-override` path is documented as inapplicable (value-types.md, the error-code remedy).

## 2026-08-27 — post-v0.132.0 blind audit (6 workers + reconciler + adversarial verify; report `workspace/repo-audit-2026-08-27.md`)

**Grade A−** (silent-loss B, five dimensions A−). Reconciler scorecard on 2026-08-26: **12 FIXED / 1 PARTIAL / 5 OPEN / 0 REGRESSED** (every OPEN a knowingly-filed residual). Promoted light area: the redact CLI-parse/selector surface. Two HIGH claims adversarially verified: A1 upheld at HIGH, A2 upheld but corrected to MEDIUM (blind convergence with the pre-tag VF finding). Remediation state:

- **A1 (HIGH, observed, upheld) — G5 door blind to MariaDB named-connection replicas.** Bare `SHOW REPLICA STATUS` returns 0 rows for `CHANGE MASTER 'conn1' TO …` multi-source replicas (observed mariadb:11.4, verifier re-confirmed on a fresh pair with a RUNNING named connection); MariaDB defaults `log_slave_updates=0` → the exact G5 silent-loss shape passes the preflight. Fix: add `SHOW ALL REPLICAS/SLAVES STATUS` spellings (`cdc_replica_source_preflight.go:150-161`) + named-connection unit fake + integration pin. → remediation batch.
- **A2 (MEDIUM) — array/interval typmod rewrites pass silently under DEFAULT forward mode** (`passesUnderSchemaForward` waves through what the classifier caught; projected signature unmoved → no boundary/WARN). Product fix = the filed TYPMOD-PROJECTION-GATE as code: refuse under forward when Kind==AlterColumnType ∧ projected signatures equal (~10 lines) + per-family signature-movement enumeration + one varchar forward-convergence case (subsumes FORWARD-CONVERGENCE-SECOND-FAMILY). — **BUILT (remediation batch, 2026-08-27):** `unforwardableTypmodColumn` in `checkSchemaRace` refuses per changed COLUMN (not the audit's table-level signature sketch — a mixed multi-column ALTER would ride a sibling's moved signature past a table-level compare), catching interval(p), array-element typmods, AND projection-identical OID swaps (time↔timetz); temporal-collapse members keep forwarding (their RAW projection moves, so a boundary emits — pinned). Policy pins `TestCheckSchemaRace_UnforwardableTypmod`; enumeration gate `TestTypmodProjectionGate_EveryTypmodFamily` (pg_type.typmodin-derived scalar roster + derived array roster, anti-vacuity floors, mutation-run both directions); ADR-0091 impl note + matrix G3 rows updated; varchar forward-convergence composition case in the same batch (A9).
- **A3 (MEDIUM, two workers converged) — same-key heal launders a tampered catalog + destroys the forensic sig.** Fix: preserve `lineage.json.sig.pre-heal-<ts>` before ResignLineage + durable heal record + presence pin (`signature_verify.go:455-507`). — **BUILT (remediation batch, 2026-08-27):** the heal preserves the non-verifying `lineage.json.sig` verbatim (`lineage.json.sig.pre-heal-<unixnano>`, collision-bumped — never overwrites prior evidence) and appends a `maintenance-heal.log` JSONL record (timestamp/op/KeyID/verify_failure/preserved-path), both BEFORE `ResignLineage` and both fail-closed; `backup verify` surfaces every heal record as an informational line (never a failure — a healed chain's signatures all verify, so the record is the only post-hoc signal). Wrong-key guard untouched (refusal precedes the evidence writes; pinned no-evidence-on-refusal). Pins: `TestMaintenanceHeal_PreservesForensicEvidence` (verbatim bytes + append-across-two-heals via both verbs), `TestMaintenanceHeal_BoundariesLeaveNoEvidence` (keyless/dry-run/healthy), `TestBackupVerify_SurfacesMaintenanceHeals` (verify-output pin both directions).
- **A4 (MEDIUM) — STATEMENT-DML refusal echoes up to 160 B of raw DML/PII into error/logs** (`cdc_statement_dml.go:168-196`, first payload-echo site). Fix: truncate at first literal / hash tail; file the sanctioned-echo AST roster (errclassgate pattern). → remediation batch.
- **A5 (MEDIUM) — pgtrigger open-path probes unbounded; relay probe reads a lockable user table** → a queued ALTER/VACUUM FULL on `sluice_cdc_state` hangs every CDC open (`preflight_replica_role.go:152`; MySQL probes got 30s, P3 got 15s, pgtrigger got none). Fix: 15–30s caps ×3 doors + probe-timeout roster test. → remediation batch.
- **A6 (MEDIUM, observed) — STATEMENT-DML remedy SQL hard-errors on stock MariaDB** (`performance_schema=OFF` → table ABSENT). Fix: precondition sentence in error-codes.md + docrows + cdc-streaming.md (+ site row). → remediation batch.
- **A7 (MEDIUM) — G2 misses the `schema add-table` sibling** (unlogged table added live to a spanning sync backfills then freezes; census predicate excludes live-added tables). Fix: census at add-table registration. → remediation batch.
- **A8 (MEDIUM) — G2's environmental premises unpinned** (FOR-ALL-TABLES silent exclusion; SET UNLOGGED asymmetry — observed-once, no named test; premise-naming rule). Fix: ~15 lines in the existing integration test → rides pg-version-matrix. → remediation batch.
- **LOW tail (13, filed not fixed):** statement-DML no-default-DB over-refusal twin; `splitDBList` ignore-arm comment; P3 gid/owner render; LocalStore stale doc line + crash-orphan `.tmp.*` accumulation (no sweeper, surfaces in List); vstream stop() latent double-close; freeze-pin shared-conn assumption; CDC-open roster hand-list + alias-invocation evasion; C-1 timing-assertion vacuity under load; dual scope spellings on Streamer; multi-db cold-start G9 census unscoped vs its own file's claim; belt DB-grained scope doc; pgtrigger double DDL-state read; capture-shape proname unqualified; matrix "no extra round-trip" sentence false for G6's failed-probe double; preflight worst-case timeout stacking (~120s/reconnect).

## 2026-08-26 — VF-review follow-ups on the M2 batch (filed pre-tag; verdict was SAFE TO TAG)

The pre-tag value-fidelity review of the v0.132.0 delta confirmed the G8 capture clean (decompression-before-decode verified against the vendored go-mysql source; the family dimension genuinely collapses to verb × size × header-shape, all pinned) and filed three follow-ups. The MEDIUM's claim-surface half was FIXED pre-tag (ADR-0091 impl note + matrix + notes now enumerate the detected-but-not-forwarded class; `interval`/`_numeric` cells added to `TestClassifyRelationChange_TypmodFamilies`, green); the remainder:

- **~~TYPMOD-PROJECTION-GATE (MEDIUM residual, the gate that would have caught it)~~ — BUILT (A2 remediation, 2026-08-27):** `TestTypmodProjectionGate_EveryTypmodFamily` enumerates every typmod-consuming family in `oidToType` (pg_type.typmodin-derived scalar roster + the array roster derived from `pgArrayElementOID`) and asserts, per family, that a typmod delta either MOVES `SchemaSignatureOf(projectRelation(...))` or the family is on the ADR-0091 documented refuse-under-both-modes list (`interval(p)`, array elements) — AND that `checkSchemaRace` actually refuses the listed members under forward (the A2 product fix). Note the filing's list named the temporal-collapse members; ground-truth showed their RAW projection moves (the collapse is the downstream normalizer lens), so they are boundary-emitting, not list members. Mutation-run both directions.
- **STATEMENT-DML-WITH (LOW):** add `WITH` to the tripwire's verb set (CTE-DML — a WITH-prefixed statement in a ROW-mode binlog is almost certainly statement-format CTE-DML, since SELECTs are never binlogged) + a unit cell both directions. Residue is documented in the belt's comment meanwhile.
- **FORWARD-CONVERGENCE-SECOND-FAMILY (LOW):** one more end-to-end forward-convergence integration case beyond numeric (varchar shrink) — the failure shape there is loud, so LOW.

## 2026-08-26 — M2 capture-completeness sweep (the audit's class-checklist follow-up, executed)

The M2 item below ("capture-completeness class checklist across ALL CDC engines") ran as a 3-investigator / 7-verifier / 1-critic workflow. Durable output: **`docs/dev/capture-completeness-matrix.md`** — 45 cells, every SILENT_GAP adversarially verified with re-run repros. Seven confirmed gaps (G1–G7, detail in the matrix); remediation state:

- **G5 (binlog: source-is-replica, `log_replica_updates=OFF`)** — **BUILT (MySQL-lane remediation batch, 2026-08-26):** `preflightReplicaSource` (`SLUICE-E-CDC-REPLICA-NO-LOG-UPDATES`) at all three CDC-open chokepoints, roster-gated (`TestCDCOpenPreflightRoster_EveryChokepointRunsAllPreflights`); refuses only the conjunction (replica ∧ log updates OFF; a chained replica with updates ON passes, unit-pinned); privilege-blocked status probe WARNs-and-passes (MariaDB 10.5+ `REPLICA MONITOR` split); real-server pin `TestCDCReader_ReplicaSourcePreflight`.
- **G6 (binlog: `binlog_ignore_db`/`do_db` filters)** — **BUILT (same batch):** dedicated `preflightBinlogDBFilter` (`SLUICE-E-CDC-BINLOG-DB-FILTERED`) beside `preflightBinlogFormat` (scanMasterStatus not overloaded), scoped to the synced databases (do-arm gets the concrete multi-db set via the new `ir.CDCDatabaseListSetter`, fail-closed without it; ignore-arm consulted only when the do-list is empty, the server's own rule); real-server pins `TestCDCReader_BinlogDBFilterPreflight_{IgnoreDB,DoDB}`. Residual filed, not built: a `backup full` chained from a filtered/blind-replica source WARNing at position capture (the chain's first incremental refuses at StreamChanges, so the gap is loud-but-late).
- **STATEMENT-DML tripwire (critic P2)** — **BUILT (same batch):** dispatch belt `statementDMLVerb` in the QueryEvent arm (`SLUICE-E-CDC-STATEMENT-DML`), scope-gated per Bug 246; stated residue: statement-format `LOAD DATA` + versioned-comment-wrapped DML; vstream sibling absorbed upstream (vendored vstreamer kills statement-category DML).
- **G2 (pgoutput: UNLOGGED under FOR ALL TABLES)** — REFUSE via `relpersistence='u'` census at spanning cold start; sibling: the backup `--chain-slot` FOR-ALL-TABLES path owes the same census. → remediation batch.
- **G3 (pgoutput: typmod-only rewrite invisible)** — add `TypeMod` to `classifyRelationChange` compare; same-type `USING` undetectable → documented; relfilenode-heuristic WARN filed as follow-up. → remediation batch (typmod half).
- **G1 (pgtrigger: polled-fingerprint tier has NO DDL detection, docs promise it)** — minimum WARN at CDC open + claim-surface fixes (Heroku recipe, setup mode string) → remediation batch. **Full fingerprint loop (sqlite's `verifyNoSchemaDrift` is the template) + relfilenode belt: FILED, open.**
- **G4 (binlog: `sql_log_bin=0`)** + **G7 (vstream: `ExecuteFetchAsDBA --disable-binlogs`)** — **DOCUMENTED (same batch):** docs/operator/cdc-streaming.md §"MySQL binlog sources: writes that never enter the binlog" (verify-as-the-only-catch, the point-in-time probe, the G7 replica-split corollary). Verdicts stay SILENT_GAP — a doc doesn't close a gap.
- **Critic follow-ups — ALL VERDICTED (second pass, 2026-08-26, real-engine ground truth; matrix rows landed):**
  - **G8 (CRITICAL, MariaDB `log_bin_compress`)** — size-conditional silent row loss: ≥256 B rows arrive as `MARIADB_*_ROWS_COMPRESSED_EVENT_V1` (all three DML verbs; a big row's DELETE compresses via its before-image), go-mysql v1.16.0 fully decodes them, `dispatchRows`' switch doesn't enumerate them → default-arm drop at `cdc_reader.go:1500-1503`, GTID/XID fold so resume advances past the loss; backups equally affected. — **CAPTURED (batch 2, 2026-08-26)**: shipped as two commits — first the two-layer refusal (`SLUICE-E-CDC-BINLOG-COMPRESSED` preflight in the combined opener + dispatch belt), then full capture: the three compressed types added to the SAME `dispatchRows` case arms as their uncompressed twins (go-mysql decompresses in `RowsEvent.DecodeData` before column decode — the Rows are shape-identical) and the now-wrong refusal removed. Family × verb matrix pinned on a REAL compressing mariadb:11.4 (`TestMariaDB_CDCReader_LogBinCompress_FamilyMatrix`, `SHOW BINLOG EVENTS` anti-vacuity floor) + dispatch-equivalence unit pin; mutation-verified both directions at unit and integration level.
  - **G9 (HIGH, binlog+vstream FK referential actions)** — **WARN BUILT (remediation batch 2, 2026-08-26):** cascaded child changes are NOT in the binlog (observed: CASCADE/SET NULL/ON UPDATE CASCADE all parent-only); sluice's appliers deliberately suppress target-side re-cascade (Bug 164: `foreign_key_checks=0` / replica-role / `foreign_keys(0)`), so child rows silently survive or keep stale FK values on MySQL, privileged-PG, and SQLite/D1 targets; converges only on unprivileged-PG. Shipped: `FK-REFERENTIAL-ACTION-CAPTURE-GAP` census WARN at CDC open on BOTH lanes (binlog: `preflightFKReferentialActions` in the combined opener, all three chokepoints roster-gated; vstream: `warnVStreamFKReferentialActions` at `StreamChanges` + the snapshot core), one shared census bound by the cross-lane roster `TestFKReferentialActionWarnRoster_BothLanes`, Bug 246 table-scoped (the child table is what must be in scope), RESTRICT/NO ACTION silent, probe error WARNs (SL-1), real-mysql:8.0 pin `TestCDCReader_FKReferentialActionWarn`, operator docs in cdc-streaming.md. Capture stays blind — matrix verdict WARNED (the F1 shape).
  - **P4 → G2 amendment**: `SET UNLOGGED` under FOR ALL TABLES succeeds mid-sync (scoped FOR TABLE is refused by PG itself) → G2's census must run at EVERY CDC open; live-window flip stays undetectable until next open (stated at the door). Relayed to the in-flight G2 implementer. pgtrigger doc cell: crash recovery truncates unlogged tables without firing the TRUNCATE trigger; relpersistence check at the F2 door filed.
  - **P3 (pgoutput 2PC) — CAPTURED**, all sub-cases observed benign (decodes once at COMMIT PREPARED; a pending prepared tx blocks slot creation so the neither-snapshot-nor-stream window cannot arise). Residual loud-failure fix **BUILT (remediation batch 2, 2026-08-26):** `PREPARED-XACT-BLOCKS-SLOT-CREATE` WARN (`warnPreparedTransactions`) at the single slot-creation chokepoint `createLogicalReplicationSlot` — reaches all three creating callers (snapshot cold start, from-now CDC open, backup anchor; resume paths reuse the slot and cannot block) — naming gid/database/age + the COMMIT/ROLLBACK PREPARED remedy; probe error WARNs (SL-1); real-PG16 pin `TestCDCReader_PreparedXactWarnBeforeSlotCreate` observes the WARN while the create is blocked and the resolution unblocking it.
  - Mid-sync `SET SESSION binlog_format=STATEMENT` tripwire → **BUILT (batch 1, above: `SLUICE-E-CDC-STATEMENT-DML`)**.
- **Standing rule (in the matrix header):** a new CDC lane or newly discovered write class owes a matrix row in the same PR.

## 2026-08-26 — full blind audit (Fable-5 fan-out; delta v0.130.0→v0.131.4, +13.7k LOC)

Full report `workspace/repo-audit-2026-08-26.md` (+ 9 per-dimension files, gitignored scratch — this section is the durable copy). **Grade A−.** Reconciler scorecard on the 08-19 audit: **13 FIXED / 1 OPEN / 0 REGRESSED** (every prior HIGH shipped with its gate). Two HIGHs, a MEDIUM cluster on the newest CDC/backup code, and one structural insight. **Remediation LANDED same-day (2026-08-26) and SHIPPED in v0.131.5 (tag `3d5cf102`):** F1 preflight WARN + doc caveat + mechanism pin (`d48dd64f`→main), F2 pgtrigger capture-shape door (`15689703`→main), T-1 fake-VStream ride-out harness — the loop now runs per-PR, both lanes (`643ff757`→main), C-1 watchdog join + armed-cancel (`4bcf6c1f`), SEC-1 wrong-key heal refusal (+ first wrong-key test cells), R-1/R-2 redact fixes + family matrix, A-1 scalar-parity gate + the ADR file→index gate (`60be8da9`-era), and the full M0 doc batch (`44129b0b`). **Still open from this audit:** C-2 lane-unification (pinned as a contract divergence — snapshot lane rides out in-process, standalone lane recovers via the outer ADR-0038 warm-resume fresh-reader; unifying is a design call), the F1 ENABLE-ALWAYS full fix (needs an ADR), F1-REFUSAL-OPTION + PGTRIGGER-ROSTER + TIMETZ-PROJECTION below, the M2 capture-completeness class checklist, and the M3 tail (perf-matrix estimator row, redact NULL-posture/mask:inner consistency, otel bump, the hooks' golangci version-guard). The per-item bullets below retain the original filing detail.

- **~~PUT-TMP-RACE (MEDIUM, loud, found by the v0.131.5 tag CI itself)~~ — FIXED `e5ae3d8a` (2026-08-26, post-tag; ships with the next release).** `LocalStore.Put` built its atomic write on a FIXED tmp sibling (`abs + ".tmp"`), so two concurrent Puts to the same key shared one temp file with independent fd offsets — one writer's truncate + the other's stale-offset chunks rename-installed a TORN file. Observed live as CI run 33000462974's fork-guard failure (`decode "lineage.json": invalid character '}' after top-level value` — two simultaneous incremental extenders in the ADR-0160 claim→Put residual window). Loud-when-hit (unreadable catalog, caught by verify + the chain-readability gate), never silent value loss. Fix: `os.CreateTemp` unique-per-call; racing test 30/30 green. Sibling enumeration in the commit: cloud `BlobStore.Put` streams to an object-store writer (per-upload all-or-nothing, no tmp file); `lineage.prefixedStore` delegates; grep confirmed the only fixed-`.tmp` writer in the tree. **Note: v0.131.5 binaries carry the OLD Put** — the tear remains a known loud residual there.

- **F1 (HIGH, silent-loss, pre-existing) — pgtrigger capture is blind to `session_replication_role='replica'` writes.** Plain `CREATE TRIGGER` (no `ENABLE ALWAYS`, `pgtrigger/setup.go:672`) does not fire under replica role — OBSERVED on real PG16. sluice's own PG applier runs `SET LOCAL session_replication_role = replica` when privileged (`postgres/change_applier.go:744`), so a B→C pgtrigger relay of a sluice apply-target silently forwards nothing (works in dev, loses in prod); a source that is a native logical-replication subscriber has the same hole. Full `ENABLE ALWAYS` fix has echo-loop implications (needs an ADR). **REMEDIATION IN FLIGHT: preflight WARN (pg_subscription probe) + doc caveat + mechanism-pinning integration test.**
- **T-1 (HIGH, testing) — the reshard-window ride-out loop (`reopenReshardWindow`) executes in no running test** (only e2e executor quarantined; unit pins reach the classifier + request-builder only; ground-truthed against run 32911519371 — zero ride-out log lines). **REMEDIATION IN FLIGHT: per-PR fake-VStream harness exercising the loop under a scripted window, both reopen paths.**
- **C-2 (MEDIUM, refined) — the ride-out loop is snapshot-path-only.** The standalone `vstreamCDCReader.Reopen` (`cdc_vstream.go:2495`) pins PRIMARY and gets the carve-out classification but is single-shot (outer streamer retry, no in-process ride-out); its window survival is unverified. NOT "falls to REPLICA" (first-report overstatement, corrected against code). Harness above pins its actual contract; a confirmed divergence escalates for design review.
- **C-1 (MEDIUM, concurrency) — watchdog-cancel ordering race.** `vstreamLiveness.stop()` never joins its watchdog (`cdc_vstream_liveness.go:488`); the snapshot pump's watchdog cancels via the dynamic `s.cancelGRPCStream()` not a captured per-stream cancel — a stale fire racing the reshard JOURNAL can cancel the fresh stream; with first-wins `setErr` + the reopen's err-clear, a continuous sync can silently exit 0 mid-stream (restart resumes). **REMEDIATION IN FLIGHT: join in stop() + capture the cancel (the COPY-pump convention) + ordering unit test.**
- **F2 (MEDIUM, silent-loss-adjacent) — pgtrigger has no capture-shape door** (sqlite/d1 got theirs in v0.131.2; a `DROP TRIGGER sluice_capture_*` is invisible to the event-trigger tag filter and the column fingerprint → silent uncaptured DML). The v0.131.2 sibling sweep scoped to `CapturedValueExpr` consumers — the mechanism roster — so pgtrigger's different mechanism was never enumerated on the door half. **REMEDIATION IN FLIGHT: ~50-line mirror door at pgtrigger CDC open.**
- **SEC-1 (MEDIUM, security) — wrong-key backup-signature-heal footgun.** `healStaleLineageSignatures` (`signature_verify.go:448`) re-signs on ANY VerifyLineage failure at the no-op maintenance doors, including wrong-key — a mistyped `--encrypt` passphrase on a no-op `backup compact` silently re-keys a signed chain at exit 0 (the restructure path validates via `UnwrapChainCEK` first; the no-op doors don't). **REMEDIATION IN FLIGHT: refuse when `sig.KeyID != signer.KeyID` + a wrong-key heal test (the family had none).**
- **A-1 (MEDIUM, gate gap) — the scalar dual-registry-drift class (schema reader ↔ CDC `oidToType`) has recurred ≥6× (bit/varbit latest) and the parity gate covers arrays only** (`TestOIDToType_ArrayParity`). **REMEDIATION IN FLIGHT: a `scalarFamilies` parity gate mirroring the array one.**
- **R-1 (MEDIUM, silent-loss, promoted area `internal/redact`) — `randomize` seeds every row from nil under a PK case-divergence** (case-exact `row[pkCol]`, no missing-key check; CDC PK names come from the target catalog, row keys carry source spelling) → every row gets one identical "randomized" value at exit 0. **R-2 (MED-LOW): `randomize:int:0,MaxInt64` wraps the span negative → constant Min per row.** **REMEDIATION IN FLIGHT: loud missing-key check + overflow-safe span + a redact family-matrix starter.**
- **Structural insight (the pass's most valuable output):** sibling enumerations scope to the *mechanism* roster (same code path) and miss siblings of the *class* in another engine with a different mechanism — F1 and F2 are both that shape. Follow-up (M2): a capture-completeness class checklist across ALL CDC engines (trigger-disable / role-bypass / DDL-tag gaps), each cell fixed-or-exempt-with-a-reason.
- **Gate also in flight:** ADR file→index completeness assertion (the ADR-0184 missing-row class; the existing `TestADRIndexStatusParity` is index→files only).
- **PGTRIGGER-ROSTER (LOW, filed by the F2 door's own gate-scope honesty):** the capture-shape door cannot reach a table whose row AND truncate triggers were BOTH dropped while other tables keep theirs — pgtrigger records no setup-time table roster (sqlite-trigger's fingerprint table is what powers its door there), and `teardown --tables` legitimately produces that shape. Fix shape: a setup-time roster artifact (which would also let re-setup rediscover tables). Until then the door covers single-trigger drops, disables, rewires, shape edits, the all-dropped floor, and the event-trigger tier — the scope is stated at the door.
- **F1-REFUSAL-OPTION (design, operator call):** the replica-role preflight ships as a WARN (both shapes can be intentional). If a refusal is wanted for the relay shape specifically, the clean fit is: refuse at open when `sluice_cdc_state` on the SOURCE has a recently-heartbeated, not-stop-requested stream row (live-relay evidence), override via a `--allow-replica-role-source` flag (the `--allow-polled-fingerprint` pattern); the subscriber shape stays a WARN (no per-table evidence distinguishes safe from lossy).
- **TIMETZ-PROJECTION (LOW, filed by the new scalar-parity gate on first contact):** schema reader maps `time with time zone` → `ir.Time{WithTimeZone: true}` while the CDC `oidToType` lumps `TimetzOID` into plain `ir.Time{}`. Value-safe today (`value_decode.go:123` carries time as text verbatim, offset included), but a CDC-projected SchemaSnapshot for a timetz column carries `WithTimeZone=false`, and `ddl_emit.go:142` would render plain `TIME` if such a projection ever drove DDL. Fix shape: give the `TimetzOID` arm `WithTimeZone: true` (one line) + a pin; verify no consumer depends on the current family-only shape first.
- **LOWs (filed, most fixed in the same-day docs batch):** value-types.md refuted-mechanism residue ✔; ADR-0184 index row + 0182 link + header ✔; roadmap 72(b) status ✔; contradictory quarantine comments + the 8-vs-15 budget note ✔; exec-timeout stale backlog line ✔; golangci `@latest` doc pin ✔ (the hooks still run the installed binary unpinned — a version-guard in the hooks is a follow-up). Still open LOWs: sqlite-trigger door is stream-open-only; D1 cold-start reads outside the render door (bounded, exemption written); FK-preflight add-table exemption unwritten; mask:inner short-value passthrough + redact NULL-posture inconsistency + non-injective `registryKey`; kmsverify 6→5 count never validated by a green run (mitigated); `cdc_vstream_snapshot.go` 3× stream-install ritual (M2 extract); perf-parity-matrix missing the ADR-0184 estimator row (M3); otel routine bump.

## 2026-08-22 — invariant sweep batch (fresh G-12 run, Fable 5; v0.112→v0.131 delta + silent-loss surfaces)

Full report was `workspace/repo-invariant-sweep-2026-08-22.md` (gitignored scratch; 12 claims, 0 FALSE, 0 live silent-loss; 7 of 12 needed a correction, gate, or named premise). Landed on `main`: `2425c0b3` (clip guard) · `d15227eb` (float-key render parity) · `9fecc074` (skip-ledger flush roster) · `3ebd2414` (value-fidelity-review pin-hardening: every clip-safe family pinned + a fail-closed `ir.AllTypes()` roster).

- **Invariant sweep batch 2026-08-22 (fresh run of the G-12 form; 12 claims, 0 FALSE, 0 live silent-loss).** Three gates + one production guard, each mutation-verified in both directions with the mutant grep-confirmed before every run. **(1) ADR-0096 fallback clip** — "the fallback never runs for those families" was true only while migrate's and backup's planner gates agreed; now `ReadChunkBatch` itself refuses a Go-side clip on an order-divergent PK family (collated strings, decimal/float-as-text, MySQL TIME text, enum), fail-by-default on unknown families. Fails if it breaks: `TestReadChunkBatch_FallbackRefusesOrderDivergentPK` (over-refusal pinned too — the integer MIN/MAX cell must still clip) and `TestFallbackClipOrder_ClassifiesEveryIRType` (whole `ir.AllTypes()` universe, anti-vacuity floor 27, allow-list size 10). **(2) backup float-repair patch key** — floatPatchKey's "%v renders match" bridges two readers and was pinned for one family; now all 15 PK families' renders are bound across `decodeValue` vs `decodeVStreamCell`. Fails if it breaks: `TestFloatPatchKeyFamilies_ReadersRenderEqualGoValues` (internal/engines/mysql); the run-time floor for the total-miss shape remains the SL-F2 tripwire (WARN by default). **(3) skip-ledger flush boundaries** — "flushed at every position-write boundary" was per-site prose; now a fail-by-default AST caller roster over every `writePositionTx`/`writePositionPipelined` caller in both engines, each graded flushes-or-exempt-with-the-caller-list, with a companion check on the flush an exemption leans on. Fails if it breaks (or a new position-write path lands unclassified): `TestPositionWritersFlushTheSkipLedger` (internal/engines). **Recorded without gates, each with the reason:** the TINYINT(1) preflight's SQL-vs-translateType agreement is defended structurally in both divergence directions; mydumper's "booleans always dump bare" has a loud refusal under it; the backup reader pool's "always a free reader" rests on `backupWithinChunkingEligible` refusing the eager fixed-supply mode (loud `errBackupPoolNoFactory` if loosened, not silence); `UnwrapDomain`'s nil-BaseType consumer enumeration is narrower in the comment than in the code (every consumer found is loud or safely-degrading; `internal/domaingate` covers new dispatch sites). **C11 residual:** the render-parity gate checks `%v` equality, not `reflect.DeepEqual` shape identity — sufficient for the patch key, weaker than the "identical Go shapes" doc sentence; widen if a second consumer starts depending on shape identity.

**Batch B (2026-08-22, second run of the day) worked all seven of the previously-named claims to a verdict** — full report `workspace/repo-invariant-sweep-2026-08-22b.md` (gitignored scratch), branch `invariant-sweep-2026-08-22b`. 0 FALSE at the claim level, 0 live silent-loss; 1 FALSE subsidiary comment, 1 gate strengthened to match its claim, 2 new pins, and one instructive wrong-fix-caught-in-batch. Per claim: **(1) backup_parallel range-partition exactly-once** — SURVIVED; the short-page end-of-range inference is not a skip-without-proof (Bug 68 gate + ctx.Err() precede it) and its residual premise — the BatchedRowReader full-page contract — is now NAMED at `backupRange` with the reason all three shipping engines satisfy it by construction. **(2) batch_loop rows_applied-IFF-position** — TRUE across every arm; the unstated premise (the carry is loop-owned and DIES on error, so a retry recounts from the persisted position) is written at `pendingRowsApplied`, and the one unpinned arm — the carry surviving a TransactionalDDL=false schema-event ApplyOne interlude mid-source-tx — is pinned by `TestRunBatchLoop_RowsApplied_CheckpointOnly_CarrySurvivesSchemaEventInterlude` (mutation-verified both directions). **(3) chain_compact commit boundary** — TRUE (the swap is one atomic Put: LocalStore tmp+fsync+rename verified; ADR-0160 CAS); one subsidiary FALSE corrected: `renameStagingToFinal` promised a "catalog-driven sweep" would clean a crashed run's final-named `seg-merged-*` dir — no such GC exists (disk-leak-only; GC filed, must reckon with concurrent-compact staging). **(4) "never report success over a chain it just made unreadable"** — was narrower-than-worded (header-level gate, the "never" resting on unchecked delete-set geometry); the gate now STATS every surviving referenced chunk file on both legs (`verifyChunksPresent`, pinned by `TestVerifyChainReadable_MissingSurvivorChunk_Refuses`, mutation-verified both directions; all claim homes updated through the G-15 content-equality gate). **(5) fkcheck/FK-preflight content sweep** — SURVIVED; the batch's own first "fix" was WRONG and reverted ("referencing columns stay indexed" is not a MySQL-source fact — it is sluice's own `applySkipForeignKeys` synthesis; grounding now written at the remedies), and the branch-scope premise (safe_migrations probed on `--planetscale-branch`-default-main, DSN never consulted) is named on the Checker. **(6) rowpredicate canonical-form premise** — the filed two-facts-pinned-argument-unpinned shape confirmed for exactly the UUID member; bound by `TestUUIDCanonicalForm_BothLegsDeliverTheEvaluatorsForm` (real PG 16, both legs → real resolver → Compile → Eval, run green locally, mutation-verified via an uppercased lens arm). **(7) pgtrigger gap-freedom** — SURVIVED; the 4-step abort proof is faithfully ordered in `pump`/`resolveHole`, lastID is the contiguous run's end, the ceiling is a prefix cut. One observability gap stated at `settledCeilingSQL`: the hole-stall WARN does not reach a CEILING stall (a long-open write tx anywhere on the source pins xmin → zero-row polls → healthy-idle logs while lag grows); fix shape filed (a MAX(id) probe at WARN cadence distinguishing idle from held).

**Highest-value un-worked claims (next batch):** compact's orphan `seg-merged-*` GC (needs the concurrent-writer design call). The other four named here were worked to a verdict in batch C (2026-08-23, below).

## 2026-08-23 — invariant sweep batch C (the four workable claims from batch B's next-batch line)

- **(1) sqlite/D1 trigger gap-freedom sibling sweep — CORRECTED: the filing was STALE; the sibling was already swept 2026-08-08 (`7719370d`, invariant-queue batch 3), and the coverage is native, not inherited from pgtrigger.** The rowid argument's two premises each have their own machinery: commit-order-under-contention is ground-truthed by `TestChangeLogIDsAreCommitOrderedUnderConcurrentWriters` (four real contending writers, anti-vacuity contention grading, independent expected value = the writers' own confirmed commits); the id-never-reused premise is graded at BOTH CDC doors by `verifyChangeLogWatermark` (DDL arm + `sqlite_sequence` floor arm), with the door matrix `TestCDCReader_RefusesChangeLogThatCanReissueIDs` covering cold-start snapshot / from-now / warm-resume plus three positive controls, the defect proof `TestChangeLog_SequenceTamperStrandsTheChange`, and the implementor roster `TestExecutorImplementorsAllGradeAllocation` (local + D1 executors). The D1 primary-path premise is bound by `TestD1Transport_NeverRoutesThroughTheSessionsAPI` with the Cloudflare-side residual marked UNVERIFIED PREMISE at `D1Conn`. pgtrigger's `verifyChangeLogSequence` explicitly scopes itself pgtrigger-only and points here. Batch-C mutation spot-check: disabling the snapshot-door `verifyChangeLogWatermark` call failed exactly the `no_autoincrement/cold_start_snapshot` cell (mutant grep-confirmed, targeted revert, gate green after). No new gate needed; the batch-B filing lagged the code by two weeks — the doc-lags-code shape, on the backlog itself, again.
- **(2) pgtrigger ceiling-stall WARN — GATE-BUILT (the filed fix shape, sharpened).** The filed probe (`SELECT MAX(id) WHERE id > $lastSeen`) could false-WARN on a row that just arrived; the built probe asks the exact question instead — visible rows above the watermark matching the ceiling's OWN not-settled predicate (`ceilingStallProbeSQL`, sharing `notSettledPredicate` with `settledCeilingSQL` so the holder and the reporter cannot drift; pinned by `TestCeilingStallProbe_SharesTheCeilingPredicate`). `ceilingStallGuard` probes only after a full WARN cadence of zero progress, at most once per cadence (`TestCeilingStallGuard_ProbePacing`), and WARNs with held count + first held id + pinned xmin + the pg_stat_activity hint. End-to-end on real PG: `TestCDCReader_CeilingStall_WarnsWhileHeldAndResumes` (three cells: idle-under-pinned-xmin stays silent; a held committed row WARNs while correctly emitting nothing; the pinner settling delivers the change — stall, never loss). Mutation-run both directions, mutants grep-confirmed: WARN disabled → cell 2 fails; warn-even-when-idle → cell 1 fails. Sibling enumeration: sqlite/d1-trigger are EXEMPT — they have no settled ceiling (no safety-lag predicate at all; the single-writer commit-order argument consumes every visible row), so the held-visible-rows state is unconstructible there; pgtrigger's cold-start settle wait already WARNs on its own cadence.
- **(3) ADR-0184 index-split content sweep — SURVIVED with one WIDER-THAN-FILED finding, closed for the big cell.** The split's content is sound and unusually well pinned: the pure flavor×size decision matrix with an anti-vacuity floor, the emission matrix asserting shape not just the flag, both emitters sharing the `emitAddIndexClause` chokepoint (so per-index vs combined cannot drift a clause's rendering), the end-to-end chokepoint pin plus the second entry point (`BuildTableIndexesFromChannel`), the resume-only-missing pin, and both directions of the source-not-target decision (`TestBuildTableIndexes_SplitUsesSourceHintNotTargetStats`). Hint keying verified consistent (`table.Name` on both sides); the MySQL estimator's schema-scoping and 0-means-unknown posture verified. **The finding:** `ir.TableByteSizeEstimator` had exactly ONE implementor (MySQL), so the split was INERT for every Postgres/SQLite source to a PlanetScale target — the same "inert on the platform it exists for" defect the PlanetScale-leg amendment fixed, one axis over (source engine instead of stats source), and the ADR's scope paragraph enumerated pinned-reader/archive cells while leaving the live-source-engine axis implied. Closed for Postgres: `RowReader.EstimateTableBytes` via `pg_table_size` (pinned-reader posture mirrors `CountRows`; capability pin added; unit pin `TestEstimateTableBytes_PinnedReaderReturnsZeroWithoutQuerying` with the nil-querier proof, integration pin `TestRowReader_EstimateTableBytes_ReadsSourceSize` on real PG with a real-floor assertion and the unknown-table 0 cell). Mutation-run both directions, mutants grep-confirmed: always-unknown → integration pin fails; pinned-guard removed → unit pin panics on the nil querier. SQLite/D1 + mydumper/flatfile cells FILED, now stated in the ADR's new source-engine enumeration paragraph rather than implied.
- **(4) `resignIfSigned` crash window — CONFIRMED, then FIXED (the claim's option A, ground-truthed first).** Both compact and prune commit the catalog restructure FIRST and re-sign SECOND; a crash between the two leaves the chain restructured under stale signatures, surfacing at verify as SIGNATURE-INVALID whose remedy text accuses tamper — while the SIGNATURE-MISSING remedy's "re-run the maintenance step with the chain's key material" was FALSE: the re-run returned through a no-op door (compact's fewer-than-2-segments / no-merge-groups, prune's two r0 returns) BEFORE its `resignIfSigned`. Fixed with `healStaleLineageSignatures` at all four no-op doors: verify the lineage signature against the CURRENT catalog, re-sign only when it fails, WARN loudly naming the verify failure + signing key id. Boundaries pinned: keyless no-op stays exit-0 and unhealed (can neither verify nor sign without material); dry-run never writes; a healthy chain's no-op run writes ZERO `.sig` objects (verify-first, proven by a Put-counting store since HMAC sigs are deterministic). Door roster stated in the tests (`TestMaintenanceNoOp_HealsStaleLineageSignatures`, one cell per door; `TestMaintenanceNoOp_HealBoundaries`). Mutation-run in three directions, mutants grep-confirmed: heal removed at a compact door and separately at a prune door → exactly the corresponding cell fails; always-resign → the zero-churn cell fails; dry-run guard removed → the dry-run cell fails. Re-sign authority = key possession, exactly as on the restructure path (which already re-signs without pre-verifying), so no new laundering class. Enumerated siblings among the other catalog writers, each exempt with a reason: the backup-writer path re-signs on every link write (its one-link crash window closes at the next backup); `ReconcileOpenSegmentCatalog` + rotation commits are healed by the next incremental's `SignLineageCatalog`; `--rebuild-catalog` on a signed chain can also leave a stale sig, and the maintenance heal now recovers that state too when run with the key. The heal re-signs only — the crashed run's unswept orphans remain the separate compact orphan-GC entry (concurrent-writer design call, still open).

## 2026-08-22 — SQLite/D1 adversarial-corpus extension (finder run): the REAL-capture render was silently lossy (FIXED on branch); one open type-mapping design call

The MySQL↔PG adversarial corpus was extended to SQLite (migrate both directions + as-target + trigger-CDC) and real Cloudflare D1 (d1verify: reader matrix, >2⁵³ keyset/rowid pagination, migrate, backup→restore, d1-trigger CDC). First run found:

- **MECHANISM CORRECTED (2026-08-23, direct experiment on bundled 3.53.3):** the loss is NOT a "SQLite 3.43 rewrite / value-based short form at any N." SQLite's `printf`/`format()` `%g` conversion CAPS output at 16 significant digits (26 with the `!` alternate-form-2 flag); precision is honoured below the cap and clamped at it (`%.15g`→15 digits; `%.16g`/`.17g`/`.20g`/`.25g` all→16). 16 sig-digits don't round-trip every binary-64 (17 needed), so `%.17g` was ALWAYS lossy here — longstanding printf behaviour, no version regression (3.43.0's only float change is the `decimal` extension; the 3.52.0 default-value-to-text change is a different code path). The π-seeded pre-corpus test passed only because π round-trips at ≤16 digits. **Premise CHECKED (2026-08-23, the premise-check pass):** the fix `%!.20g` works only where the `!` flag is honoured; on a SQLite ignoring `!` it ALSO clamps to 16 and the fix is INEFFECTIVE (not merely "degraded"). Two halves closed it: (1) history research bounds the class — the `!` 16→26 lift (`nsd = 16 + flag_altform2*10`) is in pre-3.8-era printf.c, predating the SQL printf() function (3.8.3, 2014) and format() (3.38.0, 2022), so no known release carries format()/printf() without honouring `!`, and a library without the function fails the app's write loudly ("no such function"), never a silent clamp; (2) a runtime render-fidelity door (`verifyRealRenderHonoured`, both trigger-CDC transports, at every stream open via `openCDCReaderBackend`) probes the CONNECTED engine with the production expression over a 17-digit double and refuses loudly on a clamp or an unrunnable probe — on D1 the probed engine IS the trigger-firing engine, closing the premise there at runtime; mutation-verified both directions. The one residual stays named at `CapturedValueExpr`: a third-party app library firing a LOCAL capture trigger renders with its own printf, unreachable from sluice's process — bounded to the loud shapes above. sluice's own connections + real D1 are measured-lossless. Prose corrected in trigger_seam.go, ADR-0135, CHANGELOG/archive, and the live v0.131.2 release body.
- **CRITICAL-class, FIXED: `CapturedValueExpr`'s REAL arm (`format('%.17g')`) is lossy on SQLite** — the 16-sig-digit printf cap above (`0.30000000000000004` → `"0.3"`; 2295/5007 swept doubles fail; measured on modernc 3.53.3 AND real D1). Every REAL on the D1 reader, D1 staging (`--stage-local`), and BOTH trigger-CDC capture lanes was silently altered at exit 0 — and a REAL at the float64 max rendered as an out-of-range 16-digit string that killed the stream loudly (the corpus's first trigger-CDC run died on exactly that). Fix: `format('%!.20g')` (the alternate-form-2 lossless flag; 0/5007 misses on modernc, live-D1 confirmed), a per-PR sweep gate with a negative control (`TestCapturedValueExpr_RealRenderRoundTripsExactly`), and a new **capture-shape door**: the CDC reader refuses at stream start when an installed capture trigger's body differs from what this binary installs (stale pre-fix installs, hand-edits) or is MISSING entirely (a dropped capture trigger previously meant that operation silently vanished from the stream — no guard existed). The old-SQLite `!`-flag premise is now runtime-probed at every stream open (the render-fidelity door — see the mechanism entry above); the third-party-app-library residual stays named at `CapturedValueExpr`.
- **OPEN design call (LOUD, not silent): SQLite declared `DATETIME` resolves to `ir.Timestamp`, which the MySQL writer emits as MySQL `TIMESTAMP` — range 1970-01-01..2038-01-19.** An ordinary tz-naive SQLite datetime outside that window (any pre-1970 or post-2038 value) refuses at insert on a MySQL target with a bare server 1292 and no sluice remedy hint, even though MySQL `DATETIME` would hold 1000..9999 fine. PG targets are unaffected (`timestamp` holds the full range). Question for the operator: should ADR-0129's resolver produce `ir.DateTime` (naive; → MySQL DATETIME) instead — which matches SQLite's tz-naive semantics — noting `decodeCell`/`resolveColumnType`/infer-types would need the `ir.DateTime` arm added, or should the current mapping stand with a better refusal hint? Current behavior is pinned by the corpus (`dt_max` mySkip note + the `datetime_beyond_mysql_timestamp_range` refusal cell) so a mapping change flips a named pin, not silence.
- Clean across everything else: int64 extremes (2⁵³±1, min/max, snowflake) are EXACT on every D1 path — values, >2⁵³ PK keyset bounds, >2⁵³ rowid bounds, CDC images (incl. before-images), backup→restore; D1-as-target refuses loudly; SQLite storage-class anarchy refuses in the reader; NUL/invalid-UTF-8/Inf refuse per target before any write; empty-string/empty-blob vs NULL, decimals (Bug 162 TEXT affinity), temporals, booleans all byte-exact. Float `-0.0` is unconstructible in SQLite (normalized to `+0.0` at storage; premise-pinned) — so PG `-0`::float8 lands `+0.0` on a SQLite target, the SQLite twin of ADR-0066's trigger-engine wart, now pinned by the target-round corpus cell.

## Open — 2026-08-19 Tier-3 audit (Fable 5, v0.127.1..v0.130.0)

Grade A−; 18/20 prior Tier-3 items fixed, 0 regressed, KMS charter closed. Three HIGHs found, all FIXED in the same cycle:

- **HIGH #1 — the VStream `tinyint(1)`-is-bool decode disagreed with `translateType`** (two independent bool-detection authorities with different rules). FIXED `c9cb0478`: one shared `tinyint1IsBool` predicate both call, pinned by `TestVStreamTinyint1BoolMatchesSchema` (family matrix, mutation-verified).
- **HIGH #2 — the PlanetScale FK preflight + plan report were `migrate`-only** while migcore + the error-code row claimed both entry points, so a `sync start`/fleet cold-start into a fresh FK-support-disabled PlanetScale DB burned the whole copy then walled at constraints (the v0.129.0 field report). FIXED `d8325c9e` (operator chose "wire it"): `Streamer.FKEnablementChecker` composed at both sync construction sites, the preflight + plan report run in `coldStart`, and `TestSharedPhaseFieldAssignmentParity` now holds all three sites to assigning the checker (mutation-verified).
- **HIGH #3 — `reopenForCDC` opened the auto-shard CDC tail on the caller ctx with a stale `grpcCancel`**, so a fired liveness watchdog cancelled the dead COPY context and the tail parked forever (silent sync freeze after a tablet failover). FIXED `fedb15bb`: fresh `streamCtx`/`grpcCancel` mirroring `reconnectCopy`, pinned by `TestVStreamSnapshot_ReopenForCDC_InstallsCancellableStreamCtx` (both directions mutation-verified).

### ~~SDL-1 (WATCH) — the MyISAM tolerate-1062 atomicity premise is unchecked~~ — FIXED (2026-08-23, the premise-check pass)

**FIXED with the filing's first fix shape (the ENGINE-scoped guard), placed exactly where the premise is load-bearing.** `verifyTransactionalTolerance` (`internal/engines/mysql/row_writer.go`) runs on the tolerate-1062-on-retry arm ONLY (never the hot path): before the tolerance returns nil, it reads the target table's `information_schema.TABLES.ENGINE` on the retry conn and withholds the tolerance loudly — naming the committed-PREFIX hazard and the remedy — unless the engine is InnoDB. Fail-closed on an unreadable/unknown engine (false refusal costs a restart; false tolerance costs silently lost rows — the `replayWarningsAreOnlyDuplicates` posture). The cold-copy-preflight alternative was deliberately rejected: a MyISAM copy that never hits a retry-collision is correct, and a preflight would refuse a working configuration. Pinned by `TestColdCopyReparentRetry_Retry1062NonTransactionalEngineRefuses` + `..._Retry1062EngineProbeErrorFailsClosed` (plus the pre-existing InnoDB-accept pin `..._PlainTolerates1062OnRetry`), mutation-verified both directions (guard disabled → both refuse cells fail; always-refuse → the accept pin fails). Sibling roster written at the guard: `writeBatchedConn` guarded (the only `isMySQLDupKey` call site); idempotent upsert exempt (row-wise convergence); LOAD DATA exempt (IGNORE + row-wise replay accounting, committed-prefix case stated in its doc); keyless refused upstream. Original filing follows.

`internal/engines/mysql/row_writer.go` — the ADR-0108 tolerate-1062-on-retry WART treats a `1062` on a cold-copy INSERT retry as PROOF the byte-identical batch is already durable, so it returns nil and continues. The proof rests on the batch being an ATOMIC multi-row INSERT — i.e. a TRANSACTIONAL engine (InnoDB), where the prior attempt "either fully rolled back OR committed-but-ack-lost." On a **non-transactional target (MyISAM)** a batch INSERT is not atomic: an interrupted attempt can leave a PREFIX committed, so the retry's `1062` on the first already-landed row proves only that row is durable — the suffix past the first collision is silently skipped when the INSERT aborts at the dup. The premise is NOT `ENGINE`-scoped and there is no preflight refusing a MyISAM cold-copy target. Not a delta regression (the WART long predates this). Marked in the words **UNVERIFIED PREMISE** at the code site (audit-2026-08-19 SDL-1). **Fix shape (S/M):** either an `ENGINE`-scoped guard that keeps the tolerance to transactional targets, or a cold-copy MyISAM-target preflight (refuse or warn). The premise-naming rule's written record for a finding filed-not-fixed.

- **SDL-2 (WATCH, carried) —** `cdc_table_map_guard.go` table-map ENUM-reorder/rename window; needs `binlog_row_metadata=FULL`. No delta regression; unchanged from prior audits.

### PG lane exec-timeout race — SCOPED 2026-08-20 (`workspace/scoping-pg-exec-timeout-race-2026-08-20.md`), re-estimated L→S/M

The audit deferred this as an L-sized conn-lifecycle rework. Scoping found the real fix is much smaller. `change_applier_pipelined.go` runs the pipelined `SendBatch`/`Commit` under `appliershared.RunWithDeadline`, which orphans the goroutine on timeout (it can't cancel `f`); the caller then `Rollback`s + pool-`release`s the same raw `*pgx.Conn` the orphan is still on — a `-race`-class conflict + a corrupt-conn-to-the-pool risk. Loud-failure only (frontier advances only on a reported commit; retry re-applies idempotently). **Root cause:** `RunWithDeadline` exists ONLY because `database/sql.Tx.Commit()` takes no context; the pipelined path copied that orphan hack onto `pgx.Tx.SendBatch(ctx)`/`Commit(ctx)`, which ARE ctx-aware. **Recommended fix (Option B):** pass `context.WithTimeout(b.ctx, execTimeout)` to the pgx ops instead — pgx's ctx-watcher cancels the op + closes the conn (no orphan), and `classifyApplierError` already maps `context.DeadlineExceeded` retriable. Exactly the serial `ExecContext` path's proven pattern; serial path (tracked `*sql.Tx`) unchanged. **Deliverable = the `-race` integration test that induces a pipelined timeout** (the real work; the code change is 2 call sites). Verify points in the memo: pgx cancel may surface as `57014` not `DeadlineExceeded` (confirm classification); SendBatch ctx bounds the `br.Exec()` drain loop; indeterminate-commit property unchanged. **SHIPPED — Option B landed `1d2400b5` (v0.130.2); this line previously still read "awaiting operator go-ahead" and was flagged stale by the 2026-08-26 audit reconciler. The `-race` pipelined-timeout integration test remains the open deliverable.**

### PREM-VSTREAM-AUTOINC (CLOSED 2026-08-24) — the VStream flag-512 forwarding premise: carrier-vocabulary half BOUND in code (2026-08-23), live-wire half CONFIRMED against a real Vitess cluster (2026-08-24)

**CLOSED — live-wire half confirmed (2026-08-24).** `TestVitessClusterAutoIncTinyint1PreservesInteger` (`internal/engines/mysql/cdc_vstream_cluster_autoinc_tinyint1_integration_test.go`, `//go:build integration && vitesscluster`) boots a REAL multi-process Vitess-24 cluster, seeds an AUTO_INCREMENT `tinyint(1)` PK past bool's {0,1} (ids 1,2,3) plus a plain `tinyint(1)` anti-vacuity control, runs a real sluice VStream sync (cold-start COPY + a CDC INSERT id=4 and an UPDATE of id=2), and asserts the auto-inc column round-trips as an INTEGER while the plain column classifies as bool — ground-truthed on the wire. Phase-A instrumentation observed the FIELD event Flags directly: **the auto-inc column carried Flags=49667 with bit 512 SET; the plain column Flags=32769 with bit 512 UNSET.** So a live vtgate DOES populate `AUTO_INCREMENT_FLAG` on the VStream wire. The pin is DISPOSITIVE by the soft-dependency argument below: had the flag been absent, the auto-inc PK (values ≥2) would have been classified bool and the decode-time range guard would have REFUSED the COPY (`SLUICE-E-VALUE-TINYINT1-RANGE`), so green-with-integers-preserved (guarded by the non-vacuous bool control) can only hold if the flag is present. Covered by the weekly cluster job's `-run 'TestVitessCluster'` filter; mutation-verified both directions (classifier forced to ignore the flag → the auto-inc arm reddens; classifier forced to always-integer → the bool control reddens). The `UNVERIFIED PREMISE` comment at `mysqlFlagAutoIncrement` is updated to CONFIRMED-with-the-test-name. Original filing follows.

**Code-scoped binding shipped (the premise-check pass):** `TestMySQLFlagAutoIncrementBoundToVitessCarrier` holds sluice's constant AND its consumption to the carrier's own vocabulary — `mysqlFlagAutoIncrement` must equal the vendored `querypb.MySqlFlag_AUTO_INCREMENT_FLAG`, and a field flagged with that enum value must flip `vstreamTinyint1IsBool` through the real classifier (with an anti-vacuity plain-tinyint(1) control). Mutation-verified both directions (constant drifted to 256 → the equality cell fails; classifier ignoring the flag → the consumption cell AND `TestVStreamTinyint1BoolMatchesSchema` fail). That closes the two-facts-pinned-argument-unpinned gap between "sluice reads bit 512" and "the carrier defines AUTO_INCREMENT as 512". **Still open, and unreachable in-code:** that a live vtgate POPULATES the flag on the VStream wire for an auto-increment column. The fix shape is unchanged (S): one real-cluster/vttestserver pin carrying an auto-increment `tinyint(1)` PK through the live VStream lane, asserting it arrives as an integer (not refused, not collapsed) — a `//go:build integration && vstream` test in the `integration-vstream` leg, the C-1 harness shape. Original filing follows.

`internal/engines/mysql/field_to_ir.go` — the HIGH #1 fix keeps an auto-increment `tinyint(1)` an integer on the VStream path by reading MySQL's `AUTO_INCREMENT_FLAG` (bit 512) off `query.Field.Flags`. That Vitess forwards flag 512 on the VStream wire is asserted in a comment, not ground-truthed — `field_to_ir_test.go` sets the flag synthetically, and the self-consistent gate `TestVStreamTinyint1BoolMatchesSchema` cannot catch a missing wire flag (both sides read the same `field`). The value-fidelity review of the v0.130.1 delta flagged that this delta newly makes the *value decode* depend on the premise (before, only schema mapping did). **SOFT, not silent-loss:** if the flag were absent, an auto-inc `tinyint(1)` ≥2 would decode to bool on both paths and the decode-time guard would REFUSE it loudly (`SLUICE-E-VALUE-TINYINT1-RANGE`) — a false-refuse, never a silent collapse. Marked **UNVERIFIED PREMISE** at the code site. **Fix shape (S):** one real-cluster/vttestserver pin carrying an auto-increment `tinyint(1)` PK through the live VStream lane and asserting it arrives as an integer (not refused, not collapsed) — the only check that grounds the auto-inc arm against reality rather than against itself.

## Open — 2026-08-17 Tier-3 audit (Fable, v0.112.0..v0.127.1)

### Next light-area promotion (Tier-3 §0) — KMS provider adapters against a real KMS

The Tier-3 reconciler named the **KMS provider adapters + per-provider envelope/signature encoding** the next light-area to drive to full depth — a standing crypto-charter concern (the encryption/signing machinery is exercised, but the per-provider aws/gcp/azure KMS adapters against a real KMS). **Per-provider status (2026-08-18):** **AWS** — LocalStack `kmsverify` leg (extended-suites) covers the envelope round-trip + signing; **GCP** — validated 2026-07-16 against a live `EC_SIGN_P256_SHA256` key (roadmap.md, shipped v0.99.263); **Azure — VALIDATED 2026-08-18** against a real Key Vault EC-P256 key with the v0.128.3 binary: `backup full --sign-key kms://azure/<url>` signed in-vault (scheme=kms), `backup verify --verify-key kms://azure/<url> --require-signature` valid (real GetKey + ECDSA verify), and both negatives loud — no-key `--require-signature` refused, and a wrong (local ed25519) key rejected as `signature INVALID` (non-vacuous). **Fully torn down, zero cost remaining** (vault purged, RG deleted, 0 resources in the subscription). **Ops learning (for a repeatable CI leg):** the `sluice-testing` Azure service principal can do DATA-plane crypto ops (key create / sign / GetKey) under an RBAC vault out-of-the-box, but CANNOT change the vault permission model or create role assignments (lacks `Microsoft.Authorization/roleAssignments/write` — the Azure analogue of the GCP `roles/editor`-excludes-data-plane note); so a repeatable leg pre-creates the vault+key with an owner-side grant and the SP signs against it, exactly as this run did minus the (blocked, unneeded) self-grant. So all three provider adapters are now real-KMS-verified; a repeatable Azure `kmsverify` variant (standing vault + key) is the remaining follow-up. Secondary promotion still open: the streamer/broker concurrency orchestration blind read (~6-carry broker preflight-mirror PARTIAL).

### Low tier (Tier-3 audit — gate blind spots + observability, all filed not fixed)

- ~~**T-3 — `check-shard-coverage.sh:264` TEST-1 sub-check has no anti-vacuity floor.**~~ **FIXED (`8acf9803`).** Added the floor: if the workflow HAS a `-skip`/`-run` shard but the `grep -B1` extraction returns empty, fail loudly rather than green on nothing. Mutation-verified (reordering `goflags:` before `packages:` now fails).
- ~~**T-4 — `check-leg-nonvacuity-coverage.sh:48` detection is foolable both ways.**~~ **FIXED (`26ddfe65`).** `hasleg` is now order-independent (matches `integration…-run` in EITHER order), and `hasguard` requires `grep…--- PASS` on one line so a bare `--- PASS:` in a yaml comment no longer classifies a leg as guarded.
- ~~**T-5 — `domaingate.go:327` … miss a type dispatch routed through a local variable.**~~ **FIXED.** `classifyOperand` now follows a single-assignment local back to its source (`localTypeSources` builds the per-scope map; a name assigned >1× anywhere is ambiguous and dropped, so following can only reveal a reading, never invent one), and both call sites (the `Assert` walk + the A-3 meta-walker, same package) pass it — the meta-walker inherited the fix as filed. It surfaced exactly one previously-invisible site, mysql's `row_writer.go:prepareValue:t`, which is domain-safe by construction (its first arm `t.(ir.Domain)` at :858 unwraps + recurses, so every later raw arm sees only non-domains) and is now exempt-with-reason rather than unseen. New `TestClassifyOperand_FollowsSingleAssignmentLocals` pins the capability (direct / local-raw / local-unwrapped / chained / twice-assigned-not-followed / unrelated-local). Mutation-verified both directions: disabling the Ident-following case reddens both the unit pin AND the mysql gate's now-stale exemption.
- ~~**T-6 — `check-run-filter-coverage.sh:247` weak-fallback for the Vitess cluster weekly leg has the pre-hardening substring defect.**~~ **FIXED (`8acf9803`).** The two weak-fallback legs now require the manifest pattern to appear QUOTED (`'pattern'`) rather than as a bare substring; both legs already define their pattern quoted, so a narrowing (`'TestVitessCluster'` → `'TestVitessCluster_Bug27'`) or an unquoted substring hit (`TestVitessClusterFilteredSync`) no longer satisfies it. Mutation-verified both directions.
- ~~**PG-2 — `rows_applied` counts SKIPPED (never-applied) changes.**~~ **FIXED (`d0d0d3ce`, cherry-picked to main).** `dispatch`/`dispatchPipelined`/the MySQL batch-tx dispatch now return an added `skipped bool` (a change routed to `recordSkippedTable` for an absent target returns true); every rows-counter site gates its increment on `!skipped`. **Sibling-swept — all six counter paths:** postgres serial + deferred, mysql serial + deferred, the shared `appliershared` batch-loop `rowDML` (factored into `appliedRowDML(skipped, c)`), and the concurrent `laneapply` `cumRowDML`. The concurrent path can't know the skip at route time (dispatch runs later in a lane goroutine), so a `LaneApplier.SkipsRowChange` seam probes the SAME `colTypesFor` cache the dispatch skip consults (fail-toward-counting on probe error; only a sub-second metadata-TTL straddle can drift, and it perturbs ONLY this observability counter, never the frontier-owned resume position). Pins at the engine-neutral shared layers (`appliershared` per-flush deltas `[1 0 1]` + boundary-accumulate `[2]` covering both engines; `laneapply` skip pin across W=1,2,4), mutation-verified both directions. Gate-clean; the `-race` integration exercise runs in CI.
- ~~**A-4 — `errclassgate.go:25` doc claims "every engine instantiates it" but only mysql + postgres carry gate files**~~ **FIXED — the gate, not just the doc.** Added `TestEverySetErrPackageHasAGateOrReason` (a meta-gate that derives the set of setErr-parking packages from the AST and fails unless each carries an `errclassgate.Assert` gate OR a self-pruning `setErrGateRoster` reason), real seterr gates for the two CDC surfaces (`pgtrigger` — its DDL refusal extracted into `refuseObservedDDL`; `sqlite-trigger` — single local-file poll site allowlisted as terminal-by-nature), and rostered the two bulk-read RowReaders (`mydumper`, `sqlite`) as migrate-path-terminal-correct. Doc corrected to the real reach + cites the meta-gate. Mutation-verified both directions (drop a gate → uncovered-package fail; drop a roster entry → uncovered + stale fail).
- **PREM-1 — the `ir.Domain`-transparency correctness rests on an UNVERIFIED environmental premise: "PostgreSQL reports the BASE type's OID (not the domain's) in a query's RowDescription, so pgx keys its COPY/value codec on the base type."** The v0.128.0 value-fidelity review verified this empirically (real PG 16 + pgx v5.10: domain-over-int → OID 23, domain-over-money → 790, the base OIDs) — which is *why* the whole A-3 delta is value-safe: the domain wrapper is erased at the wire before any codec is chosen, making the domain population byte-identical to the bare-base population. But **nothing in `internal/` asserts it** (premise-naming rule, CLAUDE.md). If a future pgx or PG change ever reported the domain OID instead, the base-keyed codec registrations (`registerPGVectorCodec`/`lookupVectorOID`/`timetz`/`macaddr8`) would silently miss, and the physical-write predicates (now correct *because* pgx selects the base codec) would mis-encode. **Fix shape (LOW, ~1 integration test):** one cold-start COPY round-trip fingerprint (`md5(string_agg(col::text ORDER BY pk))`) for one non-native domain family that needs no extension image — e.g. domain-over-timetz — asserting the value survives byte-exact, plus a direct assertion that a domain column's reported `DataTypeOID` equals its base type's. Non-blocking for v0.128.0 (the premise holds today and is core, long-stable PG/pgx behavior); filed so the load-bearing fact gets a named test rather than resting on a review that won't re-run. **FIXED (shipped v0.128.2):** `TestDomainColumn_WireOIDIsBaseType_PremisePin` (`internal/engines/postgres/domain_oid_premise_integration_test.go`) asserts, against real PG over a base-family matrix `{int, text, numeric, timetz, uuid, domain-over-domain}`, that a domain column's regular-query `RowDescription.DataTypeOID` equals its BASE type's OID AND is a builtin (< 16384, never the domain's own dynamic OID). Two-way assertion so it cannot pass on a coincidence. Passed on real PG 16. (The CDC/pgoutput path reports the domain's OWN OID — the opposite — which is Bug 254, separately handled by `resolveDomainBase`.)
- ~~**Bug 254 — the PG CDC source reader does not unwrap a `DOMAIN` wire-OID, so a domain-typed continuous sync halts at the first CDC change.**~~ **FIXED (`6a51e9c1`, shipped v0.128.1).** The wire-OID sibling of the Bug 233 class the audit's `domaingate` walker (which covers `col.Type` dispatch) could not see: `cdc_relations.go`'s `oidToType` fell through to `unsupported column type OID` for a domain's dynamic OID. Now `resolveDomainBase` walks `pg_type.typbasetype` to the ultimate base (base typmod carried; domain-over-domain flattened; domain-over-enum/array resolved through the existing arms), producing the base `ir` type (never `ir.Domain`, matching the applier). Real-PG CDC round-trip pin over `{int, varchar(10), uuid, enum, int[], domain-over-domain}` + a resolver unit pin; mutation-verified. Caught by the v0.128.0 post-publish regression cycle (`sluice-testing` Bug 254). **Follow-up (LOW gate proposal):** the `domaingate` meta-walker only covers `col.Type` TypeSwitch dispatch; a domain reached this path via a `switch oid` on a `uint32` wire OID — a different AST shape. A gate that derives the wire-OID→type resolver universe (there is currently one: `cdc_relations.go`) and asserts each handles the dynamic-OID/domain fallthrough would close the class mechanically, but the single-site universe makes it low-value until a second such resolver appears; noted, not forced.
- **Bug 255 — DOMAIN support covers only scalar/array base types; the non-scalar-base siblings fail LOUDLY upstream of the CDC reader (FEATURE-completeness, MEDIUM, zero silent loss, pre-existing).** **HALF 1 FIXED (shipped v0.128.2):** domain-over-ENUM (PG target) now migrates — the emitter creates the enum NAMED type (from the enum's carried `TypeName`, else `<domain>_enum`) in schema-writer Phase 1a' before the `CREATE DOMAIN` references it, deduped against the shared `createdEnumTypes` registry, `guardedCreateEnumType`-idempotent, dropped on `--reset-target-data`. Real-PG migrate pin (enum order + CHECK + rows src==dst); the stale `TestPin_DomainOverEnum_RefusedAtEmit` replaced by `TestPin_DomainOverEnum_EmitsNamedEnumType`; mutation-verified. **HALF 2 DEFERRED (nested domain-over-domain):** `information_schema` unwraps only ONE level (proven by mutation; the `UnwrapDomain` doc premise was corrected), so a clean fix needs per-level `pg_type.typbasetype` reads + nested-`ir.Domain` modelling or CHECK-union flattening — real remodelling, rare construct. Now refuses loudly BY NAME ("nested domain", both levels) instead of the generic UDT error, pinned by `TestSchemaReader_NestedDomain_RefusedLoudly`. Composite-base still refused (not modelled). **v0.128.2 value-fidelity review LOW follow-ups (non-blocking, loud-safe, reviewer-confirmed-correct-by-shared-code — cheap pins to build, e.g. v0.128.3):** (a) an end-to-end dedup pin — a schema with a plain `mood` enum column AND a domain over that same `mood`, asserting the type is created exactly once (logic is sound via the shared `createdEnumTypes` map + `guardedCreateEnumType`, just not pinned); (b) a pin that a CDC-forward `ADD COLUMN` of a domain-over-enum fails LOUDLY (PG 42704 undefined type) rather than silently — the exemption prose asserts it, a one-line pin would gate it. Enum-value-with-quote and cross-schema-inner-domain are covered by shared quoting / fall to the generic loud refusal respectively. Original filing follows: Two constructs the v0.128.0 domain matrix never exercised: (1) **domain-over-ENUM, PG target** — PG→PG migrate/cold-start dies at create-tables with `emitCreateDomainType: Enum DDL emission requires column context`. This is the **already-known, deliberately-deferred** loud refusal (the A-3 postgres remediation recorded "full domain-over-enum support is a feature, not a gate fix; currently a loud refusal — safe, not silent"); plain enum PG→PG works, and domain-over-enum PG→**MySQL** works via the loud downgrade, so it is specifically the PG `CREATE DOMAIN d AS <enum>` DDL emitter needing column context to order the enum type. (2) **nested domain-over-domain** — rejected at schema-read with `domain types are not modelled in the IR` (the IR models one domain level; the CDC reader's `resolveDomainBase` flattens N levels off the wire, but the schema reader stops at one, so a nested domain never reaches migrate/CDC). Both are LOUD and safe (no data written, no silent loss) — the same "enumerate the domain siblings" shape as Bug 254 but one axis over (base-type KIND, not dispatch SITE). Filed in `sluice-testing/BUG-CATALOG.md` (Bug 255) with repro. **Design-gated / feature scope, NOT a patch:** (1) needs the enum-DDL emitter to accept a synthetic column context (or a domain-scoped enum-type ordering pass); (2) needs multi-level domain modelling in the IR + schema reader. Operator decision on prioritization vs the other design-gated items. It further qualifies the v0.128.x domain claims for these two constructs (the v0.128.0 errata already walks back the CDC path; these are the migrate/schema-read/DDL-emit paths).

### ~~C-1 — bind the VStream mid-run-checkpoint VGTID-ordering premise (MEDIUM, premise-naming)~~ — DONE

**FIXED.** `TestVStream_MidTxCheckpointReplaysNotSkips` (`internal/engines/mysql/cdc_vstream_midtx_checkpoint_integration_test.go`, `//go:build integration && vstream`) binds the premise against a real vttestserver — and needed only the LIGHTER `vstream` harness, not the full `vitesscluster` boot the filing assumed (the wire-ordering premise is single-tablet-observable; the multi-shard cluster is only needed for reshard/journal concerns). It asserts two things, mutually reinforcing: (1) a 3-row transaction's rows all carry ONE token, DISTINCT from a later transaction's token (so a row's position excludes its own tx — equal tokens would mean rows were stamped with the trailing VGTID, the regression); and (2) reopening the stream FROM a mid-tx row position REPLAYS the whole transaction (4 rows) rather than skipping its tail. Runs in the ci.yml `integration-vstream` leg (matches `-run TestVStream_`). Mutation-verified: resuming from the POST-tx (sentinel) position instead delivers only 1 row — the silent tail-drop the pin catches. `flushPendingBoundary`'s doc updated: UNVERIFIED PREMISE → bound-by-named-test.

### ~~P-2 — smartCompactor's O(n²) per-key removal on rekey-dense chains~~ — FIXED (shipped v0.128.2)

**FIXED.** `smartCompactor.order` moved from a `[]string` (O(n) linear-scan + splice removal) to a `container/list.List` plus a cached `*list.Element` on each `rowAccumulator`, so per-key removal is O(1). A single `unlinkAccumulator` door removes from the list and the map in lockstep (no nil-skip introduced), and all 8 `order`/`accumulators` consumers were enumerated and preserved (insertion order via `PushBack` + front→back walks; the pass-2 largest-chains re-sort kept verbatim). Measured: 160k rekey churn **37.5s → 0.49s**; the gate `TestSmartCompact_PKChurnRemovalIsNotSuperLinear` uses a machine-noise-cancelling reference ratio (rekey vs equal-event in-place-UPDATE) and fails above 4× — new ~0.78×, old ~20.5× (grows with n), mutation-verified both directions. Correctness suite unaffected; no new concurrency.

Historical detail: `flushKeyEmitting` removed one key by an O(n) linear scan + slice splice, called twice per PK-changing UPDATE; at the 32 MiB accumulator cap (~80–168k live keys) a ~500k-PK-changing-UPDATE segment paid a full scan+splice each, so `backup compact` went quadratic (CPU only; memory bounded).

**Deliberately deferred, not rushed:** `s.order` has FIVE consumers that assume a non-nil `s.accumulators[k]` (eviction pass 1 `:1173`, pass-2 sort `:1192`/`:1199`, `flushTable:1253`, `flushAll:1271`). A lazy-tombstone fix (leave the key in `s.order`, delete only from the map) or a `container/list`+`map[string]*list.Element` LRU (O(1) order-preserving removal) both touch all five — a missed nil-skip crashes `backup compact` on exactly the rekey-dense chain this targets. This is a delicate hot-path refactor for a correctness-UNAFFECTED perf win on an uncommon workload, so it wants fresh focus + a rekey-dense benchmark (the row-29 "bounded output" tests cover correctness but not the CPU bound), not a tail-of-session rewrite. Fix shape: `container/list` LRU (no tombstone nil-risk) is preferred over lazy tombstones; add a benchmark that drives ~500k PK-changing UPDATEs and asserts sub-quadratic scaling.

### A-3 remediation — per-package domaingate gate files (the meta-walker's ungatedDispatchRoster)

The Tier-3 audit's A-3 meta-walker (`internal/domaingate/domaingate_meta_test.go`, `TestDomainGateMetaWalker_EveryDispatcherGatedOrRostered`) now enforces that every package under `internal/` dispatching on a RAW `ir` column type has either its own `domaingate.Assert` gate file OR a written reason in `ungatedDispatchRoster`. The roster records **8 packages with un-gated column-type dispatch** — un-audited domain-transparency surface, some carrying latent A-1/A-2-shaped hazards. Building a real gate for each (verifying each site handles `ir.Domain`, exempting the safe ones) is the tracked remediation, in priority order:

1. ~~**`engines/postgres` (PRIORITY, 34 sites).**~~ **DONE (`8bda18b8`, cherry-picked to main).** Gate built (`TestDomainTransparency_PostgresDispatchRoster`), all 34 sites (22 dedup keys) classified. **6 real fixes:** the physical-write-path predicates (COPY-vs-INSERT routing + per-conn codec registration — `tableHasVerbatimColumn`/`tableHasIntervalColumn`/`tableHasPGVectorColumn`/`tableHasHstoreColumn` in `row_writer.go`, `tableHasTimetzColumn` in `timetz_codec.go`, `tableHasMacaddr8ArrayColumn` in `macaddr8_codec.go`) took the wrong physical path for a domain-over-{verbatim,interval,vector,hstore,timetz,macaddr8[]} column — now `ir.UnwrapDomain`-ed. **No silent loss** was present (domain-over-enum fails LOUDLY at `emitCreateDomainType`, not silently); the remaining 28 sites are exempt with code-verified reasons (change_applier reads TARGET information_schema → never holds `ir.Domain`, ground-truthed on real PG 16; enum-lifecycle loud-refused upstream; identity/serial can't be domain-typed; MySQL-only `ir.Set`). Mutation-verified. **Follow-up flagged:** an end-to-end value round-trip pin for domain-over-{verbatim,timetz,macaddr8[]} through the cold-start COPY path (the routing is now correct + no-regression, but a full round-trip would strengthen it); and full domain-over-enum *support* (currently a safe loud refusal) is a feature, not a gate fix.
2. ~~`engines/mydumper`, `ir/diff`, `pipeline`, `pipeline/backup`, `pipeline/lineage`, `pipeline/migcore`, `rowpredicate`.~~ **DONE (`a206deed`, cherry-picked to main).** All seven gated; `ungatedDispatchRoster` is now EMPTY (the meta-walker's anti-vacuity floors — `scanned >= 20`, `gatedFound >= 3` — keep it honest with an empty roster). **5 real Bug-233 fixes found, all zero-regression `ir.UnwrapDomain`, each pinned + mutation-verified:** `pipeline/lineage/catalog_helpers.go` (a domain-over-verbatim column never recorded the verbatim-extension marker → restore-time `refuseVerbatim*` couldn't fire = silent-hazard restore); `rowpredicate/predicate.go` ×2 (domain-over-anything fell to `FamilyUnsupported` → over-refused a valid `--where`; domain-over-cidr rendered as inet); `pipeline/redact_preflight.go` ×2 (domain-over-uuid skipped the `mask:uuid` WARN; domain-over-int skipped the overflow check → re-opened the Bug-105 silent-clamp PII window). The `ir/diff` AutoIncrement filing was verified FALSE (a domain can't wrap an AutoIncrement, and unwrapping there would be actively wrong). **Process note:** `Assert` dedupes offenders by `file:scope:operandText`, so two `col.Type` reads in one function share a key — fixing the first surfaced a hidden sixth pipeline site; worth remembering when gating a function with multiple same-operand dispatches.

## Open — 2026-08-05 audit

Full report: `workspace/repo-audit-2026-08-05.md` (untracked — which is why its §4 queue is enumerated below rather than counted). Grade B+. The two remaining CRITICALs were filed as roadmap items **131** (lane routing / secondary unique) and **132** (GTID mid-tx checkpoint), both shipped v0.113.0; the third, A-3, plus B-5 were folded into the item-127 compaction arc. Two HIGHs were filed alongside them: **133** (`--where` inet family) and **134** (SQLite index-name scoping), both shipped v0.114.0.

### HIGH, not yet filed as items

| ID | Finding | Location |
|---|---|---|
| ~~B-2~~ | **CLOSED — both halves, and the finding's own proposed fix would have been a regression.** The Postgres half was already fixed when this entry was written: roadmap **item 135** (`494cf155`, shipped v0.113.0) replaced the content sniff with a declared lane (`decodeValueFromText` / `decodeValueFromBinary`, `provUnset` refusing rather than defaulting). It did **not** take the proposed "hex only in `decodeTuple`" fix, because a `bytea[]` scanned into `*any` arrives as PG array TEXT with every element already hex-rendered — the array lane is correct today *because* it hex-decodes on the scan path, and `TestPremise_ByteaScanShapes` now pins that against a real server across all five pgx exec modes. The MySQL sibling was real, and **narrower and differently-shaped than filed**: the `v.(string)` branch is unreachable on `migrate` (the override rewrites the type the READER decodes with, so the PG reader hands back `[]byte`), and reachable only on the two CDC doors, whose column types come from the SOURCE. Closed in two pieces because the two doors can carry different evidence: the RowWriter cores now decide on `ir.Column.SourceColumnType` (`columnIsNativelyBinary`), and the ChangeApplier — whose descriptors come from the TARGET, where an override leaves no trace — cannot decide at all, so `preflightBinaryTypeOverrideOnCDC` refuses a binary `--type-override` on a sync run outright. Also: an unreadable rendering on a natively-binary column is now REFUSED (`SLUICE-E-VALUE-BYTEA-TEXT-UNRECOGNIZED`) instead of stored verbatim, matching `byteaArrayLeaf`. | `postgres/value_decode.go` (fixed v0.113.0) · `mysql/row_writer.go` · `pipeline/bytea_override_preflight.go` |
| ~~B-4~~ | **FIXED (order flipped; window SHRUNK, not closed).** The lock-free path now captures the position BEFORE opening the snapshot, so the residual is duplicate delivery rather than loss, and a churn probe (`resolveLockFreeCapturePosition`) reports whether the window caught anything — equal tips prove it was empty. `LOCK INSTANCE FOR BACKUP` was NOT adopted: it blocks DDL, not DML, so it does not stop the commits that create the window; `performance_schema.log_status` is a better position READER but is still a separate statement from `START TRANSACTION WITH CONSISTENT SNAPSHOT`, so it leaves the ordering algebra unchanged. **Residual:** a commit inside the window is re-delivered, which the idempotent apply absorbs only for keyed tables. | `mysql/cdc_snapshot.go`, `mysql/cdc_snapshot_lockfree.go` |
| ~~B-6~~ | **FIXED (`faea5402`, shipped v0.113.0), together with C-8.** Restore now dispatches to the FLOOR segment rather than the retired chain-identity root after a prune-to-floor, `chain_retire.go` retires the root explicitly, and F-8 (prune ignoring `Delete` errors) is closed in the same change. The durable halves are `TestPruneToFloorFull_RestoreDispatchMatrix` (the G-7 cell — prune → `Restore.Run` must restore the floor full AND agree with `backup verify`, across {plaintext, encrypted, signed}) and `root_manifest_reader_roster_test.go`, a fail-by-default roster over every root-manifest reader — because "the comment enumerated one reader and `Restore.Run` was the unenumerated second" was the whole defect. | `backup/restore.go`, `backup/chain_prune.go`, `backup/chain_retire.go` |
| ~~B-8~~ | **FIXED (`56a37f49`, shipped v0.113.0).** The Bug 226 write-core roster no longer discovers cores by the `bufW` field name — this is the G-8 fix, closed in the same commit as C-3's change-chunk byte ceiling. | `blobcodec/backup_chunk_line_limit_test.go` |
| ~~B-9~~ | **FIXED by REFUSAL, not reconciliation.** `flushWithReparentRetry` now refuses to re-send a batch for a table `irbackup.TableReplayIdempotent` reports false for (`SLUICE-E-COPY-RETRY-AMBIGUOUS-KEYLESS`). Reconciliation was rejected because the writer has no trustworthy baseline (N parallel workers per table; non-empty resume targets) — see ADR-0108 "The keyless carve-out" for the full argument and the accepted cost. **Sibling swept:** the PG chunked-COPY retry carried the same hole and got the same gate; `raw_copy` and PG's plain-INSERT core never re-send and are exempt. `LOAD DATA` was exempt for that reason **at the time and no longer is** — item 114 segmented it and drove every segment through `flushWithReparentRetry`, so it now inherits the carve-out (ADR-0108, "`LOAD DATA` was on that exempt list"). | `mysql/row_writer_reparent_retry.go`, `postgres/row_writer_grow_gate.go` |
| ~~B-10~~ | **FIXED (`cefeeb3b`, shipped v0.113.0), together with C-7 and roadmap item 113.** `schema diff` now compares `Sequences` and the RLS trio, renders and COUNTS foreign keys, and records CHECK/FK in the incremental. Both halves of the G-6 roster landed with it — see the Gate-proposals section for what those rosters do and do not reach. | `ir/diff/schema_diff.go` |
| ~~CDC-4~~ | **FIXED (family-level gate).** `verifyTableMapMatchesSchema` compares the TABLE_MAP type vector against the current catalog before any row is decoded and refuses with `SLUICE-E-CDC-SCHEMA-REPLAY-MISMATCH`. Comparison is by DECODE FAMILY, not exact type code, so width changes (`INT`→`BIGINT`, `VARCHAR`→`TEXT`) do not halt a stream. **Residual, stated in the code:** a pure column RENAME and an ENUM/SET label-list reorder are same-family and are NOT caught — both need `binlog_row_metadata=FULL`, which is not the default. | `mysql/cdc_table_map_guard.go` |

### HIGH — found by the value-fidelity review of B-2's own fix (2026-08-07)

A `value-fidelity-reviewer` pass over `58d332ee` found one open silent-corruption door and three gates narrower than their descriptions. The door and the two gate repairs are **fixed on branch**; what is recorded below is the residual that was deliberately NOT taken in the same pass.

| ID | Finding | Location |
|---|---|---|
| ~~B-2a~~ | **FIXED on branch (2026-08-07).** `preflightBinaryTypeOverrideOnCDC` had ONE call site, `sync`. `schema add-table` carries its own `Mappings`, exposes `--type-override` on the CLI, creates the target column from the mapped type and then hands the table to the LIVE stream (ADR-0030) — the identical refused configuration, at exit 0 with matching row counts. Fixed by calling the preflight in `AddTable.Run` before any connection, and gated by `TestMappingsCDCLaneRoster`: a **derived** roster (every struct in `internal/pipeline` with a `Mappings []config.Mapping` field — five today) where each is enforced-or-exempt-with-a-reason, over BOTH preflights. Mutation-verified in both directions. | `pipeline/add_table.go`, `pipeline/mappings_cdc_lane_roster_test.go` |
| ~~B-2b~~ | **FIXED on branch (2026-08-07), with the blind half stated.** The refusal was keyed on CONFIG where the hazard is SCHEMA: `migrate --type-override t.c=bytea` followed by a plain `sync` sees `len(mappings)==0`, refuses nothing, and the applier hex-decodes every later update — and the refusal's own remedy text RECOMMENDED that sequence. Fixed by (a) correcting the remedy prose (pinned by `TestBinaryOverrideRemedyDoesNotRecommendTheLossyPath`) and (b) adding `preflightBinaryTargetColumnsOnCDC`, a source-vs-target type comparison run on every single-database cold-start branch and on add-table, over the target catalog read the ADR-0166 shape gate already performs (now memoized, so still ONE round-trip). **Residual, stated at the function:** warm resume reads neither schema and is uncovered except by the config check; the multi-database cold-start fan-out (`coldStartCopyOneDatabase`) has no target-catalog read of its own — the ADR-0166 shape gate is absent there too — and is likewise config-check-only. | `pipeline/bytea_override_preflight.go`, `pipeline/streamer_coldstart.go` |
| ~~B-2c~~ | **CLOSED (verified 2026-08-18 against code — the "no pin exists" claim was already false).** The `[]any` branch now routes through `marshalArrayLeaves` → `normalizeArrayLeavesForJSON` → `arrayLeafForJSON` (`mysql/row_writer.go:1670/1736/1911`), which carries a `json[]` leaf as a JSON *string* (not `encoding/json`'s base64), with a WART note on the deliberate `bytea[]` base64. Pinned by `row_writer_array_leaf_test.go`, `row_writer_array_leaf_roster_test.go`, `row_writer_array_leaf_restore_test.go`. The silent-loss shape (`json[]` document → opaque base64) is fixed and gated. | `mysql/row_writer.go` |
| ~~B-2d~~ | **FIXED on branch (2026-08-22); every new cell round-tripped clean on real servers — the value is the permanent pins, no production defect surfaced.** PG: `TestByteaProvenance_ExecModeShapeMatrix` pins 7 shapes (1-D, 2-D, 3-D, NULL-element flat and nested, empty, NULL-array) × all 5 pgx exec modes — 35 cells, each asserting the array-text-string delivery premise, routing through the real `decodeValueFromBinary`, and comparing dims + per-element bytes against the server's own `array_dims`/`encode` (a flatten fails by name); anti-vacuity floor ≥30 cells. The scan-lane (`ReadRows`/`ReadRowsBatch`/`ReadRowsBatchBounded`) and pgoutput-CDC fixtures gained 3-D-with-NULL-and-uppercase-leaf and 2-D-with-NULL arrays, floored by `assertByteaArrayShapeFloor` (shape inventory derived from the server's answers, not the case table). MySQL: uppercase `\xDEAD` + mixed-case `\xDeAd` cells on BOTH provenances across all three write cores (integration) and the full unit matrix, floored by `assertByteaProvCaseFloor` (requires both hex spellings present). | `postgres/bytea_provenance_integration_test.go`, `mysql/bytea_provenance_integration_test.go`, `mysql/bytea_provenance_test.go` |
| ~~B-2e~~ | **FIXED on branch (2026-08-22) — both stated holes closed in the gate itself.** The walker now records every matched CALL (a function with N>1 keys each site `pkg.func#1..#N`, so a second decode in a blessed function produces an unrostered key AND a stale un-suffixed entry — red both ways), and the `hex` selector resolves against each file's own `encoding/hex` import (alias matched; a foreign package aliased AS `hex` not matched; dot-import refused loudly). Pinned by `TestFindHexDecodeSites_WalkerCapabilities` against synthetic sources; mutation-verified on the real tree in both directions (second decode in `postgres.decodeByteaFromText`; aliased-import decode in a new mysql function), mutants grep-confirmed before each run. Roster keys, floors, and verdicts unchanged. | `engines/hex_provenance_roster_test.go` |

### HIGH — found by the B-9/B-4/CDC-4 sibling sweep (2026-08-05)

| ID | Finding | Location |
|---|---|---|
| ~~B-4b~~ | **FIXED (order flipped; window SHRUNK, not closed) — 2026-08-05, on branch.** `openBackupSnapshotSerial` captured the chain anchor AFTER opening the consistent-snapshot transaction while holding no lock, on the path every coordinated-FTWRL failure falls back to (i.e. every RDS backup); a commit in that window was above the read view and below the recorded EndPosition, so it was in neither the full nor an incremental taken from it. The anchor is now captured FIRST, so the full's sweep is a SUPERSET of everything at or below the recorded EndPosition and the residual is duplicate REPLAY rather than loss. A churn probe (`reportBackupCaptureWindow`) brackets the window with two binlog-tip reads and reports quiet / moved / UNKNOWN per run. **The invariants were established before the flip, not after** (the reason it was deferred): lineage widens rather than narrows, since an incremental starts at `parent.EndPosition` and an earlier anchor only replays more; `assertDataWindowEndPositionInvariant` returns early on zero change chunks so a full's anchor never reaches it; resume-anchor adoption, `BackupID` derivation and ADR-0088's coordinated path are all untouched; MySQL has no `releaseChainAckTo` slot (retention is `binlog_expire_logs_seconds`, and the extra retention needed is one round-trip); and `chain_restore` replays change chunks through `ir.ChangeApplier.ApplyBatch`, idempotent per ADR-0010. **Residual:** a keyless table (no PK, no NOT NULL UNIQUE) has no key for the apply to collide on, so a replayed INSERT can duplicate a row — the same ADR-0089 at-least-once accounting B-4 left. **Also corrected:** `captureBackupPositionInTx` is renamed `captureBackupPosition`, because the name *was* the argument — nothing it reads is transactional, so being inside the tx never bound the anchor to the read view. | `mysql/backup_snapshot.go`, `mysql/backup_snapshot_lockfree.go` |

### MEDIUM

~~`C-1` `IsTableEmpty` substring classifiers gate the not-empty refusal (classify by 1146/42P01 instead)~~ — **CLOSED on branch 2026-08-08.** Both engines classify on the code now (`mysql/missing_table.go`, `postgres/missing_table.go`), and BOTH failure directions turned out to be real and reproducible on stock containers rather than theoretical. *Lets one through:* a PG target whose database has gone away answers `FATAL: database "…" does not exist` at SQLSTATE **3D000**, and the substring read that as "the table is absent" — `IsTableEmpty` returned `(true, nil)` for a target sluice could not even reach, which is the answer that bypasses the populated-target refusal. Pinned through `IsTableEmpty` itself, not through the classifier: a first mutation run showed a classifier-only pin left the probe's own branch ungated. *Refuses a valid run:* with `lc_messages='fr_FR'` a stock `mysql:8` returns errno **1146** reading `La table '…' n'existe pas`, so the probe errored and the cold-start preflight refused a migration whose target table merely did not exist yet. The comment claiming the substring also matched "the Vitess equivalent" was **false**: vtgate words its unknown-table errors `table '%s' does not exist` (VT05004) and `… in keyspace '%s'` (VT05005) — "does not", never "doesn't" — so it matched neither, and both are now accepted by code (`vterrors.UnknownTable` → **1109**, `vterrors.NoSuchTable` → **1146**, read out of the pinned vitess.io/vitess v0.24.2 rather than assumed). Sibling sweep: `isMySQLMissingTableErr` (control table / migrate-state / query-timeout recovery) was substring-ONLY where its PG twin was already structural — now structural-first with the legacy text kept solely for a value that lost the driver type; `postgres`'s two identical 42P01 helpers one letter apart were collapsed into one. Exempt with reasons: `isUndefinedColumnErr` (already structural-first, and its fallback matches the SQLSTATE token, not a natural-language phrase), the `sqlite-trigger` `sqlite_sequence` pair (no error code exists on the D1 transport, so a fix would reach one of two transports — and it gates a counter default, not a refusal),, and `cmd/sluice`'s `isStreamNotFoundErr` (it matches sluice's own wrapped sentinel text and gates a CLI wait loop). **The deferred residual is now CLOSED too (2026-08-08), and the sweep it triggered found five more.** `postgres/change_applier.go`'s truncate no-op — `strings.Contains(msg, "42P01") || strings.Contains(msg, "does not exist")`, gating TWO `return nil`s — read a vanished DATABASE (3D000, which a pooled `*sql.DB` surfaces after a re-dial) as "the table is gone, skip it", so the applier reported a TRUNCATE that never ran and the stream advanced past it. Siblings closed in the same pass: `isSlotAlreadyGoneErr` (bare `"does not exist"`, swallowing a failed `pg_drop_replication_slot` into "already gone" — the slot survives and keeps pinning WAL on the SOURCE, and the caller deliberately suppresses that warning); `isUndefinedRelationErr` (safe only by a coincidence of conjunction, and its doc claimed the PostGIS lookup while SIX sites use it, including the control-table and migration-state existence probes); and the two `internal/pipeline` decommission classifiers plus `cmd/sluice`'s `isSlotNotFoundErr`, all three of which matched text for the SAME stated reason — the marker was package-private to an engine and those packages cannot import it. That reason was sound and its conclusion was not: **[ir.ErrSlotNotFound] and [ir.ErrSlotActive] now live in the `SlotManager` contract**, so engine-neutrality and structural classification were never in tension. The decommission one is the worst of the set — it swallowed into `SlotAlreadyAbsent` AND then cleared the control row, which is the only record of the slot NAME, leaving an operator with a WAL leak and nothing to find it by. The durable half is `errclassgate.AssertNoErrorTextClassification`, instantiated in all four engines plus `internal/pipeline` and `cmd/sluice`; it found a sixth site the manual sweep missed, and its own predicate is pinned by `TestClassifiesByErrorText` after a mechanical edit silently disabled half of it while every instantiation still reported green. · ~~`C-2` `isPGSourceEngine` hand-kept two-name list with no registry gate~~ — **CLOSED on main 2026-08-10 (`8d85c665`), by this entry's own prescription — and the entry then said "attempted and backed out; read this before retrying" for two more weeks, which is exactly the doc-lags-code shape it sat next to.** The gate is `TestPGSourceRosterCoversEveryRegisteredEngine` (`internal/engineroster/pg_source_roster_test.go`): its own package (so `internal/engines`' registry_test.go `reset()` cannot empty its registry — the floor caught that too), blank-importing every engine package the shipped binary does, holding a fail-by-default roster (every entry a classification WITH a reason) to `migcore.IsPGSourceEngine` — exported for it, the unexported wrapper kept for call sites. Floors: ≥12 registered engines (the backed-out walker's "discovered only 2" catcher, kept as prescribed), ≥2 classifying PG-source (an emptied predicate leaves every refusal it gates inert), and a stale-entry check. Re-verified by fresh mutation runs at v0.131.3 HEAD, 2026-08-23, mutants grep-confirmed and reverted by targeted edits: predicate narrowed to `postgres` alone → red twice over (roster disagreement naming postgres-trigger + the ≥2 floor); a registered engine's roster entry removed → red naming the unclassified engine; a phantom roster entry added → red naming it · ~~`C-3` `backup incremental`'s change-chunk lane has **no byte ceiling**~~ — **CLOSED on branch 2026-08-05 (roadmap item 142).** The ceiling reaches both change lanes; the durable half is `TestChunkWriteLanes_EveryLaneBoundsAChunkByBytes`, a roster derived from every `blobcodec` chunk-writer CALL SITE under `internal/pipeline`, with anti-vacuity floors on both the total lane count and the change-family count · ~~`C-4` compaction re-stamps chunk SHA/RowCount from its own writer, never re-reads~~ — CLOSED on branch 2026-08-05 (roadmap item 129) · ~~`C-6` item 118's multi-DB preflight runs per-database inside the copy loop while comments claim the N−1 half is fixed~~ — **VERDICT: the comment was the defect; the behaviour is a filed residual (2026-08-09).** Re-read against the code: `streamer_multidb.go`'s `PreflightTableEmit` call sits inside `coldStartCopyOneDatabase`, which is the per-database COPY loop, and each database's schema is read there. So the preflight does stop THIS database's rows landing before the refusal, and does NOT stop the run having copied the preceding ones — an unrepresentable index in database N is still found with N-1 databases fully copied. The old comment said it avoided being discovered "after N-1 databases had already been copied AND after this one's rows had landed", whose `AND` reads as though both were prevented. Corrected in place to state exactly what it prevents. **The residual is NOT fixed and is deliberately not urgent:** it costs wasted copy time and leaves a target holding N-1 COMPLETE databases (this database's tables are not created yet), it is loud, and the run is resumable — so it ranked below the silent-loss queue. **To fix it:** hoist the check above the copy loop, which needs all N schemas read up front — either doubling the schema reads or threading a cache through `coldStartCopyOneDatabase`. There is already an up-front all-databases loop at the `EnsureDatabase` step and an up-front `preflightNamespaceFoldCollisions`, so the shape exists to slot into · ~~`C-7` FK drift invisible in the `schema diff` text render and JSON summary counts~~ — **CLOSED (`cefeeb3b`, shipped v0.113.0) with B-10 and roadmap item 113**; the durable half is `TestDiffSurfaceRosterEveryTableDiffFieldIsRenderedAndCounted`, which requires every `TableDiff`/`SchemaDiff` field to be rendered AND counted or exempted with a reason · ~~`C-8` prune/compact never delete `.sig` siblings, so a retired root still cryptographically verifies~~ — **CLOSED (`faea5402`, shipped v0.113.0) with B-6**; the orphan-`.sig` assertion after maintenance is part of the G-7 cell · ~~`C-9` appliers diverge on nil-Before Updates~~ — **CLOSED (`e464b56d`, shipping v0.118.0) together with C-10; they are ONE defect.** The divergence is NOT between the two engines, as filed, but between MySQL's coalescing batch path and everything else: `PKChangedUpdate` answers false for a nil Before because it cannot compare what it does not have, so the one shape carrying no predicate at all was also the one shape that skipped the predicate builder. Both engines' UPDATE/DELETE builders now refuse an unusable before-image by name instead of emitting an empty `WHERE` · ~~`C-10` update-as-upsert + TOAST-omitted After + absent target row fabricates a partial row~~ — **CLOSED (`e464b56d`).** Confirmed as FABRICATION rather than mere divergence: an absent target row came back from a real MySQL as a synthesised default — a row that never existed at the source, exit 0 — and whether it was even VISIBLE depended on the target column (`NOT NULL` without a default died with Error 1364; nullable took it silently). `dispatchUpdate` routes an empty before-image and a partial after-image to the serial path; the cost is throughput, announced once per table at INFO · ~~`C-11` unknown-table drift: PG drops-with-WARN forever, MySQL halts loudly, divergence unrecorded~~ — **CLOSED on branch 2026-08-12, on the operator's decision (skip-and-count, halting rejected: the source still holds every skipped row, so a skipped table is always recoverable with add-table, while a halt lags every table and past binlog/slot retention loses the position itself).** Both engines now drop unknown-target-table events with a once-per-table WARN and a durable per-(stream, table) ledger row in `sluice_cdc_skipped_tables` (count + first/last skipped position tokens stored verbatim); `sync status`, the `sync stop` summary, and the streamer's settle log surface it, and `sync health` exits 1 while any count is nonzero. The MySQL halt was the metadata-lookup error propagating; classification is structural (an information_schema existence probe, never error text — the C-1 lesson). The sweep also removed PG's truncate exec-time swallow, which was unsound all along: the absorbed 42P01 left the open tx aborted, so the position write behind it failed with 25P02 — a poison-pill retry dressed as a benign skip; both engines now resolve the table BEFORE executing, and exec-time missing-table (a table dropped mid-stream after applies) stays loud on every path, deliberately (on the batched paths a failed statement aborts the whole batch tx, so mid-batch absorption is not implementable without re-execution). Pinned by real-container integration tests on both engines × {per-change Apply inside a CDCPOS-2 source-tx envelope, batched/pipelined apply} with hostile-ish table names + tokens round-tripped byte-exact, plus health/status/stop render pins; mutation runs (recording severed → pins red; health ignoring the counter → pin red) recorded in the closing commit · ~~`C-12` GHCR image not attested while "every release artifact" is claimed~~ — **CLOSED on branch 2026-08-07.** Both halves confirmed live before acting: the tarball's attestation listed exactly the 13 GitHub release assets, and `gh attestation verify oci://ghcr.io/sluicesync/sluice:0.116.1` returned `HTTP 404` on digest `sha256:85a93ac3…` — which `docker buildx imagetools inspect` independently confirms is the multi-arch INDEX digest, so the index is the right subject. The image is now attested at **three** digests (index + both per-arch manifests, because `:<ver>-amd64`/`:<ver>-arm64` are pullable tags an index attestation does not cover), sourced from GoReleaser's own `dist/artifacts.json` rather than a post-push registry re-read, with a loud failure if a digest cannot be resolved. The durable half is three gates in `internal/docsync`: `TestEveryPublishedReleaseArtifactIsAttestedOrExempt` (every top-level `.goreleaser.yaml` section classified attested-or-exempt-with-a-reason, each claim citing a string that must appear in `release.yml`), `TestSecurityMDAttestationRosterMatchesTheCode` (SECURITY.md's two operator-facing markers derived from that same map), and `TestReleaseWorkflowAttestationStepsAreWellFormed` (structural YAML grading — this repo has no actionlint job, so a malformed step would otherwise first surface on a tag push). **Residual, stated rather than implied: the `gh attestation verify oci://` green cannot be produced before a real publish** — the post-publish check is recorded in the commit message. The SHA-pinning MEDIUM filed alongside it was already closed: `release.yml` and `dependabot-automerge.yml`, the only two write-scoped workflows, are fully SHA-pinned; the five unpinned workflows are all `contents: read` (+ `packages: read`) test suites · ~~`C-14` per-row geometry SRID silently dropped on both engines~~ — **CLOSED (`e464b56d` + `edcdfcab`, shipping v0.118.0).** One sentence repeated verbatim in three decoders ("SRID is a per-COLUMN property") was true of sluice's IR and false of both engines. Refused loudly rather than carried, because carriage means changing the IR's geometry VALUE contract from WKB to EWKB across every decoder, both writers, the mydumper literal path, the VStream cell decoder and the backup change codec — filed as its own chunk. The sweep found two more: a DOMAIN over `geometry(POINT,4326)` reading as SRID 0, and the mydumper lexer dropping the SRID attribute, **where the fixture DDL carried it while the expectation beside it asserted SRID 0, so the dump leg and the live leg agreed on the same wrong answer.** The follow-up commit is the one worth remembering: the first cut compared against a column SRID the CDC lane never had (pgoutput carries none, so the relation cache projected a bare `ir.Geometry{}` whose SRID 0 is ALSO the legitimate unconstrained value), which refused every row of every `geometry(Point,4326)` column and wedged the stream. Caught by the PostGIS job pre-tag. `ir.GeometrySRIDUnknown` now carries the distinction the int cannot, and the anti-vacuity test drives the shape that must STILL refuse — because a green family test is equally consistent with the guard having been switched off.

### Gate proposals not yet built

**A wall-clock duty-cycle assertion is load-sensitive by construction, and one is now in the per-PR suite.** `TestGrowGate_SustainedStormIsStillBoundedByTheShareCeiling` failed once on 2026-08-08 in a worktree while four agents and several full `go test ./...` runs shared the machine; it then passed 8/8 in that same worktree and 10/10 on `main`, so it is load-induced rather than a regression. Recorded rather than shrugged at because the shape is the same one that produced the Windows SQLite-contention flake at tag time: a timing assertion that holds on an idle box and not a busy one, in a suite that CI runs on shared runners. *Options, in preference order:* assert the ceiling from the gate's own accounting (hold/idle totals it already tracks) rather than from wall clock; or keep wall clock but give the bound enough slack that only a real regression can breach it, with the slack's reason stated. **Do not simply add a retry** — a duty-cycle test that passes on the second attempt is measuring the retry, not the ceiling.

~~**G-13 — nothing mechanically checks the `file:line` citations in `docs/`, which is why `perf-parity-matrix.md` drifts a dozen at a time.**~~ **BUILT, both halves — and this entry was itself doc-lags-code: the `path:line` half had been live since 2026-08-18 (`TestDocCodePointers_ExistAndLineInRange`, dated in its own doc comment) while this entry still read "not built".** (1) *`path:line`:* every backtick-quoted `internal/….go:LINE(-LINE)` span in `docs/dev/**.md` must exist and be in range — deliberately NOT the symbol at the line, which would re-implement a compiler and false-fail on every edit above the cite; floor ≥20 spans/≥1 doc. (2) *The `pkg.Symbol` sibling (built 2026-08-22):* `TestDocSymbolPointers_ResolveAgainstTheirPackage` grades backtick-quoted `pkg.Symbol` spans in the CLAIM-surface docs against the union of top-level decl names in same-basename `internal/` package dirs (~83 graded spans today; floors ≥40 spans/≥2 docs/≥30-package universe; a stale exemption fails). **Reach, stated at the gate rather than implied:** it deliberately EXCLUDES `roadmap.md`, `item49-phase3-prep.md`, `docs/dev/design/` and `docs/dev/notes/` — planning surfaces whose house convention names PROPOSED symbols — and does not grade Type.Field/receiver refs, stdlib refs, import-alias refs (`irdiff.X`), or snake_case database-object citations (`mysql.general_log`). It caught its intended prey on first contact: the phantom `backup.SourceEngineCommitsAfterRows` in this very file (now a named exemption, since these citations name it AS the phantom). Both halves mutation-verified in both directions on 2026-08-22 (fresh phantom cite → red; out-of-range line cite → red; exemption disabled → the real-doc phantom reddens plus the stale-exemption check fires; restored → green). *Residual, stated:* symbol cites in the planning surfaces are ungated by design — a shipped roadmap entry citing a since-renamed symbol (e.g. the roadmap still spells pipeline.checkCrossEngineSupportable — unbackticked here because the gate reads a backticked span as an existence claim, which is the point — for what is now `migcore.CheckCrossEngineSupportable`) is caught by nothing; grading them would need shipped-vs-planned discrimination inside roadmap prose, which is a design decision, not a walker fix.

`G-1`/`G-2`/`G-3` ride items 131/132 (G-3 is **built** — the item-127/A-3 pins). ~~`G-4` bytea family-matrix row ground-truthed with `octet_length()`~~ — **BUILT, both engines.** Postgres: `bytea_provenance_integration_test.go` (scan × chunked-scan × pgoutput doors, scalar + `bytea[]` incl. 2-D and NULL-element, ground-truthed against `encode`/`octet_length`/`array_dims`) plus the `internal/pipeline` migrate + backup-chain rows. MySQL: `TestByteaProvenance_MySQLWriteCores` — three write cores × two column provenances × the shape matrix, ground-truthed with `OCTET_LENGTH()`. The durable half is `TestHexDecodeSitesAreProvenanceDecided`, a fail-by-default AST roster over EVERY `hex.Decode`/`hex.DecodeString`/`decodeHexByteaText` site in `internal/engines/**` (with an anti-vacuity floor and a per-engine floor): a new hex-decode anywhere in an engine fails the build until someone records what DECIDES the reading — provenance, grammar, or a written structural argument. `TestGeometryHexDiscriminatorIsTotal` is that written argument for the one site classified structural · `G-5` binlog BIT(N>1) integration column (unit matrix **built**) · ~~`G-6` reflective compared-or-exempt rosters over `ir.Schema`/`ir.Table` and `TableDiff`~~ — **BUILT, BOTH HALVES, at the CONTAINER level only; the residual is real and each gate states it.** `TestSchemaCompletenessRoster` (`internal/ir/diff/schema_completeness_roster_test.go`) walks `ir.Schema` and `ir.Table` by reflection and requires every field to be PROBED — mutated on one side of an otherwise-identical pair, with the probe naming the summary phrase its own comparator produces, so a `Policies` probe cannot be satisfied by a column diff — or exempted with a reason. `TestDiffSurfaceRosterEveryTableDiffFieldIsRenderedAndCounted` (`internal/pipeline/diff_completeness_roster_test.go`) does the same for `irdiff.TableDiff`/`SchemaDiff` against the TEXT renderer and the JSON summary counts, requiring both (a field that renders but is not counted is the CI consumer's blind spot). **The contained-struct follow-on rosters are BUILT (2026-08-22, on branch), closing the residual each container gate stated:** `TestContainedIRCompletenessRosterEveryFieldComparedOrExempt` (`internal/ir/diff/contained_completeness_roster_test.go`) walks `ir.Column`/`Index`/`IndexColumn`/`ForeignKey`/`Policy`/`Sequence` — every field probed (must produce drift AND populate the field's OWN nested-diff slot, so a Nullable probe cannot be satisfied by a Type drift) or exempt-with-a-reason; 39 probes, floors = probe counts (5/4/4/10/6/10). `TestContainedDiffSurfaceRosterEveryNestedFieldIsRendered` (`internal/pipeline/diff_contained_completeness_roster_test.go`) walks `ColumnDiff`/`IndexDiff`/`ForeignKeyDiff`/`PolicyDiff`/`SequenceDiff` — every field's own content must reach the text render and the container counter must fire (61 probes, floors 13/7/13/11/17; per-ENTRY counting stated as deliberate — the JSON body embeds the diff wholesale). Two NAMED GAPS recorded as exemptions rather than silence: `ir.Column.OnUpdateCurrentTimestamp` is NOT compared (an honest comparison first needs the Bug-234-shaped target-cannot-hold suppression, else every MySQL→PG diff reports irreconcilable drift), and the four `ir.Index.Constraint*` UNIQUE-attribute flags are NOT compared (metadata-only until the roadmap "UNIQUE-constraint attribute fidelity" carry ships; the PG reader WARNs at read time — when an emitter starts carrying them these exemptions go stale and force the decision). Not walked, stated at each gate: `ir.CheckConstraint`/`ExcludeConstraint`/`View`, `CheckDiff`/`ExcludeDiff`/`ViewDiff` (name-plus-one-verbatim-body structs whose fields the container probes already exercise), and the `ir.Type` implementations (the type-family universe, pinned elsewhere). Mutation-verified both directions on 2026-08-22, mutants grep-confirmed: de-compared `Policy.Roles` → red; `OnDelete` drift populating the wrong slot → red via the nested-slot binding; new fields on `ir.ForeignKey` and `irdiff.PolicyDiff` → red; a dropped render value → red — after the first run exposed a weak probe (`target CASCADE` satisfied by the "the target CASCADEs…" consequence prose), fixed with a pair-span probe and a comment at the site · ~~`G-7` prune-to-full-only × {plaintext, encrypted, signed} restore cell~~ — **BUILT** (`TestPruneToFloorFull_RestoreDispatchMatrix`, `internal/pipeline/backup_prune_floor_dispatch_integration_test.go`), landed with the B-6/C-8 fix · ~~`G-8` dual-signal write-core discovery~~ — **BUILT**; it IS the B-8 fix (`56a37f49`), not a separate piece of work · ~~`G-9` keyless-retry failpoint pin~~ **built** (`TestColdCopyReparentRetry_Keyless*` on both engines; the oracle is the driver's own exec/attempt count, not the writer's report) · `G-10` docsync markers — **RE-SCOPED 2026-08-07: six markers are now live**, each derived from the registry rather than from a directory listing (`9c12b7bb`, then the psdb-host pair): `<!-- index-verify-engines: … -->` (`docs/operator/error-codes.md`), `<!-- filtered-sync-engines: … -->` (`docs/operator/filtered-subset-migration.md`), `<!-- target-engines: … -->` and `<!-- cdc-modes: … -->` (both `docs/production-readiness.md`), and `<!-- psdb-mysql-host-suffixes: … -->` + `<!-- psdb-mysql-host-drivers: … -->` in all THREE homes of that claim (README, production-readiness, error-codes), plus the error-codes table contiguity check. **Outstanding: none** (the psdb-host half is CLOSED — see below). ~~(1) **README freshness floor.**~~ — **CLOSED on branch 2026-08-23.** `TestEngineEnumerationsMatchTheRegistry` grades the VISIBLE full-engine enumerations (no hidden marker — the "(`sluice engines` …): `a`, `b`" sentence is machine-parseable as written) plus every spelled-out engine COUNT near the word "engine", in both homes: README (architecture paragraph + glossary) and production-readiness's header line. Floors: per-home minimum site and count-word tallies (a reworded anchor fails loudly rather than shrinking the scan — mutation-proven), registry ≥10 with three-package spread. Mutation-verified: engine dropped from an enumeration → red; fake engine added → red; count-word staled to "fifteen" → red; anchor phrase reworded → red via the site floor; unrelated edit → green ~~(2) **`OpenChangeApplier`**~~ — **CLOSED on branch 2026-08-23.** `TestCDCApplyTargetListMatchesTheCode` derives the CDC-apply set from the `OpenChangeApplier` door two ways (the AST refusal-stub shape + calling the door with an empty DSN, the chain-restore gate's own helpers) and holds `<!-- cdc-apply-targets: mariadb, mysql, planetscale, postgres, postgres-trigger, vitess -->` in production-readiness.md to it; the prose beside it now names the six engines instead of "MySQL-family and Postgres", and `TestTargetEngineListMatchesTheCode`'s "still prose bound to nothing" scope note points at the new gate. Floors: non-empty, not-all-engines, and sqlite must never classify (its migrate-target/CDC-apply split is the marker's reason to exist). Mutation-verified both directions · ~~`G-11` lock-free-branch ordering pin~~ **built and GENERALISED with B-4b** — `TestLockFreeSnapshotCapturesPositionBeforeSnapshot` and `TestBackupSnapshotCapturesPositionBeforeSnapshot` read `mysql.general_log` (the server's own statement order, not sluice's) for the cold-start and backup lanes respectively, and `TestSnapshotCaptureOrder_EveryUnlockedSiteCapturesBeforeTheSnapshot` is the fail-by-default AST ROSTER over every function in `engines/mysql` that both opens a consistent-snapshot tx and captures a position — the per-site pin is what let B-4b survive B-4, so the roster is the durable half. Exemptions carry written reasons (both FTWRL-holding sites) and a stale one fails the build; `readBinlogTip` is named as a non-anchor capture, because grading the probe instead of the anchor was a real hole the first draft's mutation run exposed. Known limit, stated at the gate: it grades SOURCE order, not reachability · `G-12` adopt the invariant sweep as a standing queue.

**G-10's psdb-host × driver half is CLOSED (2026-08-07).** The false sentence at `docs/production-readiness.md:21` is corrected, and so are its two Go siblings — `mysql.Engine.DefaultExcludePatterns`' doc comment, which called the refused pairing "a legitimate configuration — the operator gets binlog CDC instead of VStream", and `ir.DefaultTableExcluder`'s. The durable half is `TestPSDBMySQLHostDriverClaimMatchesTheCode` (`internal/docsync/psdb_host_driver_test.go`): it reads `planetScaleMySQLHostSuffixes` out of the engine by AST, builds a DSN from each suffix, asks every registered `ir.DSNValidator` what it actually returns, and holds two markers — the suffix list and the FULL graded verdict map (`mariadb=refused, mysql=refused, planetscale=ok, vitess=ok`) — in all three homes. The verdict map rather than the works-list is deliberate: a marker naming only the working drivers reads as broader than it is. Mutation-verified in both directions, per home, plus a negative control (an unrelated doc edit does not fire). `TestPSDBMySQLHostClaimHasNoUngatedHome` is what keeps the three-home list from being the next narrow gate: any non-archival markdown naming an endpoint suffix must be a gated home or exempt with a written reason. **Known limit, stated at the gate:** it grades the DSN-string refusal only — `preflightDSNValidation` runs on `migrate` and `sync` and nothing else, so `backup` / `schema diff` / `schema preview` reach a non-VStream flavor against a PlanetScale host without asking. That split is now rostered by `TestDefaultExcludeCallSitesRecordTheirDSNPreflight` + `TestDSNValidationPreflightCallSites` in `internal/pipeline`; **whether `backup` SHOULD refuse the pairing up front is an open decision, not a fix taken here.**

### ~~HIGH — doc claims contradicted by the code (found by the 2026-08-07 psdb sweep)~~ — ALL THREE CLOSED (verified 2026-08-18 against code + markers)

The psdb correction above came with a sweep for the same shape — an operator-facing sentence describing a configuration the code refuses. All three are now corrected AND each carries the docsync marker the entry asked for.

| ID | Finding | Location |
|---|---|---|
| ~~D-1~~ | **CLOSED.** The false "works on every engine" sentence is gone and `docs/operator/filtered-subset-migration.md:59` carries `<!-- migrate-where-engines: mariadb, mysql, planetscale, postgres, postgres-trigger, vitess -->`, matching `migcore/row_filter.go`'s `RowFilterSetter` refusal. | `docs/operator/filtered-subset-migration.md` |
| ~~D-2~~ | **CLOSED.** `docs/production-readiness.md:65` now says the incremental side "needs a CDC-capable source … both refuse a CDC-less source at validate" + `<!-- incremental-backup-engines: … -->` marker. | `docs/production-readiness.md` |
| ~~D-3~~ | **CLOSED.** `docs/production-readiness.md:69` now says `expand-contract`/`deploy-ddl` are **PlanetScale-only** + `<!-- planetscale-only-commands: deploy-ddl, expand-contract -->`. | `docs/production-readiness.md` |

A gate built alongside these that was not on the G-list, recorded so it is findable: `TestBinlogTypeFamilyRoster` is a fail-by-default roster requiring every `data_type` `translateType` accepts to be either classified for the CDC-4 guard or listed as exempt with a reason, and `TestCDCSteadyState_EveryDataTypePassesTheGuard` ground-truths that classification against what a real mysqld writes into a TABLE_MAP (a single wrong entry produces a false refusal, which is how it was mutation-verified).

### Invariant sweep — the enumerated queue

55 claims: 33 verified, 17 unverified, 5 suspect-false. **The queue is CLOSED as of 2026-08-08.** All 5 suspect-false closed 2026-08-07 (table below); all 14 *nameable* unverified claims closed across four batches, with the remaining 3 unrecoverable (they existed only in the un-preserved per-worker sweep output — see the completeness caveat). **11 of the 14 verdicts differed from their filing**, which is the number that argues for re-running the sweep; the breakdown and the recommendation are below.

**Why this is enumerated here and not summarised.** The previous version of this section was the three counts in the sentence above and nothing else, with the list itself living only in `workspace/repo-audit-2026-08-05.md` — the same gitignored directory whose durability this whole file exists to fix, one level down. A count is not a work queue: it cannot be worked, and it cannot tell you which items are already closed. Ported 2026-08-07.

**Locations are as of the audited HEAD `9eaa4644` (v0.112.0) and several have moved since** — `laneapply/router.go` was rewritten by item 131, `incremental.go` and `ir/backup/manifest.go` have gained NOTE blocks from item 132. Re-resolve the site before working an entry; the CLAIM is the durable part, not the line number.

#### The 5 SUSPECT_FALSE claims — ALL CLOSED 2026-08-07

Worked as a batch on 2026-08-07. Every verdict was re-derived from the code rather than taken from the audit (the queue's own instruction: the audit's verdict is a hypothesis), and every one survived — but two were false in ways the audit had not named, and one gate repair found a sibling nobody had filed. Each row now ends with the name of the test that fails if the claim breaks again.

| # | Claim (site as audited) | Status at 2026-08-07 | Action |
|---|---|---|---|
| 1 | `backup/chain_compact_smart.go:781` — a PK-changing UPDATE "is represented as DELETE+INSERT in MySQL and as a separate event in PG" | **CLOSED.** Became CRITICAL A-3 / roadmap item 127's arc; both engines in fact emit one `ir.Update`, which is why `laneapply.PKChangedUpdate` exists | fixed + pinned |
| 2 | `backup/restore.go:1045` — the DataOnly plain-INSERT fallback "only collides on a PK the upsert would have updated to the same value" | **CLOSED 2026-08-07.** Verdict FALSE, and false in a second way the audit did not name: there is no PK branch at that dispatch at all, so "no-PK tables fall back to plain WriteRows" was never true — a no-PK table goes through `WriteRowsIdempotent` like any other, keyed on a non-null UNIQUE, and a TRULY keyless one is refused by the engine (`errKeylessIdempotent`). The skeptic's dead-code narrowing is CONFIRMED and its mechanism is now written down: `ChainRestore.Run` opens `OpenChangeApplier` before the segment walk (`chain_restore.go:344`), so only a CDC-apply target reaches DataOnly, and sqlite — the one target-capable engine with no idempotent writer — refuses that door | fixed: loud refusal (`errRestoreDataOnlyNotIdempotent`, resolved before any goroutine or channel exists) + comment rewritten + `ir/interfaces.go`'s twin corrected. Pins: `TestRestore_DataOnly_RefusesNonIdempotentWriter` / `TestRestore_NonDataOnly_AcceptsPlainWriter` (both directions) and `TestChainRestoreTargetsAreIdempotentWriters` (docsync — derives the CDC-apply engine set two ways and pins each engine's real writer type) |
| 3 | `pipeline/incremental.go:1300` — "the restore-side completeness net relies on exactly that [anchor] distinction" | **CLOSED 2026-08-07.** Verdict FALSE, confirmed against the code the sentence names: `SchemaHistoryAnchors` has exactly ONE non-test caller in the tree and it is the writer-side assert; the restore net rests on `reachedEnd := lastApplied == end` (`chain_restore.go:1408`) and says at its own definition that it does not trust the anchor. **Sibling swept:** `Manifest.CDCPositionCommitsAfterRows`' field doc made the same false claim ("the restore-side completeness backstop reads this…") — no restore-side code reads that field either. **No redesign:** roadmap item 99 is untouched and still blocked | fixed: both comments corrected in the project's correction style. Pin: `TestSchemaHistoryAnchorHasNoRestoreSideConsumer` — a fail-by-default AST roster over every read of either symbol under `internal/pipeline`, each classified write-side-with-a-reason, so a restore-side consumer fails the build and names the three comments to rewrite |
| 4 | `ir/backup/manifest.go:26` — "older sluice refuses newer manifests, **newer sluice always reads older**" | **CLOSED 2026-08-07.** Verdict FALSE unqualified. The counterexample is a recompute with no recorded algorithm version: `verifySchemaHashes` re-fingerprints an older link's schema with THIS build's `ComputeSchemaHash`, so an untagged `ir.Index`/`ir.Column` addition partitions the corpus — two boundaries inside format v8 (item 102) and one at v0.104.3 (item 104). The behavioural evidence already existed and nothing was bound to it | fixed at three live sites (`manifest.go` package doc + `BackupFormatVersion`, `docs/dev/design/logical-backups.md`, and the crossversion suite's own "true forever" header), each carrying a `forward-compat-caveats:` marker; `signature.go` / ADR-0154 / CHANGELOG / `docs/releases/` deliberately left, with reasons recorded at the gate. Pin: `TestNewerReadsOlderIsQualifiedByThePartitioningPreflights` — derives the caveat set from `restoreManifestIntegrityPreflights`' callees, classifies each by whether it keys on something the ARTIFACT records, and holds every marker to it. Deliberately NOT a golden hash (item 104's lesson): the behavioural proof stays `TestBackup_CrossVersionChainCompat` cells 5 and 6 |
| 5 | `docs/adr/adr-0067-*.md:18` — smart compaction "can never merge across a rotation boundary" | **CLOSED 2026-08-07.** Verdict FALSE, and the Status block was false **the moment it landed**: `97a6423a` (PR #85, shipped v0.88.0) is a squashed PR carrying the ADR commit AND the implementation commit, so "No code written yet" was published in the same commit as the code. **Why the existing gates missed it, which is the reusable half:** `fileStatus` read only the FIRST non-blank line of `## Status`, which here is the ADR-0087 amendment blockquote — so the file parsed as declaring NO status and fell out of the scope of both `TestADRIndexStatusParity` and `TestADRStatusSelfContradiction`, and its index row is one of the legacy no-status rows | fixed: Status + prose corrected. Gate widened (the section scan skips blockquote/admonition lines) with a new `adrStatusFilesParsed` floor, plus a new gate `TestProposedADRsAreNotContradictedByShippedCode` — a `Proposed` ADR cited by non-test code under `internal/`/`cmd/` fails until someone records which side is wrong. **It immediately found a sibling: ADR-0071** (bounded-memory VStream COPY pump, shipped v0.99.3, cited by ten non-test files, Status `Proposed.`), corrected in the same pass. ADR-0177 is exempt with a written reason (Discovery-only; the citations name its NON-adoption) |

**Correction to this file's own prior line.** It said "One suspect-false … is fixed; the C-13 binlog-width premise is fixed", which read as *two of five closed*. C-13 is **not** a member of this table — it is a §3.3 MEDIUM (premise-class), and it IS fixed (`mysql/position_orderer.go`'s `binlogFileAfter` + `TestBinlogFileAfter_SurvivesTheSuffixWidening`). As of 2026-08-07 **all five are closed**; each carries the name of the test that fails if it breaks, which is the point of the sweep.

**Two findings the sweep turned up that are NOT members of this table**, recorded here rather than lost:

- **`backup.SourceEngineCommitsAfterRows` is a phantom symbol.** `ir/backup/manifest.go`'s FormatVersion-8 paragraph cited it as the signing-independent closure for the folded `CDCPositionCommitsAfterRows` flag. No such symbol exists anywhere in the tree — it named a re-derivation that was designed and then made unnecessary when restore stopped trusting the anchor at all (item 60). The paragraph is corrected; the general gap is that `internal/docsync`'s `TestCodePointers_AllReferencedFilesExist` grades backtick-quoted FILE paths and nothing grades SYMBOL pointers. **Gate proposal (unbuilt):** the same walk over `pkg.Symbol`-shaped references, resolved against the package's exported identifiers.
- **`TestADR0087_Bug139_IdleStopCompact_SplitsAndRestores_PG` flaked once** (2026-08-07, local Docker) and passed on two subsequent runs of the same code, including a re-run of the same test pair. Both subtests logged their `PROVEN` line in the failing run, so the failure was after the assertions. Not investigated; noted so a second sighting is a pattern rather than a surprise.

#### The UNVERIFIED claims — CLOSED 2026-08-08 (14 of 14 nameable; 3 unrecoverable)

**Progress: the queue is CLOSED — 14 of 14 nameable, and the honest denominator is 14, not 17.** Batch 4 (2026-08-08) took the last two live entries (the ADR-0075 exported-snapshot premise and the codec never-sniffed half) and re-derived the third (the PG incremental replay floor), which was a **stale duplicate** — the same claim is struck in the *High* list above, closed by batch 2, and the *Medium* row was a second copy of it that nobody reconciled. **The other three of the "17" are unrecoverable**, per the completeness caveat below: they existed only in the per-worker sweep output, which was not preserved. Reporting "14 of 17" forever would be the more honest-looking and less honest answer; the queue as a *work item* has nothing left in it.

**What the queue's outcome actually was, since G-12 proposes adopting this sweep as a standing form.** The interesting number is not the count closed but how many verdicts differed from their filing: **11 of 14** did.

| Divergence | Count | Instances |
|---|---|---|
| Filed verdict survived unchanged | 3 | the D1-replica poll premise (TRUE, structurally); the MySQL DDL implicit-commit premise (TRUE); PG per-change position-write atomicity (holds on every core) |
| TRUE, but the argument rested on a SECOND premise nobody had stated | 4 | laneapply decode-type (stable across kinds, unstable across PROVENANCE); sqlite-trigger watermark (the serialization half was not the fragile half); MySQL MINIMAL-DELETE PK carriage; **ADR-0075's spanning snapshot** (true of PostgreSQL; the reader that drains it was the unstated half) |
| FALSE | 2 | `mysql/backup_position.go`'s binlog-availability claim (and understated in two ways); the empty-`EndPosition` premise, which was wearing a citation to a test asserting something else |
| Filed narrower than the defect | 2 | the pgtrigger `seqmin` preflight (three more settings reach the same loss); **the generated-PK enumeration**, whose `floatrepair.RepairByPK` exemption ("refuses on an empty effective PK") could not see the cell that corrupts |
| Stale — already built, or a duplicate row | 2 | the mydumper metadata-counts check (shipped six weeks earlier; the premise UNDER it was the real gap); the PG incremental replay floor (duplicate) |
| Unpinned BY A GATE THAT NAMED IT | 1 | ADR-0101 concurrent-cold-copy gaplessness |

**And the sweep's yield was not mostly in the queue.** Four of the entries turned up a defect larger than the claim being checked, found while building the check rather than by reading the claim: `backup stream` never drained schema-history snapshots at all; the generated-PK class's two silent PG siblings (one source DELETE became two target deletions, measured); `sqlitetrigger.CDCReader.Close` never joined its pump; and — batch 4's — **two more unjoined pumps in `pgtrigger` and the VStream reader**, after the sqlite-trigger fix had been recorded as closing that class. That is the argument for re-running the sweep: the queue is a cheap way to get someone to open the file, and what they find next to the claim is worth more than the claim.

**The recurring shape, for whoever runs it next.** In 6 of the 14 the claim as written was true and the ARGUMENT it supported was not — because a second premise, unstated, was carrying the load. The premise-naming step asks "what would have to be true for this argument to fail"; the answer was almost never the sentence under audit. Read the sentence, then find the code that DEPENDS on it, and check that instead.

Batch 1 (2026-08-07) took the silent-loss-adjacent set: the D1-replica poll premise, `mysql/backup_position.go`'s binlog-availability claim, the pgtrigger sequence preflight, and the mydumper metadata-counts premise. Batch 2 (2026-08-07) took the laneapply decode-type premise, the DDL-only-window empty-`EndPosition` premise, PG per-change position-write same-tx atomicity, and the PG incremental replay floor. Each is struck below with its verdict, its evidence, and the name of the test that now fails if it breaks.

Two of batch 1 were graded wrong by the audit in a way worth recording, because the pattern will repeat: the mydumper entry was **stale** (its proposed check had shipped six weeks earlier, and the real gap was one level down — the premise the built check rests on), and the pgtrigger entry was **narrower than the defect** (it named `seqmin`; three more sequence settings reach the same silent loss). Batch 2 kept the spread: one premise was **TRUE but resting on an unstated second premise** (laneapply — stable across change kinds, unstable across PROVENANCE), one was **an unverified premise wearing a citation** to a test that asserts something else, and that one turned out to sit on top of a live FALSE REFUSAL plus an unfiled sibling defect (`backup stream` never drained schema-history snapshots at all). The instruction to re-derive rather than trust the verdict has now earned its cost in four directions.

**Completeness caveat, stated rather than implied: the source report's §4 prose names 14 of the 17.** The other three exist only in the per-worker sweep output, which was not preserved in the report and is therefore gone. That is the durability failure this port exists to stop, recorded honestly rather than rounded to "17".

*Critical-risk:*

- ~~**ADR-0101 concurrent-cold-copy gaplessness.**~~ **CLOSED 2026-08-07. Verdict: the audit was RIGHT and the reason is sharper than "under-tested" — the property was unpinned BY A GATE THAT NAMED IT.** `TestSnapshotCaptureOrder_EveryUnlockedSiteCapturesBeforeTheSnapshot` DISCOVERS `acquireConsistentSnapshot` and then exempts it, with the claim under audit as the exemption's free-text reason; moving `snapshotMasterStatus` below `UNLOCK TABLES` left the ENTIRE `internal/engines/mysql` unit suite green (mutation-verified, mutant confirmed applied by grep, and re-run against the pre-change gate restored from git to prove the blindness rather than infer it). Fixed by GRADING the exemption instead of trusting it: `captureOrderExempt` became `ftwrlBracketed`, and a listed site must show `FLUSH TABLES WITH READ LOCK` < capture < `UNLOCK TABLES` in source order (`gradeBracket`) — both failure shapes, capture-below-unlock and freeze-removed, now fail. Two real-server pins supply what a source-order walker cannot: `TestConcurrentSnapshotCapturesPositionUnderTheLock` reads `mysql.general_log` and requires that order on ONE `thread_id` (a position read on a different connection is not covered by this connection's lock — a user-scoped assertion would have passed three statements on three connections), and `TestConcurrentSnapshotIsGaplessUnderChurn` runs four writers across the open and asserts all N readers pinned the identical cut and every committed id is in the snapshot or the CDC tail and never in neither, with the test's own committed-id set as the independent expected value. Measured green: 78 committed / 40 in the shared cut / 38 recovered from the tail / 0 in neither. Both pins fail under the §4 rejected design (readers diverged by 2 rows), so neither is vacuous. ADR-0101 §3 carries the correction.
- ~~**sqlite-trigger watermark serialization.**~~ **CLOSED 2026-08-07. Verdict: the serialization premise is TRUE and now pinned; the entry named the WEAKER of the two premises the argument rests on.** The commit-order half is ground-truthed by `TestChangeLogIDsAreCommitOrderedUnderConcurrentWriters` (four contending writers, 160/160 emitted exactly once, strictly increasing ids). The half that mattered is the other one: "AUTOINCREMENT is never reused" is not a property of SQLite's locking but of two pieces of **ordinary writable state**, and both are constructible. `sqlite_sequence` lowered + the auto-prune draining the log re-issues id 1 under a watermark of 2 (proved from the server AND from sluice by `TestChangeLog_SequenceTamperStrandsTheChange`, which logs both PROVEN halves); and a pre-existing `sluice_change_log` WITHOUT `AUTOINCREMENT` is accepted as-is by `renderSetupDDL`'s `CREATE TABLE IF NOT EXISTS` (its own comment records this, roadmap item 149b), after which SQLite reuses rowids. **This also CLOSES the pgtrigger sibling filed as ungated below.** Fixed by `verifyChangeLogWatermark` (`cdc_watermark_gate.go`) at BOTH CDC doors on BOTH transports — a new `executor.changeLogAllocation` implemented for local SQLite and D1, since `sqlite_master`/`sqlite_sequence` are ordinary tables on D1 too. Pins: `TestCDCReader_RefusesChangeLogThatCanReissueIDs` and `TestD1CDCReader_RefusesChangeLogThatCanReissueIDs`, each with a healthy control AND a **pruned-but-healthy** control (the cell that fails if the gate keys on `MAX(id)` instead of `max(MAX(id), seq)` and would then refuse every routine auto-prune); both doors mutation-verified independently. **Door coverage is stated per state rather than assumed uniform:** the no-AUTOINCREMENT arm fires at all three doors, the lowered-sequence arm only at a WARM RESUME — because the other two re-read the watermark from `MAX(id)` and so resume at 0, where there is nothing to lose and nothing to refuse. **Third defect found in the same pass and fixed** (see the new-findings note below): `CDCReader.Close` never joined its pump.
- ~~**The D1-replica poll premise.**~~ **CLOSED 2026-08-07. Verdict: TRUE, and structurally unreachable — but the argument was resting on absence, which is the weakest place to rest one.** `d1Client.queryRows` is the single request builder every D1 read, write, setup DDL and CDC poll funnels through; it constructs `/accounts/{acct}/d1/database/{db}/query` in process and sets exactly two headers (`Authorization`, `Content-Type`), so no poll can reach a replica. `endpointBase` is injectable, but only by `D1ConnForTest` — no DSN parameter reaches it, which is the half that would have turned the premise into a runtime input. Both are now asserted rather than asserted-in-a-comment: `TestD1Transport_NeverRoutesThroughTheSessionsAPI` (path + the three `x-cf-d1-session-*` headers + a session/bookmark body scan, with an anti-vacuity check that the handler was reached) and `TestD1EndpointBase_IsNotOperatorSettable`. Mutation-verified in three directions, each confirmed applied before the run: a session header added to `queryRows`, the URL re-pointed at `/sessions/query`, and `openD1Client` taught to read `endpoint_base=` off the DSN. **The residual is stated at the site in the words UNVERIFIED PREMISE**: that Cloudflare serves the REST `/query` endpoint from the primary is THEIR contract and no test here can hold them to it. Why a test rather than only the note — adding Sessions routing is a small, attractive latency win that nothing else in the tree would notice.

*High:*

- ~~**`laneapply/router.go:47` decode-type stability across change kinds.**~~ **CLOSED 2026-08-07. Verdict: the claim as WRITTEN is TRUE; the claim as USED rests on a second premise nobody had stated.** Across change kinds it is structural, not lucky — every one of the six readers that can feed a lane applier decodes the before- and after-images with ONE function invoked before the Insert/Update/Delete switch, and both leaf decoders dispatch on the IR column type (PG's key-only Before is copied out of After by `synthesizeKeyOnlyBefore`, by reference). Now ground-truthed rather than code-read: `TestCDCDecodeTypeStableAcrossChangeKinds` runs 11 PK families × {REPLICA IDENTITY FULL, DEFAULT} × 4 images against a real Postgres and asserts both the Go type and the resolved lane, 22 cells with a floor of 20. **What the audit did not name is the half that mattered:** the same-lane property also needs same PROVENANCE, and the concurrent lanes are ALSO driven by chain-restore's incremental replay and the from-backup broker at a default concurrency of 4, whose changes come out of backup chunks. `blobcodec.encodeValue` widens every sized integer to its 64-bit form, and `WriteCanonicalKeyValue` tagged `int64` as `'i'` and `int32` under the `'?'` fallback — self-consistent today only because those streams are 100% chunk-sourced, which nothing enforces. Fixed by aliasing the integer WIDTHS onto one tag per family (the fold becomes invisible by construction) and bound across the two packages by `TestCanonicalKeyValue_SurvivesTheBackupRoundTrip`, with `TestCanonicalKeyValue_FamiliesStillDoNotAlias` holding the other direction and `TestCanonicalKeyBytesFeedTheLaneHash` as the anti-vacuity floor. **Known residual, recorded at the gate:** the same fold normalises `time.Time` to UTC, so a `TIMESTAMPTZ` primary key routes differently after a chunk round trip; it is a graded cell, not an omission. **Scope stated at the gate:** the real-reader pin reaches the PG pgoutput reader only — MySQL binlog, VStream, pgtrigger, sqlite-trigger and the chunk reader are named as ungated for the cross-kind property rather than left implied.
- ~~**`ir/backup/manifest.go:851` DDL-only-window empty-EndPosition premise.**~~ **CLOSED 2026-08-07. Verdict: the premise was UNVERIFIED and wearing a citation, and the false refusal it was supposed to rule out was live.** The sentence cited `item60_anchor_schemadelta_{pg,mysql}` as ground truth for "a legitimate DDL-only window emits its snapshot with an EMPTY EndPosition"; those tests LOG `posBearing` and assert a different invariant (anchor-at-End implies a non-empty SchemaDelta). Reached by driving `IncrementalBackup.Run` with a window whose last position-bearing event is a schema snapshot: the capture loop opens a chunk writer on it, the ADR-0049 diversion means the chunk records ZERO changes, and `assertDataWindowEndPositionInvariant` then refused the backup as "a data-bearing incremental window (1 change chunks)" and blamed the CDC reader. The ordinary shape `rows → TxCommit → snapshot` reproduces it with real data in the window. Fixed at the writer so the property is true BY CONSTRUCTION: `recordedInChangeChunkStream` lets only a change the chunk stream RECORDS move EndPosition (both capture lanes), and the invariant's "data-bearing" predicate is now a RECORD count, not a chunk-entry count. Pins: `TestIncrementalWindow_SchemaSnapshotDoesNotMoveEndPosition` (3 window shapes, end to end through `Run`, asserting on the manifest the writer actually produced) and `TestRolloverWindow_SchemaSnapshotDoesNotMoveEndPosition` for the `backup stream` lane; `TestAssertDataWindowEndPositionInvariant`'s fixture was rebuilt on per-chunk record counts, because `&ChunkInfo{}` was a shape whose real producer took the other branch — the self-referential-fixture lesson again. Comments corrected at all three homes (`manifest.go`, `chain_restore.go`, `broker.go`). **Sibling defect found and fixed in the same pass, previously unfiled:** `ChangeChunkWriter.Snapshots()` had exactly ONE production caller, in `IncrementalBackup.captureWindow` — `backup stream`'s rollover lane never drained schema-history snapshots at all, so every mid-stream DDL a continuous chain observed had its position-anchored schema version dropped, on the lane that writes most change chunks. Both lanes now share one `schemaHistoryDrain`.
- ~~**PG per-change position-write same-tx atomicity.**~~ **CLOSED 2026-08-07. Verdict: the atomicity HOLDS on every core; the gap was real and the proposed `xmin` check is the right instrument, adopted.** The existing pin (`TestPipelined_AtomicityMidBatchError`) proves the ROLLBACK direction on one core — and absence-on-failure is weaker than same-transaction, since two sequential failed transactions also leave nothing behind. `xmin` is the independent expected value the 2026-08-01 rule asks for: PostgreSQL stamps every tuple with the xid that wrote it and two transactions cannot share one, so `xmin(data) == xmin(control row)` IS same-transaction, and it comes from the server rather than from anything sluice recorded. `TestPositionAndDataLandInOneTransaction` grades the full roster — serial per-change, batched pipelined, and the batched SERIAL FALLBACK (reached by clearing `pipelineCfg`, which is what `beginPipelinedTx`'s unavailable branch keys on) — and asserts the concurrent-lane path is DIFFERENT rather than omitting it, so the ADR-0104/0105 position relaxation is an exemption that cannot silently become a defect. Measured live: 750/750, 767/767, 784/784, and 801 vs 802.
- ~~**PG incremental replay floor.**~~ **CLOSED 2026-08-07. Verdict: premise TRUE, evidence was a hand-run transcript, and the branch that ACTS on it had no server-touching test.** The claim is "PostgreSQL's logical decoding begins at `max(requested_lsn, confirmed_flush_lsn)` … so the REQUESTED LSN, not the flush point, is the floor" (`pipeline/incremental.go`'s `windowAdvancedPast`), and it is load-bearing in three places: that comment, `rotationBoundaryResumeStart`'s decision NOT to build a skipThrough filter, and — the expensive one — `Engine.PreflightChainResume`'s at-or-behind pass-through, the else of a silent-loss refusal. Its only existing tests covered the AHEAD refusal, the missing slot, and the steady state where flush and request coincide, which is the one shape in which the floor cannot be observed at all. `TestChainResume_WalsenderFloorsAtTheRequestedLSN_AndThePreflightPassesThatShape` provokes a strictly-trailing `confirmed_flush_lsn` (asserting that precondition first), then binds the two halves in one test — the preflight must classify the slot as healthy AND the server must deliver only the post-request transaction, at a position >= the request. Mutation-verified in both directions. **Adjacent doc defect corrected in the same pass:** `ackLSN`'s "the applier's first commit will report a higher value via the tracker" is false by construction for `backup incremental`/`backup stream`, which hold the ack precisely BECAUSE they have no applier; the real reason startLSN is a safe floor there is that it is a parent manifest's committed EndPosition, and the one shape where that substitute argument fails (a pre-v0.16.x parent with no EndPosition, where the fallback is "now") is now written down in the words UNVERIFIED PREMISE.
- ~~**MySQL MINIMAL-DELETE PK carriage on the one belt-skipped arm.**~~ **CLOSED 2026-08-07. Verdict: the premise as stated is TRUE — and building the matrix to check it found a SILENT-LOSS defect one column-shape over, which the proposed 10-line bitmap check could not have caught.** MySQL does carry every PRIMARY KEY column in the before-image under every `binlog_row_image`: pinned across the whole family by `TestCDCReader_MinimalDeleteCarriesThePK`, 7 PK shapes (first / last / composite-split / BLOB-prefix / user-INVISIBLE / server-generated `my_row_id`) × {MINIMAL, NOBLOB, FULL}, 21 cells, asserting the emitted `ir.Delete.Before` carries every PK column with its exact value and nothing else. **The defect: a PK on a STORED GENERATED column.** The cell failed under FULL as well as the partial images — the tell that the loss is sluice-side, not the server's: `decodeBinlogRow` drops generated columns deliberately (the target's `GENERATED` clause recomputes them, correct for the INSERT/SET side), so `filterBeforeToPK` produced `{"g": nil}` and the apply renders either an EMPTY `WHERE` (a hard SQL error, when the target column is generated too and `NonGeneratedRowKeys` strips it) or `g IS NULL` (matches zero rows, ADR-0010 absorbs the miss, the position advances — Bug 88 reached from the other direction). Fixed as a loud refusal on BOTH narrowing arms (`refuseGeneratedPKIdentity`, new code `SLUICE-E-CDC-GENERATED-PRIMARY-KEY`), not lossless carriage — carriage needs three coordinated changes and the Postgres sibling cannot have it at all (see below). `filterBeforeToPK`'s "structurally impossible" comment and `refusePartialRowImage`'s generated-column exemption are both corrected in place. **Siblings enumerated, not promised** — see the new-findings note below.
- ~~**`mysql/backup_position.go:24` "the binlog stream is always available to a privileged connection."**~~ **CLOSED 2026-08-07. Verdict: FALSE, ground-truthed, and the audit understated it in two ways.** (1) The hazard is not a MySQL contrivance: `gtidModeOnFor` returns true UNCONDITIONALLY for the MariaDB flavor, so **every** binlog-less MariaDB took the GTID arm with no unusual configuration at all — and MySQL 8.0.46 separately ACCEPTS `--skip-log-bin --gtid-mode=ON --enforce-gtid-consistency=ON` (both booted and read live: `log_bin=0`, empty GTID set). (2) There were **two** capturers with the identical defect, not one — `SchemaReader.CaptureBackupPosition` and the conn-scoped `captureBackupPosition` in `backup_snapshot.go:505` that the lock-free snapshot path uses. The exact artifact, captured from the mutation run: EndPosition `{"mode":"gtid"}` — well-formed, resumable-looking, covering nothing. Note the two ARMS also disagreed: file/pos already refused loudly (`SHOW MASTER STATUS` returns zero rows), only the GTID arm invented a cursor. **Fix: a `@@GLOBAL.log_bin` preflight at both doors, ahead of both arms** — and deliberately **not** a refusal. A standalone `backup full` of a binlog-less source is legitimate; what is not is a fake cursor. So it records the empty `ir.Position` plus a WARN naming the consequence and the remedy, which lands on handling that already exists (`backup incremental` WARNs it must start from now; `sync start --position-from-manifest` refuses). Pin: `TestCaptureBackupPosition_NoBinlog` — MySQL × MariaDB × {binlog off, binlog-on control} × {both doors}, the two families being two genuinely different routes into the same arm. Mutation-verified per door in isolation (door 1 reverted before door 2 was measured, since the first assertion masks the second).
- ~~**ADR-0075 exported-snapshot-spans-all-schemas premise.**~~ **CLOSED 2026-08-08. Verdict: the premise is TRUE and is now ground-truthed — and it was never the whole argument.** The PostgreSQL half ("one exported snapshot spans every schema in the database") is checked by `TestExportedSnapshotSpansEverySchema` (`internal/engines/postgres`): three schemas each holding an identically-named table, a post-export INSERT into *every* schema, and the assertion that the one pinned snapshot hides all three — graded per schema, because a snapshot covering only the DSN's bound schema would still hide that schema's write. It reads through `OpenMultiDatabaseSnapshotStream`'s own reader rather than a hand-rolled `SET TRANSACTION SNAPSHOT`, so the environmental fact is bound to sluice's use of it rather than pinned beside it. **The unstated second premise is the half that mattered:** a spanning snapshot only yields a spanning COPY if the reader draining it qualifies by each table's own schema, and the `ir.SnapshotImporter` readers minted for the ADR-0079 parallel cold start do NOT (`qualifyBySchema=false`, DSN-bound schema). The pipeline had the counter-argument written at `runColdStartParallel` — "holds by construction today" — with nothing asserting it, and in the canonical multi-schema shape (same table name per tenant schema) the failure is not an error but N copies of `public.t` fanned into N target schemas. Fixed at BOTH levels: `TestSpanningSnapshotNeverReachesTheParallelColdStartLane` derives the two lanes' call graph from `internal/pipeline` (receiver-type-keyed, four positive controls so a negative means "no path" and not "broken walker") and fails if they meet; and the PG row reader now REFUSES a table from a schema it cannot span (`errSchemaEscape`, one chokepoint shared by `ReadRows`/`CountRows`/`RangeBounds`/`EstimateRowCount`/`SampleKeysetBoundaries`) instead of substituting its own — which is what makes the structural argument non-load-bearing. The refusal was mutation-proved against a real PG 16: with the guard removed, `tenant_b.t` returned `public`'s row (`TestNonSpanningReaderRefusesAForeignSchema`). ADR-0075's "Consistency model" carries the correction. **Stated at the gate:** no mutation of sluice can simulate a PostgreSQL whose snapshot stopped spanning namespaces, so that test's irreplaceable coverage is of a change in the server.

*Medium:*

- ~~**The codec never-sniffed half.**~~ **CLOSED 2026-08-08. Verdict: "substantially overtaken" was WRONG, and the instruction to re-derive is what caught it.** `TestHexDecodeSitesAreProvenanceDecided` walks `internal/engines/**` and explicitly EXCLUDES `internal/pipeline` at its own definition, and it is about hex-vs-bytes value rendering — a different ambiguity from the per-segment COMPRESSION codec. What item 135 supplied here was the METHOD (a declared lane plus a fail-by-default AST roster), not the coverage; both halves of the audit's proposed check were genuinely unbuilt. **The claim** (`blobcodec/codec.go`: the codec "is metadata, read from the segment, full stop", never inferred from the bytes, with one carve-out for `--rebuild-catalog`) **is a claim about CALL SITES**, which is why the audit asked for a roster. Built: `TestChunkReaderCodecsAreThreadedFromTheRecord` (`internal/pipeline/backup`) walks every non-test file under `internal/`, finds all 7 production `NewChunkReader`/`NewChangeChunkReader` calls, keys each on the codec argument's SOURCE TEXT so a silent swap fails, refuses a codec LITERAL outright (a constant cannot have come from lineage.json — no reason could make it correct), and fails on a new or a stale site. And `TestChunkReader_DeclaredCodecMismatchIsLoud` grades the full 3x3 of written-codec × declared-codec, because the arms do not share a failure mechanism: gzip and zstd reject a bad magic header at construction, while `nopCodecReadCloser` validates nothing, so **the `CodecNone` arm's loudness was an inference from reading the source, not an observation** — it is now observed (loud at the header decode, zero rows assembled). Every existing test in the package had paired a writer and a reader on the same codec constant, so the whole mismatch surface was writer-agreeing-with-writer. **Mutation note recorded at the gate because it surprised:** the mutation that bites is a SNIFF FALLBACK in `NewChunkReader` (fails all six off-diagonal cells, 64 rows silently assembled each); a naive lenient `newCodecReader` fallback leaves it green and is an EQUIVALENT MUTANT — `gzip.NewReader` has already consumed the ten header bytes off the shared reader by the time it errors.
- ~~**pgtrigger `seqmin` preflight.**~~ **CLOSED 2026-08-07. Verdict: hazard REAL and the scope was three settings wider than "seqmin".** The premise that was never checked is written verbatim at `readChangeLogAnchor`'s negative-anchor clamp — "MIN(id) − 1 can only go negative if the lowest in-flight id is 0, which BIGSERIAL never allocates (it starts at 1)". Ground-truthed on PG 16: `ALTER SEQUENCE … MINVALUE 0 RESTART WITH 0` allocates id **0**, and `MINVALUE -100 RESTART WITH -100` allocates **negative** ids. Re-deriving the class rather than taking the entry at its word found that `seqcache` was one of **four** settings that admit an id at or below the watermark — the others being `seqmin < 1`, a negative `seqincrement`, and `seqcycle` — so a preflight named for one of four was the "gate narrower than its name" shape. `verifyChangeLogSequenceCache` is now `verifyChangeLogSequence` and grades all four at both CDC doors. Pins: `TestCDCReader_RefusesSequenceConfigThatCanReissueIDs` (one cell per setting × both doors, plus a restore leg and an untouched-sequence positive control; each of the four arms mutation-verified independently, mutants confirmed applied by grep first) and the **defect proof** `TestChangeLogAnchor_MinValueZeroStrandsTheChange`, which binds the two halves that were each plausible alone — it asserts from the server that PG really allocated id 0 and from sluice that `poll(0)` emits nothing, and logs the PROVEN line. ADR-0066 amended (it cited the retired narrow test by name). ~~**Sibling filed, not fixed:** `sqlite-trigger`/`d1-trigger` use `INTEGER PRIMARY KEY AUTOINCREMENT`, so there is no `pg_sequence` row and this exact check does not port — but `sqlite_sequence` is an ordinary writable table, so the equivalent tamper is constructible there and is ungated.~~ **CLOSED 2026-08-07** by the sqlite-trigger watermark entry above: the tamper is constructible exactly as predicted (proved from the server), and `verifyChangeLogWatermark` now grades it at both CDC doors on both transports — plus a second state the filing did not name, a change-log table that is not AUTOINCREMENT at all.
- ~~**mydumper metadata-counts premise.** *Check:* bind the recorded counts to an independent number.~~ **CLOSED 2026-08-07. Verdict: the audit's stated check was ALREADY BUILT — this entry was stale — but the premise underneath it was genuinely unverified, and that is the half that mattered.** `warnIfRowCountMismatch` has bound the recorded count to the streamed count since audit-2026-07-15 MED-D0-2, at BOTH doors (`ReadRows` and `countTableRows`), pinned firing and not-firing by `TestReadRows_RowCountTripwire`. What nothing checked is the environmental fact the whole tripwire rests on: **that real mydumper writes `rows = N` in a section this parser recognises, and that the number is exact.** Every pin used a hand-written metadata fixture — sluice agreeing with sluice — so a changed key or section spelling would have left `hasMetadataRows` false for every table, the only independent evidence of a lost chunk silently inert, and every test still green. Now ground-truthed by the `real-mydumper-records-parsable-exact-row-counts` leg of `TestMydumperIntegration_RealDumpEndToEnd`: it reads the counts a real `mydumper/mydumper:v1.0.3-1` wrote and compares them against `SELECT COUNT(*)` on the live source — a number from neither the dump nor this package — with an anti-vacuity floor of 5 graded tables. Premise CONFIRMED TRUE against v1.0.3. Stated at the gate: a source mutation fires this leg but also fires the unit fixtures; its irreplaceable coverage is of a change in *mydumper*, which no mutation of sluice can simulate.
- ~~**PG incremental replay floor.**~~ **STRUCK 2026-08-08 as a STALE DUPLICATE, not re-worked.** This is the same claim as the *High* entry above, which batch 2 closed on 2026-08-07 with `TestChainResume_WalsenderFloorsAtTheRequestedLSN_AndThePreflightPassesThatShape`. Verified rather than assumed: the test exists at `internal/engines/postgres/chain_preflight_replay_floor_integration_test.go` under `//go:build integration`, it landed in `68f9b4c4`, and all three sites that rest on the premise (`windowAdvancedPast`, `rotationBoundaryResumeStart`'s call site in `stream.go`, and `Engine.PreflightChainResume`) now cite it by name. The row was a second copy of one claim, which is worth recording as its own finding: **a queue transcribed from a report can carry the same item twice, and the duplicate reads as remaining work.**
- ~~**MySQL DDL implicit-commit premise.**~~ **CLOSED 2026-08-07. Verdict: the premise is TRUE, the batch path handles it correctly — and the SERIAL path the batch path deliberately routes DDL to carried a doc asserting the OPPOSITE, behind a cross-reference that resolved to nothing.** `change_applier_batch.go`'s `TransactionalDDL=false` note said "applyOne has the same implicit-commit rough edge — see the comment there"; no such comment existed anywhere in `change_applier.go`, whose `Apply` doc instead promised "a crash between them rolls back both — progress and data can never diverge". That is false for `ir.Truncate`: `TRUNCATE TABLE` is DDL, MySQL implicit-commits around it, and the `tx.Rollback()` on the position-write error path is a no-op. Fixed by writing the missing paragraph (the divergence it admits is data-AHEAD-of-position, which resume idempotency converges — the forbidden direction is unreachable because the position write is issued strictly after the truncate commits) and correcting `Apply`, `applyBarrierNoPosition` and the ADR-0049 block. Pinned in BOTH directions against a real mysqld, because pinning only the failing one would read as a blanket disclaimer: `TestApplyOne_TruncateSurvivesTheRollback` (the truncate is still applied after a forced position-write failure — the premise, observed) and `TestApplyOne_SchemaSnapshotRollsBackWithItsPosition` (the pure-DML arm leaves nothing behind, so ADR-0007 atomicity really is available everywhere else). Mutation-verified by rendering the truncate as `DELETE FROM`. **Two facts, now bound:** the rough edge is tolerable only because no batch tx ever carries DDL, which needs `TransactionalDDL=false` AND `isSchemaEvent` covering every DDL-bearing change type — separately plausible, previously joined by nothing. `TestMySQLDeclaresNonTransactionalDDL` holds the flag; `TestIsSchemaEvent_CoversEveryDDLBearingChangeType` (`internal/appliershared`) is a fail-by-default roster classifying every `ir.Change` implementor DDL-bearing-or-not with a reason, with the implementor set **derived from `internal/ir/change.go` by AST** rather than a hand-written count (a count would keep agreeing with itself as the interface grew). Narrowing the predicate to `SchemaSnapshot` fails it.

**Three findings batch 3 turned up that are NOT members of the queue**, recorded here rather than lost. Two are fixed; the third is a confirmed, ground-truthed sibling that is NOT fixed and wants its own chunk.

- **`sqlitetrigger.CDCReader.Close` never joined its pump — an unrecovered PANIC, not a failed sync. FIXED.** It cancelled the pump's context and then immediately closed AND nil'd `r.exec`, while the pump's very next act after any select is `r.exec.pollChangeLog(...)`. Cancellation is asynchronous, so a Close landing in that window nil-dereferenced inside the goroutine and took the process down. It is also an unsynchronised cross-goroutine read/write of the field, which is what `-race` grades. Found by accident: the door matrix for the watermark gate opens and closes a reader per cell and panicked on the first run. This is the 2026-07-28 lesson's "the PG CDC reader's documented promise to join its pump, which it did not", one engine over — except here nothing even promised it. Fixed with a `pumpDone` channel joined before the executor is touched; pinned by `TestCDCReaderClose_JoinsThePumpBeforeClosingTheExecutor` (a 50-iteration open/close loop — one iteration lands in the window rarely), mutation-verified by removing the join. **Concurrency change: wants the CI `-race` job before any tag.**

  **VERIFIED 2026-08-08, and the class was NOT closed — two more engines had it.** The sqlite-trigger fix and its pin are real (`cdc_reader.go`'s `Close` joins `pumpDone` before `r.exec` is touched; the 50-iteration loop is in the plain unit gate). What was missing is the step the entry's own heading calls for: the CHANGELOG claimed the class without enumerating the siblings, and a roster over `internal/engines/**` found two more of the identical shape. **`pgtrigger.CDCReader`** was the same defect verbatim — `Close` cancelled and then closed AND nil'd `r.db` while the pump's next act is a query on it (`holes.refresh`, `captureTxidUpperBound`, or the batch `QueryContext`) — now fixed with `pumpDone` and pinned by `TestCDCReaderClose_JoinsThePumpBeforeClosingThePool`. **`mysql.vstreamCDCReader`** had the same shape with a WORSE consequence and it is not a nil deref: the pump writes `r.currentVgtid` and `r.fields` unlocked, and `applyReshardState` cancelled the old pump and *immediately* replaced `r.shards` and `r.currentVgtid` with the post-reshard layout — so a straggling VGTID from the old stream could write the OLD position back over the new one and `Reopen` would build its request from that. **A lost update on the resume position across a reshard**, not merely a reported race. Fixed via `startPump`/`joinPump` (one helper for both launch sites, so a third cannot forget the bookkeeping), joined in `Close` AND in `applyReshardState`; pinned by `TestVStreamClose_JoinsThePump` and `TestApplyReshardState_JoinsThePumpBeforeReplacingTheLayout`, the second asserting on the VALUE that survives rather than on the ordering. All three joins mutation-verified.

  **The durable half is `TestEngineClosesJoinTheGoroutinesTheyStart`** (`internal/engines/pump_join_roster_test.go`): a fail-by-default AST roster over every type under `internal/engines/**` that BOTH launches a goroutine from one of its methods AND declares a `Close`/`close`, each graded joins / exempt-with-a-reason / open-finding, with a 12-type floor, a per-engine floor, and a stale-entry check. It found 13 types — the 5 CDC readers plus 8 the sweep had not been looking at, each now graded (the RowReaders are exempt for a real structural reason: their `Close` nils nothing the stream goroutine dereferences and the goroutine's only shared write is mutex-guarded; `mysql.RowWriter`'s goroutines are `wg.Wait`ed inside the call that starts them). Mutation-verified in both directions: an unrostered type fails, and removing a join under a `joins` verdict fails.

  ~~**NOT FIXED, filed rather than promised: `mysql.vstreamSnapshotStream`.**~~ **FIXED (shipped v0.128.3).** The join went inside the pump protocol as filed: a `copyWG` tracks every COPY-phase goroutine (the 3 top-level launch sites via `goPump`, plus the concurrent path's K sub-streams dual-tracked), and `close()` now runs four ordered steps before closing `s.conn` — `cancel()` (unblocks `Recv`/interruptible backoff), `cancelCopyForShutdown()` (flips the terminal state `err=Canceled`+`copyComplete` under `mu` then broadcasts — the load-bearing part, because the backpressure `cond.Wait()` does NOT observe ctx and a bare broadcast re-parks it; guarded `!copyComplete` so a post-clean-COPY close is a no-op that never overwrites the finished Position), `joinPumps()` = `copyWG.Wait()` (NOT under `mu`, so a pump blocked on the lock can't deadlock the wait), then `s.conn.Close()`. The post-COPY CDC pump is a deliberately separate lifecycle (already joined via `copyDone` by `startPump`), documented. Roster flipped `openFinding` → `joinsItsGoroutine`. Validated: 3 Docker-free unit pins + a real-vttestserver 50×-mid-copy-close integration pin (single-stream AND concurrent K=2), gate-clean, mutation-verified both directions (remove join → roster+pin fail; bare broadcast without the terminal flip → backpressure-wake pin deadlocks). `-race` confirmed by CI's Integration matrix (concurrency-chunk rule: pushed first, tag gated on the `-race` run green).
- **The generated-PK identity class, enumerated across all six narrowing sites** (the sibling-sweep step, done as a list rather than a promise). **FIXED:** `internal/engines/mysql` binlog reader, both arms. **NOT AFFECTED, with reasons:** the MySQL **VStream** reader never narrows and Vitess transmits the generated value, so the un-narrowed WHERE still identifies the row (**amended 2026-08-08:** "never narrows" is true of the reader and was NOT true of the whole path — `--where` re-narrows a VStream before-image in `internal/pipeline`, which is why the guard there is a key-completeness check the VStream readers pass rather than a refusal they would have failed); **pgtrigger** refuses any table carrying a stored generated column at `setup.go:939` before a trigger is installed; **sqlite-trigger / d1-trigger** cannot produce the shape (SQLite forbids a generated column in a PRIMARY KEY) and never narrows; `laneapply.PKValuesFromRow` returns `ok=false` on a missing key and routes to the barrier path; ~~`floatrepair.RepairByPK` already refuses loudly when the effective PK is empty.~~

  **RE-DERIVED 2026-08-08: the enumeration is INCOMPLETE, and one of its six exemptions was FALSE.** The `floatrepair.RepairByPK` row is the false one, and it is false in the way the sibling-sweep step keeps producing: the sentence describes what the code does and not what the class needs. The effective join key is the primary key MINUS its STORED generated members, and the guard only refused an EMPTY one — so `PRIMARY KEY (a, g)` with `g` generated yields `{a}`, a non-empty PROPER SUBSET, which the empty-check cannot see. Every batch then ran `UPDATE … WHERE a = $1`, writing one row's re-read FLOAT values across every row sharing that prefix, with a zero exit code. Same "narrowed key is a proper subset of the PK" shape `pipeline`'s `narrowBefore` already refuses, one package over, and reachable outside CDC entirely (`migrate` / `backup full` float repair; `planBackupFloatRepair` builds its pkCols from `migcore.PrimaryKeyColumnNames`, which does not exclude generated members). Fixed as a completeness guard (`len(effPK) != len(pkColumns)`) at the shared driver, which is the chokepoint both the MySQL and the Postgres float-repair wrappers call — so the two engines are covered by one change rather than two. Pinned by `TestRepairByPK_RefusesAGeneratedKeyMember`, a generated-membership × key-arity matrix whose two neighbouring cells are the over-refusal controls: pinning the refusal alone would not distinguish "refuses the broken shape" from "refuses composite keys". Mutation-verified in both directions (the pre-fix empty-only check accepts the defect cell; an arity-keyed guard trips both controls).

  **The enumeration's other gap, filed not fixed:** the class's *actual* reducer is `appliershared.NonGeneratedRowKeys` — every engine applier's WHERE passes through it, and `mysql/cdc_generated_pk.go` names it as the mechanism of the empty-WHERE arm — and it is on no roster, because `TestCDCReadersRefuseAGeneratedRowIdentity` walks `StreamChanges` declarations under `internal/engines/**` and `internal/appliershared` has none. A re-derivation of the narrowing sites from the code (rather than from the filing) found roughly twenty, of which the filed eight are the CDC-facing subset; the ones outside any roster include `laneapply.PKChangedUpdate` (when a key column is absent from BOTH images it reports "not changed" and skips the barrier), `migcore.PKTracker.Observe` (documented as unguarded), `filterByUpperBound`, `redact.DeriveRowSeed`, and `backup/float_repair.floatPatchKey` (`%v` with a NUL separator and no length prefix — the non-injectivity `chain_compact_smart.pkValueKey` was already fixed for). **There is no single roster over narrowing sites**, and building one is the right next chunk; the two that exist are honestly named for what they cover and neither claims otherwise.
- ~~**NOT FIXED — the two confirmed siblings, both silent.**~~ **BOTH FIXED 2026-08-08, and the over-delete was reproduced end to end first.** The harm proof is `TestGeneratedPrimaryKey_UnpublishedIdentityOverDeletes`, whose numbers come from the servers and not from sluice — `pg_publication_tables.attnames` for the premise, the target's own `COUNT(*)` for the harm: **one source DELETE (3 → 2 rows) became two target deletions (3 → 1)**. Ground truth per replica identity on PostgreSQL **16.14**, read off the raw pgoutput stream: **DEFAULT** flags only the published PK members (`R … d numcols=3 [key]a c b`, `D … K a='1' n n`) → non-unique prefix → over-delete; **USING INDEX** over an index containing the generated column behaves identically; **FULL** publishes every *published* column and still omits the generated one, so `filterBeforeToKeyCols` refused — loudly, but naming nothing actionable; **NOTHING** is refused by PostgreSQL itself. So three of four are unsafe and the fourth was already refusing, i.e. no replica identity works and the refusal is not an over-refusal on any of them.

  **Version boundary, and it is not the one it looks like.** PostgreSQL 18 added `publish_generated_columns`; with it set to `stored` the RelationMessage carries the column flagged as a key and the tuple carries its value (verified on **18.4**). **sluice never sets the option** — every `CREATE PUBLICATION` in `postgres/publication.go` is bare — so scoping by *server version* would wrongly un-refuse PG 18. Worse, PG 18 makes the shape an item-93 harm: it rejects the **source application's own** UPDATE/DELETE with `Replica identity must not contain unpublished generated columns` (observed under DEFAULT *and* FULL) the moment a publication that publishes deletes covers the table. The refusal is therefore scoped by the **publication**, not the version, and the reader keys it on the strongest evidence available — a generated identity column ABSENT from the RelationMessage it just received — which self-scopes with no version branch and cannot over-refuse a publication that does carry the column.

  **Where it lives, two placements with distinct jobs (stated so neither reads as the other's backup).** `refuseUnpublishedGeneratedIdentity`, called from `resolveIdentityKeyCols` — so both RelationMessage arms inherit it — is the FLOOR: it reaches cold start, warm resume, `add-table` and mid-stream DDL, because every one of them goes through a RelationMessage. `PreflightReplicaIdentity` gained an `attgenerated` dimension (the audit's own pointer) because on PG 18 the reader is too late — the publication is scoped and the operator's application is already broken by the time a change arrives; it runs at cold start and add-table, *before* `EnsurePublication`. The preflight is deliberately LENIENT where the reader is exact (it exempts a column some publication carries, via `pg_publication.pubgencols` on 18+), on the principle that an early check may miss but must never refuse something that works. Two knock-on corrections: the aggregate refusal no longer claims "REPLICA IDENTITY FULL is always sufficient" (false for this class), and the candidate-index query no longer offers an index containing a generated column as the remedy.

  (b) **`narrowBefore`** is fixed as a **completeness guard, not a shape check**: it refuses when the re-narrowed key is a proper subset of the table's PK. Keying on evidence rather than on "the PK contains a generated column" is what keeps the VStream readers — which do transmit the value — from being over-refused, and it catches any other producer of a partial key for free. Engine enumeration, derived from the `ir.FilteredCDCPreflighter` pins rather than asserted, and held to the code by `TestWhereCDCEngineRosterMatchesTheFilteredSyncGate`: **mysql** (binlog reader reaches it — its own refusal sits on the narrowing arms this path bypasses; the two VStream readers reach it and pass, which is the over-refusal control) and **postgres** (reaches it; the reader refuses first, so this is the floor behind it, and the only guard on a warm resume attached to a stream an older build started). **Gate built:** `TestCDCReadersRefuseAGeneratedRowIdentity` (`internal/engines`) — a fail-by-default AST roster over every `StreamChanges` declaration under `internal/engines/**`, each package refusing-by-code or exempt-with-a-reason, with an anti-vacuity floor and a per-engine floor; a stale exemption fails it too. The sqlite-trigger exemption no longer rests on a sentence: `TestPremise_SQLiteForbidsAGeneratedPrimaryKey` ground-truths "generated columns cannot be part of the PRIMARY KEY" across STORED/VIRTUAL × sole/composite. **Known limit, stated at the gate:** the roster is PACKAGE-keyed, so one refusal satisfies mysql's three readers; and for `relreplident='f'` the preflight grades the PRIMARY KEY (what sluice narrows to) while PG 18's own rule is wider — a FULL table whose only generated column is *outside* the PK passes here and would still break on 18. Widening it would over-refuse every earlier version. Mutation-verified in both directions at every site; the run also **found a missing control** (a generated column in a non-identity UNIQUE index) that no cell covered, now added.

**G-12 is the standing form of this list**: adopt it as a queue and re-run the sweep each Tier-3 audit. It is cheap, mechanical, and it is what found A-3.

**Recommendation after working it end to end (2026-08-08): ADOPT, with two changes to the form.**

The case for it is the divergence table above — 11 of 14 verdicts differed from their filing, and four entries turned up a defect *bigger than the claim*, found while building the check rather than by reading the claim. A sweep whose filings were mostly right would not be worth re-running; this one's value is almost entirely in the re-derivation the queue forces.

The two changes:

1. **The queue must carry the claim, not the check.** Three entries were graded wrong in the same direction — the audit proposed a check, and the proposed check was either already built (mydumper), narrower than the defect (pgtrigger `seqmin`), or aimed at the wrong half of the argument (ADR-0075, laneapply). The durable field is the SENTENCE and the site; the proposed check is the auditor's first guess and should be labelled as one.
2. **Dedupe and count honestly at the end.** This queue shipped with one claim listed twice and three that no longer exist anywhere, so its own denominator was wrong in both directions. "14 of 14 nameable, 3 unrecoverable" is the true shape and took one pass to establish.

## Open — 2026-08-01 audit

§4 MEDIUMs: `verifySchemaHashes` never runs for a bare full; the broker hand-rolls a subset of the shared preflights and drops target tables off an UNVERIFIED manifest. §4 gate proposals: **6 built, 2 partial, 2 open** — open are the IR field-classification/DDL property gate and the goroutine global-read analyzer; partial are the bulk-write contract table and the shipped-default gate rule.

## Open — 2026-08-04 audit

MEDIUMs: ~~compaction re-stamps its own verify hashes (= C-4 above)~~ — **CLOSED on branch 2026-08-05 with roadmap item 129**: smart compaction now re-reads every rewritten chunk through the store with the real reader and compares the decoded count against the one it recorded; sibling sweep at the gate (the non-smart path moves chunks verbatim carrying their original hash, `prune` rewrites no bytes). `mergeAfterImage` dead DDL barrier; `mergeAfterImage` dead DDL barrier; `trigger prune` missing peer-stream warning; CI SHA-pinning stops short of the merge/publish workflows; `mergeAfterImage` allocation. Gate repairs: all four mutation-inert gates fixed with mutation proof. Still open: the manifest-consumer gate's one-directory scope, `pgIndexName`'s self-oracle, the curated `canonRoster`, the one-package dispatch pin, archgate's two inert rules, and a cited-but-nonexistent `TestAutoResnapshotReason_*`.

## Open — 2026-08-05 docs-drift sweep

A `docs-drift-detector` pass over the unreleased delta (items 114 / 115 / 129 / 130 / 131) found nine false doc claims. Seven were in-repo and are fixed in the same pass; what is recorded here is the part that is **out of this repo** or is a **gate proposal rather than a copy fix**.

### The sluicesync.com error-code table lags this repo's

`C:\code\sluicesync.com` is a separate repository; nothing in this section is fixed here.

**These counts rot within a release, so derive them, don't read them.** The 2026-08-05 version of this entry said "78 codes … 74 rows, four missing, two text divergences" and every one of those numbers was wrong a day later — the repo had reached 82, eight rows were missing rather than four, and there were three divergences rather than two. Both sides move independently: the repo mints codes continuously while the site can only carry PUBLISHED ones, so a nonzero gap is the normal steady state and only its composition is actionable. The authoritative enumerator is the site build's own `errorCodeTextDrift()`, which names each divergent code and each one-sided row against a sibling checkout — run the site build rather than trusting any number written down here.

The two structural facts that do NOT rot:

- **`docs/operator/error-codes.md` is the source of truth for the text**, because it is the file `TestRegistryDocSync_ContentEquality` pins byte-equal against `internal/sluicecode/docrows.go`. Port site edits FROM it, never the reverse.
- **A missing row is only actionable if the code is PUBLISHED.** Split the missing set by whether the code exists at the latest tag: `git show <tag>:docs/operator/error-codes.md`. Codes present only on `main` are correctly absent from the site until they ship, and adding them early documents a code no released binary can emit.

As of this commit: 82 codes on `main`, 78 at `v0.114.0`, 78 rows on the site; the four missing rows (`SLUICE-E-CONFIG-MULTI-NAMESPACE-TARGET-FLAT`, `-SCHEMA-TABLE-NAME-COLLISION`, `-SCHEMA-VIEW-NAME-COLLISION`, `-TARGET-PREEXISTING-FOREIGN-KEY`) are all unreleased and therefore correctly absent. Outstanding text divergences: `SLUICE-E-BACKFILL-CORRUPT-CURSOR` (the site renders `2<sup>53</sup>`, which flattens to `253` against the repo's `2^53` — a real failure of the drift check, which strips tags), and `SLUICE-E-INDEX-MISSING`, whose Meaning this sweep rewrote to name the MySQL-family flavors.

- **The site's `cliFlagDrift` check has a blind spot for `backup verify --depth`.** It compares flag NAMES, and `--depth` already exists on `sluice verify` — so a name-only comparison passes even though the two flags now take different value sets (`count|sample` vs `hash|read`). The check must key on (subcommand, flag), not flag name alone, or `backup verify --depth read` will stay undocumented on the site with the build green.

### Gate proposals — operator-facing engine-set claims with no docsync marker

Both are prose that names which engines a behaviour applies to, with nothing binding the claim to the code. Per the working agreement ("a claim that names an engine set belongs behind a doc-sync marker, not in prose alone"), each wants an `internal/docsync` gate on the `TestFilteredSyncEngineListMatchesTheCode` pattern — capability/roster pin → engine set → doc marker. Neither is a copy fix; do not "correct" the prose without building the gate, because the prose is currently true.

- **`SLUICE-E-COPY-RETRY-AMBIGUOUS-KEYLESS`'s Meaning** asserts "**Both engines** normally RIDE such a transient by re-sending the same rows on a fresh connection." True today, and it is exactly the sentence item 114 would have invalidated had it gone the other way. The derivable fact is the set of write cores that call a reparent-retry helper (`flushWithReparentRetry` / `copyChunkWithRetry`) — currently three on MySQL and two on Postgres, with `raw_copy` and PG's plain multi-row INSERT core deliberately outside. Note this text lives in `docrows.go` **and** `docs/operator/error-codes.md` byte-equal, so a marker has to sit outside both or be identical in both.
- **ADR-0108's write-core enumeration** ("`raw_copy` and Postgres's plain multi-row INSERT core never re-send at all and are exempt for that reason") is the same list in a second place, and it is the one that went stale: it said `LOAD DATA` was exempt for ~1 commit after item 114 made it inherit the carve-out. A roster derived from the retry-helper call sites would have failed the build on that commit.

## Open — 2026-08-06 docs-drift sweep

### ~~`sluice sync start --resume` is a phantom flag, and sluice's own RUNTIME messages hand it to operators~~ — **CLOSED for the CODE half, and the gate proposed below is BUILT**

`internal/climsggate` + `TestRuntimeMessagesNameRealCLISurface` (`cmd/sluice/cli_message_surface_test.go`) is the walker described at the bottom of this entry, built as specified: Go string literals only, fail-by-default, **no exemption list**, three anti-vacuity floors (spans / distinct commands / graded flag tokens). Truth comes from kong's model directly rather than from `docs/dev/cli-flags.txt`, because the manifest records flags only — a command owning no flags of its own leaves no line in it.

It found **16** instances, not the 9 filed. The 7 the sweep did not have were a second class each: a flag that exists on a *sibling* command (`sluice migrate --schema-already-applied` — that flag is `sync start`-only, and the partition preflight it advertised does not even run on `sync`, so the advice was worse than wrong), a flag that exists **nowhere** (`sluice sync start --reset-position`, which ADR-0022 explicitly records as never having existed; corrected to `--restart-from-scratch`), and four shorthand invocations that omit the binary name (`restore --tables` → `--include-table`, in `docrows.go` **and** its byte-equal `docs/operator/error-codes.md` row; `sync --where` → `sync start --where`; `sync --position-from-manifest`).

Two scope decisions are stated at the gate rather than left implied: it grades **string literals, not comments** (the tree carries a long-standing `sync --where` shorthand in doc comments that is prose, not instruction), and it **skips `_test.go`** — a gate's own fixtures have to be able to spell a phantom flag, and walking them would force in the exemption list the design exists to avoid.

The false-positive rule, since a no-exemption gate lives or dies on it: a span is graded only when it starts at the word `sluice` (or, for the kong-help form, at a delimited region that *begins* with a real command **and** carries a flag), and a delimited span **ends at the author's own closing delimiter**. That last part is what makes the posture honest — a flag named in prose *outside* an invocation is a REFERENCE, which is exactly how a message tells an operator a flag was removed. `schemaRaceRecoveryHint`'s trailing mention of `--forward-schema-add-column` is ungraded for that reason, and the corrected `partition_preflight` message deliberately uses the same shape to say `--schema-already-applied` is not a `migrate` flag.

**The sibling gate had the same hole and is fixed in the same pass.** `scripts/check-skills-flags.sh` validated flags and subcommands in two separate NAME-ONLY passes, so a skill saying `sluice sync start --resume` passed both — `sync`/`start` are real commands, `--resume` is a real flag on `migrate`. It now grades (command, flag) pairs against `docs/dev/cli-flags.txt` with ancestor inheritance and a vacuous-pass floor; 99 pairs graded today.

**Doc instances remain open** and are listed below — including `docs/adr/adr-0066-*:749`, which carries the `--reset-position` phantom the code half just lost.

**Update 2026-08-07 — the DOC half is now CLOSED too.** All 18 doc files listed below were swept: the flag was **deleted, not substituted**, since `sync start` warm-resumes on its own. Highest-harm first — `docs/use-cases.md` ×3 and `docs/cutover.md` ×2 were copy-pasteable command blocks. Also closed in the same pass: the two `sluice schema migrate` runtime literals (`schema_forward_intercept.go`, `shard_consolidation_probe.go`) and the doc-side `--exclude-column` in `adr-0066:789` + `roadmap.md:411`. What deliberately REMAINS spelling the phantom is the notice shape the union-level-gate decision below turns on: the 2026-08-06 ADR `## Correction` notes, roadmap item 53's record of the bug it found, `recipe-bidirectional-cutover.sh`'s explanatory comment, `docs/schema-change-runbook.md`'s "there is no `--resume` flag on `sync start`", and this entry. Those are REFERENCES, not instructions.

The original entry follows.

### `sluice sync start --resume` is a phantom flag, and sluice's own RUNTIME messages hand it to operators

`sync start` has no `--resume` and never has: it warm-resumes by default when re-invoked with the same `--stream-id`, and the flag exits 80 with `unknown flag`. (`--resume` is real on `migrate`, which is where the slip comes from. This entry originally also named `restore`; that was itself wrong — `restore` has no resume flag either, per `docs/dev/cli-flags.txt`. ADR-0030's Correction note carried the same error and was amended 2026-08-07.) Roadmap item 53 caught and fixed one instance in a cookbook recipe in 2026-07; the class was never swept.

The 2026-08-06 sweep corrected the ADRs it was scoped to — ADR-0030, ADR-0048, ADR-0054, each with a `## Correction` note recording that the flag never existed so the edit is not a silent rewrite of an accepted record. **The expensive half is still open, and it is not a docs problem:** the string appears in production code that operators read *at the moment they are recovering from a stalled stream*, i.e. exactly when a wrong command costs the most.

- `internal/engines/postgres/cdc_relations.go:309` — `schemaRaceRecoveryHint`, step (3) of the drained-model recovery for an incompatible mid-stream DDL.
- `internal/pipeline/schema_forward_intercept.go:547` — the schema-forward refusal's recovery hint (this is the text ADR-0157 pipes into the **schema-drift notification**, so it also reaches webhook/Slack/email).
- `internal/pipeline/add_table.go:624` (completion log), `:1298` (dry-run log), plus the doc comments at `:17` and `:44`.
- `internal/pipeline/shard_consolidation_engage.go:147` and `shard_consolidation_probe.go:679` — Shape A drained-model hints.
- `cmd/sluice/schema_add_table.go:78` (the `--inject-shard-column` help text) and `:135` (a refusal message).
- `cmd/sluice/preview.go:26` (doc comment).

Not fixed in the docs sweep because it changes operator-visible message text and at least one pinned assertion (`cmd/sluice/schema_add_table_shape_a_test.go:60` asserts the substring `"sync start --resume"`), so it wants its own review rather than riding a docs diff. Remaining doc-only instances left with it so the class stays in one place: `docs/use-cases.md:29` (a copy-pasteable command block — the highest-harm doc instance), `docs/postgres-source-prep.md:184`, `docs/vitess-vstream-troubleshooting.md:234,443`, `docs/adr/adr-0058*:17,606,647`, `adr-0060*:374`, `adr-0094*:18`, `adr-0157*:12`, and `docs/dev/design/{logical-backups-phase-2,logical-backups-phase-3,mid-stream-add-table,sluice-verify}.md`. CHANGELOG and `docs/releases/` occurrences are historical records and should NOT be edited.

**Gate proposal.** The derivable fact is the kong CLI's (command, flag) set, which `cmd/sluice/cli.go` already declares — a walker could extract every `` `sluice <cmd> --<flag>` `` occurrence from backticked spans in `docs/` + Go string literals and assert each names a real flag on that command. The obstacle is that ADRs and design notes legitimately describe planned-but-unbuilt surfaces (ADR-0054's `sluice schema migrate-shape-a` is correctly labelled Phase 3+), so a naive gate needs an exemption list — the curated-promise anti-pattern. A gate scoped to **Go string literals only** has no such problem: a runtime message has no business naming an unbuilt command, so it can fail-by-default with no exemptions, and it covers precisely the instances above that actually reach an operator mid-incident.

## 2026-08-07 — Bug 230: the bare-flag blind spot the v0.115.0 open list did not name

**Correcting the record first.** v0.115.0's release notes name ONE blind spot in `internal/climsggate` — that an unknown SUBCOMMAND is graded only inside a delimited span that also carries a flag, leaving two `sluice schema migrate` messages uncaught. That list was incomplete. The gate's anchoring rule (a span is graded only when it starts at the literal word `sluice`) has a second, larger consequence: **a bare `--flag` named in PROSE is never looked at at all.** The published Open list is out and is not being edited; this file is the durable correction. Both blind spots were real when that release shipped, only one was named, and a gate whose stated blind spots are narrower than its actual ones is the failure mode CLAUDE.md calls worse than no gate.

**The defect (Bug 230 — LOW, loud, zero data loss, pre-existing; byte-identical on v0.114.0).** One shared bulk-copy-failure hint said `use --resume to continue …, or --exclude-table=<name> to skip it`. The bulk-copy phase is shared: it runs under `migrate`, both `sync` cold-starts (`sync start` and the fleet's `sync run`) and `schema add-table`. `sync start` has no `--resume`; `sync run` has neither flag (its table scope is a config key); `schema add-table` has neither AND copies exactly one table, so the hint's "any earlier tables" premise is false there too — while its own `--help` says "there is no resume flag". Three of the printing commands exit 80 `unknown flag` if the operator follows the line, mid-incident.

**Fixed by making the text per-command, keyed on kong's own selected-node path** (`migcore.SetRunningCommand`, called once from `main` — derived from kong rather than a roster, so it covers commands that do not exist yet). The neutral text is the ZERO VALUE and names no command-specific flag, so a forgotten override costs precision, never correctness.

**Sibling sweep (the whole registry, not the one entry).** Grading every registry text against every command found four more entries of the same class, all fixed here: the index-phase errno-3024 wall named `--resume` / `--upfront-indexes` / `--planetscale-org` to `restore`, `sync run` and `add-table` too; the direct-DDL block named `--planetscale-org`; the constraints-phase FK wall named `--resume` / `--where` / `--skip-foreign-keys`; and the connect-phase "database does not exist" hint said `--target`, which a fleet `sync run` does not have.

**The gate: `TestHintRegistryTextsNameOnlyFlagsTheirCommandAccepts` (`cmd/sluice/hint_command_gate_test.go`).** It grades every bare flag in every text `migcore.HintTexts()` can emit — a per-command text against that command, the neutral text against ALL of them — fail-by-default, no exemption list, three anti-vacuity floors. It is DERIVED from the registry, so a new hint or override is graded the moment it is added. Scope is stated at the gate: it covers the hint registry (plus the dynamic AAAA-only classifier), not every runtime message, and it grades what a text says rather than which commands reach a phase — which is exactly why the neutral text is graded against every command rather than a rostered subset. An under-declared roster is what Bug 230 was.

### Open — the residual, with the measurement that decided it

**The union-level bare-flag class is NOT gated, deliberately.** Census over every non-test Go string literal under `cmd/sluice` + `internal/` (2026-08-07): **2100** bare `--flag` tokens, **247** distinct names, of which **58 occurrences / 31 distinct names** name nothing on any sluice command. Nearly all of those are legitimate: other tools' flags (`pg_dump --blobs`/`--schema-only`, `mydumper --build-empty-files`, `gcloud … --database-flags`, `az … --resource-group`/`--server-name`/`--name`/`--value`, `mysqld --local-infile`, `docker run --name`, `vtgate --grpc_max_message_size`, `pgcopydb --table-jobs`, MySQL's own `--read-only`/`--super-read-only` error text), family prefixes written `--notify-*` / `--planetscale-*` / `--include-*`, and two deliberate removed-flag notices. A gate over that surface would need an exemption list about as long as its finding list, which this project has repeatedly paid for. Re-decide if the ratio moves; the census is one AST walk plus `cliSurface`.

**The genuine phantoms the census DID surface were fixed in the same pass** (all loud, none silent-loss), and none of them is covered by any gate:

- `mysql/tls_ca.go` — `--tls-ca`; the real flags are `--source-tls-ca` / `--target-tls-ca`.
- `cmd/sluice/cli.go` — `--planetscale-metrics-database`; the real flag is `--planetscale-metrics-db`.
- `pipeline/supervisor.go` — `--max-consecutive-failures`; it is a fleet-config key (`restart.max-consecutive-failures`), not a flag.
- `migcore/cross_engine_supportable.go` — `--index-override`, which has never existed.
- `pgtrigger/setup.go` ×2 — `--exclude-column`, which ADR-0177 explicitly records sluice does not have (there is no column-scope filter at all), so the hint told the operator to do something impossible.
- `mysql/cdc_vstream_shard_progress.go` — `--vstream-relax-skew`; relaxed is the ADR-0120 default and the opt-out is `--vstream-preserve-skew`. **A unit test asserted the phantom**, which is the sharper half of this one.
- `migcore/row_filter.go` + `pipeline/where_cdc_filter.go` — `--include`, shorthand for `--include-table`.

**Left alone on purpose, and why:** `pgtrigger/cdc_reader.go`'s "there is no `--reset-position` flag" and `cli.go`'s "the Phase 1 `--redact-key-source` flag … w[as] removed" are removed-flag NOTICES — correct prose a naive gate would flag. They are the shape that makes a union-level gate expensive, and they are why the house idiom for an absent flag writes it WITHOUT the `--` (the eight existing "there is no resume flag" sites). The new `sync start` hint follows that idiom; the hint gate caught the first draft that did not, which is the cheapest possible evidence the gate works. ~~Also still open: the two `sluice schema migrate` unknown-subcommand instances v0.115.0 named (`schema_forward_intercept.go:546`, `shard_consolidation_probe.go:678`), and the doc-side `--exclude-column` in `adr-0066:789` and `roadmap.md:411`.~~ **All four CLOSED 2026-08-07** with the doc half of the phantom-command class. The two Go literals now say "apply the schema change … yourself (sluice has no schema-migrate command; on PlanetScale, `sluice deploy-ddl` ships one statement safely)" — note `deploy-ddl` is PlanetScale-scoped, so it is named as a qualified example rather than as the general schema-apply surface. `climsggate`'s unknown-subcommand blind spot is unchanged and still stated above; widening it was out of scope for a docs pass.

## 2026-08-09 — v0.118.0 regression cycle

### Bug 237 (filed LOW, RE-GRADED — arm (a) is a loud `migrate` refusal, not an advisory-surface defect): `schema diff` still reports phantom drift on a `migrate`-created target

**CLOSED 2026-08-10, together with roadmap item 156** — the two are the same shape and were fixed as one chunk; see item 156 in `docs/dev/roadmap.md` for the fix, the sibling enumeration, the named wart, and the two findings the work surfaced.

**The re-grading, and why it is worth writing down.** Arm (a) was filed as an advisory-surface problem that "exits 1 with no data at risk". Measured on real containers before the fix, it is also this: the ADR-0166 migrate pre-create shape gate compares the SAME `translate.RetargetForShapeCompare` rendering `schema diff` does, so a SECOND `sluice migrate` over a target the first one created REFUSED — `1 pre-existing target table(s) differ … column "ts_bare": want DateTime(unspecified), target has DateTime(6)` — for any PG schema carrying a bare `TIME`/`TIMESTAMP`/`TIMESTAMPTZ`. A re-run and a resume are ordinary operations, so this was a loud failure on a working configuration (queue priority 2), not a LOW. The filing graded the SURFACE it was found on rather than the shared code path underneath it; the general lesson is that a defect in a comparison helper inherits the severity of its most consequential consumer, and `RetargetForShapeCompare` has two — one that reports and one that REFUSES.

**Arm (a) was also WIDER than filed.** The filing named precision (`Time(unspecified)` vs `Time(6)`). The measured matrix has a second, independent axis: MySQL has no `timetz`, so a PG `TIME WITH TIME ZONE` reads back with the zone flag CLEAR at every precision — `TimeTZ(3)` mismatches `Time(3)` with no precision problem at all. A fix derived from the filed witness alone would have left it.

Two residual arms, both surviving the Bug 234 / item 158 index-and-type work and neither newly introduced:

- **Unspecified-precision PG temporals.** A PG `TIME` / `TIMESTAMP` with no precision compares as `Time(unspecified)` against the faithful `Time(6)` the target actually holds. Same family as the `PrecisionUnspecified` collapse `NormalizeForCDCComparison` already performs for CDC — the shape-compare path does not.
- **sluice's own `SET`-emulation CHECK reported as extra.** `migrate` emits a CHECK carrying the member list when it lands a MySQL `SET` on a target without one; the expected side is built without it, so the diff reports the constraint sluice itself created.

Deferred rather than fixed with Bug 236 because the two have nothing in common beyond the command: this one is a comparison-normalisation gap that has been present since the cross-engine diff existed, it exits 1 with no data at risk, and folding it into a same-day regression patch would have widened a release cut specifically to close a shipped over-refusal. **It is the same shape as item 156** (a domain's translatable CHECKs landing as auto-named table-level CHECKs the flattened expected side cannot carry) and should be picked up with it — the SET-emulation arm is very nearly item 156 with a different constraint source.

### ~~NEW (filed 2026-08-10, found while closing Bug 237 / item 156): MySQL's SchemaReader mangles any CHECK clause or generated-column body containing an APOSTROPHE~~

**FIXED (2026-08-11), with the escaping matrix the filing asked for — measured first, on real MySQL 8.0 AND MariaDB 11** (full Phase-A notes in `workspace/queue-burn-memo-mysql-check-escaping.md`; the operator chose option (a)). What the measurements changed about the filing: the `NO_BACKSLASH_ESCAPES` axis largely COLLAPSES — MySQL refuses to CREATE an apostrophe-carrying CHECK/generated column under that mode at all (1064 at DDL time), and the I_S rendering is mode-invariant on read (the stored expression is normalized to standard-mode spelling at DDL time) — both now premise-pinned against the server. MariaDB measured as NOT applying the I_S layer at all (its I_S returns the SHOW CREATE spelling), so the normalizer is flavor-dispatched.

The fix: `normalizeMySQLExpressionText` is now a two-stage literal-aware scanner (decode the measured I_S layer; walk the SHOW CREATE spelling with the package's full ADR-0161 literal decoder), replacing THREE blind passes — the filed apostrophe mangle plus two sibling defects the filing had not named (backticks stripped inside literal VALUES; introducer-looking value text partially deleted). Output contract: SQL-standard spelling ('' doubling, backslashes BARE — correct for PG, where the old spelling silently DOUBLED every backslash value); the MySQL emit boundary re-escapes backslashes per the writer's resolved sql_mode (`escapeExprLiteralBackslashes`, threaded through all four expression positions), and the shard-consolidation probe now decodes its observed side before comparing. Pinned by the unit matrices (measured bytes verbatim) and `TestSchemaReader_ExpressionEscapingMatrix` (real-server premise legs + a round trip whose CHECK enforcement and generated value the TARGET SERVER proves). Mutation-run in both halves. One consequence for operators, stated rather than implied: chains whose recorded schema carried a previously-mangled or backslash-carrying expression re-fingerprint on their next incremental (the recorded spelling changed), forcing one new full backup — the recorded expression was wrong or PG-corrupting, so the repartition is the correct outcome.

Original filing, kept for traceability:

`normalizeMySQLExpressionText` → `convertMySQLEscapedApostrophes` is a blind `strings.ReplaceAll(s, "\\'", "'")`. But `information_schema` renders a string literal DOUBLE-escaped when its value itself contains a quote: a CHECK on the pattern `^o'brien$` comes back as ``regexp_like(`quoted`,_utf8mb4\'^o\\\'brien$\')``, and a single un-escaping pass leaves `'^o\\'brien$'` — two backslashes, a literal that terminates in the wrong place, and an expression the IR then carries as malformed text.

**Measured**, real `mysql:8.0`, `internal/pipeline` scratch probe (RAW `information_schema.CHECK_CLAUSE` vs the reader's `ir.CheckConstraint.Expr`). Reachable by any PG DOMAIN CHECK or MySQL user CHECK whose literal contains an apostrophe, and by any generated-column body likewise — the same normalization serves `GENERATION_EXPRESSION`.

**Not fixed in that chunk, deliberately.** The blast radius is wider than the comparison surface that found it, and doing it right needs its own escaping matrix: quote-only, backslash-only, both, and the `NO_BACKSLASH_ESCAPES` sql_mode where the doubling convention inverts (`quoteSQLString` is already mode-keyed for exactly this reason, and the READER is not mode-aware at all). Consequence today is loud rather than silent — such a constraint reports as one missing plus one extra on `schema diff` rather than being matched — but a mangled generated-column body re-emitted on a restore lane is the shape to look at first when this is taken up.

### The Bug 236 lesson: a pre-tag fix is still a fix, and it still owes the sweep

Bug 236 is the sibling of a defect fixed **in the same release, four commits earlier**. The Postgres CDC lane could not tell "this column declares SRID 0" from "no catalog ever established what it declares"; CI caught it before the tag, it was fixed, and the MySQL lane — which had the identical defect for the identical reason, its own loader not reading `srs_id` — was never enumerated.

The reason is worth naming because it will recur: the sweep discipline is attached to *filing a fix*, and this fix arrived as **a CI failure during a release**, where the felt task is "unblock the tag" rather than "close a class". Both engines' CDC lanes are obvious siblings the moment the question is asked; nobody asked it.

**The rule, added to the release flow rather than to anyone's memory: a fix made between the release commit and the tag gets the same sibling enumeration as any other, written into the commit.** If there is no time to enumerate, there is no time to tag.

## 2026-08-09 — v0.118.1 verification cycle

### Bug 238: WITHDRAWN — NOT A BUG. MySQL and MariaDB store geographic geometry identically; the divergence is in the conversion functions, not the storage

**Withdrawn 2026-08-09, on a measurement taken while building the wiring (part 2).** Everything below this block is the ORIGINAL filing, kept because the reasoning is instructive; its central premise is FALSE and the fix it specifies would have introduced the corruption it was designed to prevent. Read this block first.

**What was measured.** Seed the same place on each server through that server's own WKT convention, then compare the STORED bytes:

```
mysql>   INSERT … ST_GeomFromText('POINT(12.5 41.9)', 4326, 'axis-order=long-lat');
mysql>   SELECT HEX(g);  →  E6100000 0101000000 <12.5> <41.9>     ST_Latitude = 41.9, ST_Longitude = 12.5
mariadb> INSERT … ST_GeomFromText('POINT(12.5 41.9)', 4326);
mariadb> SELECT HEX(g);  →  E6100000 0101000000 <12.5> <41.9>     ST_X = 12.5, ST_Y = 41.9
```

Byte-identical. And MariaDB's exact stored bytes, inserted verbatim into a MySQL `POINT SRID 4326` column, read back as `ST_Latitude = 41.9, ST_Longitude = 12.5` — Rome. The `POINT(-122.4 37.8)` (San Francisco, `|lon| > 90`) row carries the same way, with no `ERROR 3617`.

**Why the filing looked right.** MySQL's per-SRS axis order is a property of the CONVERSION FUNCTIONS — `ST_GeomFromText`, `ST_AsText`, `ST_AsBinary`, `ST_GeomFromWKB` — whose default for a geographic SRS is latitude-first. It is NOT a property of the stored form, which is `(longitude, latitude)` on MySQL, MariaDB and PostGIS alike. The original comparison put MariaDB's `ST_AsText` (lon-first) next to MySQL's `ST_AsText` (lat-first for a 4326 value) and read the differing RENDERINGS as differing storage. The `ERROR 3617` was the same artifact on the input side: `ST_GeomFromText('POINT(-122.4 37.8)', 4326)` on MySQL reads -122.4 as a latitude, which is a seeding mistake and not a carriage one. sluice reads and writes the stored form and touches neither function.

**So byte-verbatim carriage was already correct**, in every cell of the matrix — including MySQL→PostGIS, which the filing added on the same reasoning.

**What landed instead.** The premise is now a test rather than a paragraph: `TestGeometryAxisPremise_MySQLAndMariaDBStoreTheSameBytes` and `TestGeometryAxisPremise_MariaDBToMySQLCarriesThePlace` (`internal/pipeline/geometry_axis_premise_integration_test.go`) pin both halves against real `mysql:8` and `mariadb:11.4` over all seven geometry families, so a genuine future divergence in either server's stored form fails the build. `swapWKBAxes` (part 1) was DELETED by the same commit that first wrote this paragraph (`fb7c60eb` removed `internal/engines/mysql/geometry_axis.go` and its unit tests; an earlier draft of this sentence claimed it "stays in the tree, unwired" — false the moment it was written, corrected 2026-08-12 per the audit's M0). If the premise test ever fires, recover the helper from `fb7c60eb^`.

**The process lesson, and it is the expensive one.** This was filed HIGH-SILENT, graded, decided by the operator, and written up as a turnkey chunk with an implementation plan, a flag, an error code and an integration matrix — all on an environmental premise nobody had bound to a server. CLAUDE.md's premise-naming step exists for exactly this shape and was not applied to the FILING, only to fixes. **A bug report that asserts a fact about an engine's behaviour owes that fact the same runtime check a fix would**, and the cheapest form is the one used here: seed both servers, compare `HEX(col)`, before designing anything. What caught it was building the integration matrix the plan asked for — the MySQL→MySQL control passed and the MariaDB→MySQL cell failed in the direction that made the fix, not the bug, the cause.

---

**ORIGINAL FILING BELOW — PREMISE DISPROVEN, RETAINED FOR THE REASONING ONLY.**

**Verdict: confirmed, with two corrections to the filing.** The report's headline example (`POINT(37.8 -122.4)` in, `POINT(-122.4 37.8)` out) does NOT reproduce: the WKT round-trips identically and both servers read the same NUMBERS. And the error is **3617**, not 3732. Re-measured on stock `mysql:8` and `mariadb:11.4`.

**The real mechanism.** The servers disagree about which coordinate is latitude. MySQL 8 applies the SRS's EPSG axis order, so SRID 4326 is `(lat, lon)` — `ST_Latitude` of `POINT(37.8 -122.4)` is 37.8. MariaDB is axis-agnostic and stores the GIS convention `(lon, lat)`, where `ST_X` is longitude. sluice carries the WKB verbatim, so the numbers survive and their MEANING does not:

- `|lon| > 90` → `ERROR 3617 … Latitude -122.400000 is out of range` at insert. Loud.
- `|lon| ≤ 90` → **silent**. Measured: MariaDB `POINT(12.5 41.9)` (Rome) lands on MySQL as latitude 12.5, longitude 41.9 — off the Somali coast, exit 0.

Byte-verbatim carriage is correct in isolation and the cycle's controls prove it: MySQL→MySQL and MariaDB→PostGIS are both verbatim-correct. This pair is not.

**Decision (operator, 2026-08-09): handle it AUTOMATICALLY — swap so the meaning is preserved — and log a warning saying it happened.** The one shape auto-swap is wrong for is a MariaDB user who deliberately stored lat/lon; that gets an opt-out flag so the rewrite is recoverable rather than silently unfixable.

**Design note for whoever builds it — the cheap-looking route is the wrong one.** Swapping inside sluice means a WKB walker that rewrites every coordinate pair of every subtype (point / linestring / polygon / multi* / collection, × Z/M dimensions): error-prone, and it still needs a rule for which SRIDs are geographic. MySQL supplies the lever directly — `ST_AsBinary(col, 'axis-order=long-lat')` on read and `ST_GeomFromWKB(?, srid, 'axis-order=long-lat')` on write — which makes MySQL's wire form canonically lon/lat, applies each SRS's OWN axis order rather than hardcoding 4326, and needs no byte surgery. Cost: the read projection and the write binding both change, across bulk-copy AND the CDC apply path.

**Prefer normalising at the ENGINE boundary over special-casing the pair.** With the IR's geometry value defined as canonical `(lon, lat)`, only the vanilla-MySQL reader and writer change; MariaDB and PostGIS already match. Every pair then falls out correct, and MySQL→MySQL stays byte-identical because swap-on-read and swap-on-write compose to identity — which preserves the cycle's control. **It also fixes a pair nobody has tested: MySQL→PostGIS is verbatim today, and PostGIS is lon/lat, so that direction is very likely mis-landing exactly like this one. Measure it before building; if it is broken, it belongs in the same fix and the same release note.**

**Part 1 LANDED (`eaa18e8c`, `71cb943d`): `swapWKBAxes`** — the 2D WKB walker, pinned against MySQL's own `ST_SwapXY` output for all seven types, plus involution, no-input-mutation, and refuse-rather-than-pass-through for every unwalkable shape. **MySQL→PostGIS was measured for it and IS broken** (MySQL `POINT(37.8 -122.4)` lands on PostGIS as lon 37.8 / lat -122.4, stored without complaint), so it belongs in this fix rather than a follow-up.

**Part 2 — the WIRING — is NOT built, and the reason is the risk shape.** The swap must reach EVERY MySQL geometry path: bulk-copy read, binlog CDC decode, VStream cell decode, bulk-copy write, CDC apply. Reaching some but not all produces a read/write ASYMMETRY, which is strictly worse than today's consistent-but-wrong carriage — it corrupts in one direction only, and the family tests would still pass on the paths that were wired. This is the sibling-enumeration failure mode with a silent-corruption payload, so it wants doing deliberately rather than at the tail of a session.

The plan, so it is turnkey:

- **Canonical order is (longitude, latitude)** in the IR. Only vanilla MySQL swaps; MariaDB and PostGIS already match. MySQL→MySQL stays byte-identical because swap-on-read and swap-on-write compose to the identity — pinned already by the involution test.
- **Gate on two inputs at each site: the flavor (swap unless `FlavorMariaDB`) and whether the column's SRID is GEOGRAPHIC.** Do not hardcode 4326: a stock `mysql:8` has 545 geographic SRSs, and the catalog distinguishes them — `SELECT SRS_ID FROM information_schema.st_spatial_reference_systems WHERE DEFINITION LIKE 'GEOGCS%'` (3857 is `PROJCS` and must NOT swap; SRID 0 has no definition). Resolve once per connection and cache.
- **The plumbing is the work.** Neither `decodeValue` (value_decode.go:77, the `ir.Geometry` arm) nor `prepareValue` (row_writer.go, the `ir.Geometry` arm) carries the flavor today, and `decodeValue` recurses. Either thread a small policy value through both, or normalise one level out at row granularity in each reader/writer — the latter is less invasive but has MORE sites to enumerate, which is the trade to decide first.
- **Enumerate the sites in the commit, and add a roster gate**, because "wired some paths" is the failure mode. An AST roster over the MySQL geometry read/write sites with an anti-vacuity floor is the mechanical form.
- **Log once per table when a swap applies** (operator decision 2026-08-09: handle it automatically, but say so), and add the opt-out for a MariaDB user who deliberately stored lat/lon.
- **Integration matrix**: MariaDB→MySQL (the Rome case), MySQL→PostGIS (the SF case), MySQL→MySQL (must stay byte-identical), MariaDB→PostGIS (must stay verbatim — the cycle's existing control), each over the seven geometry families.

**Affected releases: NOT introduced by v0.118.x — byte-identical behaviour measured on v0.117.0, v0.118.0 and v0.118.1.** Treat the introducing tag as unbisected.

### ~~Bug 239 (MEDIUM, LOUD): a VStream source with any geometry column cannot `sync`~~ — CLOSED (verified 2026-08-18)

**CLOSED.** Fixed and pinned by `TestStreamer_Bug239Geometry_VStream_PostGIS_ColdStartAndCDC` (`internal/pipeline/streamer_geometry_vstream_postgis_integration_test.go`); the Vitess flavor `ExtGeometry` exclusion was corrected (`c13e27db`) and the 2026-08-10 SupportedTypes section records the config as "just fixed and pinned to make work". Original filing below.

`parse error - invalid geometry`, 0 rows, while `migrate` of the same table is rc=0 — so the capability exists and only the CDC path refuses. Loud and total, no data at risk. Filed against the VStream cell decoder; note that the Vitess flavor deliberately excludes `ir.ExtGeometry` from its declared `SupportedTypes` (`flavor.go`), so the first question is whether this is a missing capability refusal surfacing as a parse error, rather than a decoder defect.

### Bug 236 arm B, silent half: DOWNGRADED to LOW on measurement — the door the filing names is CLOSED, and has been; the residual is target-side tampering, not a geometry defect

**Re-measured 2026-08-10 on real servers before any fix was designed** (the Bug 238 discipline, applied to the filing rather than to a fix). The mechanism the filing describes is real — `prepareValue` stamps the SRID of the column descriptor it is handed — but its stated REACHABILITY is false in the direction that matters, and one of its two named doors does not exist.

**What was measured, and with what.**

- **The target can hold the SRID.** An undeclared MySQL 8.4 `POINT` column stores `ST_SRID` 4326 (`HEX(col)` = `E6100000 0101…`, prefix intact); an unconstrained PostGIS 3.4 `geometry` column stores 4326 likewise (`geometry_columns.srid` reports 0 for the COLUMN either way). So a re-stamp to 0 is avoidable loss rather than a target limitation — which rules a loud refusal OUT as the remedy: the right answer would be to carry the source's SRID, not to refuse a value the target holds fine.
- **Door 1, "a pre-existing target table": REFUSED, loudly, before any data moves.** Both `migrate` and `sync` cold start run `existingTablesGate` (`internal/pipeline/migrate_existing_tables.go`), which compares the source-derived intended column against the target's real one. Verbatim, from a real run: `pipeline: 1 pre-existing target table(s) differ from the schema this sync cold-start would create — refusing before any data moves … table "geo" (column "loc": want Geometry[Point,SRID=4326], NOT NULL, target has Geometry[Point], NOT NULL)`. Non-zero exit, target untouched. The filing did not know this gate existed.
- **sluice never CREATES the shape.** MySQL emits `SRID n` (`emitColumnDef`), MariaDB emits `REF_SYSTEM_ID=n` (`emitColumnType`), Postgres emits `geometry(<subtype>, <srid>)` unconditionally. Measured end to end: sluice's own cold start created `loc POINT NOT NULL SRID 4326`, and both the cold-copy row and a CDC row landed at `ST_SRID` 4326.
- **`migrate` was never the silent path at all.** Under a mutation that disables the gate, `migrate` onto an undeclared target lands `ST_SRID` **4326**, not 0 — bulk copy passes the SOURCE schema to the writer. Only the CDC applier passes the TARGET schema (`colTypesFor` reads the target's own catalog), so the re-stamp is exclusively a CDC-apply phenomenon. The filing's "or a pre-existing target table" conflates the two.
- **Door 2, "an `ALTER` between cold start and warm resume": REPRODUCES, and is the whole residual.** Cold-copy row landed 4326; then `ALTER TABLE geo MODIFY loc POINT NOT NULL` on the TARGET; then CDC rows landed **0** — same column holding both SRIDs, no `level=ERROR`, no `WARN`. (It needs a cold applier `colTypeCache` for that table: an ALTER after the cache is warm is masked until the next schema-boundary invalidation, which is a timing accident, not a protection.)

**Why no fix is landing for the residual.** The applier has nothing to compare against — an `ir.Change` carries values, not column types, and the reader already stripped the row's framing — so a refusal there is not constructible, and over-refusal is not the risk; impossibility is. Closing it properly means carrying the SRID end to end, i.e. the EWKB value-contract change `ir.CheckGeometryRowSRID`'s doc already defers as a chunk with its own family matrix. And the residual is not geometry-specific: **nothing re-validates any column property after cold start**, so a target-side type change, length shrink or charset change is equally undetected. Fixing the geometry instance would be fixing the instance, not the class.

**What landed instead — the gate the filing lacked.** The protection that closes door 1 was real but INCIDENTAL and UNPINNED: it works only because `ir.Geometry.String()` happens to render the SRID, and before this pass nothing in `internal/ir/diff`, the shape gate's tests, or the pipeline tests mentioned SRID at all. A rendering change made for unrelated reasons would have reopened a silent-value-alteration door with every test green.

- `TestTableColumnShape_GeometrySRIDDivergenceRefused` / `…_GeometrySRIDEqualAcceptsEveryFamily` (`internal/ir/diff`) — the SRID-difference verdict across every branch `ir.Geometry.String` can take: {geometry, geography} × 8 subtypes × {2D, Z, M, ZM} × {4326↔0, 0↔4326, 4326↔3857}, plus the equal control so the gate cannot pass by refusing everything.
- `TestGeometrySRIDTargetDivergence_RefusedBeforeAnyDataMoves` (`internal/pipeline`) — the comparison is CONSULTED before any data moves, at **both** entry points of the shared gate (`migrate` and `sync` cold start), with the sluice-created-target control proving it is not simply refusing spatial columns.

**Mutation-run in both directions.** Dropping the SRID from the render entirely fails both the unit matrix and both integration refusal cells. Dropping it only for `geography` fails 96 unit cells — **all of them in the geography half, none in geometry** — which is the family matrix paying for itself: the obvious single `POINT SRID 4326` representative is green under that mutant. Forcing the shape compare to always mismatch fails the equal-case control.

**Residual, filed rather than fixed:** a target-side schema change after cold start is not re-validated, for any column property. Named as a wart at `internal/engines/mysql/row_writer.go`'s geometry arm, with the measurement and the reason a refusal there cannot be built. Whoever picks up the EWKB value-contract chunk closes it as a side effect.

## 2026-08-10 — `ir.Capabilities.SupportedTypes` is a declared surface that enforces nothing

**Verified, not reported.** Every reference to `SupportedTypes` in the tree is either a DECLARATION (eight engines: mysql's three flavors + mariadb, postgres, pgtrigger, sqlite, d1-trigger, flatfile, mydumper) or a TEST assertion. `SupportedTypes.Has(` appears in exactly two files, both `_test.go`. **No production code reads it.** An engine's declaration that it does not handle an extension type has never refused anything.

The actual cross-engine enforcement is `migcore.CheckCrossEngineSupportable`, which is hand-coded, keyed on engine NAME strings (`isPGSourceEngine` / `IsMySQLFamilyEngine` — the same hand-kept list C-2 is about), and returns `nil` for every direction except PG→MySQL:

```go
pgToMySQL := isPGSourceEngine(sourceEngine) && IsMySQLFamilyEngine(targetEngine)
if !pgToMySQL { return nil }
```

So for every other pair the only backstop is the target emitter refusing a type it cannot render — which SQLite does do, loudly. That makes this **defence-in-depth missing plus a misleading declaration**, not a silent-loss class: the practical cost today is that the refusal arrives at emit time instead of preflight, and that a reader of `flavor.go` reasonably believes the declarations are load-bearing when they are inert.

### Do NOT simply wire it — the declarations are stale BECAUSE nothing enforces them

This is the trap, and it is the same shape as the C-11 revert on this branch: switching on a check whose inputs were never validated. The evidence is in the tree, admitted:

```go
//   - Spatial types are excluded from SupportedTypes here for
//     conservatism; flip the flag if a user reports they work.
```

That is the Vitess/PlanetScale flavor excluding `ir.ExtGeometry`. Wiring `SupportedTypes` as it stands would refuse geometry on a Vitess source — **the exact configuration Bug 239 was just fixed and pinned to make work** (`TestStreamer_Bug239Geometry_VStream_PostGIS_ColdStartAndCDC`, real vtgate → real PostGIS, cold start + CDC, seven WKB families). A capability audit that took the declarations at face value would have undone a shipped fix.

### The order of work, therefore

1. **Audit every declaration against measured behaviour**, engine by engine, before any of them gates anything. Each entry ends as either "measured true, here is the test" or "measured false, corrected". The Vitess `ExtGeometry` exclusion is the first known-false one and its correction is already evidenced — it needs one confirming run of the Bug 239 VStream test, which is on the weekly `vstream-pipeline` leg rather than the per-PR shard.
2. **Then** consult it in a preflight covering ALL directions, which generalises `CheckCrossEngineSupportable`'s one hand-coded direction and removes one of C-2's two name-keyed predicates as a side effect.
3. **Gate it**: a declaration must be bound to a test that fails if the engine's real behaviour diverges, or the field returns to being decoration the moment someone adds an engine.

**Not filed as a defect with a severity, deliberately.** Nothing is lost and nothing is silently wrong today; what is wrong is that a whole declarative surface reads as enforcement. Sizing it honestly: step 1 is the expensive part and is a measurement chunk, not a coding one.

### The `SupportedTypes` declaration audit (2026-08-10)

Step 1 of the plan above, done. Verdicts below are from the code (type constructors, DDL emitters, capability pins), not from the comments beside the declarations.

**The finding that frames the rest: three of the eleven extension kinds are declared by ZERO engines.** `ExtDomain`, `ExtVerbatimType` and `ExtExtensionType` appear in no engine's `SupportedTypes`. All three are LATER additions to the `ExtensionKind` enum (their own doc comments date them — domains at v0.95.1, verbatim types at ADR-0047, catalogued extension types at ADR-0032). The enum grew; no declaration was revisited. That is exactly what an unenforced declarative surface looks like after two years, and it means the rot is systemic rather than one stale entry.

| engine | declares | verdict |
|---|---|---|
| `mysql` (vanilla) | Enum, Set, Geometry | **consistent.** All three are core MySQL. Correctly omits UUID/Inet (MySQL has no native type — the BINARY(16) convention is not one) and Array/Cidr/Macaddr/Domain (absent from MySQL entirely). |
| `planetscale` / `vitess` | Enum, Set, Geometry | **CORRECTED 2026-08-10** (`c13e27db`). Geometry was excluded as self-described conservatism; measured working end to end over a real vtgate. |
| `mariadb` | Enum, Set, Geometry, UUID, Inet | **plausible, unmeasured.** UUID (10.7+) and INET6/INET4 (10.10+) are genuinely native and ADR-0170/0171 implement them; correctly omits Cidr/Macaddr. No end-to-end capability measurement was made for this audit. |
| `postgres` | Enum, UUID, Array, Inet, Cidr, Macaddr | **WRONG for `ExtDomain`, ambiguous for three others.** `CREATE DOMAIN` is core PostgreSQL, and the engine both READS it (`schema_reader.go` builds `ir.Domain`) and EMITS it (`ddl_emit.go`'s `emitCreateDomainType`) — there is no reading under which PG does not handle it natively. Geometry, ExtensionType and VerbatimType are the ambiguous three; see below. |
| `pgtrigger` | same six as `postgres` | **inherits every one of the above**, by design — its comment says "same set as vanilla PG". |
| `mydumper` | Enum, Set, Geometry | **plausible.** A MySQL dump format; the set matches vanilla MySQL minus what dumps cannot carry. |
| `sqlite`, `d1-trigger`, `flatfile` | *(empty)* | **plausible.** SQLite has no native enum/set/uuid/array/geometry/inet, and the engine refuses an `ir.Domain` column loudly at emit rather than silently flattening it. |

### The blocker: "handles natively" is underspecified, and the answer changes the wiring

PostGIS `geometry` on a PG target requires an installed EXTENSION. `ExtExtensionType` is by definition operator-opted-in (`--enable-pg-extension`, ADR-0032). So PG's omission of them is defensible under *"declares what the engine supports unconditionally"* and wrong under *"declares what the engine can handle when the relevant extension is present"*.

Both readings are coherent; the field's doc — "the extension types the engine handles natively" — chooses neither. **That ambiguity must be resolved before anything consults the field, because the wiring silently encodes whichever reading the implementer happened to hold.** Under the first reading a preflight would refuse PostGIS geometry on a PG target, which is a shipping, tested configuration. Under the second, an engine cannot declare its capabilities statically at all and the field needs to become a method taking a live connection.

`ExtDomain` is unaffected by the ambiguity and is simply missing. It is left uncorrected here on purpose: adding one kind to two engines while the semantics are undecided would make the surface look audited without making it enforceable, and it is the enforceability that has been absent the whole time.

**Recommendation:** decide the semantics first (a method over a static field is the likely answer, given PostGIS), then re-derive all eight declarations against it, then wire and gate. Sizing: the semantics decision is small and the re-derivation is a measurement chunk; the wiring is trivial once both are settled.

## 2026-08-10 — perf-parity arrears from v0.117.0 (found by the pre-v0.119.0 checker)

The pre-release checker confirmed the trigger was a **false positive on the v0.118.1..v0.119.0 delta** — no throughput or latency technique in it — but found real arrears one release back. Filed rather than fixed in the v0.119.0 commit because it is v0.117.0's missing work and the matrix edit is substantial; nothing here blocks a tag.

### ~~HIGH~~ — item 157's `GrowGateEvidenceFreeHoldCap` has no matrix cell, and row 17 currently reads as though item 138 already solved the field shape

**RESOLVED (2026-08-11, docs-only commit).** Row 17 gained the item-157 cell with the measured figures (verified against `sluice-testing/session-reports/v0.117.0.md` before writing, including the report's own explanation of why ~11% under-predicted — fleet-wide severs open one window per tripped lane), the item-138 block no longer closes on the field log as though the ceiling addressed it, the mode-dependent-trigger parity note is written into the cell, and the roadmap's projection paragraph carries the measured annotation. Gap 19's price raised in place rather than filing a duplicate.

`GrowGateEvidenceFreeHoldCap = 250ms` (`internal/pipeline/migcore/grow_gate.go:238`, applied `:696-697`) shipped in **v0.117.0** and never reached `docs/dev/perf-parity-matrix.md`. Row 17 documents item 138's three bounds and closes on the field log — "246 windows … 73 minutes of quiesce in a 91-minute run" — as the shape the run-level ceiling addresses. Item 157's own analysis is that the ceiling did **not** fix it: "half the wall clock spent in 30-SECOND units quantises a lane to one chunk per hold." So the row is not merely incomplete, it implies a solved problem that item 157 exists because it was not solved.

The cell must carry the MEASURED figure, not the projection: v0.116.1 died at 105 s having landed 309,212 of 1.8M rows; v0.117.0 completed all 1,800,000 in 95 s — **14,173 rows/s vs 2,379, 5.96×**, 86% of an ungated reference, gate duty **92.6% → 36.4%**. The roadmap entry still carries the superseded ~11% arithmetic projection.

**Parity itself is fine — the gap is the matrix.** The cap lives in the shared `migcore` primitive, so all five `NewGrowGate` sites inherit it identically (`migrate_phases.go:647`, `backup/restore.go:632`, `streamer_coldstart.go:1099`, `streamer_coldstart_parallel.go:310`, `streamer_multidb.go:808`) and both engines classify grow evidence. No code gap, no new ranked gap.

**One parity note nobody has written down**, and it belongs in the cell: the cap keys on `episodeSawGrowEvidence`, and `ir.GrowEvidenceTelemetry` has exactly one producer (`streamer_telemetry.go:201`). Migrate and restore construct `NewGrowGate(ctx, nil)` with no telemetry seam, so on those modes an episode can only earn evidence from a target-face error classification — making the 250 ms cap **strictly more likely to bind on migrate/restore than on cold start**. Same technique, mode-dependent trigger condition, conservative direction.

### ~~MEDIUM~~ — ~22 stale `file:line` citations, drifted in DIFFERENT directions

**RESOLVED (2026-08-11, same commit as the HIGH above).** Every citation in the filing was re-verified by symbol against the current tree before editing (all held except two of the filing's own examples: `raw_copy.go:217` and `cdc_vstream.go:729` appear nowhere in the live matrix — the matrix carries `:174/:234/:243` for raw_copy, which verify, and the `:729` was the BARE `(:729)` beside the `:2334` citation, now `:723`). The matrix's row 3, row 8, row 17, gap 19 and the roadmap item-157 entry now carry the re-derived values; the full list is in the matrix header's 2026-08-11 note.

Original filing, kept for traceability: re-derived by symbol lookup rather than offset, which matters: the amounts differ, so a uniform shift would encode the error (the matrix's own 2026-08-07 lesson). Notable: `mysql/cdc_vstream.go:2334`→`:2507` (this delta shifted it +43 on top of pre-existing drift), `:729`→`:723`, `:399`→`:428`; `pg/row_writer.go` `:478/:534/:580/:874/:881`→`+4` each; row 4's `raw_copy.go:217` lands on `return nil` (the two real sites are `:195`/`:240`); every `NewGrowGate` site and `migcore/grow_gate.go:225,226,410`→`:304,:305,:501` — all item 157's commit, which the matrix missed. The roadmap's item 157 entry carries the same stale set. Roughly 25 other citations were spot-checked and hold, so the sweep is not vacuous.

## 2026-08-13 — the burn-down arc's closing verdicts on the four remaining audit MEDIUMs (all parked, each with a recommendation) — UPDATE 2026-08-14: three of the four are now DISPOSITIONED

Status update (2026-08-14, shipping v0.126.0): **PROG-LAG-1 SHIPPED `231eab4d`** — the operator approved option (a), the latch-semantics change this filing recommended AGAINST (idle → `(frozenLag, known=false)`, distinguishing nothing at the seam and letting the alerter's unobserved-skip hold the latch), superseding the `--notify-stale-apply-seconds` recommendation below; **DIFF-2 CLOSED `8e51c1f5`** (the measurement ran; see its own entry above); **TESTCI-1 CLOSED 2026-08-14** (auto-issue job + publish-checklist line + corpus triage, see the extended-suites commits). PROG-BAR-1 was fixed separately (`2c5f7de7`, v0.125.0 era). The paragraph below is the filing as written, kept for the record.

The 2026-08-11 audit's autonomously-eligible queue is exhausted (ARCH-1, SPAT-2/3/4, BRK-1, VST-1, DIFF-1/PRF-2/SQT/COLD/CDCPOS shipped earlier, C-11 residuals, D-1/2/3, C1-1 on both PG and SQLite lanes, PROG-NOTIFY-1). Four MEDIUMs remain, each needing a decision — full memo in `workspace/queue-burn-memo-remaining-mediums.md`, verdicts here as the durable copy: **PROG-LAG-1** (the sync-lag latch re-arming on a wedged stream is a deliberate pinned design; recommendation: a new `--notify-stale-apply-seconds` + help-text disclosure rather than changing the latch semantics); **PROG-BAR-1** (chunked-parallel per-table bar over-counts — cosmetic, needs per-chunk attribution re-plumbing; fold into the next progress chunk); **DIFF-2** (extending the Bug 241 fold needs a bounded rendering-measurement pass first — the measurement scope is the decision); **TESTCI-1** (weekly extended-suites red needs the publish-checklist line — a CLAUDE.md working-agreement edit, the operator's document — plus a mechanical auto-issue step, and the corpus red `0a33fd43`-vs-test drift deserves its own triage).

## ~~2026-08-13 — C1-1 closed on the PG lane; the SQLite/D1 emitted-DDL siblings are OPEN (LOW-MEDIUM, same class, different drivers)~~ — SQLite + D1 CLOSED (verified 2026-08-18)

**CLOSED.** The single-statement door ported to the SQLite lane: `sqlite/ddl_single_statement.go`'s `execEmittedDDL`/`assertSingleDDLStatement` is wired at all four sqlite `schema_writer.go` emit sites AND the **D1 staging path** (`stage_d1.go:83,110` — so the D1 `/query` transport's DDL goes through the same door), enforced by the anti-drift roster `TestSQLiteEmittedDDLGoesThroughTheSingleStatementDoor` (a direct `ExecContext` of recorded content outside the exempt files fails the build) + `TestSQLiteInjection_RefusedBeforeExec`. Original enumeration below.

C1-1's single-statement door now guards every emitted-DDL ExecContext in the postgres schema-writing surface (`execEmittedDDL` + the AST roster `TestSchemaWriterEmittedDDLGoesThroughTheSingleStatementDoor`). The sibling enumeration, per the class rule: **MySQL-family targets are structurally immune, now ENFORCED not merely assumed** — go-sql-driver refuses multi-statement Exec unless the DSN sets `multiStatements=true`, and the original claim rested on sluice "never setting it" (but the operator's DSN can carry it and `cfg.Clone()` preserved it — the audit H-3 gap, 2026-08-15). `openDB` now force-clears it via `hardenAgainstMultiStatement` on every connection sluice opens, so the driver itself is the door regardless of the DSN; pinned by `TestHardenAgainstMultiStatement` (cfg mutation) + `TestOpenDB_RefusesMultiStatementEvenWhenDSNAsksForIt` (a live `multiStatements=true` DSN's injection refused, canary intact, with an anti-vacuity control proving a raw connection DOES execute it). If a future perf change ever wants multi-statement, the door pattern must come with it. **SQLite is a REAL open sibling**: modernc's driver executes every statement in an Exec string, and the sqlite/d1-trigger schema writers emit DDL that can inline recorded expression bodies (CHECK constraints ride `emitCheckConstraint` verbatim — ADR-0134 §3's loud-at-CREATE contract covers PARSE failures, not a body that parses as two statements). The same `assertSingleDDLStatement` shape ports (SQLite quoting: no dollar quotes, `''` doubling, `[]`/backtick idents the emitters don't produce), wired at the sqlite writer's exec seam with its own roster. **D1's `/query` HTTP endpoint** accepts multi-statement SQL too (it splits server-side) — the same door belongs on the d1 transport's DDL sends; whether a tampered body can reach it depends on the same manifest-restore path. Not bundled into the C1-1 commit because the sqlite writer's exec-seam inventory is its own sweep; this filing is the enumeration.

## 2026-08-13 — ARCH-2 (shared CDC-lane SRID resolver): ground-truthed, NOT a mechanical extraction; recommendation parked for the operator

The audit's "copy-paste twins" framing doesn't survive a read: the two resolvers share only the three-way POLICY (declared→stamp / absent→unknown / unreadable→disable-the-guard-never-wedge) while the substance diverges per engine — PG merges two views and treats an absent row as unknown; MySQL tries two catalog spellings first-wins and maps NULL `srs_id` to 0 *by meaning* (NULL is MySQL's spelling of "undeclared"); failure surfacing and container mutation disciplines differ too. Extracting a shared helper needs a neutral home (engines cannot import each other) and would parameterize exactly the divergences that were the Bug 236/240 substance. Full memo with options: `workspace/queue-burn-memo-arch2-srid-helper.md` (gitignored — this paragraph is the durable copy). **Recommendation: a policy-parity pin, not a refactor** — derive both lanes' unit pins from one shared case table (declared / absent / NULL / unreadable / geography) so a future policy fix landing in one lane fails the other lane's twin by name; extract the abstraction only if a third CDC lane ever needs SRID resolution. Parked because choosing refactor-vs-pin is an architecture call.

## 2026-08-13 — Bug 245 closed on the hint+docs direction; reader-side re-resolve/skip is the recorded, operator-gated alternative

Bug 245 (v0.122.0 cycle: the SQT-1 halt's printed remedy — "repair the value at the source" — cannot unblock the trigger-CDC lane, because the poison also lives in the captured `sluice_change_log` images and the repairing UPDATE captures a new poisoned before-image) is closed by making the refusal print the truth: a typed `sqlite.UnrepresentableTextError` lets the trigger lane's wrap sites (local `decodeImage` + the d1 transport's image arms) append the runnable recovery (`trigger teardown` + repair + `trigger setup` + fresh sync; byte-level image scrub as the advanced alternative), documented in `docs/operator/cdc-streaming.md` and pinned behaviourally by `TestCapture_InvalidUTF8_RepairAloneRehaltsUntilTeardown`, which drives the re-halt AND the printed recovery end-to-end. The filing's other direction — the reader re-resolving a repaired event from the base row, or skip-with-audit — was deferred, not rejected: skip semantics on a value-fidelity lane need the operator (the C-11 precedent: drop-with-durable-counter was an explicit operator decision), and re-resolve-from-base-row changes the delivered image's meaning (the base row's CURRENT value is not the value the change carried — fine for the final converged state, wrong mid-history for an audit consumer). If the wedge recurs in the field enough to matter, that design question goes to the operator with this paragraph as the starting point; the behavioural pin's claim (1) is the test that flips when it lands.

## 2026-08-13 — SPAT-4 closed by REFUSAL; the ST_AsEWKB carry is the recorded, deliberately-deferred alternative (capability, demand-gated)

SPAT-4 (audit 2026-08-11, HIGH-loud: pgtrigger setup + cold copy succeeded on a spatial table and the FIRST spatial DML wedged the stream mid-incident) is closed by a §14 setup refusal (`postgis-spatial-column`, detection at every wrapping: direct / array element / domain base / domain-over-array, pinned on real PostGIS + a plain-PG stand-in leg). The audit preferred an ST_AsEWKB capture that would CARRY spatial columns; that was deferred, not rejected, because it is not a two-sentence change: (1) the capture function is ONE shared SECURITY DEFINER function per schema with `SET search_path = pg_catalog, pg_temp`, so `ST_AsEWKB` does not resolve — the PostGIS schema must be discovered at setup time and baked in schema-qualified, and PostGIS is relocatable; (2) the function is table-generic, so the spatial column LIST must be baked per-table into TG_ARGV (like the N-16 PK list) and the jsonb image surgically rewritten per column via dynamic row-field access (`EXECUTE 'SELECT ($1).'||quote_ident(col) USING NEW`); (3) geography needs its own cast leg; (4) the decode side must route hex through `decodePGGeometry` and the C-14 SRID guard, and the pin owes the full {7 WKB families} × {geometry, geography} × {3 capture payload modes} × {I/U/D} matrix on real PostGIS. All tractable — roughly an M chunk — but it is a new CAPABILITY for a lane whose target audience (managed PG without replication slots) does overlap PostGIS-by-default hosts (Supabase), so it should be a deliberate roadmap item when demand shows up, not a queue-burn side effect. The refusal's Hint names the runnable alternatives (`postgres` engine carries EWKB faithfully; `--exclude-table`).

## 2026-08-13 — ARCH-1 residue: CHECK operators PostgreSQL parses with DIFFERENT semantics (LOW-MEDIUM, constraint-strictness shift, not row loss) — **`^` + `/` half CLOSED 2026-08-13 (operator-approved option (b))**: SeveritySilent advisory entries in `gapPatterns` via an advisory-only literal-aware symbol scan (deliberately NOT the refusal scanner — PG parses these, so refusing was option (c), rejected and now PINNED by `TestSemanticDivergenceOperators_NeverRefuse` on both gates); notes remedy-free per the Bug 247 convention; mutation-run both directions (severity flip caught by both pins, matcher blind caught by the detect pin). **The jsonb-path arm stays OPEN pending the nested/array-path measurement** (one CHECK per path shape on real MySQL, read back through the operator matrix — do not advise or refuse before measuring).

Filed from the ARCH-1 operator-family measurement (the matrix is `TestCheckOperatorFamilyMatrix_*`; its file doc points here). The matrix's acceptance criterion is deliberately parse-level — "translator-rewritten, PG-accepted, or gate-refused" — and three measured renderings land as opApplies while meaning something different on the target: **`^`** (MySQL bitwise XOR; PG numeric power), **integer `/`** (MySQL keeps fractions, `3/2 = 1.5`; PG truncates, `3/2 = 1`), and the **jsonb `->`/`->>` path arguments** the JSON_EXTRACT rewrite produces (MySQL took a JSON path; jsonb takes a key — single-key `$.x` paths rewrite correctly, which is what the matrix's op_arrow cells exercise; the question is nested/array paths). Consequence class: the CHECK applies with shifted strictness — it can reject valid rows during copy (loud, confusing) or silently under-constrain the target; it cannot corrupt row data, which is why this is not in the silent-loss queue. Options when picked up: (a) translate — `^`→`#` is exact for the integer case, but `/` would need a `::numeric` cast auto-injected into the operand (PG truncates int/int where MySQL keeps the fraction), which is too invasive for a CHECK nobody asked to rewrite; (b) a SeveritySilent advisory entry per operator in `gapPatterns` (the existing preview machinery, zero refusal risk — likely the right first move); (c) refuse like the parse-level arms (wrong: these DO apply, and `^` on numeric operands is PG-legitimate). Also pinned by the matrix rather than fixed: both flavors render a bare `!a` as a chained comparison whose MariaDB form (`a = 0 = 0`) is a 42601 parse death the token scan cannot refuse without a real parser — documented at the phase-4 pin, loud before any row moves.

## ~~2026-08-12 — C-11 skip-ledger residue from the pre-v0.123.0 value-fidelity pass (LOW, four small follow-ups, none silent)~~ — CLOSED 2026-08-13 (queue-burn; heading restored in the same pass, an earlier edit had replaced it with the ARCH-1 section above instead of inserting)

The review graded the ledger byte-exact on both real engines and SAFE TO TAG; the named residuals were: (1) the MariaDB `VALUES()` upsert-spelling arm of `upsertSkippedTable` had no real-server pin — **closed** by `TestChangeApplier_SkippedTable_MariaDBValuesSpelling`, which drives the FULL applier (flavor selection included, the Bug 180 lesson) on real mariadb:11.4 through the insert arm AND the ON DUPLICATE increment arm where the `VALUES()` spelling actually lives; (2) no >64 KB position-token round-trip pin — **closed** by `TestChangeApplier_SkippedTable_PositionTokenBeyond64KB`, a VGTID-shaped ~96 KB token through the full applier, byte-exact through the ledger's LONGTEXT columns; (3) PAD-SPACE fold (a PG `"foo "`/`"foo"` pair count-merging on a MySQL target) — **no change**: it is the deliberate, already-documented named wart at the `controlIdentifierCollation` block, and closing it needs a portable binary NO-PAD collation that does not exist across supported servers, exactly as that comment records; (4) privilege-revoked ≡ absent, wrong `add-table` remedy — **closed**: `docs/operator/cdc-streaming.md`'s skip-remedy paragraph now distinguishes the revoked-grant cause (restore the grant; the skip clears on the next change, no re-snapshot).

## ~~2026-08-12 — SQT-1 residue: the D1-verbatim premise is UNVERIFIED (MEDIUM, premise pin owed)~~ — MEASURED AND CLOSED 2026-08-13 (queue-burn): the MANGLE branch holds, on BOTH D1 lanes

Measured on real D1 (throwaway database, created and deleted in the run): storage holds invalid-UTF-8 TEXT intact (`hex(x)` = the original bytes), the `/query` JSON response replaces every invalid byte with U+FFFD **server-side**, and the trigger-lane capture (`json_object('v', CAST(x AS TEXT))`) stores the raw bytes verbatim only for the read-back to mangle identically — so the client guards cannot fire on D1 for this vector and there is no client-side independent expected value, exactly the filing's second branch. Pinned by the new **`d1verify` live-credential suite** (`TestD1Verify_InvalidUTF8TextIsMangledServerSide`, mirrors psverify/kmsverify: skip-clean without `CLOUDFLARE_API_TOKEN`/`CLOUDFLARE_ACCOUNT_ID`, run manually), which asserts all three facts with an anti-vacuity floor (the hex() check proves the poison landed) and drives `jsonString` itself to prove the guard stays silent. `d1_decode.go`'s UNVERIFIED PREMISE comment is now MEASURED PREMISE naming the pin; the operator caveat lives in `docs/operator/sqlite-d1-import.md` (unrescuable as TEXT through the API; `hex(x)` server-side exports the true bytes). **Residual observation, deliberately NOT built (design-gated, demand-gated):** the mangle IS detectable in principle — `hex(x)`/`length(x)` are independent expected values available server-side per row — but wiring a per-value hex probe into the D1 projection is a fidelity-vs-cost design call (always-hex doubles transfer; a length probe has its own TEXT-semantics subtleties) for a vector no user has reported. If demand shows up, the projection seam is `CapturedValueExpr`/`buildD1Projection`.

The SQT-1 guards (v0.122.0: `jsonString`/`jsonScalarString`/`JSONStringValue`, plus `d1CellString` after the pre-tag value-fidelity review caught the one-layer-up sibling on the d1-trigger transport) refuse invalid-UTF-8 and lone-surrogate mangles at every client-side JSON string extraction. What nothing has measured: **whether Cloudflare's D1 `/query` API returns invalid-UTF-8 TEXT bytes verbatim in its JSON response.** The dichotomy is stated at `internal/engines/sqlite/d1_decode.go` (marked UNVERIFIED PREMISE): if verbatim, the client guards are the load-bearing refusal on the D1 lanes; if D1 mangles server-side (V8 strings cannot hold invalid UTF-8), the loss happens upstream of any client guard and the D1 lanes have no independent expected value client-side — that branch would deserve an operator-docs note that D1 TEXT with invalid UTF-8 is unrescuable through the API, not just a refusal. **The pin:** psverify-style against real D1 (token at `C:\cloudflare\cloudflare.env`) — write `CAST(x'FFFE61' AS TEXT)` through the API, read it back raw, byte-compare; one arm through `/query` and one through the d1-trigger change-log poll. Sub-30-line test, needs live credentials, so it belongs in the psverify-tagged suite, not CI.

## ~~2026-08-13 — the Bug 244 sibling's OWN sibling: `CheckCrossEngineSupportable` is filter-blind on both restore paths (MEDIUM, loud)~~ — CLOSED (verified 2026-08-18)

**CLOSED.** Both `CheckCrossEngineSupportable` call sites now receive the filtered view: `restore.go:610` passes `filteredSchemaView(manifest.Schema, r.Filter)` and `chain_restore.go:226` passes `filteredSchemaView(root.Manifest.Schema, r.Filter)` — the same helper the emit-preflight door block uses, so an excluded PG-only construct no longer defeats a cross-engine restore that excludes it. Original filing below.

`restore.go` ~:603 and `chain_restore.go` ~:296 grade the UNFILTERED schema/deltas through the cross-engine supportability gate, so a PG-only construct (PostGIS geometry, EXCLUDE constraint) in an *excluded* table still refuses a cross-engine restore that excludes it — the same filter-blind-door-defeats-its-own-remedy shape `c943d7e7` closed for the emit preflights, sitting ~15 lines below that door in the same function. The `c943d7e7` enumeration scoped itself to the two door blocks and never dispositioned this gate; its comment's "BEFORE the table filter for path-consistency with the chain" rationale cites a twin that just became filter-aware. **Fix sketch:** hand it the same `filteredSchemaView` the door block now uses (both paths, one commit, per-surface pin mirroring `restore_preflight_filter_test.go`). Loud in every case; no silent loss.

## ~~DIFF-2 — the Bug 241 server-rendering fold covers exactly two measured spellings (MEDIUM)~~ — CLOSED 2026-08-14 (queue-burn): the bounded measurement pass ran, and the fold set now covers every measured portable family

The measurement (the memo's recommended shape, operator-approved): a 30-family portable CHECK corpus — one predicate per function/operator family, identical text — applied to a real MySQL 8 and PostgreSQL 16 and diffed through the shipped Differ, plus the 3 translator families through a real per-table filtered migrate. Result: **17 clean, 13 portable phantom-drift families, 3 translator pairs**, every mismatch's raw rendering pair captured verbatim. The 13 decompose into 8 mechanical folds, all now in `canonicalCheckExpr`: `substring`→`substr` and `ceiling`→`ceil` renames; PG's `TRIM(BOTH FROM x)`; MySQL's `round(x,0)` default-scale materialization; MySQL's `mod(a,b)`→`(a % b)` rewrite; PG's quoted-negative-literal decoration (`'-100000'::integer`, numeric-literal unquoting); PG's `~~`/`!~~` LIKE spellings plus NOT-pushdown of a single comparison (MySQL's catalog SIMPLIFIES `NOT (v = 5)` to `v <> 5`); and BETWEEN→its `>=`/`<=` expansion for simple operands (PG's own DDL-time rewrite). Pins: the measured pairs verbatim in the unit fold matrix (13 clean directions + 13 guard/window directions incl. NOT-over-conjunction, NOT BETWEEN, compound-operand, ILIKE `~~*`, non-zero round scale); the e2e half is a 12-constraint `folded` table riding `TestSchemaDiffAfterMigrate_MySQLToPostgres` (real migrate → clean diff → two tamper arms). Four mutants, both directions (3 fold-blinded → clean pins red; 1 over-fold `!~~`→`like` → the guard red). Deliberately NOT folded, filed instead (below): the translator pairs. Direction scope: measured MySQL↔PG both renderings; the corpus gate rides the MySQL→PG migrate direction only (PG→MySQL cannot apply PG-rendered LIKE/`~~` spellings — a loud apply-time axis, not a diff axis).

## 2026-08-14 — the CHECK-function overload/semantics matrix, MEASURED (the Bug 252 residual): one silent cell FIXED, one over-refusal FIXED, four loud gaps FILED

Measured through the real pipeline (per-table migrates, MySQL 8 → PG 16), function × {DOUBLE, DECIMAL}: **the one SILENT cell** — single-arg `log(x)` LANDS on both types and enforces a DIFFERENT predicate (MySQL log = natural, PG log = base-10) — **FIXED**: `rewriteLOGNatural` renames it to `ln()` (semantics-exact; PG ln = MySQL log), pinned unit + through the migrate-then-diff corpus. **One over-refusal FIXED**: MySQL's catalog canonicalizes `POWER(x,y)` to `pow(x,y)`, and `pow` was missing from `pgValidFunctions` — a measured-valid CHECK preflight-refused; `pow` added (PG core alias). **Agreeing cells, no action**: `log10`, `ln`, two-arg `log(b,x)` on DECIMAL, `round(x,2)`/`mod` on DECIMAL. **Correctly-loud cells, no action**: `truncate` and `log2` preflight-refused (no PG spelling; `truncate`→`trunc` would fix DECIMAL only — parked). **Loud overload gaps, FILED as the remaining class** (all die 42883 at CREATE on a DOUBLE column while `schema preview` stays silent — the preview-false-clean face): `round(x, 2)`, `mod(x, n)` (rendered as `%`), `format(x, n)`, `log(b, x)`. Remedy options per cell: a `::numeric` cast-rewrite (changes type arithmetic — needs a per-family value measurement before folding), a preview advisory (the ARCH-1 machinery), or overload-aware preflight. Design-gated; the measured matrix is the input.

## ~~2026-08-14 — DIFF-2 residue: the three TRANSLATED CHECK families phantom-drift in `schema diff` after a MySQL→PG migrate (LOW-MEDIUM, loud-ish noise, needs a design call)~~ — CLOSED 2026-08-14 (operator-approved): the recommended shape shipped — the diff renders the expected side through the target writer's OWN translator

Implemented as filed: `ir.CheckExprDialectTranslator` (an optional SchemaWriter surface; the PG writer delegates to `translateExprForPG` with zero ExprContext, mysql-dialect only, best-effort never-gating), threaded via `irdiff.Options.TranslateExpectedCheckExpr` into `diffChecks`' name-matched path (emitted-match path deliberately untouched — its expected side is already target-spelled; a writer-less degraded diff stays nil = the historical phantom, stated). One mapping, two consumers. Pins: the four-arm unit hook test (clean pair / genuinely-different-still-reports / foreign-dialect-declines / nil-preserves-today), the writer-surface pin, and a `translated` corpus table (json_extract, json_unquote, date_format, log) riding the migrate-then-diff gate live with a changed-JSON-path tamper arm. Mutants both directions: wiring blinded → all four cells phantom-report (the filing verbatim); dialect gate dropped → the foreign-dialect pin red.

Measured through a real migrate: `json_extract(j,'$.k')` lands (correctly) as `(j -> 'k')`, `json_unquote(json_extract(…))` as `(j ->> 'k')`, `date_format(dt,'%Y')` as `to_char(dt,'YYYY')` — and `schema diff` then reports each as a CHECK mismatch on a target migrate itself created, because the expected side is MySQL's rendering of the SOURCE spelling. Folding these in `canonicalCheckExpr` would mean reimplementing the translator (JSON-path decomposition, the DATE_FORMAT→TO_CHAR format-string map) inside the canonicalizer — two implementations of one mapping that would drift. **Recommended shape instead:** when the diff pair is cross-engine, run the expected-side expression through the SAME `translateCheckExprMySQLToPG` the writer used before canonical comparison — one mapping, both consumers. Needs a decision on where the dialect context threads into `diffChecks` (the differ knows both engines; `ir.CheckConstraint.ExprDialect` already exists). Until then the operator-visible cost is a phantom CHECK mismatch per translated predicate — annoying, loud, never silent.

## ~~2026-08-13 — d1verify sibling candidate: does `wrangler d1 export` preserve invalid-UTF-8 TEXT? (LOW, one measurement)~~ — MEASURED 2026-08-14 (queue-burn): it does NOT; the caveat stands, now with the export half stated and pinned

Measured on real D1: storage holds the raw bytes intact (`hex(x)` = `FFFE61`), but the `/export` dump renders the value as U+FFFD replacement characters (`EF BF BD` per invalid byte) — the export generator mangles exactly like `/query`, so export is NOT a rescuable extraction path for this vector (it remains one for BLOBs, which the dump renders as `x'…'` literals). `sqlite-d1-import.md`'s caveat gained the export sentence, and `TestD1Verify_ExportDumpManglesInvalidUTF8Text` pins the fact by driving the real `/export` polling protocol against a poisoned throwaway DB — including the tiny-DB eviction quirk ("Not currently exporting anything." between polls, handled by restart). `hex(x)` stays the only faithful readout.

Filed by the pre-v0.124.2 docs-drift check. The export dump is generated server-side; if it preserves the bytes, the file-backed guard refuses loudly on the exported file — making **export the rescuable extraction path** for D1 TEXT with invalid UTF-8, worth one sentence in `sqlite-d1-import.md`'s caveat. If it mangles like `/query`, the caveat stands as written. One d1verify-style pin (export a poisoned throwaway DB, byte-inspect the dump) decides it; do not write the doc sentence before measuring.

## ~~2026-08-11 — Bug 244 sibling, derived during the fix and deliberately not fixed with it: the emit preflights are filter-blind on BOTH restore paths (LOW-MEDIUM, loud)~~ — CLOSED 2026-08-13 (queue-burn): both doors grade a shallow filtered view (never mutating the chain's root schema), the name-fold preflight grades the KEPT set only, and the sweep found a SIXTH gate the filing had not named — the ADR-0047 verbatim-extension refusal in the same block, filter-blind the same way, closed with the rest. Per-surface recording pins on both doors × three filter modes; three mutants caught on the exact surface mutated.

Filed from the Bug 244 door enumeration (the fix commit lists all seven chain doors). `Restore.refuseUnrepresentableTargetShape` and its `ChainRestore` twin run `PreflightTableEmit` / `PreflightIndexEmit` / `PreflightViewEmit` / `PreflightColumnTypeEmit` / `PreflightTableNameFold` on the UNFILTERED manifest schema — only the Bug 243 malformed-schema door is filter-aware. So a backup whose EXCLUDED table carries an unrepresentable index/column/name refuses the whole restore even though `--exclude-table` was the operator's route around exactly that table — the same "filter-blind door defeats the documented salvage" shape as Bug 244, one phase earlier and loud rather than mid-run. **Fix sketch:** hand the preflights the filtered view (a fresh `ir.Schema` built by the same allow-loop the Bug 243 door uses — `filteredSchemaLexProblems` shows the shape); `PreflightTableNameFold` must grade the KEPT set only (a fold collision with an excluded table is not a collision on the target). Both paths in one commit, per the sibling-sweep rule, plus the include-mode direction in the pin. Derived by code-reading, not measured; loud in every case; ranked below the silent queue.

## ~~2026-08-11 — Bug 243 follow-ups from the pre-v0.120.1 review (LOW)~~ — BOTH CLOSED on branch 2026-08-11 (shipping v0.120.2)

Two non-blocking notes from the value-fidelity pass, filed rather than implied: (1) `backup compact` / `backup prune` carry a mangled recorded schema forward WITHOUT the courtesy WARN `backup incremental` now emits — **CLOSED (`fede0b85`)**: one shared helper (`warnIfChainRecordedSchemaMalformed`) wired into both, scanning every restorable segment's full + incremental manifests, WARN-once naming the code; pinned through the real `CompactChain`/`PruneChain` with mutation-run wiring pins, coverage boundaries (incremental deltas seen, retired segments unscanned) pinned at the helper. (2) The `RefuseOnLoudGaps` infix rendering (`REGEXP()` call-parens on an operator, no MariaDB provenance) — **CLOSED (`fede0b85`)**: `Gap.Infix` threads from the pattern registry, the renderer says "the infix REGEXP operator", both infix notes name the MariaDB catalog spelling, and `TestRefuseOnLoudGaps_InfixRegexpRendering` pins the message operators actually SEE — which is also what makes v0.120.1's corrected provenance claim true going forward.

## ~~2026-08-11 — Bug 243 residue: an old chain's backslash-doubled recorded expression restores SILENTLY WRONG on a PostgreSQL target~~ — CLOSED 2026-08-13 (queue-burn), and the filing's premise was STALE: the arm is target-independent

Filed while closing Bug 243's structural half (the unterminated-literal detector + its three doors), because the detector's own doc names this blind spot and a named blind spot owes a filing. A chain recorded by a pre-v0.120.0 MySQL-family reader spells a literal backslash in MySQL's doubled form (`'a\\d'`) — structurally VALID, so the new `SLUICE-E-BACKUP-RECORDED-SCHEMA-MALFORMED` gate correctly does not fire — and a v0.120.0 `restore` of that chain to a POSTGRES target emits it verbatim, where `standard_conforming_strings=on` reads TWO backslashes: a different predicate, enforced silently, exit 0. ~~Restore of the same chain to a MYSQL target is CORRECT (the old spelling is the MySQL spelling), which is what makes a blanket refusal wrong.~~ **Closure verdict (2026-08-13): that sentence was true when filed and false by the time anyone could act on it** — the same v0.120.0 release that fixed the reader gave the MySQL emit boundary the bare-contract re-escape (`escapeExprLiteralBackslashes`), so an old doubled recording re-doubles on a MySQL-target restore and the same-engine round trip silently gains a backslash. The arm is target-independent, which made the fix SIMPLER than the sketch: a second trigger arm on the same code at every existing door (restore, chain-delta, verify, broker pre-drop, incremental/compact/prune WARNs), no target keying, no door signature changes, verify's refuse-what-restore-refuses doctrine intact. The two open questions resolved: mydumper is inside `IsMySQLFamily` and its pre-v0.120.0 recordings shared the doubled spelling (same normalizer); `SluiceVersion` has been stamped by every released binary since backup shipped (v0.15.0, `5550c230`) — the stated coverage boundary is `dev`-stamped from-source chains, deliberately NOT gated because their fresh-backup remedy would also stamp `dev` and could never release the refusal (the Bugs 245/246/247 unrunnable-remedy class). Premise-pinned (`TestEscapeExprLiteralBackslashes_PreV0120DoubledRecordingReDoubles_Premise` flips if the emitter ever compensates); both IR probes share one position walker with an anti-drift count pin; four mutants caught both directions.

## 2026-08-11 — pre-v0.120.0 release QA (VERDICT: safe to tag; three ratchets closed pre-tag)

The Tier-2 delta-scoped pass over v0.119.0..HEAD. `perf-parity-checker`: clean — no technique in the delta (the trigger was a false positive on a roster-test edit); the two-name spatial-OID catalog lookup confirmed once-per-pooled-connection with unchanged round-trip count; the row-17 arrears update internally consistent with all 12 spot-checked citations holding. `value-fidelity-reviewer`: **no silent value transformation in the delta**; the expression-escaping consumer sweep, the backup-chain fingerprint consequence, and `effectiveGeographySRID`'s geography-only gating all verified independently. Its three named gaps were closed IN the release commit rather than filed: (1) the MariaDB escaping family got its live premise pin across all four supported LTS lines (`TestMariaDB_ExpressionEscaping_PremiseAndDecode` — raw I_S bytes carry no extra layer, mode-invariant, IR spellings held); (2) Bug 241's name-matched fallback had falsified `check_expr_norm.go`'s "SluiceEmitted only" header and silently inherited the grouping-collapse window on user predicates — header rewritten, window pinned as documentation (name-matched regroup reports in sync, stated and tested), memo annotated with the wider-than-weighed window; (3) two stale "no geography codec on the apply path" comments in the PG CDC reader corrected, plus the functional-index and expression-DEFAULT positions gained their missing IR assertions in the escaping matrix.

## 2026-08-10 — value-fidelity review findings, pre-v0.119.0 (VERDICT: safe to land)

Full re-derivation of the delta's matrices. **No silent-loss defect introduced.** Five gaps, all in the loud-failure direction, filed here rather than fixed in the release commit.

### ~~GAP 1~~ (MEDIUM) — `geography` has no codec AND no refusal, and I was about to claim otherwise

**FIXED (2026-08-11, same commit as this edit, together with Bug 240).** Took the make-it-WORK arm: `afterConnectRegisterGeometry` now registers `pgGeometryBinaryCodec` for BOTH spatial OIDs (`pgSpatialTypeNames`), so every pool the roster gate reaches gets geography for free — the applier's three pools and the writer's, by construction, since they share the hook. The family matrix (`TestRowWriter_PostGIS_GeometryFamiliesAcrossWriteCores`) gained the `IsGeography` axis: all 3 cores × 14 family/dimension cells × {geometry, geography}, so the 42 cells became 84 (mutation-run: dropping geography from `pgSpatialTypeNames` fails exactly the 28 parameter-bind geography cells with the verbatim field error). The stale "geography stays loud-refused / no codec on any pool by design" claims in the roster-gate header and the pipelined-applier pin header were corrected in the same pass. GAP 2 (SRID≠0 / M / ZM cells) remains open and is unchanged by this.

The Bug 240 end-to-end run surfaced a THIRD defect neither filing named: the readers recorded a bare geography column's SRID as the view's raw 0, but geography stamps 4326 on every value it ingests, so the C-14 row-level SRID guard refused every row a data-bearing bare geography column holds — and `schema diff` saw phantom drift against the 4326-normalized column sluice's own `migrate` creates (PostGIS normalizes the emitted `geography(X, 0)` typmod to 4326). Fixed by `effectiveGeographySRID` (0 → 4326, geography only) at all four catalog-read sites, with the environmental premises pinned against a real server in the new test's premise leg.

Bug 239's commit message and my release prose said geography stays "loud-refused off the COPY path." Half of that is true and the important half is not. No codec is registered for the `geography` OID on any pool — correct. But there is no REFUSAL: `prepareValue` treats `ir.Geometry{IsGeography:true}` identically to geometry (`postgres/row_writer.go:1041-1056`; `IsGeography` appears nowhere in that file), so it reaches parameter binding and dies with the raw PostGIS `parse error - invalid geometry` — **the exact symptom Bug 239 just fixed for the sibling type**, with no column named and no remedy, mid-copy rather than at preflight.

Reachability is wider than "off the COPY path" implies: `WriteRowsIdempotent` ALWAYS takes the batch core (`row_writer_batch.go:76` — no COPY variant) with four production callers, and the plain batch core is entered for any table carrying an `ir.Interval` or `ir.VerbatimType` column. A PG→PG table with a geography column plus one uncatalogued extension column is an ordinary shape.

Minimal close: register the codec against the geography OID in `afterConnectRegisterGeometry` (same lookup, one extra row) — which makes it WORK — or add a named refusal for `IsGeography` off COPY. Either way the family matrix needs `IsGeography: true` rows; its 42 cells key on `Subtype` and never touch the flag.

### ~~GAP 2~~ (MEDIUM) — the geometry family matrix has no SRID≠0 cell

**RESOLVED (2026-08-11).** The matrix gained the SRID axis ({bare column + SRID 0, typmod-constrained + SRID 4326}) and the M/ZM dimension cases (ISO WKB 2000/3000-range type codes) on top of the GAP 1 geography axis: 3 cores × 28 family/dimension cases × {geometry, geography} × {srid 0, 4326} = **336 cells, all byte-exact** on real PostGIS, each cell now also asserting the LANDED SRID (the C-14 silent-re-stamp direction the old matrix could not see). Mutation-run: a mis-stamped SRID word in `wkbToEWKB` (`srid+1`) fails every arm — the bare-column cells via the new SRID leg (landed 1, want 0 — the SILENT direction), the typed cells via the server's own typmod refusal (`Geometry SRID (4327) does not match column SRID (4326)`), geography via `spatial_ref_sys`. One correction to the filing: `wkbToEWKB` has no "no-SRID-flag branch" — it sets the `0x20000000` flag and writes the SRID word unconditionally (0 included), so the genuinely unexercised axes were the SRID *value* ≠ 0 against a typmod-constrained receive path, and the M/ZM type codes; both now covered.

### ~~GAP 3~~ (LOW, message-only) — C-14 is not literally "untouched" for a 4-byte window

**RESOLVED (2026-08-11).** The decode order in `decodeVStreamCell`'s geometry branch now checks the SRID word first whenever it is COMPLETE (`len >= 4`): a short cell carrying a non-zero SRID refuses with the C-14 shape (which names the remedy) rather than the framing shape, restoring the pre-v0.119.0 message for that window — and making the "C-14 fires for a carried SRID" claim true again rather than merely re-wording it. Pinned by the new short-cell-with-SRID subtest beside the Bug 239 framing pins; the zero-filled framing loop is unchanged and still passes.

Original filing: for `4 ≤ len(raw) ≤ 8` the payload does carry a complete SRID word, and previously a non-zero SRID produced the C-14 refusal; now the framing refusal fires first. Both are loud stream errors so there is no fidelity change — but the claim "C-14 untouched" is false for that window, and the operator sees "too short to be MySQL's on-wire form" rather than "SRID cannot be carried."

### ~~GAP 4~~ (LOW) — the CHECK tamper arm covers the range translator only

**RESOLVED (2026-08-11).** DIRTY 3 added to `TestDiff_PGToMySQL_DomainChecksAreReconciled_Item156`: drops `dcheck_chk_1` and re-adds it under the auto-name with the pattern's domain changed (com → org), on top of DIRTY 2's still-widened range constraint — the report must carry both tampers (2 missing + 2 extra; verified on real containers with the regex constraint named in both lists). The match direction was already pinned by the same test's CLEAN arm, so the pair now covers the regex canonicalization in both directions.

### ~~GAP 5~~ (LOW, latent) — two greedy traps in the CHECK canonicalizer

**RESOLVED (2026-08-11), both traps.** The greedy `pgAnyArrayRewrite` regex is replaced by `foldPGAnyArrayLists`, a balanced scan that tracks bracket depth and skips literal-sentinel spans — closing the greedy trap AND the mirror a lazy regex would have introduced (a decoded literal VALUE may carry `])` verbatim). `matchEmittedChecks` now excludes actual-side constraints any expected check claims by NAME from expression-match candidacy, so an emitted prediction can no longer steal a source-declared constraint's target twin and orphan its name-keyed pair. Pinned by `TestCanonicalCheckExpr_AnyArrayFoldIsBalanced` (both traps, both directions) and `TestDiffChecks_EmittedMatchDoesNotStealANameClaimedConstraint`; both fixes mutation-run (greedy regex restored → the two-ANY-terms pin fails; filter removed → the steal pin fails), mutants grep-verified before and after.

Original filings: `pgAnyArrayRewrite` (`check_expr_norm.go:64`) is greedy: two `= ANY (ARRAY[…])` terms would canonicalize by spanning first `array[` to last `])`. No emitted shape produces two today; failure direction is a spurious DIFFERENCE. And `matchEmittedChecks` consumes actual-side constraints greedily in sorted-name order, so an emitted prediction can pair with a source-declared constraint carrying the same canonical predicate. Both false-positive direction.

### ~~Adjacent, pre-existing, belongs in the invariant queue~~ — RESOLVED

**RESOLVED (2026-08-11).** `decodeVStreamRow`'s malformed-event branch now refuses on the error channel all five callers already propagate (verified caller-by-caller), naming the table and both counts — the empty-row-with-`ok=true` shape and its "the caller surfaces the issue" comment are gone. The same sweep closed the sibling one step later that the filing did not name: a `Values` buffer shorter than its own `Lengths` claim PANICKED on the slice expression (mutation-confirmed: `slice bounds out of range`, killing the reader goroutine), and now refuses with the same loud shape. Pinned by `TestDecodeVStreamRow_MalformedEventRefusesLoudly` (both malformed shapes, plus the nil-row `ok=false` half and the well-formed path unchanged); both mutants applied-verified-caught before trusting the run.

Original filing: `mysql/cdc_vstream.go:1813-1818`: on `len(lengths) != len(fields)` the decoder returns an EMPTY `ir.Row` with `ok=true`, commented "so the caller surfaces the issue" — a premise nothing checks. All five callers treat it as a normal row; on the snapshot/copy path an empty row reaches `flattenArgs` where every missing key becomes nil → NULL. That is the silent-loss shape, gated behind a malformed VStream event. Not introduced by this delta, but the delta edits adjacent lines.

### One behavioural consequence for the release notes, not a defect

`mysqlTargetEmitShape` drops `WithTimeZone` from `ir.Time`, and that pass now feeds the data-moving ADR-0166 existing-target gate, not only `schema diff`. A PG `time with time zone` source against a pre-existing MySQL `TIME(6)` target now compares EQUAL where it previously refused the re-run. The refusal protected nothing — `mysql/row_writer.go:890` `stripTimeZoneOffset` drops the offset on the first migrate too — so this is consistent rather than a regression, but it is the one place in this delta where a loosened comparison sits directly upstream of a silently-lossy value path.

## 2026-08-14 — ingestr closed-PR survey: 11 PlanetScale/Vitess behavior facts as verification candidates (operator-requested; LICENSE-CLEAN — facts only, no code read or ported; ingestr is FSL-1.1)

A read-only survey of bruin-data/ingestr's 15 merged planetscale-related PRs (#865–#1061; full report in the 2026-08-14 session), extracting BEHAVIORAL FACTS about PlanetScale/Vitess their engineering history reveals — no ingestr source files were opened, only PR/commit/review text; anything acted on below must be implemented independently and validated psverify-style. The verification shortlist, ranked by sluice exposure:

1. **Hosted-PS CDC reachability** (their #901: vtgate's VStream gRPC port is not externally exposed on hosted PS; external CDC needs the psdbconnect Sync API on :443). sluice HAS a live-PS CDC psverify suite (`cdc_vstream_psverify_test.go`) — so either sluice's path already works (record WHICH endpoint/protocol it uses, closing the question) or the suite exercises something other than hosted-PS reachability. One psverify run + a doc sentence settles it.
2. **gRPC max-receive on VStream COPY** (their #909: PS batches snapshot rows into >4MB gRPC messages; Vitess tooling assumes 16MB). Check sluice's VStream client's configured limit + a psverify COPY of wide rows crossing 4MB.
3. **TLS-only vtgate dial** (their #895: `vtgateconn.Dial`'s registered dialer overrides caller TLS with insecure creds). Check which dial path sluice uses + that the 16MB/keepalive tuning survives.
4. **Sharded-keyspace WRITE-target refusal** (their #908: vindex-less tables unroutable, cross-shard merge illegal, RENAME swap non-atomic; and the probe-fails-silently sub-case). Point sluice at a sharded target in the vitesscluster suite; expect loud preflight or file the gap.
5. **CREATE DATABASE unsupported via vtgate** (their #908). Missing-keyspace target → clear preflight vs raw vtgate error mid-apply.
6. **OLAP workload vs pooling** (their #865: SET workload doesn't cross `database/sql` pooling; OLTP caps reads at 100k rows — a SILENT truncation shape if unpinned). Verify sluice pins the SET to the reading connection; >100k-row pooled read against Vitess as the pin.
7. **LOAD DATA on Vitess/PS targets** (their #939 keeps PS on chunked INSERT). Check the PS flavor's write-core selection; a psverify LOAD DATA measurement settles required-vs-choice.
8. **Mixed-resume delete loss** (their #901/#911: mid-copy persisted snapshot cursors → resume re-snapshots and skips interleaved deletes; new-table-joins-mid-sync variant). Map onto sluice's snapshot→CDC handoff + move-IN family; a targeted convergence scenario if not already pinned.
9. **Stop-boundary lands ON the final change / empty-shard empty-position** (their #907/#901): audit any sluice "position ≥ target" wait (backup data-window ends, handoff anchors) for the exactly-at-boundary and zero-position cases.
10. **Flavor/server mismatch preflight** (their #910; PS reports as Vitess in @@version): vanilla-mysql flavor pointed at Vitess/PS — refuse/warn or silently run with wrong capabilities? ~20-line premise check if the latter.
11. **Metrics wrap hardening** (their #945): saturating position subtraction (transiently-ahead committed position would wrap uint64); omitted-not-zero lag fields. Inspect metrics-watch arithmetic.

Items 1–3 are one psverify session together; 4–5 one vitesscluster session; 6 is the highest silent-loss-shaped item (a truncated read at exit 0 if the pin is absent). None are filed as defects — each is a MEASUREMENT first, per the house rule that measurement changes the filed picture every time.

### 2026-08-14 verdicts (source-review verification of the 11, operator-invited): 6 fully covered, 5 gate-light — the operator's "already worked through" holds for the substance, the gates lag in five places

COVERED with evidence: **#1 reachability** (sluice dials vtgate VStream gRPC at connect-host:443 with Basic per-RPC creds — cdc_vstream.go:428/791/826 — and the psverify suite ran it live against real PS 2026-07-16, three runs zero skips per docs/dev/psverify-dispatch.md); **#2 max-receive** (128 MiB default + DSN knob + the 8 MiB oversized-row integration pin, falsified pre-fix); **#3 TLS** (vtgateconn.Dial never used — the trap is structurally absent; hand-built TLS dial options; deliberate no-gRPC-keepalive is app-level liveness, worth a one-line note); **#7 LOAD DATA** (FlavorPlanetScale declares BulkLoadBatchedInsert; vanilla-on-PS-host refused at ValidateDSN); **#9 stop-boundary** (GTID-set containment makes exactly-at-boundary at-or-after by definition; empty-position sentinels REFUSE rather than compare, pinned); **#11 metrics** (no client-side unsigned position arithmetic exists; lag via server-side signed diff / *Known-flag gauges, the SL-6 fix).

GATE-LIGHT, ranked (all code-only except #4) — **ALL FIVE now CLOSED** ((1) `910c9a69` fingerprint arm, (2) `dc7f6026` 100k cap-crossing gate, (3) `fb6c8b58` then probe-first `aaa54444` (Bug 249, `SLUICE-E-SCHEMA-KEYSPACE-MISSING`), (4) measured then preflighted `2bc51ed0` (`SLUICE-E-SCHEMA-TARGET-KEYSPACE-SHARDED`), (5) `97a7d539` delete-interleaved convergence pin; doc sentences `67d34d56`): **(1) item 10** — no runtime probe recognizes self-hosted Vitess under the vanilla flavor (only PS hosts are DSN-refused; MariaDB↔MySQL is fingerprinted), and the downstream failure includes the SILENT 100k OLTP truncation since olapFullScan is VStream-flavor-gated → ~20-line checkServerFlavor Vitess arm + unit pin; **(2) item 6** — the SET-workload mechanism is exactly right (dedicated conn, real-vtgate scoped pin) but NO test crosses 100k rows: the Bug 132 pin seeds 50 rows and passes with the SET deleted — a gate narrower than its name → a >100k vttestserver pin mutation-run on SET-deletion, plus consider clamping/warning operator bulk-batch-size ≥100k on vtgate flavors; **(3) item 5** — the multi-DB fan-out's EnsureDatabase runs CREATE DATABASE IF NOT EXISTS on ANY MySQL-family target incl. PS/Vitess; engine.go:547's "vanilla-only" is an UNENFORCED comment → ~10-line usesVStream() guard; **(4) item 4** — sharded DATA-keyspace write target: the loud-VT09001 claim is comment-only → the vitesscluster measurement (local docker, not billable) then preflight-vs-documented-loud-failure; **(5) item 8** — copy-resume architecture is right (vtgate lastpk + GTID-min stitch + idempotent apply, zero-loss pin exists) but no test interleaves a DELETE mid-interrupted-copy → one vttestserver convergence pin. Items 1–3 owe only closing doc sentences naming the verified endpoint/protocol.

### 2026-08-14 — item 4 MEASURED (2-shard vitess/lite cluster, real Migrator; memo: workspace/measurement-sharded-target-2026-08-14.md): the "loud VT09001 at CREATE TABLE" comment is FALSIFIED — the fix is now fully specified

Measured on a genuine `[-80, 80-]` keyspace: (1) `CREATE TABLE` with no vindex **SUCCEEDS** and materializes the table on BOTH shards — the refusal (`Error 1173: table does not have a primary vindex`) fires at the first **row write**, not DDL, so `control_keyspace.go:85`'s claim is wrong on the phase; (2) sluice's real migrate dies even earlier and WORSE: the migrate-state control table's CREATE succeeds, then the immediate state read hits a vtgate schema-tracker race (`Error 1105: table sluice_migrate_state not found` — the same read works ~3s later), so the surfaced error is nondeterministic and names neither sharding nor the fix; (3) **no silent loss** — zero rows landed, no partial shard write — but the failure leaves DEBRIS: control tables materialized per-shard, unroutable via vtgate, needing per-shard cleanup; (4) `SHOW VITESS_SHARDS`-class detection works (`discoverAllShardsForKeyspace` returned both shards on the same connection). **Fix (fully specified, next queue item):** a sharded-target preflight for VStream-capable flavors BEFORE the first target write (before `EnsureControlTable`), reusing `discoverAllShardsForKeyspace` and mirroring `selectControlKeyspace`'s refusal shape (loud, names the shards, remedy = an unsharded target keyspace / platform-side vschema design); and correct the falsified comment in the same commit. Caveat recorded: hosted PlanetScale (safe-migrations DDL block, different tracker config) not measured — the preflight's refusal is flavor-keyed, not host-keyed, so it covers both regardless.
