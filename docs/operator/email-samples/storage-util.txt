Subject: [sluice] storage_util threshold alert: prod-orders

[sluice] CRITICAL ALERT — target storage approaching capacity

The storage_util threshold was breached on stream prod-orders.

    0.91 >= 0.9 (threshold)

  Metric:      storage_util
  Value:       0.91
  Threshold:   0.9
  Stream:      prod-orders
  Severity:    CRITICAL
  Time (UTC):  2026-06-22T12:00:00Z

Advisory alert from sluice. Notifications are failure-isolated — a delivery
problem never affects the running sync. Re-fires are rate-limited per the
configured cooldown until the metric recovers below its threshold.
