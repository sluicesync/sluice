# Architecture Decision Records — Index

One-line summaries of every ADR in `docs/adr/`. Use this page to find the right ADR fast; click through for the full reasoning, consequences, and alternatives section.

ADRs are numbered in the order they were proposed. A few notable conventions:

- **Status** lines at the top of each ADR record whether the decision is Accepted, Superseded, or Discovery (research-only).
- Some ADRs were promoted from a design doc in `docs/dev/notes/` after extended dialogue; the dialogue artifacts stay in `notes/` for traceability.
- **ADR-0051 collision:** two ADRs share the number — `adr-0051-core-pg-type-verbatim-carry.md` (the canonical one referenced by the roadmap and `ir.VerbatimType`) and `adr-0051-pg-cdc-source-identity-pinning.md` (a sibling concern). Renumbering hasn't been done because both are widely cross-referenced; future ADRs continue from 0087.
- **ADR-0066 collision:** `adr-0066-postgres-trigger-engine-variant.md` is the actual ADR; `adr-0066-task-62-planning-brief.md` is a planning brief for the same chunk and not a true ADR.

## Foundations (0001–0009)

| ADR | Decision |
|---|---|
| [0001](adr-0001-ir-first-translation.md) | IR-first translation: a typed dialect-neutral schema + value IR is the only shared contract between engines |
| [0002](adr-0002-sealed-interfaces.md) | Sealed engine interfaces in `internal/ir`; engine packages register via `init()` |
| [0003](adr-0003-kong-koanf.md) | kong for CLI, koanf for YAML+env config |
| [0004](adr-0004-three-phase-apply.md) | `tables → bulk_copy → indexes → constraints` three-phase apply (later: + `identity_sync`, `views`) |
| [0005](adr-0005-mysql-flavors.md) | MySQL flavors (Vanilla, PlanetScale) share engine code but register under different names with different `Capabilities` |
| [0006](adr-0006-pgoutput.md) | pgoutput plugin (not wal2json) for PG CDC |
| [0007](adr-0007-position-persistence.md) | Per-target `sluice_cdc_state` control table; position commit in the same tx as data writes |
| [0008](adr-0008-go-mysql.md) | go-mysql-org/go-mysql for MySQL binlog (over alternatives) |
| [0009](adr-0009-streamer-vs-mode-flag.md) | Streamer surface choice: subcommand-shaped, not a mode flag on `migrate` |

## CDC + bulk-copy core (0010–0029)

| ADR | Decision |
|---|---|
| [0010](adr-0010-idempotent-applier.md) | Idempotent applier — INSERT…ON CONFLICT semantics across both engines |
| [0011](adr-0011-slot-manager-optional-surface.md) | Slot manager as an optional engine surface, not required for `migrate`-only paths |
| [0012](adr-0012-pglogrepl-bypass-for-failover.md) | pgxconn bypass of pglogrepl for PG 17 failover-survival flag setting |
| [0013](adr-0013-applier-value-shaping.md) | Applier value shaping — per-engine value-prep before the parameterized statement |
| [0014](adr-0014-phase-aware-error-hints.md) | Phase-aware error hints registry (`internal/pipeline/hints.go`) |
| [0015](adr-0015-migration-resume.md) | `sluice migrate --resume` via per-target `sluice_migrate_state` |
| [0016](adr-0016-layered-expression-translation.md) | Layered expression translator — 30 catalog rules + verbatim fallthrough |
| [0017](adr-0017-batched-cdc-apply.md) | Batched CDC apply with `--apply-batch-size`; default per-change (=1) per ADR-0017 |
| [0018](adr-0018-per-batch-bulk-copy-checkpointing.md) | Per-batch bulk-copy checkpointing for resumability |
| [0019](adr-0019-parallel-within-table-bulk-copy.md) | Parallel within-table bulk-copy with `--bulk-parallel-min-rows` threshold |
| [0020](adr-0020-slot-ack-after-apply.md) | Slot acknowledgment only after the apply has committed |
| [0021](adr-0021-publication-scope-by-table.md) | Publication scope by table list (not `FOR ALL TABLES`) |
| [0022](adr-0022-slot-missing-fall-through.md) | Slot-missing fall-through behavior — refuse, don't recreate silently |
| [0023](adr-0023-reset-target-data.md) | `--reset-target-data` semantics with typed confirmation |
| [0024](adr-0024-schema-preview.md) | `sluice schema preview` — show the DDL sluice would emit |
| [0025](adr-0025-graceful-drain-stop.md) | Graceful drain `sync stop --wait` via control-table polling |
| [0026](adr-0026-mysql-load-data-infile-writer.md) | MySQL LOAD DATA INFILE writer for bulk copy |
| [0027](adr-0027-source-transaction-boundary-cdc-batching.md) | CDC batching respects source-transaction boundaries (no mid-tx flush) |
| [0028](adr-0028-memory-bounded-streaming.md) | Memory-bounded streaming via channel-and-worker model |
| [0029](adr-0029-schema-diff.md) | `sluice schema diff` — diff a target against what sluice would produce |

## Live add-table + multi-source (0030–0036)

| ADR | Decision |
|---|---|
| [0030](adr-0030-mid-stream-live-add-table.md) | Mid-stream live add-table — `--no-drain` (PG path; Phase 2) |
| [0031](adr-0031-multi-source-aggregation-target-schema.md) | Multi-source aggregation via `--target-schema NAME` (PG-only) |
| [0032](adr-0032-pg-extension-passthrough.md) | PG extension passthrough catalog — 7-extension enumerated allowlist |
| [0033](adr-0033-mid-stream-live-add-strict-zero-loss.md) | Strict zero-loss correctness for live add-table (Bug 36 closure) |
| [0034](adr-0034-mysql-phase-2-live-add-table.md) | MySQL Phase 2 live add-table via streamer-side filter-flip |
| [0035](adr-0035-postgis-geometry-spatial-support.md) | PostGIS-aware geometry translation with SRID preserved cross-engine |
| [0036](adr-0036-mid-stream-loss-surface-characterization.md) | Mid-stream loss-surface characterization (post-Bug-36 audit) |

## Redaction + perf + bulk-copy (0037–0043)

| ADR | Decision |
|---|---|
| [0037](adr-0037-key-management.md) | Key management for redaction strategies (precursor to ADR-0041) |
| [0038](adr-0038-applier-retry-on-transient-errors.md) | Applier retry on transient errors (deadlock, lock wait timeout, etc.) |
| [0039](adr-0039-randomize-strategy-determinism.md) | `randomize:*` strategy determinism — per-row seed contract |
| [0040](adr-0040-dictionary-strategy-determinism.md) | Dictionary strategy determinism — PK-keyed vs input-value-keyed |
| [0041](adr-0041-operator-keyset-persistence.md) | Operator keyset persistence (`--keyset-source=file/env/db`) |
| [0042](adr-0042-mysql-bulk-copy-throughput-investigation.md) | MySQL bulk-copy throughput investigation (Discovery) |
| [0043](adr-0043-native-bulk-loader-on-parallel-copy-path.md) | Native fast loader on cold-start parallel-copy path |

## Translation policy + backup chain (0044–0050)

| ADR | Decision |
|---|---|
| [0044](adr-0044-extension-function-defaults-tier3.md) | Tier 3 extension function-defaults (uuid_generate_v4 → UUID(), pgcrypto SHA family) |
| [0045](adr-0045-expression-identifier-translation-consolidation.md) | Consolidate expression-identifier translation (Bug 64 closure) |
| [0046](adr-0046-inline-backup-chain-rotation.md) | Inline backup chain rotation — `--retain-rotate-at*` flags |
| [0047](adr-0047-verbatim-extension-passthrough.md) | Verbatim extension passthrough for uncatalogued PG extensions (`ir.VerbatimType`) |
| [0048](adr-0048-multi-source-aggregation-shape-a.md) | Multi-source aggregation Shape A (sharded → consolidated; ADR-0048-class) |
| [0049](adr-0049-cdc-schema-history.md) | CDC schema-history table for resume-across-DDL correctness |
| [0050](adr-0050-reconciling-resnapshot.md) | Reconciling re-snapshot — drift detection + targeted re-snapshot |

## Verbatim carry, source-identity, AIMD, EXCLUDE (0051–0059)

| ADR | Decision |
|---|---|
| [0051a](adr-0051-core-pg-type-verbatim-carry.md) | Core-PG-type verbatim carry beyond tsvector/tsquery (range/multirange/Stage 2 family) |
| [0051b](adr-0051-pg-cdc-source-identity-pinning.md) | PG CDC source-identity pinning (refuse to resume against a different source after failover) |
| [0052](adr-0052-aimd-apply-batch-size-controller.md) | AIMD apply-batch-size controller for batched CDC |
| [0053](adr-0053-exclude-constraint-verbatim-carry.md) | EXCLUDE constraint verbatim carry (same-engine PG → PG) |
| [0054](adr-0054-shape-a-phase-2-live-cross-shard-ddl-coordination.md) | Shape A Phase 2 live cross-shard DDL coordination |
| [0055](adr-0055-pgoutput-streaming-protocol-audit.md) | pgoutput streaming protocol audit (Discovery) |
| [0056](adr-0056-sluice-diagnose-operator-bundle.md) | `sluice diagnose` — operator capability + state bundle |
| [0057](adr-0057-hard-delete-semantics-across-engines.md) | Hard-delete semantics across engines |
| [0058](adr-0058-online-schema-change-forwarding.md) | Online schema change forwarding (`ADD COLUMN` auto-forwarding) |
| [0059](adr-0059-pg-slot-health-prewarning.md) | PG slot-health pre-warning (70%/85% retention + 30min inactivity) |

## Schema drift, heartbeat, cutover, RLS (0060–0066)

| ADR | Decision |
|---|---|
| [0060](adr-0060-cdc-schema-drift-diff.md) | CDC schema-drift diff in refuse-loudly messages |
| [0061](adr-0061-source-side-heartbeat-writer.md) | Source-side heartbeat writer to keep slots alive against quiet sources |
| [0062](adr-0062-cutover-sequence-priming.md) | Cutover sequence priming (`sluice cutover` subcommand) |
| [0063](adr-0063-pg-rls-ir-capture-and-emit.md) | PG RLS IR capture + emit; loud refusal on cross-engine PG → MySQL |
| [0064](adr-0064-backup-smart-compaction.md) | Backup smart-compaction (preserves restorability while reducing segment count) |
| [0065](adr-0065-shape-a-catalog-check-constraints.md) | Shape A catalog: CHECK constraints (PG → MySQL inline-CHECK precursor) |
| [0066a](adr-0066-postgres-trigger-engine-variant.md) | `postgres-trigger` engine variant for slot-less managed-PG sources |
| [0066b](adr-0066-task-62-planning-brief.md) | Task 62 planning brief (not a canonical ADR; companion to ADR-0066a) |

## Born-contiguous rotation, capture-payload, ready, Stage 2 (0067–0070)

| ADR | Decision |
|---|---|
| [0067](adr-0067-contiguous-rotation-handoff.md) | Born-contiguous rotation handoff — compactable rotated chains (Bug 95 closure) |
| [0068](adr-0068-changed-columns-capture-payload.md) | `sluice trigger setup --capture-payload=full/changed/minimal` — lighter capture payload mode |
| [0069](adr-0069-service-mode-readyz.md) | Service-mode `/readyz` endpoint for k8s/load-balancer probes |
| [0070](adr-0070-stage-2-verbatim-carry-promote.md) | Promote Stage 2 core-PG verbatim carry (xml/money/pg_lsn/txid_snapshot/pg_snapshot) per ADR-0051a evidence |

## VStream resilience + multi-database (0071–0074)

| ADR | Decision |
|---|---|
| [0071](adr-0071-vstream-snapshot-bounded-memory.md) | Bounded-memory VStream cold-start COPY (byte-capped backpressured pump, `--max-buffer-bytes`) |
| [0072](adr-0072-resumable-coldstart-copy.md) | Resumable VStream cold-start COPY (carry Vitess `TablePKs` cursor, checkpoint during COPY, in-place reconnect) |
| [0073](adr-0073-vitess-internal-and-online-ddl-tables.md) | Exclude Vitess internal / online-DDL `_vt_*` tables from COPY+CDC; survive an online-DDL cutover zero-loss |
| [0074](adr-0074-multi-database-mysql-migration-and-sync.md) | Accepted (shipped v0.99.16) — multi-database MySQL migrate + sync (one server → N databases → N target namespaces; server-wide binlog CDC with per-event apply-routing) |
| [0075](adr-0075-postgres-source-multi-schema-migration-and-sync.md) | Accepted (shipped v0.99.24) — Postgres-source multi-schema migrate + sync (one PG database → N schemas → N target namespaces; one database-wide logical slot, per-event apply-routing — the symmetric reverse of ADR-0074) |
| [0076](adr-0076-cross-table-copy-worker-pool.md) | Accepted (shipped) — cross-table copy worker pool for `sluice migrate` (`--table-parallelism`; bounded errgroup over tables composed with within-table `--bulk-parallelism`; combined budget split at the single chokepoint so the product can't exhaust the target's slots; resume-under-concurrency discipline; sync path stays serial). Roadmap item 3(a) |

## Index-build overlap (0077)

| ADR | Decision |
|---|---|
| [0077](adr-0077-overlap-index-builds-with-bulk-copy.md) | Accepted — overlap secondary-index builds with the bulk copy in `sluice migrate` (PG `ir.IncrementalIndexBuilder`: build each table's indexes as its copy lands, concurrently with still-copying tables, under one errgroup; combined copy+index connection budget split at the single chokepoint so simultaneously-open copy+index connections can't exhaust the target's slots; `IndexesBuilt` resume flag; MySQL falls back to the post-copy whole-schema index phase). Roadmap item 3b(a) |

## Identity passthrough — raw COPY byte-pipe (0078)

| ADR | Decision |
|---|---|
| [0078](adr-0078-pg-pg-identity-passthrough-raw-copy.md) | Accepted — PG→PG identity passthrough: byte-pipe the raw COPY stream (`COPY (SELECT …) TO STDOUT` → `COPY tbl (…) FROM STDIN` via pgx `pgconn`) to close the per-stream copy-rate gap vs pgcopydb, bypassing the typed IR. Engine-neutral optional surfaces (`ir.RawCopyExporter`/`RawCopyImporter`/`RawCopyVersionProber`/`RawCopyChunk`/`RawCopyFormat`); engages ONLY behind a single auditable value-fidelity gate (`rawCopyGate`: same-engine + no redaction/type-override/expr-override/shard-injection + per-table identity projection excluding extension/verbatim/bit/geometry) — any transform present falls back to the IR path. Slotted in the ADR-0043 cold-start fast-loader branch (`migrate`-only; resume + sync stay on the IR path); text COPY default, binary opt-in on matched server majors. Roadmap item 3b(b) |

## Fast cold-start for the sync path (0079)

| ADR | Decision |
|---|---|
| [0079](adr-0079-fast-cold-start-for-sync-path.md) | Accepted (design) — bring the migrate cold-start speedups (cross-table pool ADR-0076 + index-overlap ADR-0077 + raw passthrough ADR-0078) to `sluice sync start`'s cold-start, so copy+follow gets the fast copy (the one-command pgcopydb-`--follow`-at-full-speed equivalent). Shape (A), PG-source-first, behind a source-capability gate: qualifies only when the `ir.SnapshotStream` carries a shareable exported-snapshot name AND the source implements `ir.SnapshotImporterOpener` (wires the latent, never-called `SnapshotImporter` `SET TRANSACTION SNAPSHOT` surface to pin all N parallel readers to ONE consistent snapshot). MySQL + VStream/PlanetScale stay serial (loud INFO), deferred — the durable-watermark + idempotent-COPY coupling lives only on the VStream path, which never coexists with the raw byte-pipe. `rawCopyGate` refactored off `*Migrator` onto a shared transform-config struct so the value-fidelity guarantee is identical on both paths. CDC connection slot reserved in the budget; resume stays serial. **v1 shipped v0.99.29. v1.1 (within-table chunking on the fast path) shipped v0.99.30** — the first v1.1 attempt (relax pinned-reader `CountRows`) regressed and was reverted because the during-copy ETA probe then raced the row-stream on the pinned conn; the as-built CORRECTION keeps `CountRows` returning 0 for pinned readers and adds a separate `ir.RowCountEstimator` that reads reltuples off a FRESH non-pinned conn for the pre-stream chunk decision (ADR v1.1-CORRECTION addendum). Roadmap items 3d + 3d v1.1. |

## MySQL index-build overlap (0080)

| ADR | Decision |
|---|---|
| [0080](adr-0080-mysql-index-build-overlap.md) | Accepted (design) — extend ADR-0077 index-build overlap to MySQL targets: the MySQL `SchemaWriter` implements `ir.IncrementalIndexBuilder`/`TableIndexedNotifier` (mirrors the PG file), so every MySQL-target migrate (MySQL→MySQL, PG→MySQL) collapses the separate post-copy index phase into the copy. Orchestrator unchanged (gated on the surface). MySQL has no connection-slot prober, so the index pool self-sizes (fixed N=4, clamped [1,8], bounded by job count + `--max-target-connections`) rather than from `splitCopyAndIndexBudget` (always 0 for MySQL). PlanetScale/Vitess targets decline the overlap (internal drain-and-defer to the platform's online-DDL via the post-copy `CreateIndexes`). One index per job (parallel), idempotency via the existing `indexExists` probe (no portable `ADD INDEX IF NOT EXISTS`). No published throughput number until an at-scale MySQL measurement (PG's overlap was −60 s on disk-bound storage). Roadmap item 3c. |

## Applier control-plane extraction (0081)

| ADR | Decision |
|---|---|
| [0081](adr-0081-applier-control-plane-extraction.md) | Accepted — repo-audit M2.2: extract the engines' mirrored applier control plane into `internal/appliershared` behind a flat config-of-closures dialect seam (`BatchConfig`, exprident.Config precedent). The audit measured the two `change_applier_batch.go` files ≥85 % identical with 16/19 commits forced to touch both (the item-18 latency fix landed twice). Tier (b) — the batched-apply loop (accumulation, AIMD consult/observe, idle-grace timer, byte cap, ADR-0007/0010 position-write-then-commit ordering) — now lives ONCE in `RunBatchLoop`/`RunOneBatch`; engines fill the divergent leaves (engine name, `TransactionalDDL` flag for MySQL's implicit-commit DDL vs PG's in-tx schema events, F7 BeginTx, slot-ack AfterCommit, cache-after-commit hook). SQL builders, value codecs, classifiers stay engine-side. All item-18 timing pins + idempotency pins passed unchanged. Tiers: extraction arc COMPLETE — (a) helpers PR #170, (b) batch loop, (c) control-table CRUD PR #176, (d) lease/keyset/heartbeat PR #177. |

## Per-table migration-state rows (0082)

| ADR | Decision |
|---|---|
| [0082](adr-0082-per-table-migration-state-rows.md) | Accepted — repo-audit M2.3: make migrate-state checkpoint writes O(1) in table count. The ≤v0.99.x store re-encoded the WHOLE `map[table]TableProgress` blob into the one hot `sluice_migrate_state` row on every breadcrumb/per-5000-row cursor/chunk checkpoint — O(N²) over a 10k-table migration (~856 KB × ≥20k writes ≈ 17 GB through one row's MVCC/TOAST). Now: a header row (phase, `state_format`, timestamps) + one `sluice_migrate_table_progress` row per table; hot paths use the new `ir.MigrationStateStore.WriteTableProgress` (one upsert; measured 84× faster wall, ~12,770× less payload per checkpoint on real PG). Store logic lands ONCE in `internal/migratestate` (ADR-0081 tier-c dialect-seam precedent); engines are thin SQL shims. Legacy blobs upgrade once, transactionally, on first Read (crash-safe: commit-whole or re-run; orphan-clearing delete-first); the old blob is overwritten with a deliberately-invalid-JSON sentinel so a downgraded binary fails LOUDLY instead of silently re-copying (one-way state compat, as at v0.4.0). Cross-version pin ×2 engines against a byte-captured v0.99.x blob covering every TableProgress family × shape. |

## Backup-chain provisioning (0083)

| ADR | Decision |
|---|---|
| [0083](adr-0083-backup-chain-provisioning.md) | Accepted — task #40, found by the 2026-06-10 backup benchmark: the chain-handoff contract ("operator creates the slot before the full") had no provisioning surface, and every natural way of getting it wrong was silent or misleading. Three coordinated pieces: (1) **`backup full --chain-slot`** creates the PERSISTENT chain slot AS the snapshot anchor (consistent point == manifest EndPosition → zero-gap chain by construction) and ensures the pgoutput publication BEFORE the anchor (pgoutput's historic-catalog rule: a late-created publication can never decode the chain's first window); commit-on-success via the new `ir.BackupSnapshot.CommitFn` (failed runs drop the slot), already-exists refused loudly, openers take `ir.BackupSnapshotOptions`. (2) **Chain-resume preflight** (`ir.ChainResumePreflighter`, PG-only): refuse when the slot is missing (it may never have existed — old message blamed pruning) or when `confirmed_flush_lsn` is AHEAD of the parent position — previously the walsender silently fast-forwarded and the incremental SUCCEEDED with the in-between writes gone (silent-loss class; same hazard the live add-table preflight guards). (3) **Chain-consumer ack hold**: no applier → keepalive acked the STREAMED LSN, so a teardown-window keepalive could release WAL the chain never committed; `HoldSlotAckAtCommitted`/`ReleaseSlotAckTo` pin acks to committed window ends and bound `backup stream` WAL retention to ~one rollover window. |

## Backup cross-table parallelism (0084)

| ADR | Decision |
|---|---|
| [0084](adr-0084-backup-restore-cross-table-parallelism.md) | Accepted — backup side + restore side both shipped (restore-side PR 2 `6947c9c`, v0.99.35) — task #39, motivated by the 2026-06-10 backup benchmark (133 GB / 43 tables: `sluice backup full` 2367 s vs `pg_dump -j8` 232 s; pg_dump's own j1→j8 shows ~3.4× of the gap is pure cross-table parallelism). The full-backup row sweep fans out through a bounded errgroup pool (`backup full --table-parallelism`, 0 = auto = 4) mirroring ADR-0076's free-reader 1-slot channel, every dedicated reader minted via `ir.SnapshotImporter.ImportSnapshot` against the snapshot's exported name — the gate (`backupParallelEligible`) is the ADR-0079 capability pattern (new `ir.BackupSnapshot.SnapshotName` field; never an engine-name check). MySQL (per-session snapshot) and the v0.17.x non-snapshot fallback stay serial with a loud INFO; not-eligible collapses to the same pool at parallelism 1 (one code path). Manifest determinism via pre-staging: every entry staged in schema order before the pool starts; a `manifestCommitter` mutex serializes every entry mutation + marshal + same-key `manifest.json` Put. A crash leaves ≤ tableParallelism in-flight `Partial=true` tables (per-chunk-accurate chunk lists) plus pre-staged zero-chunk entries; the existing resume classifier handles all states. Source-side connection budget enforced at one pre-pool chokepoint, reserving a slot for the snapshot's replication conn. |

## Crash-resume anchor adoption (0085)

| ADR | Decision |
|---|---|
| [0085](adr-0085-resume-anchor-adoption.md) | Accepted — task #42, a CRITICAL silent-loss class: a resumed `backup full` kept the first attempt's completed tables (exact as-of anchor A1) but recorded the resume's fresh snapshot anchor A2 as EndPosition — writes to kept tables in (A1, A2] were in neither the row chunks nor the next incremental's window, exit 0. Worse, the `--chain-slot` already-exists refusal advised `slot drop` + retry as crash recovery, releasing the gap-covering WAL. Fix: **EndPosition = the adopted minimum anchor.** In-progress manifests carry the anchor from their first write (pre-sweep durable commit); a resume ADOPTS it and the fresh snapshot serves only read consistency. Soundness rides ADR-0010's idempotent appliers (the overlap replay (A1, A2] converges for keyed tables — ADR-0010 is now load-bearing for chains); truly keyless re-streams are refused loudly (`ir.TableReplayIdempotent`), as is schema drift between attempts. `--chain-slot` resume = adoption, not creation: preflight the standing slot, open the snapshot temp-anchored, never drop the adopted slot; `BackupSnapshot.Commit` moved to anchor-durability time (amends ADR-0083 — an interrupted run deliberately keeps the slot: it is the resume's WAL-retention guarantee); the recovery message is reversed (re-run the same command; drop + `--force-overwrite` only for a deliberate fresh start). `resolveParent` refuses in-progress parents; pre-fix anchor-less manifests re-stream everything. Pinned end-to-end by `TestBackup_ResumeAnchorAdoption_NoChainGap` (gap writes present exactly once after full → incremental → chain restore, family-matrix DML on a kept AND a re-streamed table). |

## Backup manifest progress sidecar (0086)

| ADR | Decision |
|---|---|
| [0086](adr-0086-backup-manifest-progress-sidecar.md) | Accepted — task #54, the #38 scale probe's only super-linear wall: every per-chunk / per-table backup checkpoint re-marshaled the ENTIRE manifest (schema included) and re-Put `manifest.json` — ≈ 0.018·N + 2.77e-5·N² seconds over N tables (~78 h at 100k). Fix: the in-progress manifest splits into a **base written once** (schema + ADR-0085 anchor + pre-staged entries; the pre-sweep write and its `BackupSnapshot.Commit` ordering are unchanged) plus an **append-only `manifest.progress.jsonl` sidecar** — one JSON line per chunk-finished / table-finished event via the new optional `irbackup.Appender` store capability (LocalStore: single `O_APPEND` write + fsync), O(1) per event. Truth = base + `irbackup.ReplayProgress`, folded into both manifest decoders so resume/restore/verify/broker/chain readers all see one reconstructed (and normalized — replay is not idempotent) view; torn final lines are tolerated loudly, stale attempt-ID lines skipped, anything else refuses. Finalize folds everything back into the one self-contained `manifest.json` (schema-appropriate version restored, sidecar deleted) — successful backups keep the pre-ADR layout exactly. In-progress bases are stamped `FormatVersionProgressSidecar=3` so an OLDER binary refuses the layout loudly instead of resuming off an under-reporting base (the ADR-0082 sentinel posture, proportional per Bug 116). Append-less stores (BlobStore) keep the legacy full-rewrite checkpoints byte-for-byte, WARN-named at ≥1000 tables. Pinned by the 10k-table byte-accounting scale test (manifest marshaled exactly twice per run) + the crash→reconstruct→resume end-to-end + the four-way version matrix. |

## Compact group-split + rotation-boundary resume (0087)

| ADR | Decision |
|---|---|
| [0087](adr-0087-compact-group-split-and-rotation-boundary-resume.md) | Accepted — Bug 139, the case ADR-0067's honest-stamp rule creates: a rotation-born segment whose creating session never commits an incremental (idle stop — rotate-on-timer then stop while the source is idle, so the new segment gets no rollover while the snapshot anchor `S` still sits a few WAL bytes past `P_N` with no user events between — or a crash/end at the rotation boundary) has no `IncrementalCoverageStart`, resolves to its full anchor `S` (a few WAL bytes past the prior segment's `P_N`), and so `compact` REFUSED the whole run blaming "pre-ADR-0067/imported/corrupted lineage" — for a chain rotation produced — while a later resume never healed it (resumed at `S`, stamp stayed empty forever). **Two coordinated fixes, neither stamping coverage no committed incremental proves** (the catalog's creation-time-stamp / resume-backfill suggestions are REJECTED: a crash or walsender lag can leave real `(P_N, S]` events that live ONLY in the new segment's full, which compact drops — the stamp would convert today's LOUD refusal into silent DR loss): (1) **compact SPLITS** each merge group at every coverage-gap boundary (`subdivideAtCoverageGaps`) instead of refusing — the stamp-less segment stays its own group (one operator-accurate WARN naming it; no data lost, chain restorable), the contiguous runs around it still merge, both naive + smart + `--dry-run`; `assertGroupBoundaryContiguous` kept as a now-unreachable defensive invariant. (2) **resume replays from `P_N`** (`rotationBoundaryResumeStart`, stream + one-shot incremental): a resume landing on a rotation-born zero-incremental open segment whose parent is its full resumes from the prior segment's `EndPosition`, reconstructing the creating session's post-COMMIT state so the first incremental starts at `P_N` and the existing `updateLineageForManifest` stamps `IncrementalCoverageStart = P_N` honestly — no `skipThrough` (a fresh `StreamChanges(P_N)` is strictly-after, verified on PG + MySQL), soundness on `releaseChainAckTo` WAL retention + ADR-0010 idempotent overlap replay. Amends ADR-0067. Pinned by the compact-split unit matrix (trailing OPEN/CAPPED, mid-chain stamp-less, multi-gap, contiguous-still-merges, dry-run), the resume-rule unit test, and the PG idle-stop integration repro (naive + smart split + restore == oracle; resume-heal → whole-chain N→1). |
| [0088](adr-0088-mysql-coordinated-parallel-backup-snapshot.md) | Accepted — closes the MySQL `backup full` parallelism gap (the 2026-06-13 fair-fight: `mydumper -j8` 18 s vs sluice ~251 s, dominated by sluice's single-reader serial sweep because ADR-0084's parallel gate requires a *shareable exported snapshot* PG has and MySQL doesn't). Revisits the ADR-0019 "N MySQL readers necessarily see N independent snapshots" assumption: they need not — under a brief `FLUSH TABLES WITH READ LOCK`, N connections each `START TRANSACTION WITH CONSISTENT SNAPSHOT` observe **byte-identical** views (mydumper's mechanism; PlanetScale's *import* path uses the same FTWRL→`LOCK TABLES` fallback). **Vanilla MySQL only** — Vitess/PlanetScale stays on the separate VStream COPY path (vtgate can't FTWRL across shards; `pscale database dump` itself forgoes consistency via `@replica`+`workload=olap`). Decision: thread the resolved cross-table parallelism into `SnapshotOptions.ReaderParallelism`; MySQL `OpenBackupSnapshot` opens N consistent readers under one FTWRL window (coordinator conn FTWRL → each reader REPEATABLE READ + CONSISTENT SNAPSHOT → capture position under lock → UNLOCK), returning the primary in `Snapshot.Rows` + the rest in the new `Snapshot.ExtraReaders`; `backupParallelEligible` accepts EITHER the PG exported-snapshot capability OR non-empty `ExtraReaders`; the ADR-0084 pool's reader factory pops the pre-opened extras. FTWRL-denied (no `RELOAD`) / `ReaderParallelism<=1` falls back to today's serial single-reader open, loud, byte-identical (no regression). `-race`-before-tag. Pinned by the cross-reader **consistency** oracle (writes during a paused post-open window appear in NEITHER reader), serial-fallback dispatch, zero-loss parity vs serial, crash/resume, and Vitess-untouched tests. |
| [0089](adr-0089-default-adaptive-apply-batch-size.md) | Accepted — changes the `sync start` default `--apply-batch-size` from `1` to `auto` (engine ceiling: 1000 mysql/postgres, 100 planetscale; AIMD adapts within `[1, ceiling]`). The Bug-141 soak measured >10× (240 → ~6,500 rows/s) — the controller was dormant for every default user because ADR-0017's default `=1` made the controller's cap equal its floor. Load-bearing safety: the **keyless-table guard** treats a change to a truly-keyless table (no PK *and* no usable unique index → non-idempotent plain INSERT) as a flush boundary in `appliershared.RunBatchLoop`, clamping its crash-replay blast radius to exactly 1 duplicate (= `=1`) while PK/unique tables in the same stream keep batching. `--apply-batch-size=1` / `--no-auto-tune` restore the conservative behaviour. Supersedes-in-part ADR-0017 + ADR-0052's "opt-in" clauses. |
| [0090](adr-0090-vstream-watchdog-timeouts-retriable.md) | Accepted — Bug 142 / the first PlanetScale soak: a VStream watchdog/progress timeout that previously surfaced as a fatal stream error is reclassified **retriable**, so the warm-resume reconnect path catches it instead of crash-looping the process. |
| [0091](adr-0091-default-on-schema-change-forwarding.md) | Accepted — default-on schema-change forwarding (codename F7): tristate `--schema-changes=forward\|refuse` (default `forward`) forwards every unambiguous online DDL shape (ADD/DROP COLUMN, ALTER TYPE/nullability, CREATE/DROP INDEX, CHECK add/drop/modify) across all three source paths (MySQL binlog, PG pgoutput, PlanetScale/Vitess VStream). §1d "real forwarding matrix" is the source of truth; RENAME COLUMN forwards only when provable (PG attnum-stable, F7b) and refuses loudly on MySQL-family ambiguity; never silent data loss. Shipped v0.99.45 (F7a) → v0.99.46 (F7b) → v0.99.48 (F7c). |
| [0092](adr-0092-pipelined-cdc-apply.md) | Accepted — collapses a CDC batch's N serial `tx.ExecContext` round trips into **one** pipelined `pgx.Batch` flush (data statements + the position upsert), so steady-state apply throughput stops being bounded by `N × RTT`. Measured on the 2026-06-15 soak: per-row apply ~11 ms (~7 ms cross-cloud RTT + ~4 ms upsert) → ~90 rows/s ceiling, *unchanged* by a PS-10→PS-80 upsize (the bottleneck is the wire, not the DB). Generalizes the ADR-0081 seam's tx handle to a minimal `BatchTx interface{ Rollback() error }`; PG's pipelined path reuses the proven `raw_copy.go` `*sql.Conn.Raw → *pgx.Conn` escape, reuses the existing `build{Insert,Update,Delete}SQL` + `prepareValue` codecs byte-for-byte (value fidelity unchanged), and keeps the keyless guard / AIMD / byte-cap / tx-boundary / ADR-0007 position-then-commit invariants. PG-only here (seam stays engine-neutral for a later MySQL adoption); `=1` keeps the serial path; serial fall-back (loud) if the raw escape ever fails. `-race`-before-tag. |
| [0093](adr-0093-vstream-purged-gtid-resnapshot.md) | Accepted — extends ADR-0022 to the VStream/PlanetScale source. The binlog path auto-recovers a purged resume position (`gtid_purged` advanced past it) via a `verifyGTIDSetReachable` **pre-flight** → `ir.ErrPositionInvalid` → cold-start; VStream has no clean pre-flight (vtgate is a proxy, no single `@@gtid_purged`) so the purged condition only surfaces **reactively** from the pump's `Recv`, and `classifyReaderError` mapped it to retriable/terminal — **never `ErrPositionInvalid`** — so a purged VStream resume **restart-looped** instead of re-snapshotting (the PlanetScale ~3-day-retention case; found via `planetscale/fivetran-source` PR #69/#73). Decision: (1) `isVStreamPurgedGTIDError` matches `"purged required binary logs"` (master/source variants) and wraps `ir.ErrPositionInvalid` **before** the gRPC/retriable branches; (2) `runWithRetry` routes a reactive `ErrPositionInvalid` to a **one-shot** forced cold-start (`RestartFromScratch`, non-destructive, idempotent-COPY overlap) — a second consecutive one is terminal/loud; (3) opt-out `--no-auto-resnapshot` (default auto, parity with binlog) suppresses BOTH the pre-flight fall-through and the reactive recovery, surfacing a loud actionable error instead. No silent loss either way. `-race`-before-tag; Vitess-cluster integration test advances `gtid_purged` past a captured resume position. |
| [0095](adr-0095-vstream-auto-shard-by-table-copy.md) | Accepted — the program's **#1 finding** (302 GB PlanetScale source, v0.99.62): a full multi-table Vitess/PlanetScale keyspace can't cold-copy in one command at bounded memory, because the single keyspace-wide VStream (`Match:"/.*/"`) interleaves every table and the orchestrator drains one at a time, so RAM ≈ Σ(all-but-active) (~290 GB → ADR-0071 loud refusal). Decision: the engine **auto-shards the COPY by table internally** — opens/drains/completes one single-table VStream (`Match:<table>`, constant memory, no interleave) at a time on the same conn, then opens the CDC tail; the user still runs ONE command. The correctness crux is the snapshot→CDC stitch: each table finishes COPY at a different per-table VGTID `P_i`, so CDC resumes from the per-shard GTID-set **intersection** `P_start = ⋂_i P_i` (the set-MIN, never the max). Gapless because `P_start ≤ every P_i` (no change after any table's snapshot is skipped); the overlap `(P_start, P_i]` is the same at-least-once seam ADR-0010's idempotent upsert + the Bug-125 idempotent-copy writer already absorb. Reuses the existing `Contain`/`gtidAtOrAfter` superset primitive; a genuinely-disjoint per-shard set (a single causally-ordered session should never produce one) **refuses loudly** rather than guess. Replaces ADR-0071's deferred Phase-3 disk-spill (per-table COPY needs no spill); preserves `--include-table` + the ADR-0072 resume cursor (per-table, composes unchanged); sequential per-table v1 (bounded-parallel COPY a stitch-agnostic follow-on); legacy single-stream behind an opt-out. `-race`-before-tag. |
| [0096](adr-0096-keyset-chunking-non-integer-pk.md) | Accepted — lifts ADR-0019's "single integer PK only" limit on within-table parallel bulk copy: adds a second boundary strategy, **sampled-keyset** (NTILE/`ROW_NUMBER()` over the PK index, split by actual row count → skew-free), so tables with a single non-integer orderable PK (UUID/string/binary/decimal/temporal) or a composite PK now get the same N-way fan-out integer-PK tables have had since v0.5.0. Closes the PlanetScale single-connection RTT-bound gap (PS blocks `LOAD DATA` → one pinned-conn batched-INSERT stream at ~13 GB/h; N concurrent streams scale near-linearly, ~128 GB/h at N=3) for the common non-integer-keyed table. Reuses the entire existing chunk machinery unchanged — `ReadRowsBatch`'s row-comparison cursor (already type-agnostic), `[]any` boundary tuples, resume/checkpoint, the ADR-0076/connection-budget gate; the only integer-specific code lifted is `canParallelChunkTable`, boundary computation, and `filterByUpperBound` (now a type-family-dispatched tuple comparator — the Bug-74-class exactly-once surface, pinned across every family × shape). New optional `ir.KeysetSampler` read surface; no CLI/config change (strategy is derived from PK type, so no zero-value-default trap). Exactly-once coverage rests on half-open `(LowerPK, UpperPK]` tuples over the engine's own `ORDER BY` total order. `-race`-before-tag. |
| [0097](adr-0097-parallel-writer-fanout-vstream-snapshot-copy.md) | Accepted — write-side parallel fan-out on the VStream/CDC snapshot cold-start copy (the PS-MySQL gap: vtgate blocks `LOAD DATA`, so the writer falls back to one cross-region-RTT-bound batched-`INSERT` connection → ~13 GB/h; N independent processes scaled near-linearly to ~128 GB/h at N=3). Read-side PK-range chunking is impossible (vtgate streams the snapshot; no arbitrary range-`SELECT`), so the only lever is WRITE-side: one reader goroutine **PK-hash-partitions** the single incoming row stream to N per-worker channels, each worker runs the existing idempotent batched-INSERT core on its **own** pinned connection (~1 MB buffer). PK-hash (not round-robin) pins every emission of a PK to one worker so Bug-125 COPY re-emissions serialize cleanly; ADR-0010 idempotent upsert makes cross-worker interleaving irrelevant. Engine-neutral pipeline routing + a new opt-in `ir.ParallelIdempotentCopyWriter` capability (MySQL implements; PG stays serial on its raw-COPY path). **No position advances until every worker is durably flushed** (ADR-0007 handoff guard); the mid-COPY ADR-0072 durable-progress checkpoint is **disabled on fan-out tables** (the whole-table join at COPY_COMPLETED is the sole durability guarantee — under fan-out a flat flushed-row count is not order-equivalent to an enqueue-order frontier, so a mid-COPY breadcrumb could checkpoint past an un-flushed early row); any worker error aborts loudly (no position); ctx-cancel leaks nothing. Zero-value-safe degree int (`0`→default 4, `1`→serial; the v0.99.51 trap). Resume composes via the ADR-0098 auto-shard-aware resume (the fan-out runs against the one active table at a time, whether fresh or resumed). `-race`-before-tag. |
| [0098](adr-0098-auto-shard-aware-vstream-resume.md) | Accepted — fixes a live HIGH bug ADR-0095 deferred as "a v1 limitation, not a correctness gap" (the judgment was wrong). ADR-0095 auto-shards the VStream cold-copy by table (one single-table COPY at a time, bounded memory) but gated it on `start == nil` (fresh cold-start only); a `--resume` (`start != nil`, position carrying an ADR-0072 `TablePKs` cursor) fell back to the **legacy single keyspace-wide interleaved stream** → ADR-0071 buffer-cap crash-loop. Found live: a resumed 28-table / 302 GB keyspace copy crash-looped (`table "binary_blobs" would buffer … exceeding --max-buffer-bytes … while table "audit_trail" is being copied`); loud but **fatal-for-resume** — the interleaved path can never make progress on a large keyspace, so resume of exactly the case auto-shard exists for was broken. Decision: drop the `start == nil` gate (`autoShard := len(tables) > 1 && !singleStream`); a multi-table resume drives the SAME per-table pump. The persisted cursor names exactly the one in-progress table (`resolveResumeAutoShard`); the pump re-copies the tables BEFORE it from-beginning (idempotent upsert absorbs the overlap, Bug-125), resumes that table from its cursor (`reopenForTableSeeded`, no row-0 restart), and copies the tables AFTER it fresh — so every table contributes a captured `P_i` and the ADR-0095 set-MIN stitch is the cold-start stitch verbatim (gapless; CDC recorded only at the last table's completion → never advances past an incompletely-copied table, ADR-0007). **State-shape: no new token field** — completion is re-derived from the cursor + the known table order; rejected the skip-re-copy optimization (needs a persisted GTID floor + interval-min arithmetic over GTID sets = Bug-74 risk; re-copy is provably correct by reduction to the cold-start path). Back-compat: a released-code single-in-progress-table cursor round-trips; a legacy multi-table / out-of-scope cursor is **refused loudly** (never silent re-copy/skip). Bounded memory because exactly one single-table COPY is ever in flight (the ADR-0071 interleave refusal is structurally unreachable). `-race`-before-tag. |
| [0099](adr-0099-cross-table-vstream-copy-concurrency.md) | Accepted — the read-side throughput lever for VStream/PlanetScale cold-copy, realizing the bounded-parallel sub-decision ADR-0095 deferred. Live Track-B measurement: ADR-0097's write-side fan-out reached only ~1.4× because the bottleneck is the SINGLE un-splittable vtgate read stream + one dispatcher (the write workers starve); N independent processes, each with its OWN read stream on a DIFFERENT table, scaled ~4× near-linearly. Decision: internalize that — run **K concurrent single-table COPY streams**, each over a DISJOINT subset of the in-scope tables on its own VStream (K independent read streams at K × one-table memory). Each stream is the ADR-0095 per-table auto-shard sub-sequence restricted to its group; every per-table snapshot flows into one shared collection and the ADR-0095 per-shard GTID-set **set-MIN** stitch runs across the UNION of all streams' all tables (parallelism-agnostic — the stitch ADR-0095 anticipated; a genuinely-disjoint per-shard set still refuses loudly). **Three silent-loss surfaces, three guards:** (a) the partition covers every table EXACTLY once (coverage + disjointness, unit-pinned); (b) the global CDC position advances only AFTER all K streams complete all tables (ADR-0007 join-then-stitch ordering); (c) the partition is a DETERMINISTIC pure function of (sorted tables, K) so cold-start and resume re-derive the SAME assignment (sort-before-assign, shuffle-invariant unit pin) — composes with the ADR-0098 resume per stream (the cursor's in-progress table seeds in whichever group holds it; others re-copy fresh idempotently). Opt-in DSN knob `vstream_copy_table_parallelism=K` (engine-internal, like `vstream_copy_single_stream`); zero-value-safe (0/1 → K=1 sequential, byte-identical to ADR-0095/0098 — the v0.99.51 trap). Composes multiplicatively with ADR-0097 fan-out (K read streams × D writers; budget bounds K×D). Mid-COPY durable checkpoint disabled on the concurrent path (K frontiers aren't order-equivalent — ADR-0097 §3 precedent; resume re-copies from-beginning idempotently). Any stream's failure aborts loudly with no position advance; ctx-cancel joins all K (no leaks). `-race`-before-tag. |
| [0100](adr-0100-cross-table-vstream-write-concurrency.md) | Accepted — the WRITE-side companion to ADR-0099. ADR-0099 made the VStream cold-copy READ side concurrent (K independent vtgate VStreams over disjoint table groups, all filling one shared per-table buffer), but live Track-B held at ~1.4× and the target PROCESSLIST showed exactly ONE table receiving rows (28/28 polls): the orchestrator's serial bulk-copy loop (`runBulkCopyWithOpts`) drains tables one at a time, so only one is WRITTEN at a time. K (reads) and D (ADR-0097 per-table fan-out writers) are orthogonal levers on a serial-write workload → their product is capped by the serial consumer. Decision: turn ADR-0099's K read streams into **K end-to-end read→write PIPELINES** — the engine surfaces the EXACT disjoint partition it gave the producers via a new optional `ir.ConcurrentCopyPartitioner`, and the pipeline runs W = K consumer goroutines (one per group, errgroup), each looping its group's tables through the SAME per-table copy helper (so the ADR-0097 D-way fan-out composes → W × D). Chosen over a shared write pool because it reuses ADR-0099's unit-pinned disjoint partition + per-stream byte sub-budget verbatim (no new scheduler, no new invariants) and needs no new knob (W = K, the existing `vstream_copy_table_parallelism`). **Four silent-loss guards:** (a) every table written by exactly one pipeline — inherited from ADR-0099's coverage/disjointness (the consumer reads the SAME groups); (b) global CDC position advances only after the W-way write join AND ADR-0099's K-way producer join both precede the streamer's `stream.Position` read (ADR-0007); (c) mid-COPY durable checkpoint stays DISABLED on the concurrent path — two independent code facts (no engine breadcrumb in the concurrent pump + no pipeline durable-progress wiring), the ADR-0097 §3 silent-loss-on-resume argument; (d) stable partition cold-start vs resume — inherited from ADR-0099's pure-function determinism, composes with ADR-0098 per pipeline. Connection budget W × D surfaced as the honest WARN (W = K not auto-clamped — same honesty as ADR-0099 §3; no false clamp claimed). Zero-value-safe: K = 1 / no surfaced groups → serial loop byte-identically (tied to the existing zero-value-safe knob, no new default trap). Any pipeline error aborts loudly (no position); ctx-cancel joins all W (no leaks). `-race`-before-tag. |
| [0101](adr-0101-native-mysql-concurrent-cold-copy.md) | Accepted — the NATIVE-MySQL (binlog-flavor) analogue of ADR-0099/0100's VStream cross-table cold-copy levers, closing the Track-D gap (vanilla MySQL → PS-MySQL measured ~0.66 MB/s SERIAL vs ~26 MB/s for Vitess, whose concurrency is driven by the VStream-only `vstream_copy_table_parallelism` knob). Native MySQL has no exported/shareable snapshot (PG's lever) and no VStream (Vitess's), so the consistent multi-table snapshot uses the mydumper / `mysqldump --single-transaction --master-data` pattern, generalized from sluice's existing 1-reader binlog snapshot to N: **one held FLUSH TABLES WITH READ LOCK → N reader connections each `START TRANSACTION WITH CONSISTENT SNAPSHOT` (all at the SAME cut) → record ONE binlog position → UNLOCK**. The in-scope tables partition into N disjoint groups (the SAME deterministic `partitionTablesForStreams` ADR-0099 unit-pins) surfaced via the SAME `ir.ConcurrentCopyPartitioner`, so the ADR-0100 pipeline consumer (`runConcurrentTableCopy`) drives W = N read→write pipelines VERBATIM — the only new core is the FTWRL-coordinated N-snapshot opener + a multi-snapshot RowReader that routes each table's `ReadRows` to its group's pinned connection. The snapshot→CDC seam is the ONE recorded position (no stitch — all N readers share one cut, simpler than VStream's set-MIN). **Four silent-loss guards:** (a) all N readers pin the same cut as the recorded position (FTWRL held across all N `START TRANSACTION` + the position read, then released — ordering pinned); (b) every table read by exactly one reader (the disjoint partition IS the table→connection map); (c) CDC position == the recorded position, read only AFTER the W-way errgroup joins (structural, no per-pipeline position write); (d) **FTWRL-unavailable (no RELOAD — RDS/Aurora) FALLS BACK to the SERIAL single-snapshot path with a loud WARN, NEVER opening N independent snapshots** (which could capture an inconsistent multi-table view). New source-DSN knob `copy_table_parallelism=N` (distinct from the VStream knob; stripped before the MySQL session, Bug-126 class); zero-value-safe (absent/0/1 → N=1 serial, byte-identical — reuses ADR-0099's resolver, no new default trap). The ADR-0100 consumer's "non-idempotent partition ⇒ refuse" guard is WIDENED: a gap-free non-idempotent reader (the native binlog snapshot — each table read exactly once) takes the concurrent PLAIN-INSERT path; the safety preserved because a RE-emitting reader is by definition idempotent and takes the upsert branch. Any reader/writer error aborts loudly (no position); ctx-cancel releases the lock + closes all N connections (no leak). `-race`-before-tag. |
| [0102](adr-0102-native-mysql-per-table-write-fanout.md) | Accepted — the documented ADR-0101 follow-up: takes the native-MySQL concurrent cold-copy from **W × 1** (W tables concurrent, ONE plain-INSERT writer each) to **W × D** by composing the ADR-0097 per-table write fan-out onto the PLAIN-INSERT path. Live Track-D (v0.99.70): ADR-0101 native concurrent = ~10.4 MB/s (~15.7× over serial) but W × 1; Vitess W × D = ~26 MB/s — the remaining ~2.6× is exactly the per-table fan-out (PS-MySQL blocks LOAD DATA → batched INSERT, and a single cross-region INSERT connection is RTT-bound). Decision: add `ir.ParallelCopyWriter.WriteRowsParallel` (the plain-INSERT mirror of ADR-0097's idempotent `WriteRowsIdempotentParallel`, calling the engine's plain `writeBatchedConn` core) + a `copyTablePlainMaybeParallel` pipeline helper that **reuses ADR-0097's `partitionRowsByPK` PK-hash routing VERBATIM** (only the per-worker write call differs — plain vs upsert); the ADR-0100 consumer's native branch now calls it, so each W pipeline fans its active table across D plain-INSERT workers → W × D. Chosen over routing native through the idempotent upsert path (would refuse no-PK tables, pay the upsert SQL cost on a fresh target, and conflate the gap-free vs re-emitting distinction `needsIdempotent` carries). **Three silent-loss guards:** (a) every row written by exactly one of the W × D workers — across W the disjoint partition (ADR-0101/0099), across D the PK-hash routing (ADR-0097), both unit-pinned; (b) the fresh-target precondition (#52 reset + gap-free snapshot) makes PLAIN INSERT correct under fan-out — no overlap, a stray double-write would surface as a LOUD duplicate-key error, never silent; (c) a no-PK table cannot be PK-hash-partitioned → routes to the single-writer serial `copyTable` (fully copied, NOT refused — unlike the idempotent keyless case; the gap-free snapshot has no re-emission to duplicate). Connection budget W × D + N readers is the operator's responsibility on this path (MySQL has no slot prober) with an honest WARN naming `W × D` — no false auto-clamp. Mid-COPY durable watermark stays disabled (the ADR-0097 §3 / ADR-0100 §6 argument); the whole-table join is the durability guarantee. Zero-value-safe: **D = 1 is byte-identical to ADR-0101's W × 1**; W = 1 gains 1 × D per-table fan-out (a strict improvement); no (W, D) input copies nothing. Composes the two existing knobs — `--copy-fanout-degree` (D) + the `copy_table_parallelism` source-DSN param (W = N) — no new knob. Also folds a log-clarity fix: the ADR-0079 cold-start gate no longer logs "using serial cold-start" when the source surfaced a concurrent-copy partition (the native ADR-0101/0102 path) — it bit the Track-D measurement interpretation. `-race`-before-tag. |
| [0103](adr-0103-forward-index-ddl-during-cdc.md) | **Proposed** (planning sketch, NOT implemented; demand-gated; no silent loss — a schema-fidelity gap only) — forward a secondary-index / index-only DDL added to a source AFTER cold-copy onto the target during continuous CDC sync. Confirmed live 2026-06-18 (VStream→PS-MySQL + VStream→PS-PG) + code-confirmed for all three CDC readers: sluice HAS the apply arm (`ShapeKindCreateIndex`/`DropIndex` → `applyShapeForward` → `ir.ShapeDeltaApplier`) but NO CDC reader populates index metadata into the live `ir.SchemaSnapshot` (VStream `projectVStreamFields` is columns-only; binlog `tableSchema` is columns+PK by design; PG `cdc_normalize` sets `Indexes=nil`), so `ClassifyShape` never sees the index shape on the live path. **Detection is the hard, source-specific half; apply mostly exists.** Detection tiers: (a) vanilla MySQL — extend the in-band on-DDL re-introspection + a separate index-vector forward signal (reuses the ADR-0091 F7a GAP-#2 nullability-signal pattern, kept out of the ADR-0049 decode signature) [Phase 1]; (b) Vitess/PlanetScale — primary-routed `SHOW INDEX` re-introspection on the post-cutover FIELD re-emit (preferred over parsing `SHOW VITESS_MIGRATIONS`), since online DDL cuts over via a `_vt_` shadow-rename with no literal `ALTER` (ADR-0073(c)) [Phase 2]; (c) PostgreSQL — event-trigger DDL capture (ADR-0066) or periodic re-introspection, since logical replication streams no DDL [Phase 3]. Apply tiers use each target's NON-BLOCKING primitive: PS-MySQL via a **deploy request** (`planetscale-go` `client.DeployRequests.Create`/`Apply` + `client.DatabaseBranches`; the headline path — note a deploy request carries the dev-vs-prod branch DIFF, not an inline ALTER, so apply = create dev branch → run DDL on it → open + deploy the request), self-hosted MySQL via `ALGORITHM=INPLACE, LOCK=NONE`, PG via `CREATE INDEX CONCURRENTLY`. Index-apply ordering vs the data stream does not matter for correctness (an index isn't data), which makes async apply clean. New control-plane credential surface (PS service token, distinct from the data-plane DSN) → **opt-in or report, never silent auto-apply** (index lifecycle is explicit). |
| [0104](adr-0104-mysql-pipelined-cdc-apply.md) | **Accepted** (SHIPPED v0.99.77 PREVIEW → GA v0.99.80, realized as `--apply-concurrency` key-hash lane apply; MySQL-target counterpart of ADR-0092, roadmap item 23(c)) — close the cross-region multi-shard Vitess/PlanetScale → PS-**MySQL** CDC apply-throughput deficit that CAUSES the item-23 per-shard wedge (apply < source → one shard backlogs → vtgate `MinimizeSkew` holds the peer → permanent hold; a reopen re-forms it, so the durable fix is apply throughput — the decisive contrast being the SAME source → PG keeps both shards caught up *because* of ADR-0092). The direct ADR-0092 port is NOT mechanically available (go-sql-driver/mysql has no `pgx.Batch`/`SendBatch` statement-pipelining; `multiStatements` would inline values and break value fidelity, the rejected simple-protocol class), so the MySQL lever is overlapping cross-region commit RTTs across K independent transactions, not a within-tx flush. **Phased: Phase 1 (ship first) = in-order pipelined commit** — a bounded in-flight window of W transactions on a dedicated pipeline pool (mirroring ADR-0092's pool + raw-escape-style + one-time-WARN-fallback shape), dispatched in source order and committed **strictly in submission order** (a dependent UPDATE can never commit before its INSERT; only later-batch dispatch overlaps earlier-batch commit-wait — the win, with the linearization point staying serial — the correctness); **Phase 2 = per-shard apply pipelines** (cross-shard rows disjoint by the hash-vindex → independent in-order pipelines, attacking the `MinimizeSkew`-hold root, deferred behind a merged-channel demux + Phase-1 measurement); **Phase 3 = per-key/per-table concurrency** (out of scope first cut). Reuses the build{Insert,Update,Delete}SQL + prepareApplierValue codecs byte-for-byte (value encoding unchanged; only WHEN a tx commits), preserves ADR-0007 position+data-in-one-tx (persisted position = highest contiguously-committed batch; merged `[]shardGtid` monotone under the per-shard superset `PositionOrderer`, a regression refused loudly), composes with ADR-0010 idempotent replay (crash-replay window grows to W batches, benign for keyed/unique tables), the ADR-0089 keyless guard (keyless Inserts drain the pipeline window so their at-least-once replay window stays the `=1` baseline; Bug 143 unchanged), and the AIMD + #54 `ir.TransactionKilledError`/`OnShrink` tx-killer convergence (per-pipeline-tx shrink + idempotent re-apply IS the split). Per-shard lag stays observable (item 23 B-1 WARN). New dedicated pipeline pool budget (MySQL has no connection-budget prober → operator-honest opt-in depth knob, zero-value-safe: 0/1 ⇒ serial byte-identically; WARN names the W backends, no false auto-clamp). `-race`-before-tag (concurrency chunk). |
| [0105](adr-0105-postgres-concurrent-cdc-apply.md) | **Accepted** (SHIPPED v0.99.82; the Postgres counterpart of ADR-0104, roadmap item 26) — extend the key-hash concurrent CDC apply to PG targets. Extracted the engine-neutral router + commit frontier + orchestration into a new `internal/laneapply` package (MySQL re-wired onto it byte-identically), so `--apply-concurrency` is now ENGINE-GENERAL. PG seam: per-lane `ON CONFLICT` UPSERT, in-lane `40001`/`40P01` (serialization/deadlock) split-retry, `cacheMu`-guarded metadata caches, and a geometry-codec lane pool. Live-validated on Track B2 (cleared a ~16K-GTID cross-region PG backlog in ~2 min where serial couldn't keep up); `-race` + PostGIS gates green pre-tag. The exactly-once frontier ("advances only to a fully-durable source-tx boundary; must NOT persist a partial point") is the shared contract both engines now share. (Bug 158, the PG first-boundary phantom silent-loss in this path, was caught by the post-publish regression cycle and fixed in v0.99.83.) |
| [0106](adr-0106-default-adaptive-apply-concurrency.md) | **Accepted** (implemented; roadmap item 31; operator-flagged 2026-06-21; resolution in `pipeline.Streamer.resolveApplyConcurrency`) — make `--apply-concurrency` **fast by default**: resolve `0 → auto:N` (connection-budget-bounded, `min(4,budget)` on PG via the slot probe; a fixed conservative ceiling of 4 on MySQL/PlanetScale which has no probe), keeping `=1` as the explicit serial opt-out — mirroring `--table-parallelism`'s `0=auto:4 / 1=disable`. Today CDC apply is the LONE speed axis still defaulting to serial; every cold-copy axis (`--table-parallelism`, `--bulk-parallelism`, `--copy-fanout-degree`) is already `auto`. Safe to flip NOW because the path is GA + hardened: exactly-once frontier on MySQL (ADR-0104) **and** PG (ADR-0105), per-lane AIMD self-throttle, in-lane tx-killer re-chunk (v0.99.80/81), Bug 158 fixed (v0.99.83), and the serial fallback hardened (v0.99.89/item 29). **Load-bearing design rule:** resolve the default at the `Streamer` construction level (NOT only the CLI) — the `AutoTune` precedent — or the zero-value-safe-default trap (v0.99.51 / Bug-158 family) silently reverts every test/broker/programmatic path to serial. Ships as its own release with a full regression cycle + `-race`-before-tag (changes a default for every user). Live PS-10 (item 30) is the worst-case safety evidence. |
| [0107](adr-0107-planetscale-metrics-integration.md) | **Accepted** (Phases 1–3 shipped v0.99.92/95/106, roadmap item 32; operator-flagged 2026-06-21) — consume PlanetScale's per-branch Prometheus metrics (CPU/memory/storage first; lag/connections secondary) so the apply path reacts **proactively** and operators see target health in-tool. Engine-neutral seam = new `ir.TargetTelemetry` advisory interface + plain `ir.TargetHealthSnapshot` report (committed interface-only with the ADR); the optional PlanetScale provider (control-plane service token w/ `read_metrics_endpoints`, distinct from the data-plane DSN — ADR-0103 token posture) lives in its own `internal/planetscale/telemetry` package and is CONFIG-WIRED at `cmd/sluice` (NOT a target-engine method — the credential is control-plane, so an engine method would leak PS into core), so `internal/ir`/`internal/pipeline` never import it (IR-first / contain-PS-complexity). Three uses: **(a)** proactive back-off feeding item-31 per-lane AIMD + the MySQL no-connection-probe gap; **(b)** storage-resize anticipation pairing with items 30/33 (detect→explain→ride-through); **(c)** observability via `sluice diagnose` + new `sluice_target_*` `/metrics` gauges. **Advisory-only:** the frontier/AIMD/tx-killer stay authoritative; a provider outage degrades to today's reactive behaviour (`Sample` ok=false / stale `Fresh`), never stalls or corrupts. The inverse "export sluice's OWN richer metrics" sub-item is SPLIT out (engine-neutral, no PS cred — most already ships in `internal/pipeline/metrics.go`). Non-goals: does NOT replace item-19's data-plane `SHOW VITESS_THROTTLED_APPS` (no throttler metric in the set); no PS leakage into core; never a stall/pause control input. Impl plan: `adr-0107-impl-plan.md`. |
| [0108](adr-0108-coldcopy-reparent-retry.md) | **Accepted** (roadmap item 33; the copy-phase analog of ADR-0038, scoped to the MySQL cold-copy write path) — make the bulk cold-copy ride through a transient target **primary reparent / "not serving"** (e.g. a PlanetScale non-Metal storage auto-grow at the ~39 GB boundary triggers a primary reparent) instead of aborting the process. Live Track-D: on the reparent the cold-copy EXITED and the watchdog crash-looped 9 relaunches, each re-hitting the still-in-progress reparent. The CDC APPLY path already rides this (ADR-0038 classifier → `ir.RetriableError` → pipeline backoff) but the cold-copy RowWriter flush returned the raw driver error unwrapped. Decision: a bounded reparent-retry helper (`flushWithReparentRetry`) wrapping the per-batch flush in BOTH the idempotent (`writeBatchedIdempotentConn`) and plain (`writeBatchedConn`) paths + their parallel-worker callers; it routes the error through the SAME `classifyApplierError` and retries ONLY a classified `ir.RetriableError` (terminal otherwise, unchanged). **Load-bearing:** the pinned conn is DEAD after a reparent, so every retry RE-ACQUIRES a FRESH conn from `w.db` (pool reconnects to the new primary) and re-runs the exec + `SHOW WARNINGS` probe on it — never reuses the dead one. Bounds = 12 attempts × 100ms→30s cap (~4 min, sized for a 30–60s / up to ~2–3 min reparent), loud WARN per retry, loud TERMINAL on exhaustion, honors `ctx.Done()` during backoff — never infinite, never silent (baked constants, no config field → no zero-value trap). Classifier extended with case-insensitive "not serving"/"reparent" text fallback (pinned slice `reparentRetriableSubstrings` + change-detector test) for an un-framed PlanetScale/vtgate reparent — also benefits the apply path. **Plain-path value-fidelity wart (named, commented, pinned): tolerate-1062-on-retry** — a plain batch is a SINGLE atomic INSERT; on a transient it either fully rolled back OR committed-but-lost-the-ack, so a 1062 on the RETRY of the byte-identical batch (sole writer, fresh target) PROVES those rows already landed → tolerate (no silent loss). Scoped to `isRetry` ONLY: a FIRST-attempt 1062 stays terminal (real dup-key / dirty target fails loudly). Idempotent path needs no wart (UPSERT absorbs it). Fan-out: a transient on one worker now retries locally instead of aborting siblings; only an exhausted/non-retriable flush aborts peers (loud abort preserved). **PG COPY-path gap NOTED as a follow-up, not implemented here.** Concurrency chunk: `-race`-before-tag. |
| [0109](adr-0109-coldcopy-source-read-resume.md) | **Accepted** (roadmap item 34; the READ-side sibling of ADR-0108, scoped to the MySQL cold-copy source-read path) — make the bulk cold-copy survive a transient **source-read connection drop** (reconnect + resume) instead of aborting the run. Live Track-D PS-320: the target's **binlog volume hit `errno 28` (No space left on device) during a non-Metal storage auto-grow** (fast bulk load → enormous binlog churn fills the volume before it grows) → target replica SQL thread failed → semi-sync **primary write STALLED (blocked, not errored)** → pipeline backpressure idled the source read past `net_write_timeout` (60s) → source closed it → `invalid connection`/`unexpected EOF` → copy aborted on the SOURCE side. ADR-0108 (target-write retry) cannot catch this — the write *blocked*, so the first error was on the read side. Decision: a bounded reconnect+resume retry around the per-table read, reusing `classifyApplierError` (retries only `ir.RetriableError`). **Resume-position safety by path:** (1) keyset-chunked (the big-table path) resumes from the persisted `chunk.LastPK` (`WHERE pk > LastPK`, dup-free + loss-free — existing crash-resume); (2) idempotent UPSERT resumes from the durable frontier (overlap absorbed); (3) plain single-stream / keyless / non-orderable-PK falls back to **truncate-target + restart-table** (always correct, less efficient). Bounds mirror ADR-0108 (12×100ms→30s, loud WARN/terminal, ctx-aware, baked constants); per-table (a transient resumes locally, siblings unaffected). Together with ADR-0108 the cold-copy rides through BOTH faces of a PlanetScale storage-auto-grow (reparent-*error* and disk-full-*stall*). PG COPY-source path noted as a follow-up. Concurrency/crash-recovery chunk: `-race`-before-tag. |
| [0110](adr-0110-coldcopy-coordinated-grow-pause.md) | **Accepted** (2026-06-21; roadmap item 37; v0.99.100) — the *proactive* deepening of the reactive storage-grow arc (ADR-0108 reparent-retry / ADR-0109 source-read-resume). An engine-neutral `ir.GrowGate` (+ `ir.GrowGateSetter`) seam (`internal/ir/grow_gate.go`, coordinator in `internal/pipeline/grow_gate.go`) lets all cold-copy write lanes AND the source-read lane quiesce together on a PlanetScale storage-grow / reparent window — telemetry-driven trip + recovery probe (`streamer_telemetry.go`) — instead of each lane discovering the transient independently. `-race`-before-tag concurrency chunk. |
| [0111](adr-0111-native-mysql-resumable-coldcopy-resnapshot.md) | **Accepted** (2026-06-22; task #96) — brings the **native vanilla-MySQL** concurrent cold-copy path to the resilience parity ADR-0072 gave the VStream (PlanetScale/Vitess) cold-start: in-process **re-snapshot-from-cursor** recovery so a source-read drop resumes incomplete tables instead of restarting from row 0. The CDC-anchor decision is silent-loss-class → mandatory value-fidelity review + pinned differential tests. `-race`-before-tag concurrency + value-fidelity-critical chunk. |
| [0112](adr-0112-restore-within-table-chunk-parallelism.md) | **Accepted** (2026-06-23; relates ADR-0084/0019/0076) — within-table chunk parallelism for `sluice restore`. Restore parallelized *across* tables (ADR-0084) but was single-streamed *within* a table (one in-order producer → one `WriteRows`), so a few-very-large-tables corpus or an INSERT-bound cross-region PlanetScale target collapsed restore wall-time (the operator's RTO) to "slowest single table, serially." Adds bounded intra-table chunk fan-out. |
| [0113](adr-0113-restore-reparent-reconciliation.md) | **Accepted** (2026-06-23; relates ADR-0112/0110/0108) — fixes a CRITICAL silent under-copy: ADR-0112's concurrent restore dropped rows (`rc=0`, every table logged complete, six tables short by ~17 M) into a fresh non-Metal PlanetScale MySQL target during its first storage-grow reparent — the grow reparents to a replica behind the async-acked window, so committed-and-acked rows are dropped, and 8-wide writers build a large async window. The reactive grow-gate (ADR-0110) *reduced* but couldn't *eliminate* the loss (the lost rows were acked BEFORE any worker saw a transient). Adds post-copy reparent-reconciliation that re-derives and re-copies the reparent-touched tables. |
| [0116](adr-0116-mysql-connection-budget-prober.md) | **Accepted** (roadmap item 41) — implement `ir.TargetConnectionBudgetProber` on the MySQL engine (both flavors), so the connection-budget preflight (`resolveTargetCopyParallelism`, ADR-0076) — previously a no-op for MySQL targets (only Postgres had a prober) — now auto-caps the bulk-copy parallelism for MySQL too. No pipeline change: the resolver discovers the prober structurally. **Part A (the self-hosted-MySQL bound):** mirror the Postgres impl — `@@max_connections`, `Threads_connected`, per-user `MAX_USER_CONNECTIONS`/`@@max_user_connections` → `RoleLimit` (non-positive ⇒ the `unlimited` -1 sentinel, MySQL has no per-db cap so `DatabaseLimit`=unlimited), `Available = min(max−inuse, roleLimit)`, `CopyBudget = Available − reserve(4)`, fold `requested`+`ceiling` into `EffectiveParallelism`/`Capped`/`Refuse` identically. The role term is the per-user LIMIT itself (MySQL has no cheap per-user current-conn count); the server `Threads_connected` term bounds the real exhaustion case. **Part B (the no-credential PlanetScale CPU bound):** on PlanetScale connections are abundant (vtgate pool, `conns=6/250`) but CPU is the scarce small-tier resource (PS-10 = 1/8 vCPU pins at 100%), and there's no credential-free CPU read — but `@@innodb_buffer_pool_size` scales MONOTONICALLY by tier and is NOT vtgate-masked (live PS-10 0.125 GB → PS-160 9.80 GB). Use it as a tier proxy → a parallelism CAP bucketed `<256 MB→2 / <2 GB→4 / <8 GB→6 / >=8 GB→8` (named consts, change-detector-pinned), folded as the MIN of the connection-derived CopyBudget and the cap; applied for BOTH flavors (harmless safe cap on self-hosted MySQL too). **Loud-failure-safe — worst case is wrong parallelism, never data loss.** Managed-quirk tolerance is the load-bearing property: a `mysql.user` access-denied (the common managed/PlanetScale posture) degrades the per-user term to unlimited (NOT a probe failure); an unreadable buffer pool makes the cap a no-op; only the two always-readable vars are hard requirements (failure ⇒ `ProbeFailed`+`Warning`+non-refusing budget ⇒ orchestrator proceeds blind); only a connection-OPEN failure (bad DSN) returns a non-nil error. Never raises, never below 1. Complements (does not replace) the ADR-0115/0107 metrics clamp — that's the robust always-correct CPU bound when telemetry is configured, this is the credential-free heuristic + the self-hosted connection bound (they compose; both only reduce). Pinned: the connection-budget formula matrix (role-capped, tier-capped, refuse-when-<1) + the buffer-pool bucket change-detector + integration tests (sane numbers, ceiling cap, per-user-limit dominates, `mysql.user`-denied degrades non-refusing, bad-DSN errors). |
| [0115](adr-0115-restore-headroom-parallelism-clamp.md) | **Accepted** (roadmap item 40; v0.99.119) — clamp `sluice restore`'s AUTO `--table-parallelism × --bulk-parallelism` product by the target's LIVE CPU/mem headroom, the PlanetScale-correct bound. The connection-budget split (ADR-0076) only bounds prober-equipped engines (Postgres); a MySQL/PlanetScale auto fan-out passed through unbounded, and on PlanetScale connections are abundant (vtgate pool) while CPU is the scarce small-tier resource (PS-10 pinned at 100% CPU during the Track-C restore A/B). Decision: extract the `{1,2,4}` headroom-divisor thresholds from the apply-path `clampConcurrencyByHeadroom` into a shared `headroomDivisor(ctx, ir.TargetTelemetry)` (behaviour-preserving refactor — apply-path tests stay green), add optional `TargetTelemetry` to `Restore` (wired from the `restore` command's `--planetscale-*` flags, mirroring `sync start`), and reduce the auto product in `resolveRestoreParallelism` when headroom is tight (cross-table axis absorbs first, preserving within-table chunk fan-out). Advisory + telemetry-gated (no provider ⇒ no-op, pre-ADR-0115 byte-for-byte); never raises; never clamps an explicitly-pinned axis; floors at 1. Startup-only (restore is fixed-fan-out-per-run); the per-worker reparent-retry (0108) + grow-gate (0110) remain the reactive floor. Pinned: product-clamp threshold table + pinned-axis-respect + never-below-1. |
| [0114](adr-0114-ddl-phase-reparent-retry.md) | **Accepted** — the DDL-phase sibling of ADR-0108/0109. The cold-copy DATA phase rides a PlanetScale storage-grow / reparent (writers wrapped in `flushWithReparentRetry`, reads in the reconnect loop, coordinated by the grow-gate, restore reconciled from chunks), but the POST-COPY DDL phases (`CreateIndexes`, `CreateConstraints`/FKs, `CreateViews`, `SyncIdentitySequences`) exec DDL directly with NO retry → a grow landing on the DDL phase aborted the whole migrate/restore after a byte-perfect data copy. Live Track-C cross-engine MySQL→PG restore: all 29 tables landed exactly (312,970,106 rows, 0 mismatches) riding the grow, then `create index … on live_churn` died `57P01 terminating connection due to administrator command` on a SECOND reparent. NOT PG-specific — same `sw.Create*` call sites in BOTH `restore.go` and `migrate.go` (3 variants); MySQL/Vitess reparents identically (1105 "not serving"/read-only). Decision: an engine-general `runDDLPhaseWithReparentRetry` helper wrapping every post-copy DDL phase, gated by a new optional `ir.TransientClassifier` the engine SchemaWriters implement by delegating to the SAME `classifyApplierError` the apply path uses (the DDL path returns the raw driver error, so the verdict was invisible). Bounded ~30-min wall-clock envelope (mirrors ADR-0108/0109), loud-on-exhaustion, no retry on a real DDL fault. Relies on (does not add) each phase's existing idempotency (IF NOT EXISTS / detect-then-skip / setval). No grow-gate threading (DDL runs serially after copy — no lanes to coordinate). **Residual (roadmap item 42):** the PG migrate overlapped copy+index path (`BuildTableIndexesFromChannel`) needs an engine-level retry; the live-failing restore path uses the now-wrapped `CreateIndexes`. |
| [0094](adr-0094-reshard-streamer-reopen-wiring.md) | Accepted — wires the already-built, chaos-test-proven VStream reshard `Reopen` (`internal/engines/mysql/cdc_vstream.go`: `ShardLayoutChangedError` + journal-stamped new-shard GTIDs) into `pipeline.Streamer` so a source reshard (split/merge/MoveTables) is **followed automatically and exactly-once**, instead of the prior LOUD-terminal exit (`ShardLayoutChangedError` isn't `RetriableError` → the Streamer exited; `Reopen` was dead code from the product's view). New optional capability `ir.ReshardReopener` (keeps the engine-specific typed error private; binlog MySQL / Postgres are a no-op); a **bounded** reopen loop in `runOnce`'s apply phase re-wires the intercept chain over the new channel (no cold-start seed re-injection) and re-enters `dispatchApply`; position persists via ADR-0007 (no re-snapshot, no gap/overlap — the seam rides the journal GTIDs). Rejected Option B (document halt-and-resume) — leaves `Reopen` dead, forces a full re-snapshot per reshard, and leaves the restart path untested. **Shape-A (`--inject-shard-column`) reshard interplay deferred** to a later ADR (stays loud-terminal). Gating `-race` via a fake-reader unit test of the reopen loop; real end-to-end via a new Streamer-level test in scheduled `extended-suites.yml` (`vitessreshard`). `-race`-before-tag. From the `planetscale/fivetran-source` #90 sharded sweep. |
| [0117](adr-0117-restore-chunk-read-transient-retry.md) | Accepted — transient-retry for object-store chunk reads on the `sluice restore` path; the read-side analog of ADR-0114's DDL-phase reparent retry, so a transient object-store hiccup during a chunk read retries instead of aborting the restore. |
| [0118](adr-0118-cli-flag-naming-consistency.md) | Accepted (implemented v0.99.127) — CLI flag-naming consistency via aliases over renames (reuses the ADR-0091 `--forward-schema-add-column` deprecation pattern), so the two-axis copy-parallelism / backup-restore / apply-concurrency flags read consistently without breaking existing flag names. |
| [0119](adr-0119-intra-table-pk-range-work-stealing.md) | Accepted (roadmap item 21, tier (b)) — intra-table PK-range work-stealing for the native-MySQL concurrent cold-copy: an idle copy pipeline steals a PK-range chunk of an in-progress LARGE table (building on tier (a) table-level work-stealing) so the copy stays budget-wide down to the tail instead of pinning at one reader. |
| [0120](adr-0120-relax-minimizeskew-concurrent-apply.md) | Accepted (roadmap item 27) — **DEFAULT FLIPPED to relaxed (`MinimizeSkew=false`)** on the multi-shard VStream concurrent-apply path: let both shards stream and drain concurrently during an apply-deficit backlog instead of vtgate holding the ahead shard back, a throughput/liveness fix justified by a live Vultr cross-region A/B (the old default FROZE the lagging shard). |
| [0121](adr-0121-delayed-replica-apply-delay.md) | Accepted (roadmap item 46) — delayed-replica CDC apply (`--apply-delay DURATION`): hold each change until its source commit timestamp + the delay has elapsed before applying it (the MySQL `MASTER_DELAY` "oops window" DR pattern); engine-neutral, off by default, exactly-once across a crash mid-delay-window via the upstream-of-applier gate. |
| [0122](adr-0122-sync-supervisor-command-center.md) | Accepted (roadmap item 47, minimal-first) — sync command center: `sluice sync run --config syncs.yaml` supervises N failure-isolated syncs in one process (bounded-backoff restart, loud refusal of duplicate slot-names / stream-ids) + `sync status --all` fleet rollup; §7 adds SIGHUP hot-reload (reject-and-keep-running). |
| [0123](adr-0123-pg-migrate-intra-table-work-stealing.md) | Accepted (roadmap item 21, tier (b) for the migrate/PG path) — budget-wide intra-table PK-range work-stealing for `migrate` and the PG fast-cold-start parallel copy (ADR-0079): one shared connection-budget semaphore so a large table's surplus chunks steal slots freed by finished peers, the migrate/PG companion to ADR-0119's native-MySQL tail-reclaim. |
| [0124](adr-0124-sync-fleet-web-dashboard.md) | Accepted (roadmap item 47 deferred-polish) — sync fleet web dashboard: opt-in `sluice sync run --dashboard-listen ADDR` serves a read-only auto-refreshing HTML view (`GET /`) + JSON API (`GET /api/fleet`) + `GET /healthz`, derived purely from `Supervisor.Snapshot()`; no data path, no mutation, no controls, failure-isolated like `/metrics`, no-auth (bind to localhost / trusted network). |
| [0125](adr-0125-sync-fleet-tui.md) | Accepted (roadmap item 47 deferred-polish) — sync fleet TUI: `sluice sync tui --connect ADDR`, a full-screen bubbletea/lipgloss terminal client that polls the dashboard's `/api/fleet` JSON (ADR-0124) and renders the live fleet (color-coded table, row selection, detail pane, unreachable banner); read-only, opt-in, decoupled from the supervisor, dependency confined to `internal/fleettui` + its command. |
| [0126](adr-0126-per-sync-planetscale-telemetry.md) | Accepted (roadmap item 47 deferred-polish) — per-sync PlanetScale target-health telemetry in the `sync run` fleet config: `SyncSpec` gains the `planetscale-*` + `suppress-target-metrics-history` + PS-gated `notify-*` keys, the token is env-first (`PLANETSCALE_METRICS_TOKEN_ID` / `PLANETSCALE_METRICS_TOKEN`), opt-in is per-sync all-or-nothing (refused at config load, named by stream-id), and `buildSupervisedFleet` builds + attaches one `ir.TargetTelemetry` provider per telemetry-enabled sync (closed on fleet exit). Additive plumbing — no value-path or global-state change. |
| [0127](adr-0127-per-sync-mysql-zero-date-sql-mode.md) | Accepted (roadmap item 47 deferred-polish) — per-sync MySQL zero-date via a `zero_date` DSN param (stripped before the session via `nativeSluiceParams`, parsed per-reader, `zeroDateInherit` zero-value → the `--zero-date` global default), plus a fleet `zero-date` `SyncSpec` key; `applyZeroDatePolicy` takes the mode as a param. Value-path change shipped under the full temporal family×shape×policy matrix on vanilla + VStream paths + a value-fidelity review. `sql_mode` is already per-sync via the DSN (documented, no code change). |
| [0128](adr-0128-sqlite-d1-migrate-source.md) | **Accepted (prototype)** (roadmap item 49) — SQLite / Cloudflare D1 migrate source engine: a new `internal/engines/sqlite` read-only source (pure-Go `modernc.org/sqlite`, no CGO) implementing the `ir` reader interfaces with `Capabilities.CDC = CDCNone`, reusing the whole `Migrator` pipeline to import SQLite (and `wrangler d1 export` dumps) into Postgres OR MySQL — no pgloader. The load-bearing decision is the SQLite type-affinity → IR mapping (per-row storage-class mismatches refused loudly, not coerced), pinned by a full affinity×storage-class matrix and proven by the cross-engine round-trip into both targets. Native D1 HTTP-API reader, trigger-based CDC, and within-table chunking are deferred; the date/bool convention policy is handled by ADR-0129. |
| [0129](adr-0129-sqlite-date-bool-policy.md) | Accepted (roadmap item 49 follow-up) — SQLite date/bool interpretation policy that makes the source usable on real DBs: declared `DATE`/`DATETIME`/`TIMESTAMP`/`TIME` → `ir` temporal and `BOOL`/`BOOLEAN` → `ir.Boolean` (overriding SQLite's NUMERIC affinity for those spellings); value decode governed by an explicit global `--sqlite-date-encoding` (`iso` default / `unixepoch` / `unixmillis` / `julian`) that REFUSES loudly on a storage-class/encoding mismatch rather than guessing; bool 0/1 + truthy text decoded, any other value refused. Type inferred from the unambiguous declared type, the ambiguous encoding made explicit — loud-failure preserved. `--type-override <col>=text` is the per-column escape hatch. |
| [0130](adr-0130-sqlite-sql-dump-ingest.md) | Accepted (roadmap item 49 follow-up) — the `sqlite` source accepts a `.sql` dump directly: a file-magic sniff (`"SQLite format 3\0"`) routes a binary `.db` to the existing path and any other input to an in-process materialize (modernc `Exec` of the multi-statement dump → temp DB, removed on Close), so a Cloudflare D1 import is one command (`migrate --source-driver sqlite --source dump.sql`) with no `sqlite3` CLI. Auto-skips `_cf_*` internal tables. Transport convenience, not a value-path change (the materialized DB reuses the validated ADR-0128/0129 decode). |
| [0131](adr-0131-d1-http-api-reader-deferred.md) | **Premise corrected by an empirical probe (2026-06-27).** Original (2026-06-26): export-file path recommended, D1 HTTP-API reader deferred as lossy. A real-D1 test DISPROVED the premise: `wrangler d1 export` ALSO rounds integers > 2^53 (D1's server-side export generator), so the export is NOT a lossless escape — it's equally lossy on big ints. The lossless D1 path is a query-API reader reading INTEGER columns via `CAST(... AS TEXT)` (+ `typeof()` for storage class). So the item to build is the CAST-based query reader (ADR-0132); the export path stays the simple default for non-D1 `.db` and D1 without > 2^53 ints. Evidence + corrected table in `docs/operator/sqlite-d1-import.md`. |
| [0132](adr-0132-d1-query-api-reader.md) | Accepted (roadmap item 49) — the lossless Cloudflare **D1 query-API source reader** (`--source-driver d1`, in `internal/engines/sqlite`, reusing its type/decode/date-bool logic): reads a LIVE D1 over the HTTP query API (plain `net/http`, Bearer token env-only) and defeats the JS-52-bit rounding by projecting, per column, `typeof(c)` + `CASE WHEN typeof(c)='blob' THEN hex(c) ELSE CAST(c AS TEXT) END` → exact value + storage class; keyset pagination; migrate-source/CDCNone. Big ints > 2^53 round-trip EXACTLY (the headline), and reads don't take D1 offline (unlike export). Export path stays the simple default for non-big-int/offline imports. |
| [0133](adr-0133-sqlite-schema-feature-detection.md) | Accepted (roadmap item 49 follow-up, #2 of the SQLite queue) — SQLite/D1 source now CARRIES the three schema features it used to silently drop, into the IR fields that already exist (the first-cut "detect+WARN only" premise that the IR can't model them was disproven — `ir.Column.GeneratedExpr`/`GeneratedStored`, `ir.Table.CheckConstraints`, `ir.Index.Predicate`/`ir.IndexColumn.Expression` are already read+emitted by PG/MySQL). Generated columns (via `PRAGMA table_xinfo` hidden-flag + `CREATE TABLE` expr parse), CHECK constraints (paren/string-aware `CREATE TABLE` scan), and partial/expression indexes are carried VERBATIM tagged dialect `"sqlite"` → portable constructs work, non-portable ones (e.g. `strftime`) are LOUD-rejected at target DDL, never silently mistranslated. Includes the writer dialect-guard fix (translate only from the one known other engine; unknown/`"sqlite"`/empty → verbatim, never fed through the wrong MySQL↔PG translator). Applies to both the file/`.sql` reader and the `d1` query-API reader. A SQLite→canonical expression translator for the verbatim tail is the tracked next increment. |
| [0134](adr-0134-sqlite-target-engine.md) | **Accepted (prototype)** (roadmap item 49 follow-up, #4 of the SQLite queue) — SQLite as a migrate TARGET: `internal/engines/sqlite` gains `SchemaWriter` + `RowWriter` (the faithful INVERSE of the reader — emitted declared types round-trip back through `resolveColumnType` + ADR-0129), so `migrate --target-driver sqlite --target ./out.db` works from PG / MySQL / SQLite, enabling X→SQLite→D1 via `wrangler d1 import`. Three load-bearing decisions: (1) IR→SQLite type map; (2) **decimal storage — corrected post-release for Bug 162**: the first cut used NUMERIC affinity + a significant-digit guard, which silently stored money like `19.99` as a binary REAL on disk (the guard misses it — float64-loss is dyadic, not digit-count; the §4 value-fidelity review missed it by reading back via SQLite's 15-digit text conversion). Corrected (v0.99.147) to **TEXT affinity** — the exact decimal string round-trips byte-exact, reading back as `ir.Text` (a documented type downgrade, like JSON/UUID→TEXT); (3) the **ALTER-TABLE** wart — SQLite can't add FK/CHECK post-hoc, so `CreateTablesWithoutConstraints` emits the FULL inline DDL (incl. FK+CHECK), `CreateConstraints` becomes a `PRAGMA foreign_key_check` verification, and the row-copy runs FK-off. tz-aware timestamps land instant-faithful (UTC ISO); `JSON`/`UUID`→`TEXT`. `SyncIdentitySequences` no-op (rowid auto-continues). CDC stays not-implemented (`CDCNone`). |
| [0135](adr-0135-sqlite-trigger-cdc.md) | **Accepted — shipped v0.99.148** (roadmap item 49, #5 of the SQLite queue) — `sqlite-trigger` CDC source engine (Phase 1: local SQLite file → PG/MySQL), composing the `sqlite` engine by delegation and mirroring `pgtrigger` (ADR-0066): per-table AFTER INSERT/UPDATE/DELETE triggers write before/after images into a `sluice_change_log` table; a polling reader emits `ir.Change` with a monotonic-`id` watermark for exactly-once resume; `OpenSnapshotStream` captures `MAX(id)` at snapshot start for a gap-free handoff; a captured-column fingerprint (`sluice_change_log_columns`) refuses loudly on schema drift (closing the silent ADD-COLUMN direction). **Load-bearing value-fidelity decision:** the capture trigger must NOT use `json_object()` (serializes INTEGER as JSON float64 → silently rounds > 2^53, and can't represent BLOB) — it encodes each column as a `(typeof, text/hex)` pair using the SAME proven encoding as the `d1` reader (ADR-0132), reconstructed through the shared faithful decode. Deferred to Phase 2 (ADR-0136): D1-over-HTTP variant. Further deferred: schema-change forwarding (SQLite has no DDL triggers), capture-payload trimming, change-log retention. |
| [0136](adr-0136-d1-trigger-cdc.md) | **Proposed** (roadmap item 49, #5 Phase 2) — Cloudflare **D1** trigger-based CDC over the HTTP query API (`d1-trigger`): runs the ADR-0135 trigger + change-log + polling design over D1's `/query` API instead of a local `*sql.DB`, so a live D1 streams continuously to PG/MySQL. Composes the `d1` engine (lossless cold-start reader, ADR-0132) + reuses the shared faithful `(typeof, text/hex)` capture/decode seam (big ints/blobs exact) + the Phase-1 watermark / `MAX(id)` snapshot anchor / schema-drift fingerprint. Load-bearing: poll the PRIMARY (strongly-consistent) query path, NOT read replicas, to keep commit-order = id-order (a replica-aware mode would re-add a safety-lag). Caveats documented: D1 write-amplification (billable), polling latency/rate-limits. Deferred: retention/prune, payload-trim, schema-change forwarding. |
| [0137](adr-0137-trigger-changelog-retention.md) | **Proposed** (roadmap item 49 follow-up — Bug 165 / shared with pgtrigger Bug 159) — trigger-CDC change-log retention. The trigger engines append a `sluice_change_log` row per change and never reap consumed ones → unbounded source growth (476 MB → 1.06 GB in one run). **Load-bearing correctness:** a row may be pruned ONLY if its `id` ≤ the **target-persisted durably-applied watermark** (minus a margin) — the reader's read-cursor runs ahead of the durable frontier, so pruning on it risks deleting not-yet-applied rows = silent loss on resume. Phase A (this ADR): a safe operator-run `sluice trigger prune` command that reads the target's durable cdc-state position and DELETEs source change-log rows strictly below it (margin-protected; refuses if it can't read the durable position; shared across sqlite-trigger / d1-trigger / pgtrigger, DELETE over file / D1 `/query` / SQL). Phase B (deferred): automatic in-stream pruning via a durable-checkpoint→source-prune hook. |
| [0138](adr-0138-pipeline-concurrent-apply-lanes.md) | **Accepted** (Bug 168, found by the v0.99.152 Vultr cross-region `ewr`↔`ams` round) — pipeline the ADR-0105 concurrent key-hash apply LANES on Postgres. ADR-0092 made the *single-lane* PG batch RTT-independent by queuing onto one `pgx.Batch`, but the default concurrent-lane adapter still dispatched each change as a serial `tx.ExecContext` (N round trips), so cross-region apply stayed ~1/RTT-capped. The lane adapter now uses the same `pgx.Batch` pipeline (`QueryExecModeDescribeExec`, byte-identical wire encoding). Postgres scope; MySQL (no pipelining primitive) is the ADR-0139/0140 follow-up. |
| [0139](adr-0139-mysql-multirow-insert-apply.md) | **Accepted** (Bug 169, found by the v0.99.153 PlanetScale-MySQL target phase) — multi-row INSERT coalescing for MySQL CDC apply, the MySQL counterpart to ADR-0092/0138. MySQL has no pipelining primitive — its single-lane batch loop and ADR-0104 concurrent lanes both dispatch one `tx.ExecContext` per change, so a batch of N is N round trips (~20/s to PlanetScale-MySQL over WAN vs PG's ~5,000/s). Consecutive same-table, same-column-shape, KEYED inserts coalesce into ONE parameterised `INSERT … VALUES (…),(…),… AS new ON DUPLICATE KEY UPDATE …` — one round trip for a run of N. Load-bearing safety (the Bug-74 reason it's safe): every value still binds to a `?` through the same `prepareApplierValue` codec, so the wire encoding is byte-identical — only more placeholder groups. Flushes before any non-coalescable change to preserve apply order. |
| [0140](adr-0140-mysql-coalesce-update-delete-apply.md) | **Accepted** — extends ADR-0139 coalescing to UPDATE and DELETE (the Bug 169 U/D tail; MySQL target only). A keyed, non-PK-changing UPDATE applies as the same multi-row `INSERT(after-image) … ON DUPLICATE KEY UPDATE` upsert (same PK → same lane → source order), so inserts and update-upserts to one table+shape fold into one statement; consecutive keyed DELETEs coalesce into one `DELETE … WHERE pk IN (?,…)` (single-col) / `WHERE (a,b) IN ((?,?),…)` (composite). PK values bind through the same codec (Bug-74 property preserved). Exclusions stay on the serial full-before path: keyless-table U/D, PK-changing UPDATE, non-row events. A rate-limited INFO line (`mysql: applier: coalescing ratio`) reports rows-per-statement so operators see whether coalescing is helping. |
| [0141](adr-0141-migrate-reparent-reconciliation.md) | **Accepted** (Bug 175) — extends ADR-0113 reparent-reconciliation to the `migrate` path. A `sluice migrate` into a non-Metal PlanetScale MySQL target crossing a storage-grow reparent silently under-copied rows (5,500,003 → 5,496,003, `rc=0` "complete") because ADR-0113 wired the reparent observer + reconciliation into `restore` ONLY. The migrate orchestrator now constructs the same run-shared `reparentTracker`, wires its observer onto every cold-copy writer (the same sites as the ADR-0110 grow-gate), and adds a reconciliation phase after the copy+index block (covering BOTH the `IncrementalIndexBuilder` overlapped branch that PG+MySQL actually take AND the non-IIB fallback), before constraints: TRUNCATE + serially re-copy each reparent-touched table from the (replayable, static-source-precondition) SOURCE until a pass drains clean, bounded by `reconcileMaxRounds` with a loud non-convergence error. The reconciliation IS the guarantee (no count gate). MySQL-first scope; PG path untouched. **Correction (v0.99.161):** the first cut (v0.99.160) placed the reconcile in the dead non-IIB fallback `else` branch — inert, since both engines take the overlapped branch — caught by live PlanetScale re-validation; v0.99.161 moved it to cover both branches and the unit test now uses an `IncrementalIndexBuilder` fake. |
| [0142](adr-0142-namespace-rename-map.md) | **Accepted** — per-namespace target rename for multi-namespace `migrate`/`sync` (`--map-database`/`--map-schema OLD=NEW`, repeatable + `sluice.yaml` `namespace_map:`). ADR-0074/0075 route each source namespace to a SAME-NAMED target; this adds an optional source→target rename map (default identity) applied at the target-namespace derivation in both migrate and sync (incl. CDC change-routing). Rename-only over the selection (map-key-not-selected = loud error); map-only ⇒ keys are the selection; two sources → one target refused (extends the fold-collision preflight to mapped names). Reads + `Table.Schema` stamping stay on the source name; no value path touched. Generalizes ADR-0031's single-source `--target-schema` to the fan-out. |
| [0143](adr-0143-orm-metadata-table-skip.md) | **Accepted** — recognize ORM/framework migration-bookkeeping tables (`flyway_schema_history`, `_prisma_migrations`, Rails `schema_migrations`, … ~25 frameworks) and **loud-skip them by default on the CLI** (`--include-orm-tables` opts out). Carrying a source's migration-history table to the target is almost always wrong (the ORM concludes target migrations already ran). Engine-neutral rule table in `internal/pipeline/orm_tables.go`: DISTINCTIVE names match on name alone; GENERIC names (`schema_migrations`/`migrations`/`migration`) carry a load-bearing IR-type column-SHAPE guard so a real application table sharing the name is KEPT with a loud collision warning — never silently dropped. The prune runs after `applyTableFilter` (explicit `--include-table` wins) at every post-read point: migrate single + multi-database, sync cold-start + dry-run + multi-database. `SkipORMTables` is zero-value-safe (false = no-skip for every programmatic/broker/fleet/test caller; only the CLI defaults it on), so identity holds for non-CLI callers. |
| [0144](adr-0144-infer-types.md) | **Accepted** — opt-in, data-validated rich-type inference for SQLite/D1 sources (`--infer-types`, off by default). Promotes conservatively-typed columns (`INTEGER`→`boolean`, ISO-`TEXT`→`timestamptz`/`timestamp`, JSON-`TEXT`→`jsonb`, UUID-`TEXT`→`uuid`) ONLY after EXHAUSTIVELY validating every value conforms (one aggregate `COUNT`/`json_valid`/`GLOB` per candidate, pushed to the source), falling back to the safe type otherwise. Candidate selection is name-hinted (`is_*`/`*_at`/`*_json`/`*_id`…); the data-validation is the safety gate (a non-UUID `*_id` like `cus_abc123` stays `text`). No new conversion code — it computes validated `--type-override` entries and rides the existing Bug-161 override decode, which loud-refuses on any non-conforming value, so it's double-safe (validation + loud-refuse, never silent coercion). tz-aware: `timestamptz` only when all values carry an offset, else `timestamp`. SQLite/D1-source only (other engines already carry rich types). Pinned per type-family (Bug-74 discipline). |
| [0145](adr-0145-d1-local-staging.md) | **Accepted** — lossless local staging for live Cloudflare D1 sources (`migrate --stage-local`). Live D1's HTTP query API has two limits that block `--infer-types` on real data: a `LIKE`/`GLOB` pattern-complexity cap (HTTP 400 code 7500 — D1 rejects the long char-class conformance GLOBs, `uuidGlob`≈356 chars; size-INDEPENDENT) and a per-query CPU ceiling (HTTP 429 code 7429 on a multi-GB full scan). Both were invisible because all tests + the original head-to-head ran against local modernc (permissive `SQLITE_MAX_LIKE_PATTERN_LENGTH`) — the Bug-162 "assert the real path" lesson for D1 SQL dialect. `--stage-local` replicates the live D1 into a BYTE-FAITHFUL local SQLite file first (verbatim `sqlite_master` DDL + exact storage-class copy via the ADR-0132 CAST/typeof projection → integers >2^53 exact, BLOBs/NULLs/generated-cols/indexes preserved; lossless unlike `wrangler d1 export`), then runs the whole migrate (incl. `--infer-types`) against that file via the `sqlite` engine — modernc has neither limit. AUTO-engages for `--infer-types`+D1 (the direct path is structurally broken there) unless `--no-stage-local`; explicit `--stage-local` stages without inference. D1-only; conservative default D1 migrate unchanged (streams directly, no staging). Chosen over Strategy B (chunked validation — fixes only the CPU limit, not the GLOB cap) and a standalone GLOB rewrite. Live-validated; pinned byte-faithful on a modernc mock. |
| [0146](adr-0146-coldcopy-chunk-open-reconnect.md) | **Accepted** — bounded reconnect-retry for a transient connection-drop caught at cold-copy chunk-OPEN (`acquireChunkConn`). Previously only slot-exhaustion (SQLSTATE 53300) retried; a transient `ping: invalid connection` blip aborted the WHOLE migrate (a live 49 GB PG→PlanetScale copy lost ~45 GB of progress). Now the transient-connection class — `driver.ErrBadConn` / `io.EOF` / net-timeout / driver-and-OS text shapes (incl. Windows `wsarecv … did not properly respond`) — reconnects within a bounded ~30-min wall-clock envelope, keeping the chunk's gate token. DUP-FREE by construction: the open fails at ping, before any COPY/WriteRows, so re-running from `chunk.LastPK` (ADR-0096 keyset resume) cannot double-copy. Permanent faults (auth, bad DSN, unknown DB, permission) stay fail-fast; unknown shapes stay fatal. Closes the last connection-OPEN hole in the ADR-0108/0109 reconnect fabric. |
| [0147](adr-0147-olap-large-table-counts.md) | **Accepted** (decision; implementation roadmap-tracked) — use OLAP (`SET workload='olap'; SELECT COUNT(*)` on a DEDICATED connection, the v0.99.15 lesson) for large-table exact counts on Vitess/PlanetScale. OLTP `count(*)` on a wide clustered table (no cheap secondary index) is killed by the max-statement-execution-time wall — errno 3024, ~900 s (live 49 GB PK-only failed at ~661 s); OLAP streams past it (1264 s, IDENTICAL to chunked-PK — chunking is NOT faster, both I/O-bound on the same clustered bytes). Closes the gap where composite/string/UUID/no-PK tables fall to `singleShotCount` = plain `count(*)` and fail at scale. Keep `chunkedCount` in place during the transition (single-int-PK); follow-up evaluates full removal. Narrow-indexed tables keep the auto-narrowing plain `count(*)`. |
| [0148](adr-0148-planetscale-deploy-request-index-build.md) | **Accepted — REVIVED and IMPLEMENTED** (2026-07-15, roadmap item 67, on the ADR-0162 foundations; originally validated-but-deferred 2026-07-02, when `--upfront-indexes` + the errno-3024 hint shipped instead). Build deferred secondary indexes via PlanetScale's **deploy-request workflow** (dev branch → `ALTER ADD INDEX` on it → deploy request → VReplication deploy → poll → skip-revert → delete branch) as the **automatic fallback** when a direct post-copy `ALTER` hits the errno-3024 statement-time wall or the errno-1105 safe-migrations direct-DDL block on a PlanetScale target — post-copy, on the already-migrated data, so no re-copy and no `--upfront-indexes` ~11× load penalty. Implementation posture: REQUIRE safe migrations already ON (never toggled — unavailable ⇒ the original error + the existing hint, unchanged); armed opportunistically via `migrate --planetscale-org` + the `PLANETSCALE_SERVICE_TOKEN_ID`/`_TOKEN` env pair (database derived from the target DSN); one dev branch + one DR per table batching every still-pending index DDL; a conservative 64 GiB `DATA_LENGTH` probe skips the doomed ~900 s direct attempt for clearly-huge tables; reuses the ADR-0162 client/poller/freshness gate via the new generic `expandcontract.LegRunner`; error surfaces reuse `SLUICE-E-INDEX-STATEMENT-TIME-LIMIT` / `SLUICE-E-PS-DEPLOY-REQUEST-FAILED`. Honest speed framing: the deploy is async VReplication — real wall-clock, unbounded by the wall; the value is UNBLOCKING, not speed. |
| [0149](adr-0149-backup-within-table-chunking.md) | **Accepted** — within-table read chunking for `sluice backup full` (perf-parity gap #1: one huge table backed up single-stream while migrate chunked it). Reuses migrate's chunk machinery verbatim (`canParallelChunkTable`, MIN/MAX + sampled-keyset boundary planners, `readChunkBatch` with the SQL-side inclusive upper bound, the `clampParallelChunkCount` core) inside the ADR-0084 table pool: an eligible table's worker plans N half-open PK ranges pre-stream and streams them via range readers minted from the SAME exported snapshot (the pool's free-reader channel + importer factory), all bounded by ONE run-wide gate sized to `--table-parallelism × --bulk-parallelism` (base tokens per table worker — the ADR-0123 discipline, with tail work-stealing). Gate: LAZY-importer snapshot only (eager MySQL FTWRL + non-snapshot fallback stay single-stream per table, one INFO names it); chunk-shape + surfaces + `resolveBulkParallelMinRows` threshold per table. Budget split is table-axis-FIRST (swapped vs migrate) so cross-table behaviour is byte-identical and a single-huge-table corpus gives the within axis the full width. Includes the 59c55e27 estimator-trap fix for backups: the snapshot primary gets `estimatorDSN`+`estimatorExactCount`, importer-minted readers opt in via the new `ir.ExactCountEstimateOptIn` (sync cold-start unchanged). Chunk-file naming, manifest shape (arrival-ordered chunk SET), Bug-135 re-stream resume, and the ADR-0085 anchor are all unchanged. New flags: `backup full --bulk-parallelism` / `--bulk-parallel-min-rows`. Reached: BK × PG lazy-snapshot; explicit gaps: BK × MySQL-eager / non-snapshot / SQLite / VStream (cited in the ADR). |
| [0150](adr-0150-mysql-byte-targeted-insert-batching.md) | **Accepted** — byte-targeted ~1 MiB INSERT batching for the MySQL bulk-load path (roadmap item 54's one real PlanetScale write lever: Vitess/PlanetScale has no LOAD DATA, so batched INSERT IS its bulk path, and the fixed 500-row cap produced 50–100 KB statements — 10–20× of WAN round-trip amortization unused). Both batched conn loops (plain + idempotent, and their ADR-0097/0102 fan-outs) now flush on an ESTIMATED ~1 MiB statement byte target (the pscale-dumper size, deliberately equal to ADR-0139's `maxCoalescedStatementBytes`), with the row cap raised to a 10k safety ceiling and a 60k placeholder bound (MySQL's 16-bit prepared-stmt parameter limit — load-bearing once rows can exceed 500). Statement COMPOSITION only: every value still binds to a `?` through the same `prepareValue` codec (the Bug-74 safety property); each flush stays one autocommit statement = one transaction, far inside Vitess's ~20 s killer; AIMD/connection budgets/grow-gate/reparent-retry untouched; an oversize single row ships alone (never split, never refused — `max_allowed_packet` stays the loud bound). `--max-buffer-bytes` can only LOWER the target. Companion once-per-writer INFO on hosted-PlanetScale targets: writes are tier-CPU-bound (ADR-0116), so copy parallelism beyond the auto budget won't scale linearly. Reached: MIG / CS / RST / CR-full-leg / BRK-reset-leg × MySQL-target; CDC apply + replay legs deliberately untouched (already coalesced, ADR-0139/0140); LOAD DATA path untouched. |
| [0151](adr-0151-d1-page-prefetch.md) | **Accepted** — single-page prefetch for the `d1` query-API reader (roadmap item 54 perf backlog; the chain-replay read-ahead pattern applied to the D1 HTTP page loop). A lone fetcher goroutine issues page N+1's request while page N's rows decode and stream — hiding one WAN RTT per 1000-row page — over an UNBUFFERED handoff, so exactly one page of read-ahead (bounded memory), never an N-deep pipeline. Requests stay byte-identical to the sequential loop (same order, same exact-text string bounds incl. > 2^53); errors on a prefetched page surface in sequence; the terminal page is explicitly marked so a ctx-cancelled fetch reports the cause via `Err()` instead of a clean silently-truncated read; the decode loop reaps the fetcher on every return path. NOT within-table chunking (that stays deferred at `d1PageSize`). Reached: MIG + CS D1-source read path (the `d1-trigger` cold start composes the same reader); everything else n/a. |
| [0152](adr-0152-backup-encryption-integrity.md) | **Accepted** — backup-encryption integrity hardening (audit N-8 + N-9; manifest **FormatVersion 5**, Bug-116 proportional stamp on ENCRYPTED manifests only). Chunks written under a v5 manifest carry a GCM AAD binding to (manifest identity = the CreatedAt/engine/kind prefix of ComputeBackupID — deliberately NOT BackupID, whose EndPosition is unknown at chunk-write time — + chunk path, + list ordinal for change chunks, whose replay ORDER is semantic); the chain CEK's wrap is identity-bound via the new `crypto.BoundEnvelope` surface (AWS EncryptionContext / GCP AAD / passphrase-wrap GCM AAD; Azure Key Vault DELIBERATELY not implemented — its WrapKey API has no context parameter, documented, pinned). Version gating lives in ONE function per shape (`ChunkAAD`/`ChangeChunkAAD` + read-side `…For`) and TWO CEK chokepoints (`lineage.WrapChainCEK`/`UnwrapChainCEK`): the RECORDED FormatVersion decides bound-vs-legacy-nil-AAD — never try-both. Pre-v5 chains restore byte-identically; resuming a pre-v5 encrypted in-progress full inherits its unbound shape (Bug-179-style). N-9: Azure envelopes now PIN a concrete key version (GetKey preflight + WrapKey response KID), record the versioned URL in KEKRef, and rebind restore-side unwraps at the recorded wrap-time version (`crypto.ChainKEKRebinder`) — key auto-rotation no longer breaks older chains' restores (old manifests: latest-version behavior + a WARN naming the hazard); the lying blob-metadata comment corrected. Also made two lying comments true: restore now validates every chunk header's column set against the manifest schema (renamed-column chunks refuse naming both deltas — `ChunkReader.Header()`'s first non-test caller), and chain-restore verifies every link's recorded SchemaHash against the recomputed one before anything lands (honestly a CORRUPTION check, not tamper-proofing — the hash shares the unsigned manifest; fulls now record it too, and the item-51 sequence-refresh swap re-stamps it, with a named pre-v5-sequences WARN carve-out). Honest boundary documented: the manifest is unsigned, so whole-backup rollback and tail truncation remain out of scope. Pinned by the e2e tamper matrix (swap/reorder/cross-backup-replay refuse; untampered + pre-v5 restore) + per-family crypto/codec matrices. |
| [0153](adr-0153-mysql-flavor-interpolation-default.md) | **Accepted** — the PlanetScale/Vitess flavors default to client-side interpolation (`interpolateParams=true`, 1 COM_QUERY round trip per statement); vanilla MySQL keeps the binary protocol; an explicit DSN `interpolateParams=` always wins (detected on the RAW DSN — ParseDSN collapses explicit-false with absent). Audit N-15(a): the hidden per-statement COM_STMT_PREPARE measured one full RTT in every regime on real PlanetScale (−33% bulk copy, −26% CDC burst drain, p95 flush −105 ms ≈ 1 RTT at ~100 ms client RTT), and the audit's fidelity objection did not survive the bench + the Phase-1 Bug-74 family matrix (every family × protocol × {bulk INSERT, CDC multirow apply} pinned byte-exact on real MySQL BEFORE the flip). Resolution at one choke point (`parseDSNForFlavor`); the parsed cfg carries the decision so lane pools / probe clones / snapshot pools inherit it; zero-value-safe (derived from Flavor, no config bool). The matrix caught + closed two real divergences: NaN/±Inf now refused pre-driver (SLUICE-E-VALUE-UNREPRESENTABLE — the interpolated `NaN` literal draws Error 1054, which the Bug-F8 schema-drift self-healing would retry for ~30 min) and float64 −0.0 encoded as the string `'-0'` (MySQL's parser reads the literal `-0` as unary-minus-on-zero, dropping the sign the binary protocol keeps). Guards: NBE ground-truthed CORRECT in-driver (status-flag escaper switch; pinned byte-exact, no refusal needed); unsafe-collation DSNs keep the binary protocol with a WARN (the default steps aside; explicit unsafe combos stay the driver's loud refusal; denylist copy pinned subset-of-driver); packet headroom absorbed by the ADR-0150 1 MiB target with the driver's ErrSkip→prepared fallback pinned at the boundary. Wire-pinned end-to-end (Com_stmt_prepare = 0) through the engine registry. |
| [0154](adr-0154-signed-backup-manifests.md) | **Accepted — fully shipped (Phases 1–3b, v0.99.208–212; Bug 181 hardening v0.99.213)** — optional detached signing of the backup manifest + lineage tip (a new manifest **FormatVersion 6**, Bug-116-proportional: stamped only when signing is enabled), closing the ADR-0152 honest residual (R1 whole-manifest rollback + R2 change-list tail truncation by a store-WRITE adversary). Framing kept honest: signing is the SECOND-order defense; the first-order defense against malicious-manifest-field attacks is that sluice's manifest is data-never-instructions (no executable/decompressor field — contrast Vitess CVE-2026-27965) and paths are sanitised (`absPath`/`sanitiseBlobKey` — contrast CVE-2026-27969), both already held. Ratified decisions: **key mechanism** = composite — HMAC-off-KEK as the zero-config default for encrypted chains (anyone who can decrypt can verify), Ed25519 keypair as the explicit opt-in covering plaintext + key-separated verification, KMS Sign/Verify layered on for KMS-mode operators (payload records key-id + scheme); **verification policy** = strict where intent is expressed (a `--verify-key` supplied, or `backup verify`) → refuse `SLUICE-E-BACKUP-SIGNATURE-{INVALID,MISSING}`, warn-and-proceed with no key configured so a forgotten flag never fails a DR restore, pre-v6 unsigned always restores; **freshness** = intra-chain monotonicity (signed gap-free sequence + tip-count) always-on when signing is enabled (closes R2 + intra-chain rollback for free), external high-water anchor (full R1) a documented opt-in with R1's residual honestly noted as inherent; **compact/prune without the signing key** = refuse loudly rather than emit an unsigned successor (KMS Sign is the operable path for a key-less maintenance cron). Phase 1 = FormatVersion 6 + HMAC-off-KEK + the verification policy + `backup verify` signature reporting + intra-chain monotonicity; Phases 2–3 = Ed25519, then KMS. |
| [0155](adr-0155-tty-aware-pretty-output.md) | **Accepted (rolled out across the one-shot commands, v0.99.232–241)** — TTY-aware pretty output for one-shot commands (`migrate`, `verify`, `backup`, `restore`, `cutover`, `schema *`, trigger/matview commands): a phase-panel renderer engaged ONLY when stdout is a TTY and `--log-format`/`--no-progress` don't opt out. Off-TTY / JSON / CI output stays byte-identical (pinned) — the panel is additive presentation over the same slog stream, never a data source; leaked pre-panel INFO lines were chased release-by-release (`backup` v238, `sync start`/`metrics-watch` v240, `restore` v241). |
| [0156](adr-0156-continuous-command-live-panel.md) | **Accepted (phases 1–3 shipped, v0.99.236–239)** — the live-panel counterpart for continuous commands: `sync start`, the `sync from-backup run` broker, `backup stream run`, and `metrics-watch` render an in-place TTY status panel (stream state, positions, freshness `seconds_since_last_apply` as the load-bearing cross-engine lag signal, truthful lifetime `rows_applied` — transactional with position, at-least-once honest on warm resume). Same contract as ADR-0155: additive on a TTY, byte-identical off it. |
| [0157](adr-0157-schema-drift-notifications.md) | **Accepted (implemented)** — schema-drift notifications: when a non-forwardable source DDL stalls a sync (the ADR-0060 structured refusal), the streamer now also pages the configured `--notify-*` sinks (webhook/email) with the drift diff + operator-action hint, de-duplicated per drift shape; the slot-health WARNs joined the same sinks in v0.99.246 (roadmap 64a). Notification delivery is best-effort and never blocks or reorders the apply path. |
| [0158](adr-0158-mysql-ca-pinned-tls.md) | **Accepted (shipped v0.99.243)** — CA-pinned `verify-ca` TLS for MySQL connections (`tls=verify-ca` + `tls-ca=<pem>` DSN params at one registration choke point): full chain + hostname verification is impossible against a stock MySQL server because its auto-generated cert has no SubjectAltName and Go's verifier won't fall back to CN — so the honest posture is CA-pinned verification without hostname matching, refusing loudly on any other combination rather than silently downgrading. |
| [0159](adr-0159-standalone-backfill-command.md) | **Accepted (Phase 1 shipped v0.99.244, Phase 2 verify gate v0.99.245)** — standalone `sluice backfill`, the expand-contract "migrate" step: same-DB sequential keyset-chunked bounded UPDATEs (`--set 'col = expr'` verbatim native SQL, `--where` self-describing guard, per-chunk `NextChunkUpperBound` + one bounded UPDATE ≤ batch-size rows, under PlanetScale's errno-3024 statement wall) via the new optional `ir.BackfillExecutor` engine surface (mysql all-flavors + postgres; sqlite/d1 coded-refused). Resume via `migratestate` in the SAME DB (id `backfill:<table>:<12-hex spec hash>`, cursor persisted per chunk commit, crash replays ≤1 chunk as a guard-scoped no-op); Phase 2 adds `--verify` (post-walk whole-table remaining-count on the guard; >0 ⇒ coded runtime `SLUICE-E-BACKFILL-INCOMPLETE`) and `--verify-only` (scriptable no-walk gate). Three refusal codes `SLUICE-E-BACKFILL-{NO-PRIMARY-KEY,UNSUPPORTED-ENGINE,UNKNOWN-COLUMN}`. |
| [0160](adr-0160-backup-chain-concurrent-writer-guard.md) | **Accepted (shipped v0.99.246, roadmap 64b)** — backup-chain concurrent-writer guard: `lineage.json` was the chain's one unguarded read-modify-write object (two writers on one chain could silently orphan a segment), and portable object stores offer create-only conditional writes (gocloud `IfNotExist`), not `If-Match` overwrite — so the guard is generation-claim markers (`lineage.gen/g-<N>`, create-only CAS): claim the next generation before the catalog write, refuse `SLUICE-E-BACKUP-CHAIN-CONFLICT` on collision, orphaned claims are liveness-safe (observed as the new base, never a stale lock), prune reordered commit-then-sweep, markers GC'd to a trailing window. The claim→Put window residual any create-only CAS carries is documented honestly. |
| [0161](adr-0161-mydumper-source-engine.md) | **Accepted (shipped v0.99.247, roadmap item 55 Phase 1)** — mydumper flat-file source engine (`--source-driver mydumper`): migrate-only reads of mydumper/pscale-dump directories (metadata + per-table schema files + extended-INSERT data chunks, plain/.gz/.zst) through a bounded single-statement CREATE TABLE parser and a faithful dump-value decoder (escape-class + hex-blob binary shapes; MySQL's string grammar KEEPS the backslash for `\%`/`\_`; UTC-only `SET TIME_ZONE` gate over every spelling with a once-per-table WARN when TIMESTAMP columns arrive headerless — real pscale dumps write no TZ header and an empty metadata file, live-verified 2026-07-15). String-family columns must be UTF-8-compatible charsets (bytes carry verbatim; latin1 etc. refused loudly, ADR §5); writers/CDC refused; value fidelity pinned dump==live per family (the ADR-0161 F1–F6 pre-land review closed DOUBLE-rounding, TZ-gate-bypass, BIT/Boolean, FLOAT-WARN, `\%` live-parity, and geometry gaps). |
| [0162](adr-0162-planetscale-expand-contract-orchestration.md) | **Accepted (implemented; roadmap item 62 Phase 3)** — `sluice expand-contract`: the full expand→migrate→contract pattern against a PlanetScale database as one opt-in command — dev branch + verbatim `--expand-ddl` deploy request, the ADR-0159 backfill (reused whole) + verify gate, then a `--yes`-gated `--contract-ddl` deploy request, with best-effort branch cleanup, deterministic branch names + refuse-on-leftover + `--resume-from` (no persisted state machine), `--dry-run` with a pinned zero-control-plane-calls plan, and refuse-on-disabled safe migrations (never auto-enabled). Introduces the thin shared PS control-plane client (`internal/planetscale/api`, no SDK; telemetry's authenticated leg rides it too) and a tolerant terminal-state DR poller. Three live-caught pre-release findings (see the ADR's Live-validation section): the nested `deployment.deployable` field, safe-migrations vs the backfill's control-table bootstrap (tables now ship inside the expand DR), and stale dev-branch schema bases (freshness gate + one-shot backup rebase, `SLUICE-E-PS-BRANCH-STALE-BASE`). |
| [0163](adr-0163-flatfile-csv-tsv-ndjson-sources.md) | **Accepted (implemented; roadmap item 55 Phases 2+3)** — CSV/TSV/NDJSON source drivers (`--source-driver csv|tsv|ndjson`) via stage-into-SQLite: schema-less files materialize into a temp all-TEXT SQLite db read through the sqlite engine's staged readers, with `--infer-types` auto-engaged (validated timestamp/jsonb/uuid promotions; numbers deliberately stay TEXT). Explicit-declaration flags, never sniffed: `--csv-null` (quoted fields are always data; unquoted-empty with no declaration refuses `SLUICE-E-CSV-NULL-AMBIGUOUS`), `--csv-header`/`--csv-no-header` (else `SLUICE-E-CSV-HEADER-UNDECLARED`), `--csv-delimiter` (tsv = a fixed-TAB registered flavor). Own strict RFC 4180 lexer (quoted-empty vs unquoted-empty is load-bearing; encoding/csv collapses them); NDJSON numbers carried as raw text end-to-end (the D1 2^53 lesson), duplicate keys refused. Phase 3: content-signature refusals for plain mysqldump/pg_dump `.sql` and PGDMP with scratch-server-replay recipes (`SLUICE-E-SOURCE-FOREIGN-DUMP`) + cross-driver misuse refusals naming the right driver (`SLUICE-E-SOURCE-WRONG-DRIVER`) on the csv/tsv/ndjson, sqlite, and mydumper open paths. |
| [0164](adr-0164-backup-export-as-parquet.md) | **Accepted (implemented; roadmap item 63)** — `sluice backup export-as-parquet`: the analytics EXIT surface — a one-shot, read-only transcode of an existing backup's JSON-Lines row chunks into one zstd Parquet file per table + a `parquet_index.json` export manifest, over the existing `BackupStore`/lineage/blobcodec machinery (restore-grade integrity: SHA-256 chunk fetch, GCM AAD binding, plaintext-splice refusal, signed-chain verification, layer-2 row counts). Exit-only (sluice never reads its Parquet back; restore keeps JSON-Lines); library = `parquet-go/parquet-go` (pure Go, no arrow-go tail); DuckDB stays a cookbook recipe, never a dependency. Faithful family-dispatched type mapping (exact DECIMAL over INT32/INT64/FLBA16, full-range unsigned INT64, micros temporals, WKB + GeoParquet metadata, enum/set universes in footer metadata); unrepresentable values refuse coded `SLUICE-E-EXPORT-UNREPRESENTABLE` (multi-dim arrays, ±838h TIME, NUMERIC NaN, sub-µs) — the two string downgrades (unbounded NUMERIC, TIMETZ) carry exact text with WARN notes. Chain-to-a-point at snapshot granularity (`--backup-id`; trailing incrementals WARN loudly, never fold silently). Named wart: parquet-go's map-row deconstruction nulls every Go ZERO value in optional columns (false/0/""/±0.0 → NULL) — closed by pointer-boxing at one chokepoint and pinned by explicit non-null assertions per family. Row groups align 1:1 with source chunks. |
| [0165](adr-0165-deploy-ddl-and-control-table-bootstrap.md) | **Accepted (implemented; roadmap item 66)** — `sluice deploy-ddl` + the control-table bootstrap: ship ONE verbatim `--ddl` statement to a PlanetScale production branch through the governed deploy-request channel with the full ADR-0162 safety wrapper (safe-migrations preflight, the stale-base freshness gate, refuse-on-leftover deterministic branch, tolerant DR poller, skip-revert finalize, always-cleanup, `--dry-run` with pinned zero control-plane calls) — an EXTRACTION of the expand leg into a shared package-internal `legRunner` both commands compose, never a fork. Companion `sluice control-tables ddl` prints the five-table bootstrap set (`sluice_migrate_state`/`_table_progress`, `sluice_cdc_state`, `sluice_cdc_schema_history`, `sluice_shard_consolidation_lease`) via the new optional `ir.ControlTableDDLProvider` surface, single-sourced byte-identical with the engine's own Ensure\* DDL. Error 1105 "direct DDL is disabled" is classified at EVERY mysql control-table DDL site (CREATEs, column ALTERs, the item-65a widen) into the new coded refusal `SLUICE-E-PS-DIRECT-DDL-BLOCKED` naming the bootstrap path and echoing the refused statement; the CDC applier's three ensures became detect-then-create (zero DDL when current) so a bootstrapped `sync` actually starts — the companion fix without which shipping the tables would not have unblocked anything. |

## Notes / dialogue prep / readiness briefs

Some ADRs were drafted from dialogue artifacts in `docs/dev/notes/`. Notable companions:

- [`docs/dev/notes/adr-0048-dialogue-prep.md`](../dev/notes/adr-0048-dialogue-prep.md)
- [`docs/dev/notes/adr-0049-implementation-readiness.md`](../dev/notes/adr-0049-implementation-readiness.md)

These aren't ADRs themselves — they're the source material the ADR was promoted from. Treat them as historical context.

## How to read an ADR

The shape is: Context → Decision → Consequences → Alternatives considered. Each ADR is meant to be self-contained — you should be able to read it without reading the previous ones. The cross-references (`[ADR-XXXX]`) are bidirectional safety nets: forward-direction "this builds on X" and reverse-direction "X was relevant when this was written."

If a decision was reversed or superseded, the original ADR isn't deleted — it's marked superseded with a pointer at the top, and the new ADR explains the change. Reading the chain in number order is also a (slow) way to trace sluice's design evolution.

## When to write a new ADR

Read [CLAUDE.md](../../CLAUDE.md) § Tenets first. The current rule of thumb: write an ADR when a design choice has **non-obvious consequences a reader two years from now would benefit from knowing about.** Refactors and bug fixes generally don't warrant an ADR; they're documented by the change itself + the closure note. Adding an engine, changing a tenet, introducing a new IR shape, or making a backwards-incompatible API choice all do.
